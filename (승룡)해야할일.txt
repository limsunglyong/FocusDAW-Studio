
- 브라우저의 debug console이 열리지 않도록 release 시 removeMenu 필요.

- mixer 창 30fps -> 60fps 로 올리면 무슨문제가? (지금은 살짝 버벅이는듯?)

- Mastering 체크리스트 (앱 내 가이드로 제공):

- A/B 비교: Original vs Processed.

- Output Fx 트랙에 모든 effect 끄기 버튼 추가 필요. (기존 reverb, echo 버튼은 제거 할까?)

- Key 변조 기능

- batch 작업 기능


=======================================================================================


버그


이 코드는 메모리(힙) 손상 크래시입니다.

코드 해석

3221226356 (10진수) = 0xC0000374 (16진수) = STATUS_HEAP_CORRUPTION
0xC0000374는 Windows의 NTSTATUS 코드로, 네이티브 엔진 프로세스(FocusDAW-AudioEngine.exe)가 힙 손상(heap corruption)으로 강제 종료됐다는 뜻입니다. C/C++ 코드가 메모리를 잘못 다뤄서, Windows 힙 관리자가 손상을 감지하고 그 즉시 프로세스를 죽인 것입니다.

전형적 원인 (모두 네이티브 C++ 측):

double-free — 같은 메모리를 두 번 해제
use-after-free — 해제된 메모리를 계속 사용
버퍼 오버런 — 할당 범위를 넘어 쓰기
스레드 경합 — 한 스레드가 컨테이너를 수정하는 동안 다른 스레드가 읽기
이 프로젝트에서의 이력
이 정확한 코드는 전에도 나왔던 항목입니다 (앱개발.md 기록):

#151 (v1.8.8): SoundTouchAudioSource 이중 소유(double-free) → AddressSanitizer로 원인 확정·수정. 당시도 Process exited with code 3221226356였습니다.
#149: 마스터 EQ 계수 스레드 경합(use-after-free) 수정.
⚠️ 제 최근 변경과의 연관 가능성 (정직하게)
제가 방금 v1.9.16에서 추가한 볼륨 오토메이션 코드에 새로운 힙 손상 가능성이 하나 있습니다:

TrackAudioSource의 autoPointT/autoPointV/autoTan(std::vector)을 오디오/익스포트 스레드의 getNextAudioBlock이 락 없이 읽는데, setTrackAutomation은 같은 벡터를 (engineMutex 하에) clear+재할당합니다. 평소엔 export 중 실시간 콜백이 suspend돼 안전하지만, export 도중(또는 재생 중) 오토메이션을 편집해 setTrackAutomation이 들어오면 읽기 중 벡터가 재할당되어 use-after-free → 힙 손상이 날 수 있습니다.

(기존 volume/pan은 단일 float이라 최악이어도 "값 깨짐"이지 힙 손상은 아니지만, 벡터는 재할당이 일어나 위험합니다.)

확인하고 싶은 것
이 에러가 언제 나오나요? (Export 중 / 재생 중 / 트랙 불러올 때 / 오토메이션 편집 중 등)
제 v1.9.16 변경 이후에 처음 보셨나요, 아니면 그 전부터 있었나요?
만약 v1.9.16 이후 + 오토메이션을 만지는 상황에서 난다면 제 변경이 원인일 가능성이 높으니, 위 벡터 접근을 락으로 보호하거나 원자적 교체(스왑) 방식으로 바꿔 바로 잡겠습니다. 트리거를 알려주시면 정확히 짚어 수정하겠습니다.

(토큰 사용량 끝.)

(이후 다른 곳에서 작업 내용, 이전에 있었던 버그인지 직전 패치 때문에 생긴 버그인지 알 수 없으나 여러가지 많은 문제가 발생한 듯 함)

1. 음악 플레이, 스톱 반복시 트랙들이 sync가 맞지 않는 것 처럼 들리는 현상.
2. 음악 플레이, 스톱 반복시 mixer 창을 열고 닫고, 플레이/스탑을 반복하면 트랙들이 sync가 맞지 않는 것 처럼 들리는 현상.
3. mixer 창에서 EQ 프리셋이 제대로 작동하지 않습니다. 수동으로 그래프를 조절하면 적용되는 것 같아요.
4. New project 선택시 mixer 창의 일부 effect 값들이 초기화 되지 않고 남아있음.
5. mixer 창의 level meter가 동작하지 않습니다.
6. Widener, Exciter / Enhancer 동작이 이상.
7. 아래 상황에서 playbar가 움직이지 않고 앱 상태가 이상해짐.
- 0번~8번까지 트랙을 로딩
- 플레이
- 0번 트랙 뮤트
- playbar가 멈취서 떨리고 음악이 이상해지며 플레이가 뮤트 되지도 않음.

8. mute 이후 다시 mute 버튼을 눌러 mute를 해제해도 트랙이 소리가 나지 않음.

9. 뭔가 크게 잘못된것 같습니다. (오늘 수정이 이상한 방향으로 간것만 같습니다)
- 방금 시험에서 8개의 트랙중 3개 트랙만 켜면 소리가 급격히 커짐.
- 스피커에서 찢어지는 듯한 소리와 띠리띠리~ 거리는 고주파 음이 납니다. 앱을 종료하면 없어집니다.

1~9 까지 진행 후 트랙들의 동작이 많이 이상해짐. 특정 트랙이 solo로 켜질때 갑자기 소리가 엄청 커진 뒤 다시 solo를 끄면 트랙들의 sync 문제가 발생되거나 플레이가 이상해짐. 심지어 듣기 힘든 고주파음까지 스피커로 들림.
이후 코드를 처음으로 돌림.





남은 후속 과제 (급하지 않음)
네이티브 밴드레벨 브로드캐스트 — 지금 스펙트럼/밴드 미터가 web로 폴백(무음 구간 잔향 꼬리 표시). native가 밴드 레벨도 보내면 정리됨
네이티브 메모리 하드닝 — reverbSendBuffer 블록 바운드 안전화 + mute/solo 원자화 (가끔 비복구 증상 예방)
Ambience wet 절대레벨 — JUCE 정규화 vs web 미세 차이 (필요 시 계수 조정)
seek 순간 글리치 — 경미, 구조적 재프라임 비용



프리뷰에서 limiter 스위치 켜는 법.
