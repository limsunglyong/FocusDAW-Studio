# Pitch Editor (v2.0.0) — 설계 문서

> 작성: 2026-08-25 · 상태: **🟡 Stage A 완료(v2.0.0~v2.0.1) · Stage B 완료(v2.0.2, 시험 대기) — 다음은 Stage C(노트 세그멘테이션)**(미결 4건 2026-08-25 전부 해소, §10) · 관련: [보컬-채널-스트립-설계.md](보컬-채널-스트립-설계.md) §8(이 기능을 후속 페이즈로 분리한 근거), [앱개발.md](앱개발.md) "남은 항목 및 계획", [(승룡)해야할일.txt](<(승룡)해야할일.txt>)
>
> **목적** — SUNO stem(MR) 위에 녹음한 보컬의 **음정을 음표 단위로 보정**한다. 채널 스트립이 "음색"을 담당했다면 Pitch Editor는 "음정"을 담당하며, 두 기능이 합쳐져 아래 워크플로가 완성된다.

```
보컬 녹음 (Audio In)
  → [Pitch Editor]  음정 수정 → 오프라인 프린트(클립 오디오 교체)
  → [보컬 채널 스트립]  HPF · Gate · EQ · Comp · De-esser (기존, 재생 시 인서트)
  → Export
```

**이 순서가 성립하는 이유**: 피치 보정은 **클립의 소스 오디오 자체를 교체**하고, 채널 스트립은 그 위에 걸리는 **재생 인서트**다. 따라서 스트립·Export·네이티브 경로는 **한 줄도 고치지 않아도** 보정된 오디오를 그대로 받는다. 반대 순서(스트립 뒤에 피치)라면 이미 컴프레션된 신호를 다시 늘였다 줄여야 해서 아티팩트가 커진다.

---

## 사용자 결정 (2026-08-25)

- **버전**: **v2.0.0** (Major +1 — 사용자 요구에 따른 대형 신규 기능).
- **v2.0.0 범위**: **Pitch Editor 하나**. 레포지토리 분리·자동 업데이트 등 배포 인프라와 그 밖의 백로그는 v2에 끼워넣지 않고 별도 트랙으로 둔다.
- **적용 방식**: **오프라인 프린트**. 편집 결과를 새 오디오로 렌더해 클립을 비파괴 교체한다. 실시간 네이티브 DSP는 채택하지 않는다.
- **엔진**: **자체 구현**. 검출은 YIN/pYIN 계열, 시프트는 PSOLA. 외부 라이브러리(Rubber Band 등)는 라이선스 부담으로 제외하고, SoundTouch는 노트 단위 짧은 구간에서 품질 한계가 뚜렷해 제외한다.

---

## 1. 현재 기준선 (착수 전 확인 사항)

| 항목 | 현재 상태 | 이 기능에 대한 의미 |
|---|---|---|
| 피치 관련 기능 | `tempo.key / keyShift`([audio-engine.js](audio-engine.js#L346)) = **프로젝트 전체 반음 일괄 전조**. 웹은 soundtouchjs bake, 네이티브는 `SoundTouchAudioSource`([AudioEngine.h](juce_skeleton/Source/AudioEngine.h#L456)) | **음표 단위 편집은 전무.** 전조와 목적이 완전히 다르므로 재사용 대상이 아니다(공존해야 함 — §3 주의) |
| 오프라인 프린트 | Stage F De-noise: `denoiseClip`([audio-engine.js](audio-engine.js#L3252))이 새 버퍼를 렌더 → `_registerSource` → `clip.sourceId` 교체 → `_pendingConsolidations` → `_ensureBaked` | **그대로 재사용.** 파일 기록·Undo·재열기 복원이 이미 이 경로에 붙어 있다 |
| 디스크 기록 | `persistConsolidatedSources`([audio-engine.js](audio-engine.js#L3094)) ← app.jsx `persistConsolidated()`([app.jsx](app.jsx#L2896))가 `<Project> Audio/Consolidated/`에 WAV 기록 | 프린트 결과가 저장/재열기에서 살아남는 경로가 **이미 검증됨**(v1.37.x) |
| 별창 UI | `open-vocal-strip` IPC + BrowserWindow([electron/main.js](electron/main.js#L1505)) + BroadcastChannel 메시지(app.jsx `REQUEST_*` 스위치, [app.jsx](app.jsx#L2400)) | **같은 패턴 복제.** `pitch-editor.html` + `pitch-editor-app.jsx` |
| FFT | `fft` / `_ifft` / `_hann` + Stage C `computeTrackSpectrum` | 피치 검출 가속·시각화에 재사용 |
| 트랙 종류 게이팅 | `kind`가 `audioIn` / `bounce`일 때만 스트립 노출([ui-tracks.jsx](ui-tracks.jsx#L761)) | Pitch Editor도 **동일 게이팅**. 스템(`file`)에는 노출하지 않는다 |

---

## 2. 비파괴 원칙 — "항상 원본에서 재렌더"

De-noise는 프린트 후 `clip.sourceId`를 새 소스로 갈아끼운다. 피치는 **여러 번 고치는 것이 정상**이므로 같은 방식을 그대로 쓰면 **보정본 위에 다시 보정**이 쌓여 아티팩트가 누적된다.

**규칙**: 클립은 **원본 소스 id를 계속 기억**하고, 프린트는 **매번 원본에서** 다시 렌더한다.

```js
clip.pitch = {
  baseSourceId: "src_17",    // 최초 원본(녹음 결과). 재편집해도 절대 안 바뀐다
  printedSourceId: "src_44", // 현재 클립이 재생 중인 보정본(없으면 미프린트)
  notes: [ ... ],            // 편집 상태(아래 §4)
  analysis: { hop, fmin, fmax, version }, // 재분석 없이 창을 다시 열기 위한 메타
}
```

- 두 번째 프린트는 `printedSourceId`가 아니라 **`baseSourceId`의 버퍼를 읽어** 새로 렌더한다 → **몇 번을 고쳐도 1세대 품질**.
- "Revert to original"은 `clip.sourceId = baseSourceId` 한 줄이면 된다(파일 삭제 불필요).
- 이 필드는 `params` 통째 직렬화와 같은 원리로 **저장/복원 자동**이며, 구프로젝트는 `clip.pitch` 부재 = 미편집으로 자연 처리된다.

---

## 3. 전조(Vari Key)와의 공존 — 주의 지점

- **타임라인 오디오는 항상 원본**이라는 v1.46.0 규칙(Merge가 Vari BPM/Key를 굽지 않도록 확정)이 여기에도 적용된다. Pitch Editor의 프린트는 **Vari Key/BPM을 적용하지 않은 원본 도메인**에서 이뤄져야 한다.
- 반대로 **표시**는 사용자가 듣는 것과 맞아야 한다. Vari Key가 켜져 있으면 피아노롤의 음이름이 재생음과 어긋나므로, **창 상단에 "Vari Key +2 적용 중 — 편집은 원본 기준" 배지**를 띄운다(끄게 강요하지 않는다).
- Vari BPM도 같다. 분석·렌더는 원본 샘플레이트/속도 기준.

---

## 4. 데이터 모델

```js
// clip.pitch.notes[] — 편집 단위
{
  id: "n7",
  t0: 12.480, t1: 12.902,     // 클립 소스 기준 초(타임라인 아님 — 클립을 옮겨도 유효)
  midi: 62,                   // 검출된 원 음정(반올림 전 중앙값에서 산출)
  target: 62,                 // 사용자가 지정한 목표 음정(드래그로 변경)
  strength: 1.0,              // 0=원음 유지 … 1=목표로 완전 이동
  keepVibrato: true,          // 곡선의 미세 편차(비브라토/글라이드) 보존 여부
  confidence: 0.87,           // 검출 신뢰도(낮으면 흐리게 표시)
}
```

- **편집은 노트 단위만**(v2.0.0). 곡선을 직접 그리는 자유 편집은 범위 밖 — 필요하면 후속.
- `strength`는 노트별. 전역 기본값은 창의 슬라이더로(프리셋 §7).
- **스케일 스냅**: 프로젝트에 이미 `tempo.detectedKey`가 있으므로, 드래그 스냅을 **크로매틱 / 프로젝트 Key 스케일** 중 선택할 수 있게 한다(기존 Key 감지 기능의 자연스러운 재사용).

---

## 5. 엔진 — 3단 구성 (전부 오프라인)

### 5-1. 어디서 계산하나 — **렌더러 JS**(De-noise 전례)

De-noise Stage F가 자체 STFT를 **렌더러 JS에서** 돌려 프린트했고 그것으로 충분했다. Pitch도 같은 자리에 둔다.

- 장점: **네이티브 재빌드 불필요**, 브리지 무변경, 디버깅 쉬움, 웹/네이티브 이중 구현 없음.
- 위험: 긴 클립에서 느릴 수 있다 → **Stage B에서 5분 보컬 실측**을 먼저 하고, 필요하면 Web Worker로 옮긴다(계산이 순수 함수라 이관 비용이 낮다). 네이티브 이관은 최후 수단.
- ✅ **실측 완료(v2.0.2, 2026-08-26)** — 5분 테이크 **4.2 s**(28,120 프레임, 실시간의 1.4%). 문제는 속도가 아니라 **그 4.2초 동안 창이 얼어붙는 것**이었으므로, **Worker 대신 90 ms 청크 분할 + 진행률**로 확정했다. Worker는 번들 하나를 더 빌드·패키징·동기화하고 버퍼를 전송해야 하는데, 이벤트 루프 양보만으로 같은 문제가 사라진다. 12 kHz 데시메이션 + 무음 게이트가 비용의 대부분을 이미 걷어낸 결과다.

### 5-2. ① 피치 트래킹 — YIN/pYIN 계열

- 프레임: N=2048(@48k ≈ 42.7ms), hop=256(≈5.3ms). 검출 범위 **65~1100Hz**(C2~C6, 보컬).
- YIN 차분함수 → CMND(누적 평균 정규화) → 임계 0.15 이하 첫 골 → 포물선 보간으로 서브샘플 주기. 자기상관은 FFT로 가속(기존 `fft` 재사용).
- **후처리**: 중앙값 필터(5프레임)로 옥타브 점프 제거, 무성음/저에너지 프레임은 `unvoiced`로 표시.
- 산출물: `f0[]`(Hz), `voiced[]`(bool), `conf[]`(0~1).

### 5-3. ② 노트 세그멘테이션

- `f0` → 반음 단위 로그 스케일 변환 후, **유성 + 안정** 구간을 노트로 자른다.
  - 이력(hysteresis) 임계 ±0.6반음, **최소 길이 60ms**, 무성 40ms 이상이면 노트 경계.
- 노트의 `midi`는 구간 중앙 60% 구간의 **중앙값**(글리산도·어택의 흔들림 배제).
- 결과가 사용자 눈에 이상하면 노트 경계를 **수동 분할/병합**할 수 있어야 한다(Stage D).

### 5-4. ③ 렌더 — TD-PSOLA

- **왜 PSOLA인가**: 시간영역 피치동기 중첩가산은 **포먼트를 자연히 보존**해 보컬이 "다람쥐 소리"가 되지 않는다. 위상보코더는 트랜지언트·무성음에서 떨림(phasiness)이 크고, 리샘플은 포먼트가 함께 움직인다.
- 절차: 유성 구간의 **피치마크(에폭)** 추출 → 목표 f0 곡선에 맞춰 마크 **재배치** → 각 마크 주변 2주기 Hann 창을 잘라 새 위치에 중첩가산. **무성 구간은 원본 그대로 통과**(자음 보존).
- **길이 불변**: 피치만 바꾸고 타이밍은 건드리지 않는다(타임 편집은 이 문서 범위 밖).
- 목표 곡선 = `target` + (`keepVibrato` ? 원 곡선의 노트 중앙값 대비 편차 : 0), `strength`로 원 곡선과 선형 보간.
- 스테레오: 채널을 **독립 처리하면 위상이 어긋난다** → 한 채널(또는 합)에서 뽑은 **동일 피치마크**를 두 채널에 적용. Audio In 녹음은 대개 모노라 실사용 영향은 작다.

---

## 6. 프린트 경로 (기존 인프라 재사용)

De-noise와 **같은 순서**를 따른다 — 새로 만들 배관이 없다.

1. `pushUndo()` (app.jsx가 창 메시지를 받은 자리에서)
2. `DAW.printPitch(trackId, clipId, notes, opts)` — `baseSourceId` 버퍼에서 PSOLA 렌더
3. `_registerSource(...)` → `clip.sourceId` 교체 + `clip.pitch.printedSourceId` 갱신
4. `_pendingConsolidations.push({ ..., suffix: "Pitched" })` → `persistConsolidated()`가 `<Project> Audio/Consolidated/`에 WAV 기록
5. `_ensureBaked(track)` → 파형·재생·Export·네이티브가 한 번에 따라옴
6. 실패 시 `cancelUndo(savedRedo)` (v1.23.1 규칙)

**원본 WAV는 손대지 않는다.** Undo는 스냅샷 복원으로 이전 `sourceId`가 되살아나고, 미사용 프린트 WAV는 기존 **Clean Up Unused Recordings**가 회수한다(undo 스택 참조까지 보는 판정이라 그대로 안전).

---

## 7. UI — 별창 피아노롤

- 진입점: 트랙 헤더의 **FX 버튼 옆**이 정석이나, 헤더(244px)에 **가로 여유가 거의 없다**([ui-tracks.jsx](ui-tracks.jsx#L755) 주석 — 텍스트 버튼이 이미 정렬을 깨뜨린 전례). → **클립 우클릭 메뉴 "Pitch Editor..."를 1차 진입점**으로 두고, 헤더 아이콘은 자리 확보가 되면 추가한다(미결 §10).
- 창 구성(위→아래): 트랙·클립 이름 + Vari Key 배지 → 툴바(스냅: Chromatic/Key · 전역 Strength · Keep vibrato · **Analyze** · **Apply(프린트)** · **Revert**) → **피아노롤**(좌측 건반 + 시간축) → 하단 상태줄(검출 노트 수·진행률).
- 피아노롤 표시: **노트 블록**(목표 음정 위치) + **원 피치 곡선**(반투명 오버레이) + 파형 미니맵. 신뢰도가 낮은 노트는 흐리게.
- 조작: 노트 세로 드래그 = 반음 이동(스냅), 더블클릭 = 원 음정 복귀, 드래그 선택 후 일괄 이동, 경계 드래그 = 분할/병합.
- 테마: 앱 CSS 변수만 사용(`var(--amber)` 등). **하드코딩 액센트 색 금지** — v1.44.21에서 52곳을 일괄 정리한 전례.
- 프리셋: **Natural**(strength 0.7 · vibrato 유지) / **Tight**(1.0 · 유지) / **Hard**(1.0 · 미유지).

---

## 8. 단계별 구현 계획

| 스테이지 | 내용 | 산출/판정 |
|---|---|---|
| **A. 골격** | `pitch-editor.html` + `pitch-editor-app.jsx` 별창, IPC/BroadcastChannel 배선, 클립 선택·파형·그리드 표시, `clip.pitch` 데이터 모델 + 저장/복원 | ✅ **v2.0.0 (2026-08-25)** · **사용자 시험 통과(2026-08-26)** — 테마 전환 결함 1건은 v2.0.1에서 수정(재시험 T-2.0.1-1) |
| **B. 피치 트래킹** | YIN + 후처리, 곡선 오버레이(읽기 전용), **5분 클립 분석 시간 실측** | ✅ **v2.0.2 (2026-08-26)** · 정확도 최대 오차 **0.005반음** · 5분 = **4.2 s**(실시간 1.4%) → **Worker 불필요, 청크 분할로 확정** · 시험 대기(T-2.0.2-1) |
| **C. 노트 세그멘테이션** | 노트 분할 + 피아노롤 블록 표시 + 선택 | 사람이 부른 음표와 눈으로 일치 |
| **D. 편집** | 드래그/스냅(Chromatic·Key)/strength/vibrato 토글/분할·병합, Undo 정합 | 편집 상태가 저장·복원되고 Undo가 맞는다 |
| **E. 렌더 + 프린트** | TD-PSOLA 렌더 → 프린트 경로(§6) → 재생·Export·네이티브 반영, Revert | **가청 품질 판정**(사용자 시험). 원본 WAV 불변, Undo 복원 |
| **F. 마무리** | 프리셋, 진행률 UI, 재편집(원본 재렌더) 검증, 메뉴얼 신설 장, 창 영문화 | v2.0.0 릴리즈 |

각 스테이지는 기존 관례대로 **버전을 올리고 시험 항목을 [시험.md](시험.md)에 등록**한 뒤 사용자 시험을 받는다(v2.0.0-alpha 식이 아니라 v1 계열과 같은 `2.0.x` 진행).

---

## 9. 시험 관점 (착수 시 시험.md에 항목화)

- 스템(`file`) 트랙에는 Pitch Editor가 **안 보인다**(회귀 없음).
- 분석 결과가 실제 노래의 음정과 일치(신뢰도 낮은 구간이 과잉 노트로 쪼개지지 않음).
- 보정 후 **재생·Export·네이티브가 모두 같은 소리**(웹/네이티브 A/B).
- **채널 스트립과의 결합**: 보정 → 스트립 ON 상태에서 Export가 정상(순서 §목적).
- **재편집**: 같은 클립을 두 번 보정해도 아티팩트가 누적되지 않는다(=원본 재렌더 확인).
- 저장/재열기 후 노트 편집 상태 유지, 구프로젝트 정상 로드.
- Undo/Redo 정합, Revert로 원음 복귀, 원본 WAV 불변.
- Vari Key/BPM이 켜진 상태에서도 프린트 결과가 **원본 음정·길이**.
- 클립 이동/분할/Merge 후에도 `clip.pitch`가 따라간다(또는 명확히 무효화된다 — 미결 §10).

---

## 10. 결정 사항 — **전부 해소됨 (2026-08-25)**

1. ✅ **진입점**: **클립 우클릭 메뉴 "Pitch Editor..."**. 편집 대상이 트랙이 아니라 **클립**이라 의미가 정확하고, 244px 트랙 헤더의 가로 여유 문제(FX 버튼이 아이콘으로 축소된 전례)를 피한다. 헤더 아이콘은 넣지 않는다.
2. ✅ **클립 편집과의 상호작용**: 보정한 클립을 split/Merge/Flatten 하면 **프린트된 오디오만 남기고 `clip.pitch`는 폐기**한다. 소리는 변하지 않으며(이미 프린트됨), 이후 그 클립을 다시 열면 **현재 오디오를 새 원본으로 삼아 재분석**한다. 구간별로 노트를 쪼개 갖는 방식은 `baseSourceId`가 클립마다 갈라지는 경우까지 다뤄야 해서 복잡도·버그 위험이 크다.
3. ✅ **스테레오**: 한 채널(또는 채널 합)에서 뽑은 **피치마크를 두 채널에 공유**해 위상 어긋남을 막는다(§5-4).
4. ✅ **Auto 보정**: 창을 열 때는 **보정을 걸지 않고**(target = 검출 음정) 시작한다. 일괄 스냅은 툴바의 **"Snap all to key"** 버튼으로 사용자가 명시적으로 실행한다 — 열자마자 소리가 바뀌어 있으면 무엇이 원음인지 알 수 없다.
