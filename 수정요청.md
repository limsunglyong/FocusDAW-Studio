# 지침

*중요*
1. 하단의 수정요청사항을 읽고 문제점을 파악한뒤 수정계획을 `앱개발.md`에 기록한다.
2. 앱 버전 {Major}.{Minor}.{Patch} 을 변경한다.
   - Major 버전 : 사용자의 요구에 따라 변경
   - Minor 버전 : 기능 추가 또는 큰 구조 변경이 있을 시 +1
   - Patch 버전 : 버그 수정, 기능 수정, 그 밖에 사소한 변경이 있으면 +1
3. 사용자에게 내용을 알리고 수정에 대한 승인을 받은 뒤 코드/문서 수정을 진행한다.
4. 수정이 완료되면 구현된 사항을 `앱개발.md` 에 기록하고 이에 대한 시험 방안을 쉽고 간단하게 [시험항목], [시험방법 및 절차], [시험 환경 준비] 등의 내용을 시험.md 에 내용을 추가한다.
5. 아래 수정요청사항의 각 항목에 대한 수정이 완료되면 항목을 삭제한다. 만약 어떤 이유로 수정을 하지 못했다면 그 이유와 함께 적어두고 혹시 추가 수정사항이 필요한 경우 해당 내용을 추가해둔다.

# 수정요청사항

(현재 대기 중인 요청 없음)

---

> 처리 완료 — v2.3.4 (2026-08-28): **v2.3.3 회귀 — Undo/Redo가 소리에 닿지 않던 문제.** 상세 [앱개발.md](앱개발.md) v2.3.4, 시험 [시험.md](시험.md) T-2.3.4-1. **renderer 재빌드만.** ⚠️ 사용자 시험 대기.
>
> 🔴 **제가 v2.3.3에서 만든 회귀입니다.** 네이티브 엔진이 mute/solo/volume/pan/sends/automation을 알게 되는 경로는 ① `setTrackParam`(버튼) ② `syncTrackToNative`의 마지막 줄 `sendTrackRuntimeStateToNative` 둘뿐인데, **`applySnapshot`은 둘 다 지나가지 않는다**(값을 LocalDAW 트랙 객체에 직접 써넣는다). v2.3.2까지는 undo가 전 트랙을 재적재했으므로 ②가 딸려 실행돼 우연히 동작했고 — 그 대가가 1초였다. 재적재를 걷어내자 파라미터가 엔진에 도달할 길이 사라져 **화면만 되돌아오고 소리는 옛것**이 됐다. 사용자 진단("이전에 시간이 걸리던 문제를 대충 알 수 있을 듯")이 정확했다.
>
> 수정 = 트랙 서명을 **`audio`(재적재가 필요한 것)와 `params`(작은 명령이면 되는 것)로 분리**해, 파라미터만 바뀌면 재적재 없이 값만, 그것도 **바뀐 트랙에만** 보낸다. 비용이 전혀 다른 두 가지를 하나로 묶고 있었던 것이 뿌리였다.
>
> **검증 방식도 고쳤다** — 이 회귀는 "웹 계층만 계측했다"는 구멍에서 나왔으므로, 프리로드에서 `WebSocket`을 가짜 엔진 소켓으로 바꿔 브리지가 **네이티브 경로**를 타게 하고 엔진으로 나가는 명령을 전부 붙잡았다: 버튼 Mute → `setTrackParam mute=true` · **그 Undo → `mute=false` 1건, `clearTracks`/`loadTrack` 0건** · Automation 변경 → `setTrackAutomation` 2건 · **그 Undo → 해당 트랙에 1건, `loadTrack` 0건** · 클립 이동 Undo → 재적재 발생(프로젝트 길이가 바뀌면 `songLength` 때문에 필요). 성능 이득(623 ms → 1.1 ms)은 그대로다.

> 처리 완료 — v2.3.3 (2026-08-28): **Undo/Redo 지연 규명·수정 + 보완 3건.** 상세 [앱개발.md](앱개발.md) v2.3.3, 시험 [시험.md](시험.md) T-2.3.3-1·2·3. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> 🔴 **Undo/Redo가 매번 프로젝트 전체를 다시 계산하고 엔진을 다시 실었다.** 실측 재현(스템 5개 3분 + 클립 2개 Audio In, 파라미터만 바뀐 스냅샷 왕복): **623.3 ms → 1.1 ms(566배)**. 세 겹이었다 — ⓐ **피크 재계산**(오디오 전체 3회 스캔 × 전 트랙 × 매 Undo, ~420 ms): 스냅샷은 AudioBuffer를 **참조로** 들고 있으므로 같은 객체면 피크도 그대로다. 게다가 클립 편집 트랙은 그 피크를 몇 줄 뒤 베이크가 버리고 다시 계산했다. ⓑ **클립 레이아웃 재베이크**(~200 ms/트랙): 베이크는 입력의 순수 함수이므로 **서명 기반 캐시**를 뒀다(클립 배치 + **원본 버퍼의 객체 동일성** — 같은 길이의 다른 테이크는 같은 오디오가 아니다). ⓒ **네이티브를 통째로 헐고 재적재**: `clearTracks` → 전 트랙 재적재라 임시 WAV를 디스크에 쓰고 출력이 웹 엔진으로 떨어졌다(재생 중 Undo가 소리를 건드린 이유). **트랙별 서명을 전후 비교해 달라진 것만** 다시 보내도록 바꿨다 — 새 장치가 아니라 클립 편집이 이미 쓰는 `syncTrackToNative`·`removeTrack` 경로다.
>
> **정확성 검증이 핵심**이라 베이크 결과를 샘플 값으로 지문화해 실구동 확인했다: 무변경/스냅샷 왕복→재사용·동일 · 클립 이동→재베이크(6.0→7.5 s) · 그 Undo→원본과 완전 일치 · gain·trim→재베이크 · **같은 길이의 다른 버퍼로 교체→재베이크**(문자열 서명이었다면 놓쳤을 경우).
>
> **① Shift+Mute가 트랙 선택까지 건드리던 문제.** 헤더의 mousedown이 트랙 선택 핸들러이고 거기서 Shift는 "선택 확장"이라, 한 번의 Shift+클릭이 두 제스처를 동시에 실행했다. Shift가 눌린 Mute 클릭만 헤더로 전파되지 않게 했다(Shift 없는 클릭은 종전대로 트랙을 선택한다).
>
> **② Pitch Editor 가로 스크롤바 제거.** 위쪽 CLIP MAP 띠가 같은 일을 하면서 롤 높이를 10px 먹고 있었다.
>
> **③ Pitch Editor를 열 때 재생 정지.** 창이 클립과 트랜스포트 상태를 처음 아는 순간 한 번만 멈추고 클립 시작에 선다. ⚠️ **녹음 중에는 무시** — 창을 열었다고 테이크가 끊기면 안 되므로, 에디터가 스스로 내는 정지에 `fromEditor` 표식을 붙이고 스튜디오가 녹음·카운트인 중에는 그 표식이 붙은 정지만 버린다.

> 처리 완료 — v2.3.2 (2026-08-28): **File Tracks 일괄 Mute(Shift+클릭)가 bounce 트랙을 건너뛰던 버그.** 상세 [앱개발.md](앱개발.md) v2.3.2, 시험 [시험.md](시험.md) T-2.3.2-1. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> 🔴 **그룹의 소속 기준과 일괄 동작의 대상 기준이 달랐다.** `File Tracks` 그룹은 `!t.kind || file || bounce`로 묶는데 일괄 Mute는 **`kind === "file"` 하나만** 골랐다 — 화면에는 한 그룹인데 동작에서는 두 집합이라, **bounce 트랙이 그룹 안에 앉은 채 일괄 Mute만 비껴갔다**(같은 구멍이 `kind`가 없던 옛 프로젝트 트랙에도 있었다). 어긋난 자리는 **다섯 곳**이고, 그중 둘은 "Shift가 먹히는 트랙" 판정이라 **bounce의 M을 Shift+클릭하면 일괄 동작이 시작조차 되지 않았다.**
>
> 수정 = 다섯 곳이 각자 조건을 들고 있던 것이 본질이므로, **그룹의 정의를 공유 술어 `isFileGroupTrack`으로 뽑아** 그룹 필터까지 여섯 곳이 같은 함수를 부르게 했다([ui-kit.jsx](ui-kit.jsx) — 스튜디오·믹서 두 창에서 가장 먼저 로드되는 공용 스코프). 전수 확인 결과 코드베이스에 남은 `kind === "file"` 비교는 이 술어 한 줄뿐이다.
>
> 검증 = **실제 studio.html을 띄워** `DAW.tracks`를 합성 5종(stem×2 · bounce · kind 없음 · audioIn)으로 바꾸고 **진짜 채널로 `MUTE_ALL_FILES`를 태웠다** → `stem1 · stem2 · bounce1 · legacy1` mute, `vox`(audioIn) 그대로. 술어 단위 확인 file/bounce/legacy ✓, audioIn/null ✗. 실제 Shift+클릭 제스처는 사용자 시험에서 확인한다.

> 처리 완료 — v2.3.1 (2026-08-28): **T-2.3.0 시험 보완 5건.** 상세 [앱개발.md](앱개발.md) v2.3.1, 시험 [시험.md](시험.md) T-2.3.1-1. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> **①~③ UI 문구 전면 영문화 — 지침으로 확정.** 우측 패널 안내를 영문으로 교체하고, `Alt`+wheel 설명을 "음정 줌"에서 **"Key zoom"** 으로 바로잡았으며(v2.2.1에서 라벨만 KEY로 바꾸고 안내가 옛 이름으로 남아 있었다), `KEYBOARD` 구역을 삭제했다. 다만 "건반을 누르면 소리가 난다"는 발견 수단이 통째로 사라지므로 **상태줄에 `click a key to hear it`** 한 줄을 넣었다(참조 디자인이 상호작용 안내에 배정한 자리). **이후 모든 UI 문구는 영어**로 쓴다 — 앱개발.md 상시 지침에 기록.
>
> **④ 밝은 테마에서 스크롤바 손잡이가 보이지 않던 문제** 🔴 참조 디자인이 지정한 `--surface3`가 **밝은 테마에서는 트랙 색과 거의 같았다**(실측 Classical Ivory **1.07:1** · Sage 1.23 · lime 1.31 · default 1.40 — 마우스를 올리기 전에는 위치를 알 수 없다). 10개 테마 × 후보 6종을 실구동 계측해, **UI 컨트롤 기준선 3:1을 모든 테마에서 넘는 유일한 후보인 `--cream-2`** 로 교체(최저 **3.95:1**, Ivory / default 10.61 · Sage 8.37 · Slate 12.02). 곁다리로 hover를 `--amber-deep` → `--amber`로 바꿨다 — 어두운 테마에서 amber-deep은 새 손잡이보다 어두워서 **마우스를 올리면 바가 흐려지는 것처럼** 보였을 것이다.
>
> **⑤ 클립 끝에서 자동 정지.** `CLIP` 루프를 끄면 재생이 클립 밖으로 계속 흘러갔다. **단, 이 창이 시작한 재생만 멈춘다** ⚠️ — 에디터를 열어 둔 채 스튜디오에서 곡 전체를 듣는 것을 클립 가장자리에서 끊으면 안 되므로, 소유권을 이 창의 ▶에서 주장하고 관측된 재생→정지 전이에서 놓는다. `CLIP` 루프가 켜져 있으면 건너뛴다(엔진이 같은 경계에서 되감는다). 정지는 **■와 같은 경로**를 탄다 — 엔진 `stop()`이 오프셋을 곡의 0으로 되돌리므로 두 경로 모두 그 뒤에 클립 시작으로 seek한다.
>
> 검증 = Electron 실구동(패널 3구역 · **렌더된 DOM + 모든 title 속성 한글 0건** · 손잡이 대비 4종 · 클립 끝 통과 시 STOP 1건 / 통과 전 0건 / 스튜디오 재생은 0건 / CLIP 루프 시 0건 · 콘솔 오류 0건). **계측 함정 1건**: Chromium은 `color-mix()`를 `color(srgb 0.5 …)`(0~1 실수)로 직렬화하는데 첫 스크립트가 이를 0~255로 읽어 대비비가 완전히 틀렸다(Ivory가 13.65:1로 나옴). 파서를 고친 뒤의 값이 위 수치다.

> 처리 완료 — v2.3.0 (2026-08-28): **Pitch Editor UI 재구성 5건.** 상세 [앱개발.md](앱개발.md) v2.3.0, 설계 [피치-에디터-설계.md](피치-에디터-설계.md) §11, 시험 [시험.md](시험.md) T-2.3.0-1·2. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> **① 참조 디자인 반영.** 채택(건반 토큰·스크롤바 규격·오토스크롤·노트바 규격·노트 amber/곡선 red·상태줄 2단) / 변형(고정 700×252 카드 → 유동 · DOM 렌더 → 캔버스 유지 · 박자 그리드 → 시간 기본 + BPM 있을 때만 마디 병기) / 비채택(Record 버튼 — 이 창은 스튜디오 트랜스포트의 원격이다 · 참조본 4개 테마 — 우리는 10개) 판정을 설계 §11-1에 기록.
>
> **② 레이아웃 재편.** 트랜스포트 줄에는 트랜스포트와 신원만 남기고 줌·분석·보정을 **고정 300px 우측 패널**로. 이전 툴바는 창을 좁히면 두 줄로 접히며 롤 높이를 먹었다. **구현 중 결함 1건** 🔴 개요 띠가 46px가 아니라 416px로 부풀어 롤을 눌렀다 — 플렉스 아이템의 `min-height` 기본값이 `auto`(콘텐츠 크기)라 안의 캔버스 고유 높이가 `flex-basis`를 밀어 올린 것. `min-height:0` + 캔버스 절대 배치로 해결.
>
> **③ 클립 개요 띠.** 전체 파형 + 현재 구간 상자(바깥을 덮는 방식) + 플레이헤드 + 검출 구간 붉은 리본. 클릭=건너뛰기, 상자 드래그=스크롤, 가장자리 드래그=줌. 이미 받아둔 4000버킷을 쓰므로 추가 분석 없음(단, 픽셀당 **극값**을 취한다 — 샘플링하면 짧고 센 소리가 사라진다).
>
> **④ 상하·좌우 이동.** 좌우는 사실 되고 있었으나 **스크롤바도 지도도 없어 화면이 그렇다고 말해주지 않았다**. 위아래는 정말로 없었다. 음정 범위를 실수로 바꾸고(반음 단위면 한 행씩 튄다), 양축 스크롤바 + 휠 규약 정리(그냥=상하 · Shift=좌우 · Ctrl=시간 줌 · Ctrl+Shift=음정 줌, Alt는 호환 유지) + **가운데 버튼 드래그** 팬(왼쪽 버튼은 이미 seek 면이라 못 쓴다).
>
> **⑤ 클립 범위 트랜스포트.** 스튜디오의 Repeat 구간을 **빌려 쓰고 해제 시 돌려준다**(승인 ⓒ). 두 번째 루프 장치를 만들면 맞춰야 할 것이 둘이 된다. **녹음 중에는 거부** — punch가 같은 구간을 [in,out]으로 읽는다. ▶는 클립 밖일 때만 클립 시작으로 이동, ■는 클립 시작 복귀. Undo 스냅샷 없음(트랜스포트 상태는 `getSnapshot()`에 없다).
>
> **⑥ 건반 클릭 = 피아노 소리.** 배음 4개 합성. 창이 자기 `AudioContext`를 열되 **출력 장치는 상속되지 않으므로** `focusdaw-audio-device`를 읽어 `setSinkId`로 맞춘다. 재생 중에는 **그 순간 부른 음의 건반에 점등** — T-2.0.2-1 ④(실제 음정 대조)의 상시 확인 수단.
>
> 검증 = Electron 실구동 13개 항목 통과(줌·스크롤·팬·개요 클릭·건반 점등·루프 메시지·트랜스포트 순서·패널 접기, 콘솔 오류 0건). **app.jsx의 `REQUEST_LOOP_RANGE` 실제 동작과 프리뷰 음의 출력 장치는 사용자 시험(T-2.3.0-2)에서 확인한다.**

> 처리 완료 — v2.2.1 (2026-08-28): **Pitch Editor 시험 보완 3건 + 곡선 색 선점.** 상세 [앱개발.md](앱개발.md) v2.2.1, 시험 [시험.md](시험.md) T-2.2.1-1. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> **① 줌 라벨 `PITCH` → `KEY`.** 창 전체 주제가 pitch라 세로 줌 그룹까지 `PITCH`면 "오디오 음정을 바꾸는 버튼"으로 읽힌다는 지적. 참조 디자인도 두 축을 TIME / KEY로 쓴다.
>
> **② 건반 음이름 대비** 🔴 원인은 글자가 아니라 **건반을 텍스트 색으로 칠한 것**이었다. 건반 면에 `--cream`/`--bg`를 쓰고 있었는데 이 둘은 텍스트 색이라, **밝은 테마에서 `--cream`은 거의 검정**(ivory `#3b3327`) → **"흰 건반"이 새까맣게** 칠해졌다(실측 ivory `rgb(59,51,39)` · sage `rgb(25,28,26)`). 거기에 음이름을 팔레트에서 가장 흐린 `--faint`로, 8px weight 400 고정으로 그렸다 — 이 굵기는 사실상 전부 안티에일리어싱이라 **지정한 색에 도달조차 못 한다**(가장 진한 픽셀 `rgb(111,102,89)` vs 지정 `#5f574a`). 수정 = 건반 전용 토큰 4종 신설(`--key-white`/`--key-white-2`/`--key-black`/`--key-ink`) + 흰 건반 그라디언트 면 + 어두운 잉크 **weight 700**, 행 높이에 따라 7~9px, 우측 정렬. 실측 대비 **default 4.55 → 6.88:1 · blue 4.37 → 10.76:1 · slate 5.48 → 11.30:1 · navy 5.67 → 12.99:1 · lime 5.11 → 11.76:1**, 밝은 테마는 건반 흑백 뒤집힘 자체가 해소(ivory 면 `rgb(231,223,202)` / 8.96:1 · sage 14.60:1).
>
> **③ 창 안에서 Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y.** "왜 스튜디오를 선택해야 하나"는 문의에 대한 답은 **설계 의도가 아니라 미배선**이다 — 다른 위성 창은 전부 넘기고 있었고, Stage A·B의 이 창은 자기 편집이 없어 아무도 요구하지 않았을 뿐이다. **사용자 결정으로 Stage D부터는 창 로컬 스택을 앞단에 둔다**(설계 §11-2).
>
> **④ 검출 곡선 amber → red.** Stage C가 amber 노트 블록을 이 곡선 위에 얹으므로 같은 색이면 둘 다 못 읽는다(참조 디자인도 노트 amber / 곡선 red). 나중에 바꾸면 Stage C 시험이 색 문제와 세그멘테이션 문제를 함께 지게 되어 **지금 선점**했다.
>
> 검증 = Electron 실구동 캔버스 픽셀 계측(테마 7종 대비비 표는 앱개발.md) + 스모크(라벨 `["TIME","KEY"]` · Ctrl+Z/Shift+Z/Y → UNDO/REDO/REDO · 합성 분석 주입 후 `--red` 1600px / `--amber` 0px · 콘솔 오류 0건).

---

> 처리 완료 — v2.2.0 (2026-08-26): **Pitch Editor 트랜스포트 · 플레이헤드 · 시간/음정 줌.** 사용자 문의에서 출발. 상세 [앱개발.md](앱개발.md) v2.2.0, 시험 [시험.md](시험.md) T-2.2.0-1·2. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.

>

> 필요한 조각은 이미 있었다 — `transportRef`의 play/pause·stop, 공용 채널의 `REQUEST_PLAY_PAUSE`, 녹음 중 seek를 막는 `guardedUserSeek`. 추가한 것은 `REQUEST_STOP`·`REQUEST_SEEK`·`REQUEST_TRANSPORT`(폴링) 세 가지와 창 쪽 UI다. **플레이헤드는 방송이 아니라 ~30fps 폴링**으로 받는다(스튜디오의 프레임 피드는 믹서/Advanced 창 가시성에 묶여 있어서 — 보컬 스트립 GR 미터와 같은 선택). **seek는 클로저가 아니라 `transportRef`를 거친다**(이 effect는 매 렌더 다시 돌지 않아 stale 참조가 된다). 창에는 ▶/⏸·■·시간 표시(Space 지원), **DOM 오버레이 플레이바**(초당 30회 움직이는 선 때문에 캔버스를 다시 그리지 않는다), 클릭 seek, **커서 기준** Ctrl+휠 시간 줌, Alt+휠 음정 줌, 분석 결과에 맞추는 세로 맞춤.

>

> **시험 중 버그 1건 발견·수정**: 표시가 클립 상대 시간이라 트랜스포트가 클립보다 앞에 있으면 음수가 되고, ui-kit `fmtTime`이 음수를 다루지 않아 `-1:-5.-15`가 나왔다 → 클립 밖이면 `--:--` + 플레이바 미표시(둘이 같은 조건). Electron 2창 실구동 16개 단언 통과.



> 처리 완료 — v2.1.0 (2026-08-26): **클립을 다른 트랙으로 복사(Copy to track).** 사용자 문의 "bounce 클립을 Audio Input 트랙으로 복사해 올 수 있나?"에서 출발. 상세 [앱개발.md](앱개발.md) v2.1.0, 시험 [시험.md](시험.md) T-2.1.0-1·2. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.

>

> **조사에서 밝혀진 것**: 엔진은 이미 하고 있었다 — `pasteClip`이 클립보드의 소스 메타와 raw 버퍼를 대상 트랙에 심어 주므로 bounce → Audio In 붙여넣기가 완전히 동작한다(실측 확인). 막고 있던 것은 **대상 트랙을 지목할 방법**뿐이었다: `pasteTargetTrackId()`가 ①선택된 클립의 트랙 ②마지막으로 만진 클립 트랙 순으로 고르는데, bounce 클립을 복사하면 둘 다 **그 bounce 트랙**을 가리켜 **Ctrl+V가 제자리에 다시 붙었고**, 빈 Audio In 트랙은 클릭할 클립이 없어 지목 자체가 불가능했다.

>

> **구현**: 클립 우클릭 메뉴에 `COPY TO TRACK` 목록(사용자 결정). 엔진 `copyClipToTrack`은 `_clipboard`를 건드리지 않아 사용자의 Ctrl+C 내용이 살아남고, **원래 타임라인 위치**를 유지하며(차 있으면 기존 규칙대로 밀림), `takeId`·`recordedStart`/`recordedOffsetMs`·`pitch`는 떨어뜨린다 — 각각 존재하지 않는 레인 소속·녹음된 적 없는 사본·측정한 클립의 것이기 때문. 스템은 **목적지로는 거부하되 출발지로는 허용**. 엔진 단언 25건 통과.



> 처리 완료 — v2.0.1 (2026-08-26): **T-2.0.0-1 ④ 결함 — Pitch Editor를 열어 둔 채 테마를 바꾸면 피아노롤이 옛 색을 유지.** 상세 [앱개발.md](앱개발.md) v2.0.1, 시험 [시험.md](시험.md) T-2.0.1-1. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 재시험 대기.
>
> 원인 두 가지가 겹쳤다. ⓐ **캔버스는 CSS 변수 변경으로 재도색되지 않는다** — 창 외곽은 DOM이라 `data-theme`이 바뀌면 브라우저가 다시 칠하지만, 피아노롤은 이미 칠해진 픽셀이라 **다시 그리라고 해야** 바뀐다. 그리기 의존성이 `[size, info]`뿐이어서 테마 변경이 재그리기를 일으키지 못했다(창을 다시 열면 고쳐진 이유 = 그때는 새로 그리니까). ⓑ 의존성만 추가했다면 **한 박자 늦은 색**으로 칠할 뻔했다 — React가 자식 effect를 부모보다 먼저 실행하므로, 부모 effect에서 `data-theme`을 붙이면 자식이 옛 속성 상태에서 변수를 읽는다. 수정 = 의존성에 `theme` 추가 + `data-theme`을 **테마가 도착한 자리에서 동기 적용**. 검증 = Electron 실구동 회귀 시험(테마 3종 전환 후 **캔버스 픽셀 해시** 비교) 11개 단언 통과, 수정 전 코드에서는 보고된 증상 그대로 재현(외곽만 통과·캔버스 실패).

> 처리 완료 — v1.46.3 (2026-08-25): **v1 계열 최종 릴리즈 마무리.** 상세 [앱개발.md](앱개발.md) v1.46.3, 시험 [시험.md](시험.md) T-1.46.3-1. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 설치본 스모크 테스트 대기.
>
> **① 마지막 `NO AUDIO` 문구 정합.** 트랙 헤더 칩이 v1.44.20에서 `NO SRC`로 바뀌고 메뉴얼도 v1.45.0에서 따라갔는데, **"오디오 파일을 찾을 수 없음" 모달**([app.jsx](app.jsx):4700)만 옛 라벨로 설명하고 있었다 — 정작 그 트랙들이 `NO SRC` 칩을 달고 있는 상황에 뜨는 창이라 화면과 안내가 어긋났다. 이로써 "도움말 문구 미추종" 항목 종결.
>
> **② 재빌드·재패키징.** 마지막 메뉴얼 커밋(Merge Tracks 다중 선택 안내)이 v1.46.2 인스톨러 빌드보다 **25분 늦어 설치본에 빠져 있었다.** `dist/release-1.46.3/`으로 출력해 잠긴 `win-unpacked.tmp` EBUSY를 회피.

> 처리 완료 — v1.46.2 (2026-08-18): **시험 결함 1건 + UI 요청 2건.** 상세 [앱개발.md](앱개발.md) v1.46.2, 시험 [시험.md](시험.md) T-1.46.2-1~3. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 재시험 대기.
>
> **① T-1.46.0-4 결함 — Flatten이 punch와 무관한 이웃 클립까지 합치던 문제** 🔴 원인 = `canFlattenComp`가 "연속된 클립 = 한 run" 규칙을 **렌더 대상 전체**에 적용하고 run 하나를 새 오디오 파일 하나로 렌더했다. 이 규칙은 comp 조각을 이어 붙이려던 것인데, **take와 무관한 일반 클립(takeId=null)** 도 맞닿아 있으면 같은 run에 휩쓸렸다 — 복사/이동으로 정확히 붙어 있던 4·5번 클립이 그래서 하나가 됐다. 수정 = flatten이 **커밋할 take 재료만** 다루도록 범위를 좁힘. 일반 클립은 위치·소스·id 그대로 **통과**(재렌더 없음), run은 **같은 Take Group 안에서만** 이어짐. 보고 배치 재현 검증: 클립 수 7→7 유지, 맞닿은 두 클립 독립 유지, 재렌더 0회. 진짜 comp(한 그룹 안 swipe)는 여전히 한 클립으로 확정되는 것도 회귀 확인.
>
> **② UI 한글 잔여 문구 영문화.** Merge Tracks 창 안내문을 포함해 Vari BPM/Key 툴팁, Key 목록 툴팁, 트랙 헤더 FX 배지 툴팁, Automation 초기화·트랙 삭제 확인창, Advanced Ambience의 룸 설명·캡션까지 정리. 실구동 전수 스캔으로 **한글 0건** 확인(데모 곡 이름 `그 여름의 우리 - focustone`은 실제 파일/곡 제목이라 유지).
>
> **③ `MERGE TRACKS...` 버튼 대비 반전.** 원인 = 활성이 `--amber-soft`(반투명 액센트) 배경 + 액센트 글자라, 액센트와 `surface2`가 가까운 테마에서 **활성이 비활성보다 흐려졌다**(Ocean 활성 1.43:1 / 비활성 2.10:1, Modern Blue 2.06 / 2.84 — 지적하신 그대로). 수정 = 활성은 **액센트로 꽉 채운 칩 + 어두운 글자 + 은은한 글로우**, 비활성은 **배경에 녹아드는 외곽선**. 실측 활성 라벨 대비 **Ocean 7.08:1 · Modern Blue 5.80:1**. (부수 발견: `background` 단축 속성 + transition이 Chromium에서 보간 값에 고착돼 칠이 안 되던 문제도 longhand로 교체해 해결.)

> 처리 완료 — v1.46.1 (2026-08-18): **시험 피드백 2건.** 상세 [앱개발.md](앱개발.md) v1.46.1, 시험 [시험.md](시험.md) T-1.46.1-1·2. **renderer 재빌드만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 재시험 대기.
>
> **① T-1.46.0-3 결함 — 두 번째 punch 녹음이 앞 클립의 take를 빼앗던 문제** 🔴 원인 두 가지가 겹쳤다. ⓐ `_sliceActiveAtRegion`이 "punch 구간이 자른 조각"과 "구간과 무관하게 바깥에 있던 클립"을 **한 배열로 반환**했고, loop-punch가 "구간 밖은 공유 base"라며 **그 전체를 `takeId=null`로 detach** → 첫 punch 그룹의 선택 테이크(Take E)가 클립을 잃고 삭제되어 오디오만 메인 레인 클립으로 남음(= 사라진 Take E, 클립 6→7). ⓑ `_groupSelection`의 fallback이 "그룹의 **마지막** 테이크"라, `activeTakeId`가 새 punch로 넘어가면 첫 그룹이 **꼬리 partial 테이크로 바뀌**었다(스크린샷의 `Take E (partial) active`). 수정 = 잘린 조각(`remnants`)만 base로 detach하고 **다른 클립의 테이크는 유지**, fallback을 **마지막 완전 패스**로 변경, 녹음 전 각 클립의 재생 선택을 **comp에 고정(pin)** 해 새 녹음이 남의 선택을 건드리지 않게 함. 보고 시나리오 그대로 시뮬레이션 + 실구동 검증(첫 그룹 6테이크 유지 · base 누출 0 · 클립 5→6 · 선택 유지).
>
> **② T-1.46.0-1 — BPM 안내창 영문화.** "Project BPM required" + Detect / TAP / Type it in 안내로 교체(툴팁 포함). 테마 대응은 그대로.

> 처리 완료 — v1.46.0 (2026-08-18): **수정요청 6건 전부 처리.** 상세 [앱개발.md](앱개발.md) v1.46.0, 시험 [시험.md](시험.md) T-1.46.0-1~6. **renderer 재빌드 + `npm run build:manual`만 · 네이티브 재빌드 불필요.** ⚠️ 사용자 시험 대기.
>
> **① BPM 미측정 상태에서 Vari BPM이 속도를 바꾸던 문제** — 원인 = **웹 엔진이 아니라 네이티브의 잔류 상태**. `_projectRate()`는 projectBpm이 없으면 항상 1.0인데, 네이티브는 "미측정"을 표현할 수 없고(기본 120/120) [audio-bridge.js](audio-bridge.js) `syncTempoKeyToNative()`가 **`if (t.projectBpm)` 가드 때문에 null이면 아예 보내지 않았으며**, `clearTracks()`(New Project)도 tempo/key를 재푸시하지 않았다 → 직전 프로젝트의 비율(예: 100→128)이 남아 있다가 Vari BPM을 켜는 순간 1.28배로 재생. **같은 구멍이 Key(keyShift)에도 있었다.** 수정 = ① tempo/key를 **항상 명시값으로 전송**(미측정이면 중립 120/120, 키는 빈 문자열) ② `clearTracks`·`clearTracksKeepMaster`·마지막 트랙 `removeTrack`에서 재동기화 ③ **엔진 `setVariBpm`이 projectBpm 없이 켜기를 거부**(모든 진입점 일괄) ④ `---`일 때 스위치를 **흐리게** 표시하고 클릭하면 **테마에 맞는 안내 모달**(Detect / TAP / 직접 입력+APPLY)을 띄움 ⑤ BPM 없이 저장된 프로젝트는 스위치 OFF로 열림.
>
> **② Merge가 BPM/Key의 영향을 받던 문제** — 원인 = `mergeTracks()`가 Export와 같은 `renderMix()`를 옵션 없이 호출해 **Vari BPM의 템포와 Vari Key의 이조가 바운스에 구워졌다**(길이·음정이 원본 트랙과 어긋남). 수정 = `applyTempo:false` + 신설 `applyKey:false`로 **항상 원본 렌더**(호출자가 덮어쓸 수 없음). 규칙 확정: **타임라인 오디오는 항상 원본, Vari BPM/Key는 재생과 Export 전용.** Consolidate·Flatten은 raw 버퍼를 잇는 구조라 이미 원본 기준(확인 완료). 대화창·메뉴얼에 안내 추가.
>
> **③ 클립별 Take 개별 선택** — 확인 결과 **기존 구조로는 불가능**했다(테이크가 트랙 단위 레인이라 `activeTakeId` 하나로 트랙 전체가 정해지고, 다른 구간 테이크는 통째로 숨겨짐). **Take Group(클립별 테이크 묶음)** 을 도입해 재편: 그룹은 클립 구간 겹침으로 **유도**(파일 포맷·undo·네이티브에 새 필드 없음), 클립마다 **Take A부터** 번호, 레인 목록은 `CLIP 1/2/3` 머리글로 그룹화, 레인 클릭은 **그 클립만** 바꾸고, 메인 타임라인은 선택된 테이크를 그대로 표시하며 재생·Export도 일치. swipe 컴프는 자기 클립 구간으로 클램프, `Clear`는 컴프만 취소(선택 유지), **Flatten은 클립마다 선택 테이크를 각각 확정**. Punch도 다른 자리면 기존 테이크에 합류하지 않고 새 그룹을 만든다. ⚠️ **알려 드린 영향(승인받고 진행)**: 옛 모델에서 숨겨져 있던 다른 구간의 테이크가 다시 들릴 수 있고, 테이크 이름이 클립별 A·B·C…로 재번호된다(오디오 파일에는 영향 없음).
>
> **④ Ocean 테마 메뉴얼 표 가독성** — 원인 = `.manual-th`가 `--surface2:#178CA4` 배경에 `--amber:#18B7BE` 글자로 **명암비 1.6:1**. 수정 = 배경을 `--bg` 쪽으로 섞고 좌측 열 글자를 **본문색(`--cream`)** 으로 → **10종 테마 전부 ≥7:1**(Ocean 19.6:1). 같은 색쌍이던 `.manual-code`도 함께 보정(Ocean 1.6 → 6.2:1).
>
> **⑤ 메뉴얼 2장 개발용 실행 안내** — `npm start` 문단을 **설치본 사용자 기준**(설치 파일 → 아이콘 실행 → 최근 세션 자동 복원 → 장치 설정 안내)으로 교체. 훑는 중에 발견한 **14장 `npm install` 안내**도 사용자가 할 수 있는 조치로 재작성. 생성된 메뉴얼에 두 명령 모두 0건.
>
> **⑥ 7장 Key 리스트 설명** — **지적이 맞았다.** 하단 리스트는 신뢰도 후보가 아니라 **읽기 전용 전체 조성 목록(장조 12·단조 12)** 이고 감지된 원곡 Key만 강조되며 선택도 불가하다. ko/en 캡션과 Detect 본문을 사실대로 정정.

---

> 처리 완료 — v1.43.23 (2026-07-23): **take 개수 배지 클릭 시 play bar가 원치 않게 이동되던 버그**. 원인 = take 배지 버튼([ui-tracks.jsx](ui-tracks.jsx))이 `onClick`에서만 `stopPropagation`을 했는데, play bar seek는 **부모 레인 div의 `onMouseDown`**(`laneClick`→`onSeek`)에서 일어남 → mousedown이 click보다 먼저 부모로 버블링돼 이미 seek가 실행되고, 뒤늦은 onClick의 stopPropagation은 무력. 수정 = 배지 버튼에 `onMouseDown={(e)=>e.stopPropagation()}` 추가로 mousedown이 레인 seek(및 트랙 선택)에 도달하지 못하게 차단(토글 onClick은 유지). **renderer 재빌드만·네이티브 재빌드 불필요.** 빌드 통과·정적 분석 확인. ⚠️ 사용자 시험 대기([시험.md](시험.md) T-1.43.23-1).

---

> 처리 완료 — v1.32.1 (2026-07-19): **play bar seek 버그 2건**. **문제점2**(01:66·02:66 — 마우스로 찍은 소수 초가 화살표 넛지에 계속 붙어 다님) 원인 = 화살표 넛지가 `seek(getPlayhead()+delta)`로 **현재 값에 ±1초만 더해** 정수 그리드로 스냅하지 않음([app.jsx](app.jsx)). **문제점1**(0에서 몇 초 깜박이다 뒤늦게 움직임) 원인 = 네이티브 `seek()`가 **음수 clamp 없음** + bridge가 native로 **unclamped `t` 전송** + 100ms 위치 브로드캐스트가 `nativeState.offset`을 native의 음수 playhead로 덮어씀([audio-bridge.js](audio-bridge.js)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)) → 왼쪽 화살표를 0 너머로 누르면 native playhead가 음수로 내려가 오른쪽 화살표를 눌러도 0에 닿을 때까지 화면 정지. 수정 = ① 넛지를 **정수초 그리드 스냅**(왼쪽 `ceil(cur)-1`/오른쪽 `floor(cur)+1`) ② `Bridge.seek`가 native로 보내기 전 `t`를 `[0,duration]`로 **clamp**(음수 완전 차단) ③ 네이티브 `seek`에 방어 clamp(다음 네이티브 재빌드 때 반영). **renderer 재빌드만·네이티브 재빌드 불필요.** 스냅 로직 시뮬레이션·부팅 무에러 확인. ✅ **사용자 시험 통과(2026-07-19, T-1.32.1-1)** → [시험-아카이브.md](시험-아카이브.md).

---

> 처리 완료 — v1.21.7 (2026-07-16): **클립 Join 후 Trim 시 join된 클립이 사라지던 버그**. 원인 = `joinClips`가 병합 클립을 `sourceOffset=first, duration=타임라인전체`로 만들어 **소스를 한 덩어리로 읽는데**(split 재결합 가정), 복사-붙여넣기한 비연속 클립은 소스 밖을 읽어 재베이크에서 뒤쪽 오디오가 스킵됨. join이 `_ensureBaked`도 안 해 trim 때 비로소 드러남([audio-engine.js](audio-engine.js)). 수정 = 비연속 join은 **Consolidate**(`_consolidateClips` — 두 오디오를 이어붙인 새 소스 생성, gain 반영+이음매 micro-fade), 연속(split 재결합)은 단일 병합 유지. split/join에 **`_ensureBaked` + Bridge 네이티브 sync 래퍼** 추가(누락 보강). 시뮬레이션 검증. renderer 재빌드만. ⚠️ 사용자 시험 대기([시험.md](시험.md) T-1.21.7-1).
>
> 처리 완료 — v1.21.6 (2026-07-16): **정지 중 클립 붙여넣기/편집 시 플레이바가 0으로 튀던 버그**(네이티브). 원인 = 클립 편집이 `loadTrack`으로 트랙을 재로드하는데 네이티브 `decodeAndInstallTrack`이 새 소스 위치를 플레이헤드로 맞추는 걸 **재생 중일 때만** 해서 정지 중엔 위치 0이 되고, `getPlayhead()`가 정지 중에도 `juceTracks[0]` transport(=0)를 읽어 상태 브로드캐스트가 플레이바를 0으로 끌어감([AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)). 수정 = `getPlayhead()`가 **정지 중에는 권위값 `playheadSeconds` 반환**(재생 중에만 트랜스포트 읽기). 자연 종료 0복귀 등 기존 동작 무회귀. **네이티브 재빌드→`bin/FocusDAW-AudioEngine.exe` 교체**. ⚠️ 사용자 시험 대기([시험.md](시험.md) T-1.21.6-1).
>
> 처리 완료 — v1.21.5 (2026-07-16): **Undo/Redo 후 클립 편집 시 다른 클립이 사라지던 버그** 수정. 원인 = `getSnapshot`이 편집 트랙의 **baked(타임라인 인덱싱·짧은) 버퍼를 그대로 저장** → `applySnapshot`의 트랙 재생성 경로가 이를 세팅하고 `_rawBuffers`가 비어, 다음 편집의 `_captureRawBuffers`가 **baked를 "원본 raw"로 캐시** → 재베이크에서 `sourceOffset`가 짧은 raw 끝을 넘는 클립(=사라진 c)이 `n<=0`으로 스킵됨([audio-engine.js](audio-engine.js) `_bakeTrackLayout`). 수정 = ① `getSnapshot`이 **원본 raw 저장**(`_rawBufferForSource`) ② `applySnapshot`이 복원 후 **raw 재시딩 + 재베이크**(다른 소스 raw 보존) ③ 베이크 스킵 시 **진단 `console.warn`**(재발 시 즉시 특정). undo/redo 사용 확인·시뮬레이션 검증. Phase 5 스냅샷 인프라 버그(v1.21.3/4 변경과 무관). renderer 재빌드만. ⚠️ 사용자 시험 대기([시험.md](시험.md) T-1.21.5-1).
>
> 처리 완료 — v1.21.4 (2026-07-16): **클립 이동 규칙 개선 + 화살표 정밀 넛지**. ① 드래그 이동을 **방향성 스냅**(끝 침범→앞 / 시작 침범→뒤 / 감쌈→가까운 쪽)으로 바꾸고, 안 맞으면 **원위치 복귀**(`_resolveMovePosition`, 드래그 중 고스트 실시간 미리보기). ② 선택 클립을 **화살표로 정밀 이동**(1ms / Ctrl 10ms / Shift 100ms, `nudgeClip`, 이웃 경계 butt-up, 연속 넛지 Undo 1회). 다중선택 통째 이동은 **v1.22.0**으로 이관. 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.21.4, 시험 T-1.21.4-1·2.
>
> 처리 완료 — v1.21.3 (2026-07-16): 클립 완전 겹침 버그(빈 편집불가 클립) 근본 수정 + Split/Join 우클릭 메뉴 노출.
>
> **클립 겹침·빈 클립**: 클립 복사 후 같은 자리(playhead)에 Ctrl+V를 두 번 하면 2차가 원본에 **정확히 겹쳐** 생성되고, 그 자리를 삭제하면 편집 불가 빈 클립이 남던 문제. 원인 = [audio-engine.js](audio-engine.js) `_clampStartNoOverlap`(겹침 방지)이 **단일 그리디 패스**라, 오른쪽 클립으로 밀렸다가 그 다음 클립과의 **동점 처리에서 이미 확인한 클립 위(s=0)로 되돌아가고 재검사하지 않아** 완전 겹침 생성. 수정 = ① 클램프를 **수렴형(겹치면 오른쪽 끝으로 밀며 깨끗해질 때까지 반복, s 단조증가로 종료)** 으로 교체 ② `_reindexClips`에 **"트랙 내 겹침 없음" 불변식 강제**(정렬 후 겹치면 뒤로 밀기) 안전망 추가 → 기존에 이미 만들어진 빈 클립도 다음 편집 때 자가치유. 재현 절차를 새 로직으로 시뮬레이션해 2차 붙여넣기 비겹침·사전 겹침 자가치유 확인. move/duplicate/paste 공통 함수라 일괄 해결.
>
> **Split/Join 우클릭 메뉴**: 툴바는 계속 숨긴 채 Audio In/Bounce 클립 우클릭 메뉴에 **Split(C)/Join(J)** 을 구분선 그룹으로 추가([ui-tracks.jsx](ui-tracks.jsx)). 메뉴 최상단 **Deselect(Esc)** 행 추가. **C/J 툴 고착 버그** 수정 = 공용 `handleDeselect`(선택 해제+`setTool("select")`)로 **ESC·Deselect가 툴을 select로 되돌림**([app.jsx](app.jsx)). Split은 우클릭 지점 분할, Join은 인접 병합, 둘 다 Undo 1회 유지.
>
> 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.21.3, 시험 [시험.md](시험.md) T-1.21.3-1·2. renderer 재빌드만·네이티브 재빌드 불필요. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.21.2 (2026-07-16): v1.21.1 클립 메뉴 회귀 5건 + Recent saved 목록 수정. ("1. 클립 move 이상없음"은 정상 확인.)
>
> **② 메뉴 위로 플레이바 관통 / ③ Delete·Duplicate 클릭 불가 / ⑤ Paste가 메뉴 클릭 위치에 붙음 — 원인 하나**: v1.21.1이 `ClipContextMenu`를 **트랙 레인 div 안에** 렌더한 것. ② 메뉴가 레인의 **쌓임 맥락**에 갇혀 DOM 뒤의 **OUTPUT FX 레인**이 위에 그려짐(사용자 지적이 정확했음). ③ 그 레인이 메뉴 아래쪽 행을 **덮어 클릭이 닿지 않음**(위쪽 Copy만 되던 이유). ⑤ 레인이 `onMouseDown`으로 seek하므로 메뉴 항목을 누르는 순간 플레이헤드가 그 위치로 끌려감. 수정 = `ReactDOM.createPortal(document.body)` + zIndex 4000, **그리고 React는 DOM이 아니라 React 트리로 이벤트를 전파하므로 포털만으론 부족** → 메뉴 루트에서 `stopPropagation()`, `startClipDrag`·레인 mousedown에 `e.button !== 0` 가드 추가.
>
> **④ Ctrl+V·Ctrl+D 무동작**: v1.21.1의 "바깥 클릭 시 선택 해제"가 레인 클릭(seek)에서도 선택을 지우는데 Ctrl+V가 대상 트랙을 selection에서 얻고 있었음(복사 → 위치 클릭 → 붙여넣기 흐름이 정확히 이 조합). 수정 = `lastClipTrackRef`로 대상 트랙을 selection과 분리 보관 → 선택이 없어도 Ctrl+V 동작. **Ctrl+D는 선택 필요**(= 선택 클립 복제)로 유지.
>
> **⑥ 빈 곳 우클릭 메뉴 없음**: 레인에 `onContextMenu` 추가 → 빈 구간 우클릭 시 `Paste at playhead` 단일 항목 메뉴(클립보드 비면 비활성).
>
> **프로젝트 이름 — Recent saved 미표시**: `MenuBar`가 `recentList.filter(item => item.id !== currentRecentId)`로 **현재 열린 프로젝트를 Recent saved에서 제외**하고 있었음(저장 직후엔 그게 곧 현재 프로젝트라 사라짐. v1.21.1 이전부터의 동작이며 이름 수정과 무관). 필터 제거 — Recent project(autosave 스냅샷)와 Recent saved(디스크의 .focus)는 의미가 다르므로 양쪽에 나오는 것이 맞음.
>
> 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.21.2, 시험 [시험.md](시험.md) T-1.21.2-1~3. renderer 재빌드만·네이티브 재빌드 불필요. 인앱 검증(메뉴 최상단 표시·Duplicate/Delete 실행·플레이헤드 불변·Paste 위치 정확·빈 레인 메뉴·Ctrl+V) 완료. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.21.1 (2026-07-16): 수정요청 3건 처리 + 인앱 검증 중 발견한 Phase 5 구멍 1건 동반 수정.
>
> **①  프로젝트 이름이 저장 파일명을 따르지 않던 문제**: 원인 = [app.jsx](app.jsx) `saveProject`가 Save 대화상자에서 고른 파일명을 전혀 반영하지 않음(json은 대화상자 **이전에** 옛 이름으로 export, recent 등록도 옛 이름). 게다가 열기 경로가 `json.projectName || projectNameFromPath()`로 **json 우선**이라 파일에 박힌 옛 이름이 계속 이김. 수정 = **"프로젝트 이름 = 파일 이름" 규칙 확립** — [electron/main.js](electron/main.js) `save-project`가 쓰기 직전 `stampProjectNameFromPath()`로 최종 파일명을 json에 새기고, `saveProject`는 저장 후 파일명을 채택해 rename + recent 등록, `loadProjectJson`은 **경로가 있으면 파일명이 이김**(구버전이 쓴 기존 파일도 교정). ⚠️ 부수 효과(의도): 경로 있는 프로젝트를 인앱 rename 후 저장하면 이름이 파일명으로 되돌아옴 → 다른 이름은 Save As로.
>
> **② 클립 선택 해제**: [app.jsx](app.jsx)에 Esc → `setSelectedClip(null)` + 전역 mousedown으로 클립 rect(`[data-clip-hit]`)·메뉴(`[data-clip-menu]`) 바깥 클릭 시 해제(bubble+`closest()` 마커라 다른 클립 클릭 시 선택이 지워지지 않음).
>
> **③ 클립 우클릭 메뉴**: [ui-tracks.jsx](ui-tracks.jsx) `ClipContextMenu` 신설 — Copy `Ctrl+C` / Paste at playhead `Ctrl+V`(클립보드 비면 비활성) / Duplicate `Ctrl+D` / Delete `Del` + 드래그·Esc 힌트. MenuBar Dropdown과 스타일 통일, 뷰포트 클램프.
>
> **④ (동반 수정) Bounce 트랙 클립 편집 무동작**: `fileTracks` 필터가 `kind==="bounce"`를 포함해 Bounce가 File Tracks 그룹에서 렌더되는데, Phase 5가 클립 편집 props를 `nonFileTracks` TrackRow에만 연결 → Bounce는 오버레이만 뜨고 이동/trim/Copy가 전부 no-op이었음. Phase 5 완료 기준 "Audio In/**Bounce**만 이동 가능"이 Audio In에서만 성립하던 상태(시험이 전부 녹음 클립으로 진행돼 미발견). fileTracks TrackRow에도 동일 props 전달로 수정(일반 파일 트랙은 `lockedToZero`라 무회귀).
>
> 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.21.1, 시험 [시험.md](시험.md) T-1.21.1-1~3. renderer 재빌드만·네이티브 재빌드 불필요. 개발자 인앱 검증(구동·Bounce 생성·메뉴·Copy→Paste 활성 전환·Esc) 완료. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.20.12 (2026-07-14): 특정 zoom-in 배율(트랙이 화면에 꽉 차는 부근, 실측 px/s≈13)에서 파형 아랫부분(음수 엔벨로프)이 통째로 사라지고 처음~끝을 잇는 직선만 남던 문제 수정. 원인 = [ui-tracks.jsx](ui-tracks.jsx) 파형 렌더의 `x1 = Math.min(drawW, ceil(clipEndX-drawStart))`에서 `drawW`가 소수(=길이×pxPerSec 또는 소수 host width)일 때 `x1`이 소수가 됨. 채우기 하단 루프는 `for(x=x1; x>=x0; x--)`로 **소수 x1에서 시작**해 `colMin[소수]`가 `Float32Array`에서 `undefined→NaN` → `lineTo(x,NaN)` 무시 → 하단 미표시(상단 루프는 정수 x0 시작이라 무사). 콘솔 계측으로 `x1=1990.077`·`topPx≫botPx` 확정. 수정 = `x1 = Math.min(Math.floor(drawW), ceil(clipEndX-drawStart))`로 정수 픽셀 열 보정. 임시 디버그 로그 제거. 상세 [앱개발.md](앱개발.md) v1.20.12, 시험 [시험.md](시험.md) T-1.20.12-1. renderer 재빌드만·네이티브 재빌드 불필요. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.20.11 (2026-07-14): 녹음 진행 중 Audio In 트랙 헤더·믹서 창의 ARM 버튼 조작 잠금. 승인 범위 = **실제 녹음 중 + 3초 카운트인 중** 모두 잠금. 수정 = ① 동작 잠금(권위): ARM 토글 두 진입점 [app.jsx](app.jsx) `param()`(헤더)·`SET_TRACK_PARAM`(믹서, 둘 다 메인 창 실행)에서 (녹음 활성 || 카운트인)이면 `arm` 변경을 무시(undo 스냅샷도 없음) → 진행 중 테이크 무장해제/입력 재라우팅 방지. ② 시각적 비활성화: 헤더 ARM([ui-tracks.jsx](ui-tracks.jsx) `TrackRow`→`TrackHeader` `recordingActive` prop), 믹서 ARM([ui-mixer.jsx](ui-mixer.jsx))은 잠금 플래그를 LEVEL_METERS에 `recLock`으로 실어 매 tick 반영([mixer-app.jsx](mixer-app.jsx) `window.DAW._recLock`), 잠금 시 disabled+흐림. MON은 요청 범위 밖이라 유지. 상세 [앱개발.md](앱개발.md) v1.20.11, 시험 [시험.md](시험.md) T-1.20.11-1·2. renderer 재빌드만·네이티브 재빌드 불필요. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.20.10 (2026-07-14): 재생 중 `+`로 Audio input 트랙 추가 시 플레이바 정지 후 처음으로 되감겨 재시작되던 문제 수정. 원인 = 부팅 시 디바이스를 **출력 전용**으로 열어 두어, `+`가 유발하는 **최초** `setAudioInput`이 입력 채널을 붙이려 **WASAPI 디바이스를 재오픈**(1~3초)하며 재생 콜백을 멈춤 → 정지·되감김. 한 번 열린 뒤엔 warm 빠른경로라 재발 없음(=최초 실행 지문). "매 실행 재현"은 시작 워밍업이 **이름 있는 저장 입력장치일 때만** 동작해 `name:""` 기본 입력을 놓쳤기 때문. 수정 = ① 시작 시 과거 입력 사용 이력(`AUDIO_INPUT_KEY`)이 있으면 **유휴 상태에서 기본 입력을 1회 워밍업**(`startup-warm`, 출력 보존, 실패해도 저장장치 미리셋) ② `addAudioInTrack`은 **재생 중이면 사전 워밍업을 건너뜀**. 입력 미사용 프로필은 워밍업 안 해 마이크 불필요 개방 없음. 상세 [앱개발.md](앱개발.md) v1.20.10, 시험 [시험.md](시험.md) T-1.20.10-1·2. renderer 재빌드만·네이티브 재빌드 불필요. ⚠️ 사용자 시험 대기.

---

> 처리 완료 — v1.18.11 (2026-07-11): 플레이바와 녹음 바의 진행율(스케일) 불일치 수정. 원인 = `recordingPeaks`가 라이브 진행바 타이밍에 쓰는 sampleRate로 **init 시 출력 장치에서 캐시된 `engine.sampleRate`**를 보고했는데, `setAudioInput()`이 이를 갱신하지 않아 **입력 SR ≠ 초기 출력 SR**이면 시간축이 비율로 어긋남(캐시 SR이 크면 진행바가 느리게 → 정지 시 파형이 확 늘어남, 동시 재생 시 플레이바가 더 빠름). 수정 = `InputRecorder`가 take 실제 SR(WAV 헤더와 동일)을 보관·노출하고 `recordingPeaks`가 이를 보고하도록 변경 + `setAudioInput`에서 `engine.sampleRate` 캐시 갱신. 상세 [앱개발.md](앱개발.md) v1.18.11, 시험 [시험.md](시험.md). ⚠️ 네이티브 재빌드 + 사용자 시험 대기.

> 처리 완료 — v1.18.6 (2026-07-09): ① Audio In 짧은 녹음 트랙이 1초마다 반복되던 문제 — 네이티브가 트랙을 자기 버퍼 길이로 루프하던 것을 `loadTrack`에 `startSeconds`·`songLength`를 넘겨 [앞 침묵+오디오+뒤 침묵(곡 길이)]로 패딩, 곡 경계에서 루프 + 녹음 시작 위치 반영(50ms 허용오차로 스템 무회귀). ② 믹서 창 Audio In 채널 배경을 블루 틴트로 구분. ③ 마스터/채널 볼륨 페이더를 선형 게인→dB(로그) 테이퍼(−60 dB 플로어, −∞)로 변경해 이동량 대비 체감 음량을 균일화. 상세 [앱개발.md](앱개발.md) v1.18.6, 시험 [시험.md](시험.md). ⚠️ 네이티브 재빌드 + 사용자 시험 대기.

> 처리 완료 — v1.17.8 (2026-07-08): B-207 후속 `NO AUDIO` Solo 불일치 및 다른 위치 새 오디오 트랙 저장/재오픈 재연결 보강.

---

> 처리 완료 — v1.17.7 (2026-07-07): B-206 후속 프로젝트 로드/누락 오디오 재연결/프로젝트 drag&drop/Save As 메뉴 수정.

---


> 직전 처리 완료 — v1.17.2 (2026-07-05): 믹서를 닫았다 다시 열 때 노브 펄스가 안 나오던 버그 수정 (v1.17.0 후속)
> - 증상: 믹서가 열려 있을 땐 재클릭 펄스 정상. 믹서를 닫은 뒤 배지 클릭하면 창은 열리는데 펄스 안 나옴(다시 열린 상태 재클릭은 나옴).
> - 원인: Electron 믹서 "닫기"는 파괴가 아니라 hide → 렌더러 생존, `MIXER_READY`는 최초 1회만 발생. FOCUS_KNOB pending flush를 MIXER_READY에서만 처리해 재오픈 시 flush 안 됨.
> - 수정: 믹서가 다시 보일 때마다 `visibilitychange`→`MIXER_SHOWN` 발신([mixer-app.jsx](mixer-app.jsx)), 메인은 MIXER_SHOWN에서도 pending flush([app.jsx](app.jsx)). 이미 보이는 상태 클릭은 focusMixerFx에서 즉시 flush. Electron 로그로 open→close→reopen 전 구간 펄스 확인.
> - 대상: [app.jsx](app.jsx)·[mixer-app.jsx](mixer-app.jsx), `build:renderers` 재빌드.
> - 상세 [앱개발.md](앱개발.md) v1.17.2, 시험 [시험.md](시험.md) B-205. ⚠️ 사용자 시험 대기.
>
> 직전 처리 완료 — v1.17.1 (2026-07-05): 노브 펄스가 "다시 클릭" 시 안 나오던 버그 수정 (v1.17.0 후속)
> - 증상: 배지 클릭 1회는 정상(2번 깜빡)인데 곧이어 다시 클릭하면 펄스가 안 보임.
> - 원인: 펄스 종료용 `setTimeout(1400)`가 다음 클릭의 애니메이션 도중 발동해 클래스를 제거→재시작된 펄스를 죽임(직전 타이머 잔존 레이스). Electron 로그로 확인(2번째 트리거 `hadTimer: true`).
> - 수정: 재트리거 시 이전 타이머를 clearTimeout으로 취소 후 클래스 재적용(`el._fxPulseTimer`). [mixer-app.jsx](mixer-app.jsx). `build:renderers` 재빌드.
> - 상세 [앱개발.md](앱개발.md) v1.17.1, 시험 [시험.md](시험.md) B-205. ⚠️ 사용자 시험 대기.
>
> 직전 처리 완료 — v1.17.0 (2026-07-05): 트랙 헤더 VRB/ECHO 배지 클릭 → 믹서 열고 해당 노브 강조 + `VOL AUTO`→`AUTO` 라벨 단축
> - VRB/ECHO 배지를 클릭 가능하게 하여, 클릭 시 믹서 창을 열고(이미 열려 있으면 앞으로) 해당 트랙 리버브/에코 노브를 화면 중앙 스크롤 + 펄스(glow)로 강조. 사용자가 바로 조절 가능.
> - 실제 OS 커서 이동은 네이티브 모듈 필요라 제외(사용자 협의). 믹서가 별도 창이라 기존 BroadcastChannel에 `FOCUS_KNOB{trackId,param}` 신설, 여는 중이면 `MIXER_READY`에 flush. 믹서는 `[data-track-id][data-fx]`로 노브 찾아(없으면 rAF 재시도) scrollIntoView + `.fx-knob-pulse`.
> - 플럼빙: [ui-tracks.jsx](ui-tracks.jsx)(FxTag onClick→onFocusFx), [app.jsx](app.jsx)(focusMixerFx·FOCUS_KNOB), [ui-mixer.jsx](ui-mixer.jsx)(data-fx 태그), [mixer-app.jsx](mixer-app.jsx)(focusFxKnob·펄스). Electron 실구동으로 클릭→오픈→수신→펄스 로그/스크린샷 확인.
> - `VOL AUTO`→`AUTO` 라벨 단축(툴팁 유지) [ui-tracks.jsx](ui-tracks.jsx).
> - 대상: [app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx)·[ui-mixer.jsx](ui-mixer.jsx)·[mixer-app.jsx](mixer-app.jsx), `build:renderers` 재빌드. 네이티브 재빌드 불필요.
> - 상세 [앱개발.md](앱개발.md) v1.17.0, 시험 [시험.md](시험.md) B-205. ⚠️ 사용자 시험 대기.
>
> 직전 처리 완료 — v1.16.0 (2026-07-05): 트랙별 리버브/에코 웹·네이티브 동작 통일 + 헤더 VRB/ECHO 인디케이터 추가
> - ① 에코 파라미터 통일: 웹 per-track echo(딜레이 0.25s·wet=노브값)를 네이티브 기준(딜레이 0.3s·feedback 0.34·wet=노브×0.45)에 맞춤. 실시간+오프라인(Export) 그래프 모두. echoReturn(0.45) 게인 추가·delayTime 0.3. n번째 에코 = 입력×feedback^(n-1)×(노브×0.45)로 두 엔진 동일. [audio-engine.js](audio-engine.js)(네이티브 무변경).
> - 리버브는 알고리즘이 다르나(웹 컨볼루션 / 네이티브 Freeverb) 네이티브 wetLevel 0.4가 이미 웹에 맞춰 튜닝돼 레벨 정합 → 유지. (bit-identical은 네이티브 컨볼루션 포팅 필요, 범위 외.)
> - ② VRB/ECHO 인디케이터: 트랙 헤더 VOL AUTO 옆에 VRB(violet)·ECHO(blue) 배지, 값>0.001일 때만 표시. 위치 고정 위해 visibility:hidden으로 슬롯 예약(둘 중 하나만 켜져도 안 밀림). 색은 믹서 노브와 동일. [ui-tracks.jsx](ui-tracks.jsx) FxTag.
> - 헤더 244px가 좁아 배지 예약 공간에 VOL AUTO가 줄바꿈되던 문제 → 배지 compact(7.5px)+VOL AUTO nowrap/flex:0 0 auto로 한 줄 유지. Electron 실구동 스크린샷 확인.
> - 대상: [audio-engine.js](audio-engine.js)·[ui-tracks.jsx](ui-tracks.jsx), `build:renderers` 재빌드. 네이티브 재빌드 불필요.
> - 상세 [앱개발.md](앱개발.md) v1.16.0, 시험 [시험.md](시험.md) B-203·B-204. ⚠️ 네이티브 연결 상태에서 에코 청감 비교 사용자 시험 대기.
>
> 직전 처리 완료 — v1.15.4 (2026-07-05): 재생 중 seek-back + zoom-in 시 플레이바가 (일반+Output FX) 트랙 헤더까지 침범하던 버그 수정
> - 증상: 재생 도중 현재 재생 시점보다 뒤로 seek + time zoom-in 하면 플레이헤드 세로선이 레인을 벗어나 좌측 sticky 헤더까지 그려짐(첨부 이미지). 일반 트랙뿐 아니라 Output FX 트랙 레인에서도 발생.
> - 원인: 레인 플레이헤드 `zIndex:10`이 sticky 헤더 `zIndex:6`보다 높음. 레인이 z-index auto라 stacking context를 못 만들어 레인 안 요소(플레이헤드·Output FX loop 오버레이)가 헤더와 직접 경쟁 → 화면 x=`HEADER_W+phx−scrollLeft`가 `phx<scrollLeft`(플레이헤드 좌측 스크롤 아웃)일 때 헤더 구간(0..HEADER_W)에 들어오며 헤더 위로 덮음. (플레이헤드 `zIndex:10`은 v1.14.15 도입, 휠 줌 v1.15.0이 표면화.)
> - 수정: 각 레인 div에 `isolation:isolate`를 주어 레인을 독자 stacking context로 만듦 → 헤더가 레인 전체보다 위에 그려져 레인 안 어떤 요소도 헤더를 넘지 못함(레인 내부 순서는 유지, 회귀 없음). 일반 트랙·Ruler 레인([ui-tracks.jsx](ui-tracks.jsx)) + Output FX `.outlane`([ui-mixer.jsx](ui-mixer.jsx)) 적용.
> - 대상: [ui-tracks.jsx](ui-tracks.jsx)·[ui-mixer.jsx](ui-mixer.jsx), `build:renderers` 재빌드 반영. 네이티브 재빌드 불필요.
> - 상세 [앱개발.md](앱개발.md) v1.15.4, 시험 [시험.md](시험.md) B-202. ⚠️ 사용자 시험 대기.
>
> 직전 처리 완료 — v1.15.3 (2026-07-05): Repeat 중 구간 밖 클릭 무시 + 드래그 선택/창 테두리 점선 방지
> - ① Repeat ON 재생 중 구간 밖 클릭 동작 비일관(이전 클릭=밖에서 재생 / 이후 클릭=구간 시작 튕김) → "구간 밖 클릭은 무시" 규칙으로 통일. 브리지 `userSeek(t)` 신설(구간 밖 타깃이면 no-op), [app.jsx](app.jsx) 타임라인 클릭 seek 3곳을 userSeek로 교체. 명시적 트랜스포트(0/화살표)는 제한 없음.
> - ② 앱 내부 텍스트/요소가 HTML5 드래그로 마우스에 붙고 창 테두리에 굵은 점선(드롭존 아웃라인)이 그려짐 → [studio.html](studio.html) 전역 `dragstart` preventDefault + `user-select:none`(입력 예외)·`-webkit-user-drag:none`, [app.jsx](app.jsx) 드롭존을 `Files` 드래그일 때만 활성. OS 파일 드롭 임포트는 영향 없음.
> - 대상: [audio-bridge.js](audio-bridge.js)·[app.jsx](app.jsx)·[studio.html](studio.html). app.jsx는 `build:renderers` 재빌드 반영, 네이티브 재빌드 불필요.
> - 상세 [앱개발.md](앱개발.md) v1.15.3, 시험 [시험.md](시험.md) B-201. ⚠️ 네이티브 연결 상태에서 사용자 시험 대기.
>
> 직전 처리 완료 — v1.15.2 (2026-07-05): 구간 반복(Repeat) ON 시 플레이바가 구간으로 진입 못 하던 버그 수정 (토글 + 드래그 종료 스냅)
> - 증상: 재생 중 플레이바보다 뒤쪽(이후 시점)에 반복 구간을 (재)설정하면 화면 플레이바만 구간으로 들어온 듯 보이고 실제 오디오는 밖에서 선형 재생.
> - 원인: 반복 유지 로직이 끝(end) 경계로만 설계(`offset >= end`일 때만 seek)되어 '구간 앞(offset < start)'은 교정 로직 없음 + `getPlayhead` clamp가 시각적으로 가림. (반복 기능·wrap은 v1.14.15 도입분이며 휠 줌 v1.15.0과 무관함을 git 이력으로 확인.)
> - 1차(v1.15.1): `setRepeatPlayEnabled`가 플래그 켜기 전에 참 위치를 읽어 seek → Repeat 토글 순간 커버. 정지 스냅 위해 `isPlaying` 게이트 제거.
> - 2차(v1.15.2): repeat가 이미 켜진 채 구간을 드래그/이동하는 경로 대응. (wrap에 `offset < start` 상시 추가는 seek 폭주로 '징~~~' 잡음 → 원복.) 대신 브리지 `snapPlayheadToLoop()` 신설 — **드래그 종료(mouseup) 시 1회**, raw 위치로 구간 밖이면 seek. 잡음 없음. [ui-mixer.jsx](ui-mixer.jsx) 드래그 `up` 핸들러에서 호출.
> - 대상: [audio-bridge.js](audio-bridge.js)·[audio-engine.js](audio-engine.js)·[ui-mixer.jsx](ui-mixer.jsx). ui-mixer는 `build:renderers` 재빌드 반영, 네이티브 재빌드 불필요.
> - 상세 [앱개발.md](앱개발.md) v1.15.1→v1.15.2, 시험 [시험.md](시험.md) B-200. ⚠️ 네이티브 연결 상태에서 사용자 시험 대기.
>
> 직전 처리 완료 — v1.15.0 (2026-07-05): Timeline zoom/스크롤 마우스 휠 단축키 추가
> - Ctrl(+Cmd)+휠 = 커서 위치 중심 zoom in/out, Shift+휠 = 좌우 이동. 수식어 없는 휠은 기존 세로 스크롤 유지.
> - [app.jsx](app.jsx) `Studio`의 arrange 스크롤 영역(`arrangeRef`)에 `{passive:false}` wheel 리스너 추가(React 17 passive 위임 회피). 확대 시 커서 아래 time을 화면상 고정하도록 목표 `scrollLeft`를 `pendingZoomScrollRef`에 담아 `pxPerSec` 변경 시 `useLayoutEffect`에서 적용. zoom 범위는 `[timelineMinPx, TIME_ZOOM_MAX]` 클램프.
> - 상세 [앱개발.md](앱개발.md) v1.15.0, 시험 [시험.md](시험.md) B-199. ⚠️ 사용자 시험 대기(코드만 수정, 네이티브/재패키징 불필요).
>
> 직전 처리 완료 — v1.14.4 (2026-06-21): 트랙 헤더 `-` 삭제 후에도 삭제된 트랙이 계속 재생되던 버그 수정
> - 원인: [app.jsx](app.jsx) `removeTrack`이 `DAW.tracks.splice`로 배열에서만 제거하고 엔진에 미통지 → ① 웹 엔진의 라이브 `BufferSource`가 fader→masterBus에 연결된 채 계속 재생, ② 네이티브 엔진엔 트랙 삭제 명령 자체가 없었음.
> - 수정: [audio-engine.js](audio-engine.js)에 `removeTrack(id)`(라이브 소스 stop+노드 disconnect) 신설, [audio-bridge.js](audio-bridge.js)에 네이티브로 `removeTrack` 명령 포워딩, [app.jsx](app.jsx)는 `DAW.removeTrack(id)`에 위임. 네이티브 [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)/[.h](juce_skeleton/Source/AudioEngine.h)·[WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp)에 `removeTrack` 구현·디스패치 추가.
> - 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.14.4, 시험 [시험.md](시험.md) B-197. ⚠️ 네이티브 재빌드(`build_native.bat`) 및 사용자 시험 대기.
>
> 직전 처리 완료 — v1.14.2 (2026-06-20): Dist에서 BPM·Key 감지 안 됨 — meyda.min.js 패키징 누락 수정
> - 원인: BPM·Key 감지가 의존하는 전역 `Meyda`(studio.html이 루트 `meyda.min.js` 로드)가 dist에 누락. [package.json](package.json) `build.files`에 `meyda.min.js`가 빠져 asar에 미포함 → dist에서 meyda 404 → `Meyda` undefined → `estimateBpm`은 null·`accumulateChroma`(Key)는 false를 조용히 반환. `npm start`는 소스 폴더에 meyda가 있어 정상.
> - 수정: `build.files`에 `"meyda.min.js"` 추가. 재발 방지로 [audio-engine.js](audio-engine.js)에 Meyda 미로드 시 최초 1회 콘솔 경고(`warnMeydaMissing`) 추가.
> - 상세 [앱개발-아카이브.md](앱개발-아카이브.md) v1.14.2, 시험 [시험.md](시험.md) B-195. ⚠️ 새 dist 재패키징(`npm run dist:win`) 및 사용자 시험 대기.
>
> 직전 처리 완료 — v1.14.0 (2026-06-20): Fade in/out 네이티브 엔진 적용 (재생 + Export) ✅ B-194 사용자 시험 완료
> - 네이티브(JUCE) 엔진에 fade 처리가 전무하던 문제 해결. 마스터 체인 출력단에 출력-샘플 카운터 기반 1회성 페이드(웹 fadeVal과 동일 선형)를 추가해 실시간 재생·오프라인 Export 모두 적용. 브리지가 fade 파라미터를 export 명령·연결/New Project/Import 시 푸시.
> - 네이티브 재빌드(build_native.bat) → bin/FocusDAW-AudioEngine.exe 갱신. 상세 [앱개발.md](앱개발.md) v1.14.0, 시험 [시험.md](시험.md) B-194.
>
> 직전 처리 완료 — v1.13.2 (2026-06-20): Advanced Pan Gain 슬라이더 추가 보완 ✅ B-193 사용자 시험 완료
> - ① dB 자릿수 변동(+1.9↔-11.0) 시 레일 흔들림 제거(오버레이 고정 폭) ② studio 파형 saturation(peak×volume>1) 캐치 → 슬라이더 적색 glow(엔진 `peakAmp` 전달).
> - 상세 [앱개발.md](앱개발.md) v1.13.2·#174, 시험 [시험.md](시험.md) B-193.
>
> 직전 처리 완료 — v1.13.1 (2026-06-20): Advanced Pan Gain 슬라이더 보완 ✅ B-192 사용자 시험 완료
> - ① 머리말 트랙 이름 → "Gain(dB)" 고정(색은 트랙색 유지) ② 하단 캡션 'Gain'→'Silent' ③ 슬라이더 조작 후 선택 유지(조작 시 슬라이더가 사라지던 버그 수정).
> - 상세 [앱개발.md](앱개발.md) v1.13.1·#174, 시험-아카이브 [시험-아카이브.md](시험-아카이브.md) B-192.
>
> 직전 처리 완료 — v1.13.0 (2026-06-20): Advanced Pan 선택 트랙 세로 Gain 슬라이더 오버레이 추가 ✅ B-192 사용자 시험 완료
> - advanced-pan.html 방사형 음장 오른쪽에, 트랙 선택 시 세로 Gain 슬라이더 오버레이 표시. 드래그로 게인 조절(0~2 선형, 중앙=0dB), 음장 노드와 양방향 동기화. 트랙 색·현재 dB·눈금(+6/0/−6/−12/−∞) 표시.
> - 상세 [앱개발.md](앱개발.md) #174·v1.13.0, 시험-아카이브 [시험-아카이브.md](시험-아카이브.md) B-192.
>
> 직전 처리 완료 — v1.10.1 (2026-06-19): Key 설정창 개편 + 시험 후 개선
> - v1.10.0: Key 설정창 Detect 아래에 원Key 표시창 + +/- 스텝퍼(오프셋 ±6) + Apply 버튼 추가, "OR SET"→"KEY SET". Detect는 원Key(`detectedKey`)만 채워 하단 표시·상단 `--` 유지, **Apply**를 눌러야 상단 적용 Key(`key`)+오프셋 첨자 갱신. Key 리스트는 원곡 Key 강조만(선택 불가). Vari Key 스위치는 재생 변조 여부만 제어(유지).
> - v1.10.1(B-191 시험 후 개선): ① 오프셋 숫자가 +/- 버튼에 가려지던 것을 [표시창 | 숫자 | 버튼(2)] 3열 배치로 변경. ② 패널이 길어져 하단 Key 리스트가 잘려 안 보이던 문제(`keyPanelDrop` max-height 200→360px)를 수정해 리스트 상시 표시.
> - 상세 [앱개발.md](앱개발.md) #172·#173, 시험 [시험.md](시험.md) B-191.
>
> 직전 처리 완료 — v1.9.22 (2026-06-19): Keep pitch 연동 및 Advanced Ambience 하단 액티브 이펙트 나열
> - #170 studio.html Export 창에서 variBPM 스위치 상태에 따라 Keep pitch 활성화/비활성화 및 초기값 연동.
> - #171 Advanced-ambience.html 하단 bottom bar 오른쪽에 현재 적용된 5대 이펙트(Reverb, Delay, Saturation, Widener, Exciter) 목록 나열.
> - 상세 [앱개발.md](앱개발.md) #170, 시험 [시험.md](시험.md) B-189, B-190.
>
> 직전 처리 완료 — v1.9.17 (2026-06-18): 오토메이션 **동시성 하드닝**(힙 손상 0xC0000374 위험 제거)
> - #165 오토메이션이 `std::vector`를 오디오/export 스레드에서 락 없이 읽어, 재생/익스포트 중 편집 시 use-after-free(STATUS_HEAP_CORRUPTION) 가능했음.
> - **불변 스냅샷 + 원자적 교체**(`std::shared_ptr<const TrackAutomation>` + autoMutex)로 변경 — 편집은 새 객체로 교체, 읽기는 락 하에 복사 후 lock-free 사용. 무회귀(결과 비트 동일) 확인.
> - 상세 [앱개발.md](앱개발.md) #166, 시험 [시험.md](시험.md) B-188(항목 6).
>
> 직전 처리 완료 — v1.9.16 (2026-06-18): 네이티브 Export에 **볼륨 오토메이션** 적용
> - 네이티브 JUCE export는 정적 볼륨만 곱해 오토메이션이 빠져 있었음(Web Audio 경로만 적용). 네이티브 엔진에 오토메이션 평가를 이식.
> - 새 `setTrackAutomation` 명령(interleaved `[t,v]` 평탄 배열·autoOn·curved)으로 브리지→네이티브 전송. `TrackAudioSource`가 직선/단조큐빅(Web Audio `makeAutoSampler`와 동일)으로 평가해 offline 렌더에서 `applyGainRamp`로 적용. exportMix가 블록마다 글로벌 phase 설정.
> - 검증(raw WS probe): 페이드/ V-dip이 결과 파일에 반영됨(직선·곡선 모두). 새 exe를 [bin/FocusDAW-AudioEngine.exe](bin/FocusDAW-AudioEngine.exe)·dist 갱신.
> - 상세 [앱개발.md](앱개발.md) #165, 시험 [시험.md](시험.md) B-188.
>
> 직전 처리 완료 — v1.9.15 (2026-06-18): **Export 무음 근본 원인 2건 확정·수정**
> - **주 원인**: 마스터 9밴드 EQ의 `juce::dsp::ProcessorDuplicator` 계수 미전파 버그 — `prepare()` 후 `state` 포인터 재할당이 채널 필터에 반영 안 돼 **빈(0) 계수로 EQ가 신호를 0으로** 만듦(0dB 평탄에서도). 실시간 JUCE도 무음이었으나 Web Audio 재생을 들어 미인지. → 계수를 **제자리 복사**(`*state = *newCoeffs`)로 수정.
> - **부 원인**: 오프라인 export read를 transport 우회 direct-read(리샘플링 누락)에서 **실시간과 동일한 `transportSource` 경로**로 통일.
> - 단계별 magnitude 계측(raw WS probe)으로 `postEQ=0`을 확정. 수정 후 다중 샘플·normalize on/off에서 정상 음량 export 확인(`Measured LUFS` −70 사라짐). 새 exe를 [bin/FocusDAW-AudioEngine.exe](bin/FocusDAW-AudioEngine.exe)·dist에 반영.
> - 상세 [앱개발.md](앱개발.md) #164, 시험 [시험.md](시험.md) B-187.
>
> 그 이전(같은 무음 이슈 추적 단계) — v1.9.14: LUFS raw RMS fallback(#163). v1.9.13: 트랙별 peak 계측(#162). v1.9.12: PreservePitch OFF SoundTouch 우회(#161). v1.9.11: offline direct-read(#160). v1.9.10: realtime 콜백 suspend(#159). v1.9.8: fallback 표시(#157). v1.9.7: 무음 방지 fallback(#156).

> 직전 처리 완료 — v1.9.13 (2026-06-18): JUCE Offline Export 트랙별 peak 계측 추가
> - `tracks=9, soundTouch=0`인데도 `Measured LUFS: -70`이 반복되어, 트랙별 reader 출력과 파라미터 적용 후 출력을 분리해서 확인하는 probe 로그를 추가.
> - 새 로그: `Offline track probe: id=..., sourcePeak=..., postPeak=..., volume=..., pan=..., mute=..., solo=..., soloActive=..., readPos=..., totalLength=..., soundTouch=...`
> - mute/solo 차단 시 `Offline track muted: ...` 로그가 표시됨. 상세 [앱개발.md](앱개발.md) #162, 시험 [시험.md](시험.md) B-185.

> 직전 처리 완료 — v1.9.12 (2026-06-18): PreservePitch OFF export에서 SoundTouch 우회
> - `tracks=9`인데도 `Measured LUFS: -70`이 반복되어, `preservePitch:false` 일반 export가 불필요하게 SoundTouch 경로를 타는 문제로 범위를 좁힘.
> - `preservePitch:false`에서는 `AudioFormatReaderSource`를 직접 읽고, `preservePitch:true`에서만 SoundTouch direct-read를 사용하도록 수정.
> - 새 로그 `Offline direct track rendering enabled: tracks=N, soundTouch=0/1`로 적용 여부 확인 가능. 재현 조건에서는 `soundTouch=0`이 떠야 함. 상세 [앱개발.md](앱개발.md) #161, 시험 [시험.md](시험.md) B-184.

> 직전 처리 완료 — v1.9.11 (2026-06-18): JUCE Offline Export direct-read 렌더링 적용
> - v1.9.10에서 realtime callback 분리 후에도 `Measured LUFS: -70`이 반복되어, offline 렌더가 `AudioTransportSource`에서 빈 버퍼를 받는 문제로 범위를 좁힘.
> - offline export 중에는 transport를 우회해 `SoundTouchAudioSource`/`AudioFormatReaderSource`를 직접 읽도록 `TrackAudioSource`에 `offlineRendering` 모드를 추가.
> - 새 로그 `Offline direct track rendering enabled: tracks=N`으로 적용 여부 확인 가능. 새 `FocusDAW-AudioEngine.exe`를 [bin/FocusDAW-AudioEngine.exe](bin/FocusDAW-AudioEngine.exe) 및 기존 `dist/win-unpacked` 패키징 폴더에 복사. 상세 [앱개발.md](앱개발.md) #160, 시험 [시험.md](시험.md) B-183.

> 직전 처리 완료 — v1.9.10 (2026-06-18): JUCE Offline Export native 무음/fallback 표시 원인 수정
> - `JUCE Offline Export completed successfully` 후에도 `Measured LUFS: -70`으로 측정되던 경우는 native 임시 WAV가 사실상 무음이라 앱이 Web Audio fallback으로 다시 렌더한 흐름으로 확인.
> - offline export 중 실시간 오디오 디바이스 callback이 같은 source graph를 pull하지 못하도록 `sourcePlayer` callback을 일시 분리하고 export 종료 후 복구하도록 수정.
> - 새 `FocusDAW-AudioEngine.exe`를 [bin/FocusDAW-AudioEngine.exe](bin/FocusDAW-AudioEngine.exe) 및 기존 `dist/win-unpacked` 패키징 폴더에 복사. 상세 [앱개발.md](앱개발.md) #159, 시험 [시험.md](시험.md) B-182.


> 직전 처리 완료 — v1.9.9 (2026-06-18): JUCE/CMake 빌드 환경 준비 및 네이티브 엔진 exe 재생성
> - Visual Studio BuildTools 내장 CMake를 사용하고, JUCE v8.0.10을 `third_party/JUCE`에 설치해 Release 빌드 성공.
> - 새 `FocusDAW-AudioEngine.exe`를 [bin/FocusDAW-AudioEngine.exe](bin/FocusDAW-AudioEngine.exe) 및 기존 `dist/win-unpacked` 패키징 폴더에 복사.
> - 새 exe에 네이티브 Export 트랙 0개 방지 가드가 포함된 것을 문자열 검사로 확인하고, `--port 18083` 스모크 실행으로 초기화 로그 확인. 상세 [앱개발.md](앱개발.md) #158, 시험 [시험.md](시험.md) B-181.
>
> 그 이전 — v1.9.8: Web Audio fallback 발생 여부 표시(#157). v1.9.7: Export 무음 방지 fallback(#156). v1.9.6: Export 오프라인 렌더에 Saturation·Widener·Exciter 누락분 반영(#155). v1.9.5: 사용자 메뉴얼 대규모 업데이트(#154). v1.9.4: 초기 화면 Import Folder 폴더명→프로젝트명(#153). v1.9.0: Edit ▸ "Delete all tracks"(#152). v1.8.8: SoundTouchAudioSource double-free(#151). v1.8.7: getPlayhead 무락 접근(#150). v1.8.6: EQ 계수 스레드 경합(#149).
> 사용자 확인 완료는 `시험-아카이브.md`로 이동함.
