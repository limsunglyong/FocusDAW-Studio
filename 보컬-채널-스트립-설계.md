# 보컬 채널 스트립 — 설계 문서

> 작성: 2026-07-24 · 개정: 2026-08-04 · 상태: **✅ 구현 완료 — Stage A~F 전 단계 시험 통과(v1.44.0~v1.44.25)**. 이 문서는 이제 설계 의도·결정 이력의 기록이며, 실제 구현과 다른 부분은 §9 표와 §11에 명시했다(특히 **C는 analyser 실시간 → 정적 FFT로 변경**). 후속은 §8 Pitch Editor뿐. · 관련: [(승룡)해야할일.txt](<(승룡)해야할일.txt>), [앱개발.md](앱개발.md) "남은 항목 및 계획", UI 목업 [_refer/Vocal Channel Strip 2.dc.html](<_refer/Vocal Channel Strip 2.dc.html>)
>
> **목적** — SUNO stem(MR) 위에 사용자가 보컬을 녹음해 완성본을 만드는 워크플로에서, **스템 트랙과 보컬 트랙의 이펙트를 분리**하고 보컬 트랙에 **채널 스트립**(HPF · Noise Gate · 9밴드 EQ · Compressor · De-esser)과 **적용 전/후 FFT 스펙트럼 비교** + **오프라인 광대역 De-noise**를 제공한다.

## 사용자 결정 (2026-07-24 → 2026-07-25 확정)

- **이펙트 범위**: 인서트 채널 스트립 — **HPF + Noise Gate + 9밴드 EQ + Compressor + De-esser + 광대역 De-noise(오프라인)**.
- **Fader / Pan 제외**: 트랙 볼륨(fader)·팬은 **이미 트랙 노드로 존재** → 채널 스트립 신규 개발에서 **제외**. 기존 컨트롤을 그대로 쓴다.
- **Reverb / Echo 제외**: 트랙별 reverb/echo 센드는 **이미 웹·네이티브 양쪽에 구현** → 신규 개발에서 **제외**. 스트립에 노출하지 않는다.
- **EQ**: **9밴드 그래픽 EQ**(마스터 EQ 인프라 재사용)로 확정.
- **FFT 스펙트럼 비교**: 적용 **전/후 실측 FFT** 비교를 **1단계부터 함께 구현**(시뮬레이션 아님).
- **Pitch Editor**: 피아노롤 기반 피치 보정은 **별도 후속 페이즈**(이 문서 범위 밖, UI 셸만 참고).
- **모니터링**: **재생 경로만**(1단계). 실시간 입력 모니터에 이펙트 거는 것은 범위 밖.
- **착수 방식**: 설계 문서(이 문서) → 단계별 구현.
- **UI 목업**: [_refer/Vocal Channel Strip 2.dc.html](<_refer/Vocal Channel Strip 2.dc.html>)은 **시각 스펙**일 뿐(프로토타입 런타임). 앱의 `.jsx`/테마 변수 체계에 맞게 재구현한다.

---

## 1. 현재 이펙트 구조 (착수 기준선)

| 위치 | 이펙트 | 대상 | 코드 |
|------|--------|------|------|
| 트랙별 | reverb send, echo(FeedbackDelay), **volume(fader), pan** | **모든 트랙** | 웹 [audio-engine.js:772-790](audio-engine.js#L772-L790) · 네이티브 [AudioEngine.h:771-796](juce_skeleton/Source/AudioEngine.h#L771-L796) |
| 마스터(Output FX) | 9밴드 EQ, reverb, echo, widener, saturation, exciter, room, fade | **전체 믹스** | [audio-engine.js:619-714](audio-engine.js#L619-L714) · [AudioEngine.h:1070+](juce_skeleton/Source/AudioEngine.h#L1070) |

핵심:
- **트랙별 fader·pan·reverb·echo는 이미 존재.** → 채널 스트립은 이들을 **재사용/우회**하고 신규로 만들지 않는다.
- **HPF · Noise Gate · per-track EQ · Compressor · De-esser · De-noise는 전무.** 이게 실제 신규 개발.
- **적용 전/후 FFT 스펙트럼 비교 UI도 신규.**
- 트랙에 `kind`(`file`/`audioIn`/`bounce`) 구분이 이미 있다([audio-engine.js:795](audio-engine.js#L795)) → **kind 게이팅으로 "스템엔 없고 보컬에만 있는 이펙트"가 구조적으로 자연스럽다.**
- **저장은 `params: { ...t.params }` 통째 직렬화**([app.jsx:1422](app.jsx#L1422)) → **새 파라미터는 저장/복원 자동.** 구프로젝트 호환은 로드 시 `?? 기본값`으로 채우면 끝.

## 2. 인서트 vs 센드 · 신호 체인

- **인서트(직렬, 신규)** — 신호를 순서대로 통과: HPF → Gate → EQ → Compressor → De-esser.
- **센드(병렬, 기존·제외)** — reverb, echo. 트랙별로 이미 존재하며 **스트립 외부**에 그대로 둔다(스트립에 노출 안 함).
- **fader / pan(기존·제외)** — 트랙 볼륨/팬 노드. 인서트는 이 **fader 앞(pre-fader)** 에 삽입된다.

### 보컬 신호 체인(고정 순서, v1)

```
[보컬 클립/소스]
  ├─▶ (탭 A: BEFORE 스펙트럼)          ← FFT 분석용, 소리에 영향 없음
  → HPF (럼블/플로시브, 40~300Hz)
  → Noise Gate / Expander (구간 무음)
  → EQ (9밴드 그래픽, 63Hz~16kHz)
  → Compressor (다이내믹)
  → De-esser (치찰음)
  ├─▶ (탭 B: AFTER 스펙트럼)           ← FFT 분석용, 소리에 영향 없음
  → [기존 fader / pan]                 ← 인서트는 pre-fader (신규 아님)
  → [기존 reverb send / echo send]     ← 병렬, 스트립 외부 (신규 아님)
  → 마스터 버스
```

게이트→EQ→컴프→디에서는 보컬 채널 스트립의 정석. v1은 **고정 순서**(재정렬 UI는 추후). 인서트는 **pre-fader**(표준). BEFORE/AFTER 탭은 인서트 체인의 **입·출력에 붙는 분석 탭**으로, 오디오 경로에는 영향을 주지 않는다.

## 3. 데이터 모델

`track.params`에 보컬 FX 상태를 추가한다. 통째 직렬화라 저장 자동. **fader/pan/reverb/echo는 기존 필드를 그대로 쓰므로 여기 넣지 않는다.**

```js
params: {
  // ...기존 (volume, pan, reverbSend, echoSend 등)...
  vocalFx: {
    enabled: false,          // 채널 스트립 전체 bypass (A/B)
    hpf:   { on: false, freq: 90, slope: 24 },                 // slope: 12 | 24 dB/oct
    gate:  { on: false, threshold: -42, ratio: 4, attack: 2, release: 120 },
    eq:    { on: false, geq: [0,0,0,0,0,0,0,0,0] },            // 9밴드 게인(dB), 63~16k ISO
    comp:  { on: false, threshold: -18, ratio: 3, attack: 8, release: 120, makeup: 0 },
    deEss: { on: false, freq: 6800, threshold: -24, amount: 0.5 },
  },
  denoise: { printed: false, profile: null }, // 오프라인 De-noise 결과 메타
}
```

- **9밴드 EQ**: 밴드 주파수는 마스터 EQ와 동일한 ISO 세트 `[63,125,250,500,1000,2000,4000,8000,16000]`Hz. `geq[i]`는 각 밴드 게인(dB, ±12).
- **kind 게이팅**: `vocalFx`는 `kind === 'audioIn' || 'bounce'`에서만 UI 노출·엔진 적용. `file`(스템)은 무시.
- **구프로젝트 로드**: `params.vocalFx ??= 기본값`으로 채운다(hydrate 경로).
- 각 이펙트에 개별 `on` + 스트립 전체 `enabled`(A/B용 bypass).

## 4. 엔진 구현 — 인서트 체인

### 4-1. 웹(fallback) — [audio-engine.js](audio-engine.js)

- 현재 그래프: `source → fader → autoGain → panner → (meter, dry, reverbSend, echoSend)`.
- 인서트를 **source와 fader 사이**에 삽입. `_addTrack`에서 보컬 트랙이면 인서트 노드 생성:
  - HPF: `BiquadFilterNode(type=highpass)` — slope 24dB/oct는 2단 직렬.
  - EQ: **9밴드** `BiquadFilterNode(peaking)` 체인 — **마스터 EQ 코드 재사용**([audio-engine.js:619-627](audio-engine.js#L619-L627)).
  - Compressor: `DynamicsCompressorNode`(내장).
  - Gate: WebAudio에 게이트 노드 없음 → 간이 구현(엔벌로프 팔로워 + gain) 또는 v1은 네이티브 권위·웹은 근사(gate 생략 가능).
  - De-esser: HF 사이드체인 → 간이는 고역 `DynamicsCompressorNode`.
- **원칙 6**(웹은 fallback, 네이티브가 가청 권위) 적용 → 웹은 **근사 허용**. 단 파형/레벨이 크게 어긋나지 않을 만큼.

### 4-2. 네이티브(가청, 권위) — [AudioEngine.h](juce_skeleton/Source/AudioEngine.h) `TrackAudioSource`

- `getNextAudioBlock`에서 **transportSource 블록을 뽑은 직후, echo/reverb 센드([AudioEngine.h:771](juce_skeleton/Source/AudioEngine.h#L771)) 전에** 인서트 적용:
  1. HPF — `dsp::IIR::Filter` (highpass), slope 24 = 2단
  2. Gate — 임계값 기반 직접 구현(가벼움, 블록 RMS/피크 → gain 엔벌로프)
  3. EQ — `dsp::IIR::Filter` peaking ×9 (**마스터 EQ의 `dsp::IIR` 패턴 재사용**, [AudioEngine.h:1102-1106](juce_skeleton/Source/AudioEngine.h#L1102))
  4. Compressor — `dsp::Compressor<float>`
  5. De-esser — 대역 분리(HF 밴드패스) + comp
- 파라미터 전달: `TrackDef`에 필드 추가([AudioEngine.h:140-141](juce_skeleton/Source/AudioEngine.h#L140) reverbSend/echoSend 옆) + `TrackAudioSource`에 `std::atomic` 추가([AudioEngine.h:1053-1054](juce_skeleton/Source/AudioEngine.h#L1053)) + `setTrackParam` 디스패치 확장([AudioEngine.cpp:911,942](juce_skeleton/Source/AudioEngine.cpp#L911)).
- **재빌드 필요**(네이티브). 증분 빌드로 `bin/FocusDAW-AudioEngine.exe` 교체(build_native.bat).

### 4-3. 브리지 — [audio-bridge.js](audio-bridge.js)

- `setTrackParam`([audio-bridge.js:473](audio-bridge.js#L473))에 새 키 전달(중첩이면 `vocalFx.comp.threshold` 식 평탄화 또는 전용 커맨드).
- **네이티브 (재)연결 시 파라미터 재동기화 목록**([audio-bridge.js:1551-1558](audio-bridge.js#L1551-L1558))에 보컬 FX 키 추가 — 안 하면 native 재접속 때 보컬 이펙트가 초기화됨(기존 reverb/echo와 동일 함정, 메모리 `native-has-no-importproject-sync-via-commands`).

### 4-4. Export / 오프라인 렌더 — [audio-engine.js](audio-engine.js) `exportMix`

- 오프라인 그래프도 **인서트 체인을 동일 복제**해야 Export ≠ 재생이 안 생긴다. 현재 오프라인은 per-track reverb/echo를 복제한다([audio-engine.js:4181-4183](audio-engine.js#L4181-L4183)) — **같은 자리에 인서트도 추가.**
- 네이티브 Export도 재생과 같은 `TrackAudioSource` 경로를 타므로 자동 반영(설계상). 웹/네이티브 A/B 시험 항목 필수.

## 5. 적용 전/후 FFT 스펙트럼 비교 (1단계부터 구현)

**목표** — 선택한 audio input 트랙에 대해 **인서트 적용 전(BEFORE) vs 후(AFTER)** 의 **실측 주파수 스펙트럼**을 겹쳐 보여, EQ/HPF/De-esser 등의 효과를 눈으로 확인. (목업의 시뮬레이션 곡선이 아니라 실제 FFT.)

### 5-1. 분석 소스 — 웹 그래프 권위

- 스펙트럼은 **가청 엔진과 무관하게 항상 웹 오디오 그래프에서 측정**한다. 네이티브 연결 시에도 웹 그래프는 모델/파형용으로 계속 돌고(메모리 `dual-engine-output-and-playhead`: web은 output만 mute), 인서트 노드도 동일 파라미터로 존재하므로 **AnalyserNode 탭이 항상 신호를 받는다**(출력 mute보다 앞단이라 영향 없음).
- 즉 네이티브가 소리를 내더라도 **BEFORE/AFTER 곡선은 웹 인서트 체인의 실측**으로 그린다. 파라미터가 양 엔진 동기이므로 표시와 가청이 일치.

### 5-2. 두 개의 분석 탭

```
source ─┬─▶ analyserBefore (FFT)          ← 인서트 입력
        └─▶ HPF→Gate→EQ→Comp→DeEss ─┬─▶ analyserAfter (FFT)   ← 인서트 출력
                                      └─▶ fader → ...
```

- 웹: 인서트 입력에 `analyserBefore`, 출력(De-esser 뒤·fader 앞)에 `analyserAfter` — 둘 다 `AnalyserNode`(fftSize 예: 4096~8192, `getFloatFrequencyData`). 분석 탭은 오디오 경로로 다시 합류하지 않으므로 소리에 영향 없음.
- **재생 중 실시간**: `requestAnimationFrame` 루프로 두 analyser의 magnitude(dB)를 로그-주파수 축(20Hz~20kHz)에 그린다. 스무딩(`smoothingTimeConstant`)으로 안정화.
- **정지 상태 미리보기(선택)**: 재생이 없으면 곡선이 안 그려지므로, 보조로 클립 버퍼를 `OfflineAudioContext`에 인서트 체인 복제로 렌더해 정적 BEFORE/AFTER를 1회 계산하는 경로를 둘 수 있다(후순위 옵션). v1 필수는 **재생 중 실시간**.

### 5-3. UI

- 목업의 Spectrum 패널([_refer/Vocal Channel Strip 2.dc.html](<_refer/Vocal Channel Strip 2.dc.html>) `spectrum()`) 레이아웃 채택: 로그 주파수축(100/1k/10k 그리드), dB 눈금(0/-20/-40), BEFORE(점선·muted) vs AFTER(실선·amber) 범례.
- 다만 데이터는 시뮬레이션이 아닌 **analyser 실측**으로 대체.

## 6. 광대역 De-noise — 오프라인 "프린트" (별도 스테이지)

실시간 스펙트럼 감산은 지연·아티팩트 부담이 커서 범위 밖. 대신:

- **노이즈 프로파일 학습**: 사용자가 보컬 클립의 **무음 구간(숨소리·룸톤만)을 선택** → 그 구간 스펙트럼을 노이즈 지문으로.
- **스펙트럼 감산 렌더**: 클립 전체에 STFT 기반 감산 → **새 WAV로 프린트**(비파괴).
- **인프라 재사용**: 이 앱은 이미 bake/offline-render + `<Project> Audio/Consolidated/` 기록 + 비파괴 클립 교체(Phase 5 `deleteRange`+삽입, Merge Clips)가 있다 → **De-noise 프린트를 그 위에 얹는다.** 원본 WAV 보존, Undo 복원 가능.
- 실행 위치: 네이티브(오프라인 커맨드) 권장. ffmpeg `afftdn` 활용도 후보(이미 ffmpeg 의존).

## 7. UI

- **채널 스트립 패널** — `kind === audioIn/bounce`에서만 노출. 스템(`file`)은 기존 컨트롤(볼륨/팬/reverb/echo)만.
- 배치: 목업 헤더(최소화/최대화/×)는 **별도 BrowserWindow** 전제 → Advanced 창(EQ/Pan/Ambience 별창) 패턴 재사용([advanced-eq-app.jsx](advanced-eq-app.jsx), 메모리 `mixer-is-separate-window-broadcastchannel`). 또는 믹서([ui-mixer.jsx](ui-mixer.jsx)) 인라인. **별창 권장**(목업 구조와 일치).
- 구성(위→아래, 목업 기준): 트랙명+kind 배지 → **PRESET 행 + 스트립 전체 bypass(A/B)** → **Spectrum(전/후 FFT)** → **01 HPF / 02 Noise Gate**(2열) → **03 9밴드 EQ** → **04 Compressor** → **05 De-esser**. (Fader/Echo/Pitch 섹션은 **미포함**.)
- 프리셋: Clean Lead / Warm Pop / Bright Air / Podcast (목업 `applyPreset` 값 참고).
- 각 이펙트: on 토글(dot) + 파라미터 노브 + 상태 태그(ON/BYP).
- 목업의 프로토타입 런타임(`DCLogic`/`{{ }}`/`sc-for`/`<helmet>`)은 앱에 없다 → **로직(React.createElement)만 이식**, 테마 `<style>`은 재정의 말고 앱의 기존 CSS 변수 소비. `var(--amber)` 등 테마 변수 사용(하드코딩 액센트 색 금지, 앱개발.md 이슈).

## 8. 별도 후속 페이즈 — Pitch Editor (이 문서 범위 밖)

피아노롤 기반 피치 보정은 **채널 스트립과 목적·규모가 다른 별개 기능**이라 **후속 페이즈**로 분리한다(목업의 Pitch Editor 섹션은 UI 참고용).

- 현재 pitch = `tempo.key/keyShift`([audio-engine.js:331](audio-engine.js#L331))는 **프로젝트 전체 반음 일괄 전조**(웹은 soundtouchjs bake, 메모리 `webaudio-fallback-pitch-bake`).
- 원하는 Pitch Editor = **음표 단위 그래픽 피치 편집**(Melodyne류): ① 모노 피치 트래킹 ② 노트 세그멘테이션 ③ 노트별 PSOLA/위상보코더 리샘플 ④ 피아노롤 편집 UI. **채널 스트립 전체보다 큰 신규 DSP.**
- 채널 스트립(스테이지 A~F) 완료 후 별도 페이즈(예: Phase 7)로 착수.

## 9. 단계별 구현 계획

| 스테이지 | 내용 | 상태 |
|---|---|---|
| **A. 골격** | `vocalFx` 데이터 모델 + kind 게이팅 + 별창 UI 셸 + 전체 bypass(A/B) + 저장/복원 + 프리셋 | ✅ v1.44.0 (B에 흡수) |
| **B. 9밴드 EQ + Compressor** | 웹·네이티브·Export·저장. 보컬 프리셋 | ✅ v1.44.0~12 · **Stage B 전건 통과 2026-07-26** |
| **C. FFT 스펙트럼 비교** | 적용 전/후 곡선 | ✅ v1.44.14 · **T-1.44.14-1 통과** — ⚠️ **설계 변경**: analyser 실시간 탭(§5) 대신 **트랙 버퍼의 정적 FFT(N=4096)** 로 구현. 재생 중이 아니어도 보이고, 네이티브가 소리를 낼 때도 웹 그래프 의존이 없어 더 견고 |
| **D. HPF + Noise Gate** | 럼블 컷 + 구간 무음 | ✅ v1.44.15~16 · **T-1.44.15-1 통과** (Gate는 네이티브 권위, 웹 fallback 미적용 — §4-1 근사 허용) |
| **E. De-esser** | 치찰음 대역 comp | ✅ v1.44.17 · **T-1.44.17-1 통과** (스플릿밴드, comp 뒤) |
| **F. 광대역 De-noise** | 오프라인 프린트(프로파일 → STFT 감산 → 새 WAV, 비파괴) | ✅ v1.44.20 · **T-1.44.20-1 통과** (자체 STFT) |
| *(부가)* GR 미터 · 프리셋 확장 · Reset | Gate/Comp/De-esser 실시간 감쇠 미터, 프리셋이 HPF·De-esser까지 세팅, PRESET Reset | ✅ v1.44.18~19 · v1.44.23~24 |
| *(별도)* **Pitch Editor** | §8, 후속 페이즈 | ⬜ 착수 결정 없음 |

**→ A~F 전 단계 완료(2026-08-04).** 실제 진행도 권장안대로 B(+A) → C → D → E → F 순이었다. 남은 것은 §8의 Pitch Editor뿐이며 별도 페이즈다.

## 10. 시험 관점(착수 시 [시험.md](시험.md)에 항목화)

- 스템(`file`) 트랙엔 채널 스트립이 **안 보인다**(회귀 없음).
- 보컬 트랙 EQ/Comp/Gate/HPF/De-esser가 재생·Export(웹/네이티브 A/B)에 동일 반영.
- **FFT 스펙트럼**: EQ 밴드/HPF/De-esser 조작 시 AFTER 곡선이 즉시 반영, BEFORE는 원음 유지. 재생 없을 때 동작(정적 옵션 채택 시).
- 저장/재열기 후 보컬 FX 파라미터 유지. 구프로젝트(필드 없음) 정상 로드.
- 네이티브 재접속 후 보컬 FX 유지(재동기화 확인).
- bypass(A/B)로 완전 우회. Undo/Redo 정합.
- De-noise: 원본 WAV 불변, 프린트 결과 Undo 복원.
- **fader/pan/reverb/echo 회귀 없음**(스트립이 기존 트랙 노드를 건드리지 않는다).

## 11. 미결 결정 — **전부 해소됨(2026-08-04)**

- ✅ **인서트 순서**: **고정**(HPF → Gate → EQ → Comp → De-esser). 재정렬 UI는 만들지 않음.
- ✅ **De-esser 위치**: **comp 뒤**(제안대로, v1.44.17).
- ✅ **FFT 스펙트럼 방식**: 실시간 analyser가 아니라 **트랙 버퍼의 정적 FFT**(v1.44.14). 재생 중이 아니어도 보이고, 네이티브 가청 시에도 웹 그래프에 의존하지 않아 더 견고하다. POST 곡선은 EQ·HPF 응답을 실측 PRE에 얹어 계산(가청 필터와 동일한 RBJ 설계).
- ✅ **De-noise 엔진**: **자체 STFT + 프로파일 학습**(2026-08-03 결정, v1.44.20). `afftdn`은 사용자가 고른 무음 구간의 프로파일을 사실상 쓸 수 없고 WAV↔임시파일 왕복이 필요해 제외. 적용 범위는 **선택한 클립**(대상은 Repeat 구간이 지목).
- ✅ **채널 스트립 UI**: **별창**(2026-07-25 결정).
- ✅ *(추가 결정)* **프리셋 범위**: HPF·EQ·Comp·De-esser는 세팅하되 **Noise Gate는 제외**(2026-08-04) — 적정 임계값이 녹음별 노이즈 플로어에 좌우돼 고정값이 무의미하고, 잘못 걸리면 말끝이 잘린다.
