*지침*
1. 시험은 반드시 **앱 버전을 대항목(`## vX.Y.Z — 제목`)**으로 기록한다.
2. 각 시험은 **시험번호 + 시험 내용 + 판정 기준**을 갖춘 개별 항목으로 작성한다. (버전만 적고 여러 항목을 단순 나열하는 방식 금지)
3. 시험 완료된 항목은 시험-아카이브.md로 옮기고 시험.md에서는 삭제한다.

# FocusDAW Studio 시험(Test) 계획서

> 최종 갱신: 2026-09-23 · **현재 버전 v2.9.0**
>
> 🟡 **미해결 1건** — [버그.md](버그.md) `B-PSOLA-Quality`(보정된 보컬이 **거칠어진다**). **후순위 심층 검토**(사용자 결정 2026-09-22). **Stage E 의 기능 자체는 동작한다** — 음정·원본 안전·저장·Export·Undo 가 전부 맞는다.
>
> 📌 **통과한 시험은 전부 [시험-아카이브.md](시험-아카이브.md)에 있다.** 이 문서에는 **대기 중인 것만** 남긴다(위 지침 3). 통과 기록을 찾을 때는 아카이브를 본다.
>
> 공통 환경 준비: 데스크톱 앱 + 네이티브 엔진(`bin/FocusDAW-AudioEngine.exe`).

## 대기 중인 시험

| 시험 | 내용 | 상태 |
|---|---|---|
| **T-2.9.0-1 ~ -4** | v2.9.0 — Stage F: 보정 프리셋 · 라벨/레이아웃 · **메뉴얼 신설 장** | 🔴 **신규** |
| **T-2.8.4-1 ~ -5** | v2.8.4 — 🔴 **타임라인 길이**(하한 제거) · Export 길이 · 이동량 ±6 제한 · Apply 소요 시간 | 🔴 **신규** — ①은 타임라인 전반에 걸리므로 무회귀 범위가 넓다 |
| T-1.20.8-1 | 앱 종료 시 엔진 비정상 종료 코드 | 장기 관찰(버전 무관) |

> **미해결 / 추후 과제** (상세: [앱개발.md](앱개발.md) "알려진 버그 / 한계")
> - 🟡 **`B-PSOLA-Quality`** — 보정된 보컬이 **거칠어진다**. **후순위 심층 검토**(사용자 결정 2026-09-22). Stage E 기능 자체는 동작한다.
> - 🟡 **`B-Project-MinDuration`** — 짧은 클립을 **처음** 실으면 타임라인이 16초(데모 루프 길이)로 늘어난다. 원인 규명 완료, **정책을 먼저 정해야 착수 가능**([버그.md](버그.md)).
> - 🐛 **`B-Vibrato-Fragment`** — 깊은 비브라토(±70¢ 이상)가 한 음을 여러 블록으로 쪼갠다. 원인 규명은 끝났고 **자동 판정 수정은 보류**(사용자 결정 2026-09-14). ✅ **v2.7.5 의 `Merge` 가 실질적 해결책이고 현장에서 확인됐다** — 쪼개진 블록을 골라 합치면 재분석·NOTES 변경·재열기를 건너 유지된다.
> - ⚠️ **합창(2~3성) 녹음은 시험 대상이 아니다** — 단성 추적기의 원리적 한계이며 고칠 항목이 아니다([버그.md](버그.md) `B-Chorus-Polyphonic`). **T-2.4.0-1은 단성 테이크로 시험한다.**
> - 🎚️ **라우드한 목표(−12 이하)의 리미터 압축 아티팩트** — 실측 가청 경계는 crest 감소 −3 dB / LRA −0.3 부근. 남은 개선안은 오버샘플링·프로그램 종속 릴리즈·다밴드이며, **별도 마스터링 앱이 맞는 자리일 수 있음**(사용자 결정으로 보류).
> - 🐛 **Consolidate 소급 불가** — v1.37.1 이전에 병합해 저장한 clip은 복구 불가(다시 병합 필요).

## v2.9.0 — Stage F: 프리셋 · 메뉴얼 · UI 정리

> **renderer 재빌드만.** 메뉴얼은 브라우저로 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html) 를 열어 봅니다.

### T-2.9.0-1. 보정 프리셋
- **시험 내용**
  1. Pitch Editor → `Analyze` → `CORRECTION` 칸에 **Natural · Tight · Hard** 세 버튼이 보이는지.
  2. 아무것도 선택하지 않은 채 **Natural** → `AMOUNT` 가 **70%**, `VIBRATO` 가 **Keep vibrato** 가 되는지. 버튼이 **켜진 것으로** 표시되는지.
  3. **Tight** → 100% · Keep. **Hard** → 100% · Flatten.
  4. `AMOUNT` 슬라이더를 **50%** 로 직접 옮긴다 → 🔴 **세 버튼이 모두 꺼지는지**(어느 프리셋과도 다르므로).
  5. **노트를 몇 개 골라** 프리셋을 누른다 → **그 노트들에만** 적용되는지(라벨이 `N NOTES SELECTED`).
  6. 프리셋을 누른 뒤 `Ctrl+Z` 한 번에 되돌아가는지.
  7. 🔴 아무것도 선택하지 않고 **Tight** 를 누른다 → 값이 기본값과 같으므로 **노트가 붉어지지 않아야** 한다.
- **판정 기준**: 세 프리셋이 설계값대로 동작하고, 직접 만진 값에서는 켜지지 않는다.

### T-2.9.0-2. 라벨 · 레이아웃
- **시험 내용**
  1. 우측 패널의 라벨이 **`AMOUNT`** · **`VIBRATO`** 로 보이는지(전에는 `AMT` · `VIB`).
  2. 글자가 **잘리거나 줄바꿈되지 않는지**.
  3. `MIN` · `SNAP` · `TIME` · `KEY` 줄도 정렬이 흐트러지지 않았는지.
  4. 창을 **좁게/짧게** 줄여도 `CORRECTION` 칸 전체에 스크롤로 닿는지.
  5. 10개 테마를 몇 개 바꿔 가며 프리셋 버튼의 **켜짐/꺼짐이 구분되는지**.
- **판정 기준**: 라벨이 온전히 보이고 기존 정렬이 유지된다.

### T-2.9.0-3. 메뉴얼
- **시험 내용**
  1. `manual/사용자 메뉴얼.html` 을 브라우저로 연다.
  2. 목차에 **피치 에디터 / Pitch Editor** 항목이 있고, 눌렀을 때 그 장으로 가는지.
  3. 장 번호가 **1~15** 로 이어지는지(피치 에디터가 11, 문제 해결이 15).
  4. **한/영 전환**이 정상인지 — 두 언어 모두 피치 에디터 장이 나오는지.
  5. 내용이 실제 화면과 맞는지 — 특히 **버튼 이름**(Analyze · Split · Merge · Snap all to key · Natural/Tight/Hard · Apply · Revert)과 **단축키**(`Ctrl+R` · `Ctrl+A`).
  6. 단축키 장에 **피치 에디터 칸**이 있는지.
- **판정 기준**: 장이 제자리에 있고 번호·목차가 맞으며, 적힌 내용이 실제 동작과 일치한다.
- 📌 **스크린샷은 없습니다** — 캡처에 앱 구동이 필요해 본문만 썼습니다. 그림이 있어야 읽기 어렵다고 느끼시면 알려 주세요.

### T-2.9.0-4. 무회귀
- **시험 내용**
  1. `AMOUNT` 슬라이더를 끌면 여전히 **Undo 한 번**인지.
  2. `VIBRATO` 토글 · `SNAP` · `Snap all to key` · Split/Merge · `Ctrl+A` · 드래그가 종전대로인지.
  3. `Apply` / `Revert` 와 `applied in n.n s` 표시가 정상인지.
  4. v2.8.4 의 타임라인 길이가 여전히 내용과 맞는지.
- **판정 기준**: v2.8.4 까지의 동작이 그대로다.

## v2.8.4 — 타임라인 길이 · 이동량 ±6 제한 · Apply 소요 시간

> **renderer 재빌드만.** ①은 **타임라인 전반**에 걸리므로 무회귀 범위가 넓다.

### T-2.8.4-1. 🔴 타임라인 길이가 내용과 맞는가
- **시험 내용**
  1. **앱을 새로 실행**하고 **11~12초짜리 짧은 파일**을 싣는다 → 🔴 트랜스포트 총시간과 **OUTPUT FX 레인 길이가 파일 길이와 맞는지**(이전에는 `00:16.00` 이 됐다).
  2. 그 상태에서 **클립을 조금 옮기거나 게인을 바꾼다** → 🔴 **16초로 늘어나지 않는지**(이전 진단의 예측이 바로 이것이었다).
  3. **저장 → 앱 재시작 → 프로젝트 열기** → 길이가 그대로인지.
  4. 🔴 **옛 프로젝트**(v2.8.3 이전에 저장한 것)를 연다 → 파일에 16초가 적혀 있어도 **내용 길이로 열리는지**.
  5. **긴 파일**(1분 이상)도 정상인지.
  6. 클립을 **트림해 짧게** 만든다 → 눈금이 따라 줄어드는지.
  7. **빈 프로젝트**(File→New)에서 눈금이 정상으로 보이는지(내용이 없으면 16초).
  8. **데모 세션**(있다면)을 열어 16초가 유지되는지.
- **판정 기준**: 🔴 **어느 경로로 오든 길이가 같다.** 첫 로딩·재열기·편집 후가 모두 내용과 일치한다.

### T-2.8.4-2. Export 길이
- **시험 내용**
  1. 짧은 파일(11~12초)을 싣고 **클립을 한 번 움직인 뒤** WAV로 Export.
  2. 🔴 **내보낸 파일 길이가 내용과 맞는지**(이전에는 뒤에 무음이 붙어 16초가 될 수 있었다).
  3. 긴 파일에서도 Export 길이가 정상인지.
  4. Repeat 구간을 지정한 Export(구간 내보내기)가 종전대로인지.
- **판정 기준**: 내보낸 파일에 **불필요한 꼬리 무음이 없다.**

### T-2.8.4-3. 이동량 ±6 제한과 경고
- **시험 내용**
  1. Pitch Editor 에서 노트를 **7반음 이상** 끌어올린다 → 🔴 `CORRECTION` 칸에 **붉은 경고**가 뜨는지(*"N notes move more than 6 semitones…"*).
  2. 끄는 것 자체는 **막히지 않는지**(자유롭게 옮겨진다).
  3. `Apply` 한 뒤 들어 본다 → 🔴 **6반음까지만 올라가는지**(12반음을 요청해도).
  4. **6반음 이내**로 옮기면 경고가 사라지는지.
  5. 6반음 이내의 보정이 종전대로 정상인지.
- **판정 기준**: 경고는 뜨되 조작은 막히지 않고, 실제 렌더는 6반음에서 멈춘다.

### T-2.8.4-4. Apply 소요 시간 표시
- **시험 내용**
  1. 노트를 옮기고 `Apply` → 끝나면 `CORRECTION` 칸에 **`applied in n.n s`** 가 뜨는지.
  2. 🔴 **짧은 구간만 고른 경우**와 **전체를 고른 경우**의 숫자를 적어 본다 — T-2.8.1-5 의 *"비슷하다(???)"* 를 이제 **정확한 수치로** 확인할 수 있다.
  3. `Revert` 후에는 표시가 사라지는지(프린트된 것이 없으므로).
- **판정 기준**: 시간이 표시되고, 값이 그럴듯하다. ②의 두 수치를 알려 주시면 남은 의문이 닫힌다.

### T-2.8.4-5. 무회귀 — 타임라인 전반
- **시험 내용**
  1. 녹음(일반 · 펀치) 후 길이가 정상인지.
  2. 클립 이동·복사·분할·Merge 후 길이가 정상인지.
  3. 트랙 추가·삭제 후 길이가 정상인지.
  4. Undo/Redo 로 길이가 따라오는지.
  5. Repeat 구간 · 가로 스크롤 · 눈금이 종전대로인지.
  6. Pitch Editor 의 기존 기능 전부(드래그 · Ctrl+A · Split/Merge · Snap · Apply/Revert).
- **판정 기준**: v2.8.3 까지의 동작이 그대로다.

## 추후 확인 항목 (보류·장기 관찰) — 버전 무관

### T-1.20.8-1. 앱 종료 시 엔진 비정상 종료 코드 재발 관찰 (장기 tracing)
- 시험 내용: 평소처럼 사용하며 앱을 시작/종료할 때(믹서 창을 열어 둔 채 종료 포함) 콘솔/로그의 `[AudioEngine] Process exited with code ...` 메시지를 관찰한다. 단발성 경합이므로 일정 기간 반복 관찰이 필요하다.
- 판정 기준: 종료 코드가 항상 정상(1 = Electron kill, 또는 0)이고, `3221225477`(0xC0000005) 등 비정상 코드가 재발하지 않는다. 재발 시 `%LOCALAPPDATA%\CrashDumps`의 새 덤프 유무를 함께 확인해 보고한다.
