*지침*
1. 시험 내용은 반드시 앱 버전을 기록한다.
2. 시험 완료된 내용은 시험-아카이브.md에 추가하고 시험.md에서는 삭제처리 한다.

# FocusDAW Studio 시험(Test) 계획서

> 대상: **v1.7.12**
> 최종 갱신: 2026-06-17 (v1.7.12)
> 상태: Studio Output FX WIDE chip / Mixer Widener 연동 검증 대기
>
> **문서 운영 방식**: 본 문서는 **미완료 또는 최종 확인이 필요한 시험 항목만** 유지합니다. 완료된 항목은 `시험-아카이브.md`로 이동합니다.

---

## 1. 시험 개요

| 항목 | 내용 |
|------|------|
| **앱 버전** | v1.7.12 |
| **시험 대상** | Studio Output FX WIDE chip / Mixer Widener 연동 |
| **핵심 검증** | 메인 `studio.html` arrange 화면의 `OUTPUT FX` 트랙 헤더에서 `ECHO` 대신 `WIDE`가 표시되고, 이 chip이 mixer 창의 `Widener` 버튼/값과 같은 master widener 상태를 토글함 |
| **시험 종류** | 자동 빌드 확인 + 수동 동작/청취 검증 |
| **시험 환경** | Windows Electron 앱 (`npm start`), 오디오 트랙 2개 이상 로드 |

---

## 2. 자동 시험항목

| ID | 앱 버전 | 시험 항목 | 시험 방법 및 절차 | 기대 결과 |
|---|---|---|---|---|
| B-148 | v1.6.0 | Ambience 빌드/문법 무결성 | 1. `node --check audio-engine.js` 실행.<br>2. `npm run build:renderers` 실행. | 두 명령이 오류 없이 통과하고 `build/advanced-ambience-app.js`, `build/app.js` 등 모든 번들이 정상 생성됨 |

---

## 3. 수동 시험항목

| ID | 앱 버전 | 시험 항목 | 시험 방법 및 절차 | 기대 결과 |
|---|---|---|---|---|
| B-151 | v1.7.0 | Fine-tune LED링 노브 + undo/redo | 1. 프리셋 선택 후 Fine-tune 노브(Mix/Decay/Pre-delay/Size/Echo/Damping/Width)를 드래그/휠로 조절.<br>2. 노브 모양(둘레 LED 링이 값까지 점등 + 클래식 캡·포인터, 값 위·이름 아래)을 확인.<br>3. 조절 후 **Ctrl+Z**로 직전 상태 복원, **Ctrl+Y/Ctrl+Shift+Z**로 재실행. | 7개 컨트롤이 LED링 회전 노브로 표시됨. 조절 시 라벨 `Custom`. Ctrl+Z/Y가 ambience 창 포커스 상태에서도 동작하여 직전/다음 값으로 노브·그래프·소리가 함께 복원/재실행됨 |
| B-152 | v1.7.0 | far field — 잔향 최소 + 슬랩 에코 | 1. `Far Field` 프리셋 선택 후 재생. | 긴 잔향(reverb tail)은 거의 없고, 가벼운 **슬랩 에코**(약 10% 수준)가 들림. 그래프에 강조색(에코) 스파이크가 표시됨 |
| B-153 | v1.6.0 | 음장 재생/Export 일치 | 1. 임의 프리셋(또는 Fine-tune 커스텀) 적용 후 재생 모니터링.<br>2. 같은 설정으로 Export(WAV/MP3) 후 파일 재생. | 내보낸 파일에 음장(잔향+에코)이 포함되며, 재생 모니터링음과 공간감이 일치함 |
| B-155 | v1.7.0 | Ambience 창 Space 재생/정지 | 1. Ambience 창에 포커스를 둔 채(노브/카드 클릭 후) 스페이스바를 누른다. | 포커스가 ambience 창에 있어도 스튜디오 재생/정지가 토글됨(포커스된 버튼이 눌리지 않음) |
| B-156 | v1.7.0 | Echo/Size 노브 + 그래프 파라미터 반영 | 1. **Mix** 노브를 올리면 그래프 곡선 높이가 커지는지 확인.<br>2. **Echo** 노브를 올리면 그래프에 강조색 에코 스파이크가 생기고 재생에 슬랩 에코가 들리는지 확인.<br>3. **Size** 노브 변경 시 에코 간격(스파이크 위치)과 공간감이 바뀌는지, **Pre-delay/Decay**가 그래프 갭/꼬리에 반영되는지 확인. | Mix=곡선 높이, Echo=스파이크, Size=에코 간격/시간축, Pre-delay=갭, Decay=꼬리 길이가 모두 그래프에 반영되고 소리와 일치함(이전엔 일부 파라미터가 그래프에 반영되지 않던 문제 해소) |
| B-157 | v1.7.1 | Ambience Fine-tune 레이아웃 | 1. Ambience 창을 열고(가능하면 창 너비를 넓혀)본다.<br>2. 프리셋 카드, 그래프 크기, 노브 배치를 확인. | 프리셋 카드가 창을 넓혀도 **늘어나지 않고 고정 크기**를 유지함. 감쇠 그래프가 이전보다 약 2배 크게 표시됨 |
| B-158 | v1.7.2 | 노브 2행 + Damping/Width 그래프 시각화 | 1. Fine-tune 노브가 **2행**(4+3)으로 배치되었는지 확인.<br>2. **Damping** 노브를 낮추면 그래프의 **파선(HF) 곡선**이 더 빨리 떨어지는지 확인.<br>3. **Width** 노브를 조절하면 그래프 상단 **스테레오 미터 바**의 좌우 폭이 변하는지 확인. | 노브가 2행으로 배치됨. Damping 변화가 파선 HF 곡선으로, Width 변화가 상단 바 폭으로 그래프에 반영됨 |
| B-159 | v1.7.4 | Demo 최초 로딩/재시작 후 Time Zoom 화면 맞춤 | 1. 앱을 실행하고 **Load Demo Session**을 선택한다.<br>2. 최초 Demo 로딩 직후 전체 파형이 arrange 영역 폭을 채우는지 확인한다.<br>3. Time Zoom을 `-` 방향 최소값으로 당겨도 전체 파형이 화면 폭을 채우는지 확인한다.<br>4. 앱을 종료한 뒤 다시 실행해 자동 복원된 Demo 프로젝트에서도 같은 상태인지 확인한다.<br>5. 외부 오디오 파일 로딩 시 기존처럼 전체 파형이 화면 폭에 맞는지 회귀 확인한다. | 최초 Demo 로딩 직후와 재시작 복원 후 모두 전체 파형이 화면 폭을 채움. 짧은 Demo에서도 최소 Time Zoom이 28px/sec 상한에 막히지 않으며, 외부 오디오 파일의 기존 화면 맞춤 동작도 유지됨 |
| B-160 | v1.7.5 | Ambience Fine-tune 컨트롤/그래프 직관화 | 1. Ambience 창을 열고 Fine-tune 영역을 확인한다.<br>2. **Mix/Echo/Width**가 LED 원형 노브로, **Decay/Pre-delay/Room Size/Damping**이 슬라이더로 표시되는지 확인한다.<br>3. 각 슬라이더를 조작해 값 표시와 Dark/Bright, Short/Long 등 스케일 라벨을 확인한다.<br>4. 그래프가 잔향/에코 시간축만 표시하고, Width/Damping은 그래프 아래 보조 미터로 표시되는지 확인한다.<br>5. 각 컨트롤 조작 후 소리와 그래프/미터가 함께 갱신되는지 확인한다. | 컨트롤 타입이 파라미터 성격에 맞게 구분됨. 시간/거리/톤 계열은 슬라이더로 직관적으로 조작되고, 그래프와 보조 미터가 서로 역할을 나눠 표시됨. 조작 시 `Custom` 상태와 undo/redo 경로가 유지됨 |
| B-161 | v1.7.6 | Ambience Fine-tune 2열 정렬 | 1. Ambience 창을 열고 Fine-tune 영역을 확인한다.<br>2. 왼쪽에 그래프/Width·Damping 미터/캡션이 세로로 정렬되는지 확인한다.<br>3. 오른쪽에 Decay/Pre-delay/Room Size/Damping 슬라이더와 Mix/Echo/Width 노브가 한 컬럼 안에 정돈되는지 확인한다.<br>4. 창 폭을 줄였을 때 1열 fallback으로 겹침 없이 접히는지 확인한다. | 기본 폭에서는 왼쪽 시각화, 오른쪽 조작부의 2열 배치가 유지됨. 그래프가 이전보다 세로로 커져 읽기 쉬우며, 좁은 폭에서도 요소가 겹치지 않음 |
| B-162 | v1.7.7 | Ambience 프리셋 카드 확대/Custom 선택 | 1. Ambience 창을 열고 Sound Environment 카드 영역을 확인한다.<br>2. Reset, Concert, Home, Far, Studio, Tunnel, Custom 총 7개 카드가 균등 간격으로 가로 영역을 채우는지 확인한다.<br>3. 카드 hover 시 이전보다 더 크게 떠오르며 확대되는지 확인한다.<br>4. 임의 프리셋을 선택한 뒤 Fine-tune 값을 하나 변경한다.<br>5. Reset을 선택한 뒤 Fine-tune 값을 하나 변경한다. | 카드가 기존보다 크게 보이고 가로폭에 균등 배치됨. 우측 끝 Custom 카드가 표시됨. 프리셋 또는 Reset 이후 세부값을 변경하면 Custom 카드가 활성화됨 |
| B-163 | v1.7.8 | Ambience 2섹션 레이아웃 + Output Effects | 1. Ambience 창을 열고 상단 `SOUND ENVIRONMENT` 제목이 중앙 정렬되는지 확인한다.<br>2. `PRESET` 라벨 아래 프리셋 카드, 구분선, Fine-tune 3열(좌: Mix/Echo/Width 다이얼, 중: 그래프, 우: 슬라이더)을 확인한다.<br>3. 하단 `OUTPUT Effects` 제목과 Reverb/Delay/Saturation/Widener/Exciter 카드가 표시되는지 확인한다.<br>4. Output Effects 값을 변경하고 Mixer/메인 출력 효과 값과 동기화되는지 확인한다.<br>5. 창 높이를 줄여 스크롤이 필요할 때 스크롤바 대신 위/아래 화살표가 표시되고 이동하는지 확인한다. | 화면이 상/하 2섹션으로 정리됨. Output Effects는 Mixer와 동일한 master effect 값을 조절하고 저장됨. 스크롤바는 보이지 않고 화살표로 이동 가능함 |
| B-164 | v1.7.9 | Ambience 표시 보정 | 1. Ambience 창을 열고 `SOUND ENVIRONMENT`, `OUTPUT EFFECTS`가 모두 대문자이며 Cinzel normal weight 느낌으로 표시되는지 확인한다.<br>2. `PRESET`, `FINE-TUNE` 라벨은 bold로 보이는지 확인한다.<br>3. `OUTPUT EFFECTS` 제목 위 구분선이 보이고, 이전보다 여백이 넓게 확보되는지 확인한다.<br>4. Reverb/Delay/Saturation/Widener/Exciter / Enhancer 카드의 조절 슬라이더가 모두 보이고 드래그 가능한지 확인한다.<br>5. 스크롤이 필요한 창 높이에서 위/아래 버튼이 원형 테두리와 solid 삼각형 화살표로 표시되는지 확인한다. | 제목/라벨 위계가 명확하고, Output Effects 슬라이더와 구분선이 누락 없이 보임. 스크롤 버튼은 원형 버튼 안의 solid 화살표로 표시됨 |
| B-165 | v1.7.10 | Ambience 높이 정렬 + Advanced Effects 메뉴 라우팅 | 1. `Advanced Effects > Ambience`를 선택해 `advanced-ambience.html`이 열리는지 확인한다.<br>2. Fine-tune 영역에서 중앙 그래프 세로 높이와 우측 Decay/Pre-delay/Room Size/Damping 4개 슬라이더 묶음의 전체 높이가 맞는지 확인한다.<br>3. `OUTPUT EFFECTS` 제목 아래 카드 목록과의 간격이 한 줄 정도 확보되는지 확인한다.<br>4. `Advanced Effects > Auto Panning`을 선택해 `advanced-pan.html`이 열리는지 확인한다.<br>5. `Advanced Effects > Equalizer Setup`을 선택해 `advanced-eq.html`이 열리는지 확인한다. | 우측 슬라이더 간격만 재분배되어 그래프와 높이가 맞고, Output 제목 아래 여백이 보임. Advanced Effects 메뉴 3개 항목은 각각 Ambience/Pan/EQ 화면으로 라우팅됨 |
| B-166 | v1.7.11 | Studio Output FX Advanced 버튼 제거 | 1. `studio.html` 메인 화면을 연다.<br>2. arrange 하단의 `OUTPUT FX` 트랙 헤더를 확인한다.<br>3. `Advanced` 버튼이 더 이상 보이지 않는지 확인한다.<br>4. `MUTE Clr` 버튼과 REV/ECHO chip은 그대로 보이고 동작하는지 확인한다. | Output FX 트랙 헤더에는 `Advanced` 버튼이 없고, 기존 MUTE/REV/ECHO 조작은 유지됨 |
| B-167 | v1.7.12 | Output FX WIDE chip / Mixer Widener 연동 | 1. `studio.html` 메인 화면을 열고 `OUTPUT FX` 트랙 헤더를 확인한다.<br>2. 기존 `ECHO` chip 대신 `WIDE` chip이 보이는지 확인한다.<br>3. Mixer 창을 열어 `Widener` effect 카드의 on/off 상태와 값이 보이는지 확인한다.<br>4. `OUTPUT FX`의 `WIDE` chip을 눌러 끄고 켤 때 Mixer의 `Widener` 상태/값이 함께 바뀌는지 확인한다.<br>5. Mixer의 `Widener` 버튼 또는 슬라이더를 조작한 뒤 `OUTPUT FX`의 `WIDE` chip 활성 상태가 같은지 확인한다. | `WIDE` chip은 `DAW.master.widener`를 토글하며 Mixer Widener와 같은 bypass/restore 상태를 공유함 |
| B-154 | v1.6.1 | Advanced PAN 악기색 = 스튜디오 트랙색 | 1. studio.html에서 각 트랙의 색상을 서로 다르게 설정/확인.<br>2. `Advanced Effects > Auto Panning`(Spatial Field)을 열고 소리무대의 악기 아이콘과 하단 PAN 노브를 확인.<br>3. studio에서 트랙 색을 바꾼 뒤 다시 확인. | 각 악기 그림(아이콘)과 노브 LED·PAN 수치·미니바 도트·선택 연결선 색이 **해당 트랙의 studio 색상과 동일**하게 표시되고, 트랙 색 변경이 반영됨 |

---

## 4. 확인 메모

- v1.2.33부터 Vari BPM 실시간 재생은 캐시형 Time Stretch 프리뷰를 우선 사용합니다. 첫 재생 또는 BPM 비율 변경 직후에는 트랙별 프리뷰 버퍼 준비 시간이 있을 수 있습니다.
- v1.2.40부터 Playback BPM 입력은 500ms 대기 후 마지막 값 기준으로 프리뷰 계산을 시작하며, 계산 중 들어간 pending 입력이 뒤늦게 중복 계산을 일으키지 않도록 정리합니다.
- Export `Keep pitch`는 Electron Export 전용 단기 안정 기능입니다. 데스크톱 경로는 `ffmpeg atempo` 기준선을 사용하며, 브라우저 fallback의 JS Time Stretch는 품질 기준 미달 실험 경로로 취급합니다.
