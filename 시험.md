# FocusDAW Studio 시험(Test) 계획서

> 대상: **v0.16.8 버그 수정** — Loop 엔진 연결 · IntersectionObserver 분리 · scrollLeft 클램핑 · Passive Wheel 해결  
> 목적: BUG-1~4 수정이 **올바르게 동작하는지** + **기존 기능을 깨뜨리지 않았는지** 확인  
> 최종 갱신: 2026-06-09 (v0.16.8)
>
> **문서 운영 방식**: 본 문서는 **현재 검증해야 할 항목**만 유지한다. 확인 완료(Pass)된 항목은 `시험-아카이브.md` 로 이관한다.

---

## 1. 시험 개요

| 항목 | 내용 |
|------|------|
| 시험 대상 | `audio-engine.js`, `ui-tracks.jsx`, `app.jsx` (v0.16.8 변경분) |
| 핵심 변경 | Loop 엔진 연결 / IntersectionObserver useEffect 분리 / scrollLeft 클램핑 / Passive Wheel |
| 시험 종류 | (A) 구문 검증(node --check), (B) 브라우저 수동 기능 시험 |
| 시험 환경 | Chrome / Edge 최신 버전 (Electron `npm start` 또는 브라우저 직접 실행) |

---

## 2. 사전 준비

### 2-A. 브라우저에서 직접 실행 (빠른 시험)

1. `studio.html` 을 Chrome/Edge 에서 직접 열거나, 로컬 정적 서버로 서빙
   ```bash
   # Python 간이 서버 예시 (프로젝트 루트에서)
   python -m http.server 8080
   ```
   → `http://localhost:8080/studio.html` 접속
2. 브라우저 DevTools → **Console** 탭 열어두기 (에러 모니터링)
3. DevTools → **Performance** 탭도 열어두기 (성능 측정용)

### 2-B. Electron 앱으로 실행

```bash
# 별도 Windows Terminal / PowerShell 에서 실행 (Claude 내부 터미널 ❌)
npm start
```

> **주의**: `npm start`는 반드시 별도 터미널에서 실행할 것.  
> Claude Code 내부 터미널에서 실행하면 `ELECTRON_RUN_AS_NODE=1` 상속으로 실패한다.

---

## 3. (A) 자동 검증 — 명령 실행

> **`.jsx` 파일 주의**: `node --check`는 `.jsx` 확장자를 ESM으로 인식해 실패한다.  
> JSX 파일은 브라우저의 Babel이 파싱하므로, 구문 검증은 **§4 B-01(앱 로드)**에서 브라우저 Console 에러 유무로 확인한다.

| ID | 명령 | 기대 결과 |
|----|------|-----------|
| A-1 | `node --check audio-engine.js` | **에러 없음** 종료 |
| A-2 | `node --check electron/main.js` | **에러 없음** 종료 |
| A-3 | `node --check electron/preload.js` | **에러 없음** 종료 |
| A-4 | 브라우저에서 `studio.html` 열기 → Console 탭 확인 | **빨간 에러 없음** (Babel 파싱 에러, undefined 참조 등) |

---

## 4. (B) 기능 시험 — 브라우저 수동

### 범례
- ✅ Pass  ❌ Fail  ⚠️ 부분/조건부 통과  🔲 미시험

---

### B-01 ~ B-05 : BUG-1 — Loop 버튼 엔진 연결

> Loop(Repeat) 버튼 On/Off가 실제 재생 동작에 반영되는지 확인.

| ID | 시험 항목 | 조작 방법 | 기대 결과 | 결과 |
|----|-----------|-----------|-----------|------|
| B-01 | Loop ON 재생 | Loop 버튼 켠 상태에서 Play | 곡 끝에서 처음으로 루프 재생됨 | 🔲 |
| B-02 | Loop OFF 재생 | Loop 버튼 끈 상태에서 Play | 곡 끝에서 자동 정지됨 (루프 안 됨) | 🔲 |
| B-03 | 재생 중 Loop 전환 | 재생 중 Loop 버튼 토글 | 토글 직후부터 새 모드(루프/정지)로 동작 | 🔲 |
| B-04 | Loop OFF → 정지 후 재생 | Loop OFF로 곡 종료 후 Play 재클릭 | 다시 처음부터 재생됨 | 🔲 |
| B-05 | Console 에러 없음 | Loop 토글 전반 | DevTools Console에 관련 에러 없음 | 🔲 |

---

### B-06 ~ B-10 : BUG-2 — IntersectionObserver 분리 (줌 드래그 응답성)

> TIME 슬라이더 드래그 중 파형이 지연 없이 따라오는지 확인.

| ID | 시험 항목 | 조작 방법 | 기대 결과 | 결과 |
|----|-----------|-----------|-----------|------|
| B-06 | 줌 슬라이더 드래그 응답 | TIME 슬라이더를 빠르게 좌우로 드래그 | 파형이 지연 없이 즉시 갱신됨 (눈에 띄는 랙 없음) | 🔲 |
| B-07 | 반복 줌 전환 | 슬라이더를 최대↔최소 빠르게 반복 이동 | 흰 화면 없음, 파형이 항상 표시됨 | 🔲 |
| B-08 | 수직 스크롤 + 줌 동시 | 수직 스크롤 후 줌 변경 | 화면 밖 트랙은 줌 중 드로 생략, 화면 진입 시 즉시 표시 | 🔲 |
| B-09 | 기존 파형 정확도 유지 | 줌 변경 후 정지 | 파형이 올바르게 표시됨 (깨짐 없음) | 🔲 |
| B-10 | Console 에러 없음 | 줌 드래그 전반 | IntersectionObserver 관련 에러 없음 | 🔲 |

---

### B-11 ~ B-15 : BUG-3 — scrollLeft 클램핑 (minimap sync)

> 줌을 줄였을 때 minimap 뷰포트 표시와 스크롤 위치가 올바르게 동기화되는지 확인.

| ID | 시험 항목 | 조작 방법 | 기대 결과 | 결과 |
|----|-----------|-----------|-----------|------|
| B-11 | 줌 최대 후 스크롤 | TIME 슬라이더 최대로 이동 → 가로 스크롤로 오른쪽 이동 | 파형 오른쪽 끝이 화면에 보임 | 🔲 |
| B-12 | 줌 줄이기 — minimap 동기화 | B-11 상태에서 TIME 슬라이더 줄이기 | minimap 뷰포트 박스가 새 lane 폭 범위 내에 표시됨 (범위 초과 없음) | 🔲 |
| B-13 | 줌 줄이기 — TIME 슬라이더 동기화 | B-11 상태에서 TIME 슬라이더 줄이기 | TIME 값과 minimap이 같이 움직임 (sync 어긋남 없음) | 🔲 |
| B-14 | 줌 최소까지 줄이기 | 슬라이더를 최소로 이동 | 전체 파형이 한 화면에 들어옴, scrollLeft=0 | 🔲 |
| B-15 | Console 에러 없음 | 줌 전환 전반 | DevTools Console에 관련 에러 없음 | 🔲 |

---

### B-16 ~ B-20 : BUG-4 — Passive Wheel 오류 해결

> TimelineMinimap에서 마우스 휠로 줌이 동작하고 Console 경고가 사라졌는지 확인.

| ID | 시험 항목 | 조작 방법 | 기대 결과 | 결과 |
|----|-----------|-----------|-----------|------|
| B-16 | Minimap 휠 줌 동작 | Timeline minimap 위에서 마우스 휠 위로 스크롤 | 줌이 확대됨 (pxPerSec 증가, 파형 커짐) | 🔲 |
| B-17 | Minimap 휠 줌 축소 | Timeline minimap 위에서 마우스 휠 아래로 스크롤 | 줌이 축소됨 (pxPerSec 감소, 파형 작아짐) | 🔲 |
| B-18 | 페이지 스크롤 차단 | minimap 위에서 휠 스크롤 | 페이지가 스크롤되지 않음 (줌만 변경됨) | 🔲 |
| B-19 | Console 경고 없음 | 앱 로드 → minimap 휠 조작 | `Unable to preventDefault inside passive event listener` 경고 없음 | 🔲 |
| B-20 | 반복 휠 조작 | minimap 위에서 빠르게 반복 휠 | 줌이 연속적으로 변경됨, 에러 없음 | 🔲 |

---

### B-21 ~ B-25 : v0.16.7 잔여 시험 항목

> v0.16.7에서 미완료된 항목. B-11/12(⚠️)는 재확인, B-13(❌)은 BUG-2 수정 후 재시험.

| ID | 시험 항목 | 조작 방법 | 기대 결과 | 결과 |
|----|-----------|-----------|-----------|------|
| B-21 | 줌아웃 파형 품질 재확인 | TIME 슬라이더 최소 (전체 보기) | 파형 실루엣이 매끄럽게 표시됨 (계단 현상 없음) | 🔲 |
| B-22 | 줌인 파형 품질 재확인 | TIME 슬라이더 최대 | 세밀한 파형 디테일이 보임, 픽셀 부족으로 뭉개지지 않음 | 🔲 |
| B-23 | 줌 전환 응답 재확인 | 슬라이더 빠르게 좌우 드래그 | 파형이 지연 없이 즉시 갱신됨, 흰 화면 없음 (BUG-2 수정 결과) | 🔲 |
| B-24 | 긴 파일 줌아웃 | 1분 이상 파일 로드 → 전체 보기 | coarse(512버킷) 사용 → 빠른 드로, 파형 형태 정상 | 🔲 |
| B-25 | 전체 Console 에러 없음 | 줌 조작 전반 | DevTools Console에 에러 없음 | 🔲 |

---

## 5. 성능 측정 가이드

### 5-A. IntersectionObserver 효과 측정

1. Chrome DevTools → Performance 탭 열기
2. 트랙 8개 이상 추가 (Demo + Import)
3. **Record 시작** → 트랙 목록을 수직으로 빠르게 스크롤 (3~5초) → **Record 중지**
4. Flame chart에서 `draw` 함수 호출 횟수 확인
   - **기대**: 화면 밖 트랙의 `draw` 호출이 없거나 매우 적음

### 5-B. Peak 레벨 선택 확인

DevTools Console에서 현재 트랙의 peak 레벨 구조 확인:
```js
DAW.tracks.forEach(t => {
  console.log(t.name, {
    coarse: t.peaksCoarse?.length,
    medium: t.peaksMedium?.length,
    fine: t.peaks?.length,
  });
});
```
- **기대**: 각 트랙에 coarse(1024), medium(4096), fine(가변) 세 배열이 존재함

---

## 6. 현재 버전 검증 항목 (v0.16.8)

| ID | 항목 | 상태 |
|----|------|------|
| B-01~05 | BUG-1: Loop 엔진 연결 | 🔲 미시험 |
| B-06~10 | BUG-2: IntersectionObserver 분리 (줌 드래그 응답성) | 🔲 미시험 |
| B-11~15 | BUG-3: scrollLeft 클램핑 (minimap sync) | 🔲 미시험 |
| B-16~20 | BUG-4: Passive Wheel 오류 해결 | 🔲 미시험 |
| B-21~25 | v0.16.7 잔여 항목 (파형 품질 재확인) | 🔲 미시험 |

---

## 7. 알려진 한계 / 시험 시 유의사항

- **Loop OFF 정지 타이밍**: `_loop()` 콜백은 ~100ms 간격으로 실행되므로 duration 도달 후 최대 100ms 후에 `stop()`이 호출될 수 있음. 즉각 정지가 아닌 ~100ms 이내 정지는 정상 동작.
- **IntersectionObserver 최소 지연**: 옵저버가 비동기로 동작하므로 트랙이 뷰포트에 들어오는 순간 극히 짧은 빈 프레임이 보일 수 있음. 200ms 이내라면 정상 동작.
- **scrollLeft 클램핑 timing**: `requestAnimationFrame` 기반이므로 줌 변경 후 1프레임(~16ms) 후에 클램핑 적용됨.

---

## 8. 시험 결과 기록

| 회차 | 일자 | 시험자 | 환경 | A 항목 | B 항목 Pass/Fail | 비고 |
|------|------|--------|------|--------|-----------------|------|
| 1 | | | | | | |

> 발견된 결함은 `앱개발.md` § "알려진 이슈 / 제약" 섹션에 추가하고 심각도·조치를 기록한다.
