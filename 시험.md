*지침*
1. 시험은 반드시 **앱 버전을 대항목(`## vX.Y.Z — 제목`)**으로 기록한다.
2. 각 시험은 **시험번호 + 시험 내용 + 판정 기준**을 갖춘 개별 항목으로 작성한다. (버전만 적고 여러 항목을 단순 나열하는 방식 금지)
3. 시험 완료된 항목은 시험-아카이브.md로 옮기고 시험.md에서는 삭제한다.

# FocusDAW Studio 시험(Test) 계획서

> 최종 갱신: 2026-09-22 · **현재 버전 v2.8.2**
>
> 🟡 **미해결 1건** — [버그.md](버그.md) `B-PSOLA-Quality`(보정된 보컬이 **거칠어진다**). **후순위 심층 검토**(사용자 결정 2026-09-22). **Stage E 의 기능 자체는 동작한다** — 음정·원본 안전·저장·Export·Undo 가 전부 맞는다.
>
> 📌 **통과한 시험은 전부 [시험-아카이브.md](시험-아카이브.md)에 있다.** 이 문서에는 **대기 중인 것만** 남긴다(위 지침 3). 통과 기록을 찾을 때는 아카이브를 본다.
>
> 공통 환경 준비: 데스크톱 앱 + 네이티브 엔진(`bin/FocusDAW-AudioEngine.exe`).

## 대기 중인 시험

| 시험 | 내용 | 상태 |
|---|---|---|
| **T-2.8.2-1** | v2.8.2 — 프린트 뒤 되돌리기(`Apply` 가 꺼지던 결함 재시험) | 🔴 **신규** |
| **T-2.8.1-6** | 🆕 음정 정확도 — **맥놀이로 판정**. `node tools/make-pitch-accuracy-audio.js` 먼저 실행 | 🔴 **신규** — 실녹음으로는 판정할 수 없던 것 |
| **T-2.8.1-4** | Stage E — De-noise 와의 순서 | 🟡 사용자 보류(2026-09-22), 이후 시험 |
| **T-2.8.1-5** | Stage E — 긴 클립 · 무회귀 | 🟡 사용자 보류(2026-09-22), 이후 시험 |
| T-2.8.1-3 ④ | 네이티브 vs 웹 폴백 A/B | 미시험 — 방법 안내가 틀렸었다(정정 완료) |
| **T-2.4.0-1** | Pitch Editor Stage C — 노트 블록이 부른 음표와 맞는가 | 🔴 **재시험** — 앞선 두 번(09-01·09-02)은 **다른 결함에 오염돼 판정 불가**였다. 오염원(창 스로틀·옥타브 오검출)은 모두 제거됐으므로 **이번이 첫 정상 판정**이다 |
| T-2.0.2-2 | Pitch Editor Stage B — 긴 클립 분석 속도·무회귀 | 재측정 — v2.4.4로 기존 참고치가 무의미해졌다 |
| T-1.20.8-1 | 앱 종료 시 엔진 비정상 종료 코드 | 장기 관찰(버전 무관) |

> **미해결 / 추후 과제** (상세: [앱개발.md](앱개발.md) "알려진 버그 / 한계")
> - 🐛 **`B-Vibrato-Fragment`** — 깊은 비브라토(±70¢ 이상)가 한 음을 여러 블록으로 쪼갠다. **유일한 미수정 결함**이고 원인 규명은 끝났으나, **비브라토와 실제 음 변화의 경계가 곡마다 모호하다**는 판단으로 **착수 보류**(사용자 결정 2026-09-14). 후보 3안은 [버그.md](버그.md)에 있다.
> - ⚠️ **합창(2~3성) 녹음은 시험 대상이 아니다** — 단성 추적기의 원리적 한계이며 고칠 항목이 아니다([버그.md](버그.md) `B-Chorus-Polyphonic`). **T-2.4.0-1은 단성 테이크로 시험한다.**
> - 🎚️ **라우드한 목표(−12 이하)의 리미터 압축 아티팩트** — 실측 가청 경계는 crest 감소 −3 dB / LRA −0.3 부근. 남은 개선안은 오버샘플링·프로그램 종속 릴리즈·다밴드이며, **별도 마스터링 앱이 맞는 자리일 수 있음**(사용자 결정으로 보류).
> - 🐛 **Consolidate 소급 불가** — v1.37.1 이전에 병합해 저장한 clip은 복구 불가(다시 병합 필요).

## v2.8.1 — Pitch Editor Stage E: Apply / Revert (처음으로 소리가 바뀐다)

> 🔴 **이 시험이 진짜 판정이다.** v2.8.0 의 계측(음정 오차 0.3센트 · 고역 88% 등)은 **"그럴듯함"의 대용물**이지 가청 품질 자체가 아니다. 보정한 소리가 쓸 만한지는 귀로만 알 수 있다.
>
> **renderer 재빌드만** · 네이티브 재빌드 불필요.

### [시험 환경 준비]
1. 개발 앱 실행(`npm start`).
2. **Audio In 트랙에 보컬 녹음 클립**. **음정이 살짝 어긋난 구간이 있는 실제 노래**가 가장 좋다.
3. 클립 우클릭 → `Pitch Editor...` → `Analyze`.
4. ⚠️ **시험 전에 프로젝트를 저장해 두시면** 언제든 되돌릴 수 있다(Undo 로도 되지만 안전하게).

> 📌 **2026-09-22 중간 판정**
>
> | 시험 | 결과 |
> |---|---|
> | **T-2.8.1-2** | ✅ **통과** — Revert 시 원곡 복귀 + 노트 편집 유지 · Undo/Redo 정상 · 반복 Apply 문제 없음 |
> | **T-2.8.1-3** | ✅ **통과**(저장·재열기·Export). ⚠️ 네이티브 vs 폴백 A/B 는 **미시험** — 방법 안내가 틀려서였다(아래 ④ 정정) |
> | **T-2.8.1-1** | 🟡 **부분** — 소리는 확실히 바뀐다. **음질 저하**는 [버그.md](버그.md) `B-PSOLA-Quality` 로 등록해 **후순위 심층 검토**(사용자 결정). 음정 **정확도**는 실녹음으로 판정 불가 → 🆕 `node tools/make-pitch-accuracy-audio.js` 로 판정 가능해졌다(아래 T-2.8.1-6) |
> | **T-2.8.1-4 · -5** | 보류 — 이후 시험 |
>
> 🔴 **결함 1건은 v2.8.2 에서 수정** — 프린트 뒤 노트를 전부 제자리로 되돌리면 `Apply` 가 꺼져 화면과 소리를 맞출 수 없었다. 재시험은 아래 **T-2.8.2-1**.

### T-2.8.1-1. 소리가 실제로 바뀌는가 · 쓸 만한가 (핵심)
- **시험 내용**
  1. 노트 하나를 **반음 정도 끌어 올린다**. `Apply` 버튼이 켜지는지 본다(노트를 옮기기 전에는 꺼져 있어야 한다).
  2. `Apply` 를 누른다 → 버튼이 **`Applying… NN%`** 로 바뀌고 진행률이 오르는지, 그동안 **앱이 얼지 않는지**(다른 창을 클릭하거나 스크롤해 본다) 확인한다.
  3. 끝나면 재생한다 → 🔴 **그 노트의 음정이 실제로 바뀌어 들리는가.**
  4. **소리의 질**을 본다 — 기계적이거나, 떨리거나(phasiness), 자음이 뭉개지거나, 딸깍거리는 곳이 없는지. **보정하지 않은 구간은 원음 그대로여야 한다.**
  5. `AMT` 를 **50%** 로 내리고 다시 `Apply` → 보정이 **절반만** 걸리는지 귀로 확인한다.
  6. `VIB` 를 `Flatten vibrato` 로 바꾸고 긴 음에 `Apply` → 떨림이 눌리는지 확인한다.
- **판정 기준**: 지정한 대로 음정이 바뀌고, **쓸 만한 소리**다. 어색한 곳이 있으면 **그 구간의 시간 위치와 어떤 느낌인지**(기계적/떨림/뭉개짐/딸깍)를 함께 보고.

### T-2.8.1-6. 🆕 음정 정확도 — 맥놀이로 판정 (실녹음으로는 알 수 없었던 것)
- **준비**: `node tools/make-pitch-accuracy-audio.js` → `test-audio/` 에 음원 4개 + **`pitch-acc-howto.md`**(절차서). 먼저 절차서를 읽을 것.
- **요점**: 같은 음을 두 트랙에 **겹쳐 재생**하면 음정이 정확히 같을 때만 하나로 뭉쳐 들린다. 어긋나면 **주기적으로 출렁인다**(맥놀이). 🔴 **사람은 이 흔들림을 음정 차이보다 훨씬 민감하게 알아챈다** — A3 에서 5센트 차이가 1.6초에 한 번 출렁이므로, 2초짜리 음이면 들린다.
- **시험 내용**
  1. `pitch-acc-source.wav` 를 Audio In 트랙으로 가져온다(A3 · C4 · E4 · G4, 각 2초).
  2. Pitch Editor → `Analyze` → 네 블록이 **A3 · C4 · E4 · G4** 로 잡히는지 먼저 확인한다.
  3. 넷을 모두 골라(첫 블록 클릭 → 마지막 Shift+클릭) **+2 반음** 올리고 `Apply`.
  4. 다른 트랙에 `pitch-acc-ref+2.wav` 를 가져와 **시작 위치를 정확히 0 으로** 맞춘다.
  5. 두 트랙을 함께 재생한다 → 🔴 **출렁임이 들리는가?**
  6. `+5` · `−3` 도 각각 해당 기준 파일로 반복한다.
- **판정 기준**: **출렁이지 않으면 합격**(5센트 이내). 느리게 한두 번 출렁이면 5~10센트, 확연히 떨리면 20센트 이상으로 불합격.
- ⚠️ **두 클립의 시작이 어긋나면 맥놀이가 아니라 에코로 들린다.** 스냅을 켜고 둘 다 0 에 붙일 것.
- 📌 계측은 이미 통과했다(하네스 오차 중앙값 0.3센트, 생성 음원 검증 0.2센트). 이 시험은 그 수치가 **실제 앱에서 · 실제 파일로 · 귀로도** 성립하는지를 본다.

### T-2.8.2-1. 🔴 프린트 뒤 되돌리기 (v2.8.1 결함 재시험)
- **시험 내용**
  1. 노트 몇 개를 옮기고 `Apply` 한다.
  2. 그 노트들을 다시 골라 **`Reset`** 한다(또는 마우스로 원래 자리까지 끌어 되돌린다).
  3. 🔴 **`Apply` 버튼이 여전히 켜져 있는지** 본다(v2.8.1 에서는 꺼졌다).
  4. `Apply` 를 누른다 → *"No corrections remain — the original take is back."* 가 뜨고 **원곡으로 돌아가는지** 재생해 확인한다.
  5. `Ctrl+Z` 로 보정된 상태가 되살아나는지.
  6. 일부만 되돌린 경우(한 노트는 그대로 두고 나머지만 Reset) `Apply` 가 **남은 편집만** 반영하는지.
- **판정 기준**: 화면의 편집 상태와 들리는 소리를 **언제나 맞출 수 있다.** `Apply` 가 꺼져서 막히는 경우가 없다.

### T-2.8.1-2. 원본은 안전한가 · Revert · Undo
- **시험 내용**
  1. `Apply` 후 `Revert` 를 누른다 → **원래 노래로 돌아오는지** 재생해 확인한다.
  2. 🔴 Revert 후에도 **노트 편집이 그대로 남아 있는지** 본다(블록이 옮긴 자리에 그대로, 색도 유지). 바로 다시 `Apply` 할 수 있어야 한다.
  3. `Apply` 후 **`Ctrl+Z`** → 한 번에 원래 소리로 돌아오는지. `Ctrl+Y` 로 다시 보정된 소리가 되는지.
  4. 🔴 **같은 클립에 `Apply` 를 두 번, 세 번** 한다(중간에 노트를 조금씩 더 옮기며) → **아티팩트가 쌓이지 않는지** 확인한다. 매번 **원본 테이크에서 다시 그리므로** 세 번 한 결과가 한 번 한 것과 같은 품질이어야 한다.
  5. **원본 WAV 파일이 그대로인지** — 프로젝트 폴더의 원본 녹음 파일 크기·수정시각이 변하지 않았는지 본다. 새 파일은 `<프로젝트> Audio/Consolidated/` 에 `...Pitched...` 이름으로 생긴다.
- **판정 기준**: 원본은 절대 변하지 않는다. Revert·Undo 가 소리를 되돌리고 편집은 남는다. 반복 Apply 가 품질을 떨어뜨리지 않는다.

### T-2.8.1-3. 저장 · 재열기 · Export · 네이티브
- **시험 내용**
  1. `Apply` 후 **프로젝트를 저장하고 앱을 껐다 다시 연다** → 🔴 보정된 소리가 **그대로 재생되는지**.
  2. 재열기 후 Pitch Editor 를 열고 `Analyze` → **곡선과 노트 편집이 살아 있는지**, `Revert` 버튼이 여전히 활성인지.
  3. **Export** 한다(WAV) → 🔴 **내보낸 파일에서도 보정된 소리가 나는지.**
  4. 🔴 **네이티브 엔진**과 **웹 폴백**의 소리가 같은지(A/B).
     - ⚠️ **장치 설정에 엔진을 고르는 스위치는 없다**(2026-09-22 확인 — 앞선 시험서의 안내가 틀렸다). 앱은 시작할 때 JUCE 엔진에 자동으로 붙고, 붙지 못하면 웹 오디오로 **자동 폴백**한다.
     - **웹 폴백으로 들어 보는 법**: 앱이 켜진 상태에서 작업 관리자로 **`FocusDAW-AudioEngine.exe` 를 종료**한다 → 브리지가 연결이 끊긴 것을 알아채고 웹 엔진으로 내려간다(콘솔에 `Falling back to Local Web Audio`). 그 상태로 같은 구간을 재생해 비교한다.
     - 원래대로 돌리려면 앱을 다시 시작한다.
  5. 보정한 클립을 **다른 트랙으로 Copy to track** 하거나 **Merge** 해 본다 → 소리가 유지되는지.
- **판정 기준**: 🔴 **재생 · Export · 네이티브가 모두 같은 소리**다. 이 중 하나만 원음이면 결함이다.

### T-2.8.1-4. De-noise 와의 순서 (v2.8.1 에서 닫은 자리)
- **시험 내용**
  1. 노트를 옮겨 편집을 만든다(아직 `Apply` 하지 않는다).
  2. 보컬 스트립에서 **De-noise 를 프린트**한다.
  3. Pitch Editor 로 돌아와 `Analyze` → `Apply`.
  4. 🔴 **잡음이 되돌아오지 않는지** 확인한다.
- **판정 기준**: De-noise 결과 위에 보정이 얹힌다. 잡음이 되살아나면 실패다.
- 📌 **배경**: 피치 렌더는 언제나 "원본"에서 다시 그리는데, De-noise 가 그 원본을 갱신하지 않으면 **De-noise 이전 오디오로 되돌아간다.** v2.8.1 에서 De-noise 가 원본 기준을 함께 옮기도록 고쳤다.

### T-2.8.1-5. 긴 클립 · 무회귀
- **시험 내용**
  1. **1분 이상 클립**에서 여러 노트를 옮기고 `Apply` → 진행률이 정상적으로 오르고 **앱이 얼지 않는지**. 걸린 시간을 대략 적어 주시면 좋다.
  2. `Apply` 중에 **창을 다른 창으로 가려** 본다 → 🔴 가린 상태에서도 렌더가 계속 도는지(끝나는지).
  3. 노트를 옮기지 않은 상태에서 `Apply` 가 **꺼져 있는지**.
  4. v2.7.x 기능 무회귀 — 드래그 · Ctrl/Shift 선택 · Split/Merge · Snap · Reset · Undo 가 종전대로인지.
  5. 스템(`file`) 트랙에는 Pitch Editor 가 **안 보이는지**(v2.6.0 규칙).
- **판정 기준**: 긴 클립도 앱이 반응하는 채로 끝난다. 기존 기능이 그대로다.

## v2.4.0 — Pitch Editor Stage C (노트 세그멘테이션)

> Stage C도 **오디오를 건드리지 않는다** — 화면과 판정만 바뀐다. 합성 신호 시험 24건과 Electron 실구동은 이미 통과했으므로, 여기서 볼 것은 **"직접 부른 노래에서 블록이 부른 음표와 맞는가"** 하나다. 이것은 합성 신호로는 대신할 수 없다.

### [시험 환경 준비]
1. 개발 앱 실행(`npm start`).
2. **Audio In 트랙에 보컬 녹음 클립**이 있는 프로젝트를 연다. 가능하면 두 가지 — ⓐ **BPM이 설정된 프로젝트**(스템에서 감지했거나 직접 입력한 것), ⓑ **BPM이 없는 프로젝트**.
3. 클립 우클릭 → `Pitch Editor...` → 우측 패널 `Analyze`.

### T-2.4.0-1. 노트 블록이 부른 음표와 맞는가 (핵심)
- **시험 내용**
  1. Analyze가 끝나면 곡선(빨강) 위에 **노트 블록(amber)** 이 올라온다. **블록 하나가 부른 음 하나와 맞는지** 본다. 특히 **비브라토를 준 긴 음**이 여러 조각으로 쪼개지지 않는지 확인한다.
  2. **블록 안의 음이름**(`A3` 등)이 실제로 부른 음과 맞는지 본다. 확인 방법 — 블록이 놓인 줄의 **왼쪽 건반을 클릭**해 소리를 듣고 그 자리를 재생해 비교한다.
  3. 블록이 넓을 때 음이름 옆에 **센트 편차**(`A3 +12¢`)가 붙는다. 줌을 당겨(`Ctrl`+휠) 넓혔다 좁혔다 하며 **글자가 블록을 넘치지 않는지** 본다.
  4. 프로젝트 Key가 감지돼 있으면 **스케일 밖 음은 테두리가 점선**이다. 실제로 그 음이 조성에서 벗어난 음인지 확인한다.
  5. **블록을 클릭**하면 테두리가 밝아지고 **상태줄 왼쪽**에 그 노트 정보가 뜬다(`F#4 · 0:01.98 → 0:02.40 · 0.42 s · +25¢ · 75% conf`). `Ctrl`/`Shift`+클릭으로 여러 개를 고르면 `N notes selected`로 바뀐다. **블록이 아닌 빈 곳을 클릭하면 종전대로 재생 위치가 이동**하는지도 확인한다.
- **판정 기준**: 블록 개수가 **부른 음표 수와 대체로 일치**하고(한두 개 차이는 정상 — 글리산도·꾸밈음의 경계는 사람마다 다르게 볼 수 있다), 블록 안 음이름이 실제 음정과 맞는다. **한 음이 서너 조각으로 쪼개지는 일이 없어야 한다.** 어긋나면 **그 구간의 시간 위치와 부른 음**을 함께 보고.

> **T-2.4.0-2(BPM 규칙) · T-2.4.0-3(무회귀)은 2026-09-02 통과** → [시험-아카이브.md](시험-아카이브.md). **여기 남은 것은 T-2.4.0-1 하나다.**
>
> ⚠️ **재시험 시 참고** — 합성 시험 신호(`node tools/make-pitch-test-audio.js`, 프로젝트 BPM **105**)를 쓰면 정답을 알고 대조할 수 있다. 정답표는 `test-audio/pitch-test-notes.md`. 이미 알려진 결함 2건은 **다시 보고하지 않으셔도 된다** — ① 깊은 비브라토(±70¢ 이상)가 한 음을 쪼갬([버그.md](버그.md) `B-Vibrato-Fragment`, 미수정) ② 하모니 구간은 단성 추적기의 원리적 한계.

## v2.0.2 — Pitch Editor Stage B (피치 트래킹)

> Stage B도 **오디오를 건드리지 않는다**. 곡선의 정확도를 보는 T-2.0.2-1은 2026-08-28 통과했고(실제 음정 대조는 개발 중 지속 확인), **여기 남은 것은 긴 클립에서의 속도와 무회귀뿐**이다.

### [시험 환경 준비]
1. 개발 앱 실행(`npm start`).
2. **Audio In 트랙에 보컬 녹음 클립**이 있는 프로젝트를 연다. 가능하면 두 가지를 준비한다 — ⓐ **또렷하게 부른 30초 안팎의 짧은 클립**, ⓑ **1분 이상의 긴 클립**(속도·진행률 확인용).

### T-2.0.2-2. 긴 클립에서의 속도와 무회귀
- **시험 내용**
  1. **1분 이상 클립**에서 `Analyze`를 눌러, 푸터의 `analysed in n.n s`를 확인한다(참고치: 합성 신호 기준 1분 ≈ 0.9초, 5분 ≈ 4.2초).
  2. 분석 중과 후에 **재생·정지·클립 편집**이 평소대로 되는지 확인한다.
  3. 프로젝트를 **저장 → 재열기** 후 클립·테이크가 정상인지 본다(분석은 저장하지 않으므로 곡선은 사라지는 것이 정상 — 다시 Analyze하면 된다).
- **판정 기준**: 체감상 앱이 멈추지 않고, 기존 기능에 회귀가 없다. 분석 시간이 **참고치의 3배를 넘으면** 실제 클립 길이와 함께 보고.

## 추후 확인 항목 (보류·장기 관찰) — 버전 무관

### T-1.20.8-1. 앱 종료 시 엔진 비정상 종료 코드 재발 관찰 (장기 tracing)
- 시험 내용: 평소처럼 사용하며 앱을 시작/종료할 때(믹서 창을 열어 둔 채 종료 포함) 콘솔/로그의 `[AudioEngine] Process exited with code ...` 메시지를 관찰한다. 단발성 경합이므로 일정 기간 반복 관찰이 필요하다.
- 판정 기준: 종료 코드가 항상 정상(1 = Electron kill, 또는 0)이고, `3221225477`(0xC0000005) 등 비정상 코드가 재발하지 않는다. 재발 시 `%LOCALAPPDATA%\CrashDumps`의 새 덤프 유무를 함께 확인해 보고한다.
