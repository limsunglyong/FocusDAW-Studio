# FocusDAW Studio — 버그 기록

> 사용자 보고 버그와 원인/수정 이력. 수정 완료 후에도 재발 관찰이 필요한 항목은 남겨둔다.

---

## B-Ghost-Breath — 숨 쉬는 구간에 없는 노트가 생김 (Pitch Editor)

- **상태**: ✅ **완화(v2.4.7, 2026-09-02)** · 사용자 시험 대기(T-2.4.7-1)
- **보고**: 2026-09-02, 사용자. 실제 녹음(`Audio In 1 20260825T083958 (Consolidated) 4.wav`)에서 **프레이즈 사이 숨 쉬는 구간에 곡선이 크게 튀며 노트가 생긴다** — "서~"와 "다" 사이에 `C4`, "우리~"와 "이야기" 사이에 `A5`, "그리운" 뒤 숨 구간에 `C4`.
- **원인**: 유성 판정이 **RMS 게이트(peak × 0.02)** 와 `_yinFrame`의 자체 폴백(`cmnd ≤ 0.6`)뿐이었다. 숨소리·자음은 RMS를 넘고, 폴백은 **conf 0.4까지 통과**시키므로 "자신 없는 추정"이 그대로 노트가 됐다.
- **수정**: `PITCH_MIN_CONF = 0.5` 유성 게이트. conf가 그 아래면 **무성으로 남긴다** — 숨에 대한 정직한 답은 "음이 없다"이다.
- **계측(위 녹음)**: 국소중앙값에서 6반음 이상 벗어난 구간 **68 → 45**, 그중 **위로 튀는 것 26 → 10**(유령 노트의 전형적 모습). 합성 solo는 **곡선 40/40 그대로, 유성 비율 80% → 79%** — 깨끗한 단성에서는 비용이 사실상 0이다.
- ⚠️ **0.6으로 더 올리지 말 것(재계측 없이는)**. 그 녹음에서는 42로 조금 더 좋았지만 **작게 부른 진짜 음까지 버리기 시작**한다 — 사용자에게는 "노트가 그냥 사라진" 것으로 보인다.

---

## B-Chorus-Polyphonic — 합창(2~3성) 녹음은 음정 추적의 원리적 한계 밖

- **상태**: ⬜ **수정 대상 아님 — 한계 기록**(2026-09-02)
- **경위**: 사용자가 T-2.4.6-1에 쓴 시험 녹음이 **"한 명이 아니라 2~3명 이상의 여성 합창"** 이었다. v2.4.6·v2.4.7로 오류를 크게 줄였으나(구간 114 → 45) 나머지는 임계 조정으로 사라지지 않는다.
- **근거 — 신뢰도로 걸러지지 않는다**: 벗어난 프레임의 **중앙 신뢰도가 0.81**로 정상 프레임(0.91)과 거의 같다. conf 하한을 0.4로 둬도 나쁜 프레임의 **21%** 만 지워진다. 즉 YIN이 **자신 있게 다른 성부를 짚고 있다.**
- **원리**: YIN은 단성 추적기다 — 신호에 주기가 하나라고 가정한다. 2~3성이 동시에 울리면 차분함수에 **경쟁하는 주기가 여럿** 생기고, 어느 하나를 고르는 것이 원래 임의적이다. 완전5도처럼 단순한 음정에서는 두 음의 **공약수(한 옥타브 아래)** 가 가장 깊은 딥이 되기도 한다 — 알고리즘상 정상이다.
- **의미**: Pitch Editor의 대상은 **단성 보컬 테이크**다. 이는 분석만의 문제가 아니라 **Stage E(음정 보정)의 문제이기도 하다** — 합창을 보정하면 모든 성부가 함께 밀리므로 애초에 원하는 결과가 나오지 않는다.
- **조치**: 고치지 않는다. 대신 **시험은 단성 녹음으로 한다**(사용자와 합의 필요). 합창에서 어디까지 되는지는 위 계측치를 기준선으로 남긴다.

---

## B-Octave-Down — 유성 구간 중간에서 한 옥타브 아래로 잘못 읽힘 (Pitch Editor)

- **상태**: ✅ **수정(v2.4.6) · 실녹음 확인 후 보강(v2.4.7)** — 사용자 시험 대기(T-2.4.7-1)
- ⚠️ **2026-09-02 실녹음 시험 결과**: 합성 신호는 완벽했으나 **실제 녹음(2~3성 합창)에서는 오류가 남았다.** 국소중앙값에서 6반음 이상 벗어난 구간 기준 **v2.4.5 = 114 → v2.4.6 = 68 → v2.4.7(유성 게이트 추가) = 45.** 남은 35건은 대부분 아래로 벗어나는 것이고 **합창이라는 조건 자체가 원인**이다 → `B-Chorus-Polyphonic`. ⚠️ **`RATIO`에 상한을 두는 안도 계측했으나 기각했다** — 위로 튀는 것은 줄지만(26→6) 아래로 벗어나는 것이 **35 → 60으로 악화**된다. 무제한이 총량에서 낫다.
- **정보**
  - **앱 버전**: v2.4.5 (Stage B가 들어온 v2.0.2부터 존재)
  - **보고**: 2026-09-02, 사용자. 최초 보고는 실제 녹음에서 "마지막 노트가 F2인데 실제로는 F4/F3로 들린다". 재현 파일은 `Pitch-Test-Solo(2).wav`(합성 시험 신호에 노이즈를 섞은 것) **26~28.5초 구간에서 F4가 중간에 한 번 F3로 바뀜.**
- **증상**: 한 음이 이어지는 도중 **약 300 ms 동안만 정확히 한 옥타브 아래**로 읽힌다. 음정이 흔들리는 것이 아니라 정확히 −12반음이고, 앞뒤는 정확하다. **실제 보컬 녹음에서 자주 나타난다**(사용자).
- ⚠️ **신뢰도로는 거를 수 없다** — 틀린 구간의 conf가 **0.85~0.93**으로 정상 구간과 같다.
- ⚠️ **기존 중앙값 스무딩으로도 못 잡는다** — 오류가 **연속 25프레임(270 ms)** 이라, 반경 2프레임(53 ms) 스무딩의 사정거리 밖이다.
- **원인**: `_yinFrame`이 **절대 임계(0.15)로 상대 질문을 판정**하고 있었다. 진짜 주기의 딥은 **사라진 적이 없고**, 임계 바로 양옆에서 오락가락할 뿐이다 —

  | 프레임 | cmnd @ τ(진짜 F4) | cmnd @ 2τ | 결과 |
  |---|---|---|---|
  | 27.08 s | 0.1216 (< 0.15) | 0.0739 | F4 ✅ |
  | 27.28 s | **0.1523 (> 0.15)** | 0.0875 | F3 ❌ |
  | 27.40 s | **0.2019 (> 0.15)** | 0.1431 | F3 ❌ |
  | 27.49 s | 0.1129 (< 0.15) | 0.0858 | F4 ✅ |

  차이가 **0.03**이다. 노이즈가 진짜 딥을 임계 위로 살짝 밀어 올리는 순간, "임계를 처음 밑도는 τ"를 찾는 주사가 진짜 주기를 **건너뛰고 2τ를 집는다.** 실제 녹음에서 자주 나오는 이유도 이것이다 — 0.03은 어떤 녹음에서든 프레임 간 요동 범위 안이다.
- **수정(v2.4.6)**: [audio-engine.js](audio-engine.js) `_yinFrame`에 **하위배수 검사**를 넣었다. 딥을 고른 뒤, 그 τ의 **정확한 약수(τ/2, τ/3, τ/4)** 자리에 거의 비슷한 깊이의 딥이 있으면 그쪽이 진짜 주기이므로 **가장 짧은 것(=가장 높은 음)** 을 택한다. 임계값을 조정하는 대신 **상대 질문을 상대적으로 판정**하는 것이다.
- **`RATIO = 2.5`의 근거(추측 아님, 계측)**: 반대 방향 오류(옥타브 위로 잘못 올라감)가 걱정거리라 따로 쓸었다 — F3~F4를 기본파 60/30/10/0%로 줄이고 짝수 배음을 지배시킨 신호에 −60/−32 dB 잡음, 비율 1.9/2.5/3.5/5.0. **새로 생기는 옥타브 위 오류 0건**(몇몇 +12는 수정 전에도 똑같이 나며, 기본파가 아예 없고 짝수 배음만 있는 신호는 실제로 τ/2에서 반복되므로 모호한 것이 맞다). YIN은 스펙트럼이 아니라 **파형 주기**를 보므로 기본파를 없앤다고 τ/2에 딥이 생기지 않는다 — 여유를 이만큼 줄 수 있는 이유다.
- ⚠️ **기각한 대안 — "국소 중앙값에 가장 가까운 옥타브로 스냅"(사후 보정)**. 같은 케이스를 고치기는 한다(±350 ms 창에서 0건). 그러나 **추적기가 옥타브를 뛴 것과 가수가 옥타브를 뛴 것을 구분하지 못한다** — 300 ms 음으로 된 레가토 `C4 C5 C4 C5 C4`에서 **진짜 도약 5개 중 3개를 뭉갰다.** 창을 좁히면(±250 ms) 이번엔 오류 구간이 스스로 다수파가 되어 원래 버그를 못 고친다. 채택한 하위배수 검사는 **자기 딥이 이미 최선인 프레임은 건드리지 않는다.**
- **계측 결과**

  | 파일 | 수정 전 옥타브 오류 프레임 | 수정 후 |
  |---|---|---|
  | `Pitch-Test-Solo(2).wav` (재현) | **32 / 1576** | **0** ✅ |
  | `pitch-test-solo.wav` (회귀) | 0 / 1575 | 0 ✅ |
  | `pitch-test-full.wav` (회귀) | 0 / 1569 | 0 ✅ |

  곡선 정확도는 세 파일 모두 **40/40 유지**. 구간 A(레가토 스케일) 8/8 유지, 구간 C 순서(1/8<1/16<1/32) 유지.
- **덤**: 하모니 구간에서 **유령 옥타브가 줄었다** — `F3+A3+C4`가 F2 → **F3**, `G3+B3+D4`가 G2 → **G3**. 단성 추적기의 한계는 그대로지만 최저 성부를 짚는 쪽으로 옮겨갔다.
- **성능**: 47.4초 클립 607→638 ms. 프레임당 덧붙는 것이 약 15회 비교뿐이라 차분함수(프레임당 ~90k 곱셈-덧셈)에 비해 무시할 수준이다.
- **회귀 확인 방법**: `node tools/make-pitch-test-audio.js` 후 세 파일을 분석해 **옥타브 오류 프레임 0** 과 **곡선 40/40**을 함께 볼 것.

---

## B-Vibrato-Fragment — 깊은 비브라토 한 음이 여러 블록으로 쪼개짐 (Pitch Editor Stage C)

- **상태**: 🔴 **원인 규명 완료 · 수정 미착수**(2026-09-02 발견). 수정은 사용자와 트레이드오프를 정한 뒤 착수.
- **정보**
  - **앱 버전**: v2.4.5 (Stage C가 들어온 v2.4.0부터 존재)
  - **발견 경로**: T-2.4.0-1을 위해 만든 **합성 시험 신호**([tools/make-pitch-test-audio.js](tools/make-pitch-test-audio.js))로 계측 중 발견. 사용자 육안 보고가 아니라 정답을 아는 신호로 잡았다.
- **증상**: 비브라토가 깊은 긴 음 하나가 **한 블록이 아니라 여러 조각**이 된다. 음이름은 대체로 맞게 읽히므로(F4가 F4 여섯 조각) **음정 오검출이 아니라 세그멘테이션 결함**이다.
- **계측 (A4 · 4박 · 105 BPM, 각 깊이 3회 반복 중 최악값)**

  | 비브라토 깊이 | MIN 1/8 | MIN 1/16 | MIN 1/32 |
  |---|---|---|---|
  | ±20 ~ ±60¢ | 1 ✅ | 1 ✅ | 1 ✅ |
  | ±70¢ | 1 | **5~9** ❌ | **19~24** ❌ |
  | ±80¢ | 1~2 | **4~9** ❌ | **19~27** ❌ |
  | ±120¢ | 1~2 | **7~11** ❌ | **21~28** ❌ |

  비브라토 **속도(4.5 / 5.5 / 6.5 Hz)는 거의 무관**했다. 깊이만이 변수다.
- **원인**: 파단점 **±60¢가 `PE_HYST_ST = 0.6반음 = 60센트`와 정확히 같은 값**이다. 우연이 아니다.
  - 설계 §12-1은 "임계를 넘고 `minNoteSec`의 절반 이상 유지해야 경계로 인정 — **비브라토는 앞을 늘 만족하고 뒤를 결코 만족하지 못한다**"고 적었다. **그 뒷문장이 사실이 아니다.**
  - [pitch-editor-app.jsx](pitch-editor-app.jsx) `peSegmentPass`에서 기준선은 `ref += 0.08 * (v - ref)`로 **천천히 따라간다**(시상수 ≈ 12.5프레임 ≈ 134 ms). 비브라토 주기(4.5~6.5 Hz → 154~222 ms)와 **같은 크기**라, `ref`가 비브라토를 **위상 지연된 채 부분적으로 따라간다.** 그래서 순간 편차 `|v − ref|`가 비브라토 진폭보다 **커지고**, 임계를 넘은 상태가 `holdCount`보다 오래 유지된다.
  - MIN 설정에 따라 갈리는 것도 이것으로 설명된다 — `holdCount = (minNoteSec/2)/hop`이므로 1/8은 9프레임(96 ms)을 요구해 대부분 버티지만, 1/16은 5프레임(53 ms), 1/32는 3프레임(32 ms)이라 쉽게 뚫린다.
- ⚠️ **손대기 전에 정할 트레이드오프**: 단순히 `PE_HYST_ST`를 올리면 **반음 계단(1.0반음)을 잡을 여유가 사라진다.** 현재 0.6은 반음의 60%다. 0.9로 올리면 90%가 되어 구간 A(반음 계단 포함 레가토 스케일, 지금 8/8 정확)가 무너질 수 있다. 그래서 후보는 임계 상향이 아니라 —
  1. **`ref`를 비브라토보다 느리게** 만든다(계수 0.08 → 더 작게, 또는 중앙값 기반). 비브라토를 따라가지 않으면 편차가 진폭을 넘지 않는다.
  2. **주기성 검출**: 편차가 임계를 넘어도 **주기적으로 부호가 뒤집히면** 비브라토로 보고 경계를 열지 않는다. 원인에 가장 직접적이다.
  3. `holdCount`를 비브라토 반주기(≈100 ms)보다 길게 강제한다. 가장 싸지만 빠른 패시지의 진짜 경계도 함께 둔해진다.
- **회귀 확인 방법**: `node tools/make-pitch-test-audio.js`로 신호를 만들고 **구간 B가 세 MIN 설정 모두 4개**가 되는지 본다. 동시에 **구간 A가 8/8을 유지**하는지 반드시 함께 볼 것 — 위 트레이드오프가 실제로 터지는 자리다.

---

## B-ShiftMute-NoRepro — File Tracks의 Shift+Mute가 한 세션에서 동작하지 않았음 (재현 안 됨)

- **상태**: 🟡 **관찰 대기 — 손대지 않음**(사용자 결정 2026-08-31: 원인이 분명하지 않은 상태에서 고치면 다른 문제를 만든다)
- **정보**
  - **앱 버전**: v2.4.0
  - **보고 시각**: 2026-08-31
- **보고**: 스템 9개를 불러와 Drums / Keyboards / Vocals 세 개의 **bounce 트랙으로 합치고 원본을 삭제**한 뒤, Vocals 클립 하나를 Audio In 1로 복사한 세션에서 **File Tracks의 Shift+Mute가 동작하지 않았다.** 같은 세션에서 [Audio In 트랙 무음 버그](앱개발.md)(v2.4.1에서 수정)도 함께 발생했다. **앱을 재시작하니 정상 동작.**
- **조사 결과(v2.4.1 세션)**: 경로를 다시 읽었으나 **깨질 지점을 찾지 못했다.**
  - Shift+Mute는 **순수 렌더러 경로**다 — [ui-tracks.jsx](ui-tracks.jsx) `MuteBtn`의 `onClick`이 `isFileGroupTrack(track)`을 보고 [app.jsx](app.jsx) `muteAllFileTracks`를 부르며, 그 안은 `DAW.setTrackParam` 반복이다. **네이티브 엔진과 무관**하므로, 같은 세션에서 함께 나온 무음 버그(브리지 래퍼 누락)와는 **원인을 공유하지 않는다.**
  - v2.3.2에서 판정을 `isFileGroupTrack` 한 곳으로 모았으므로 **bounce 트랙이 대상에서 빠지는 옛 결함은 아니다**(그 시험 T-2.3.2-1 통과).
  - **현재 재현되지 않는다.**
- **재발 시 확보할 것**: ① 누른 트랙이 **File Tracks 그룹의 것**이 맞는지(Audio In 트랙의 M은 Shift가 걸리지 않는 것이 설계된 동작) ② DevTools 콘솔(`Ctrl+Shift+I`)의 **빨간 오류** 유무 — 핸들러에서 예외가 나면 그 뒤 클릭이 먹지 않을 수 있다 ③ 그 시점의 `DAW.tracks.map(t => [t.name, t.kind, t.params.mute])` ④ **버튼 표시는 바뀌는데 소리만 안 바뀌는 것인지**, 버튼 자체가 반응하지 않는 것인지의 구분.

---

## B-TakeSibling-OverShift — 일반 clip 이동 시 비활성 Take clip이 함께 이동함

- **상태**: ✅ 수정 완료(v1.43.7, 2026-07-21) · 사용자 시험 통과(T-1.43.7-1, 2026-07-21)
- **정보**
  - **앱 버전**: v1.43.6
  - **버그 발견 시간**: 2026-07-21 14:21 KST
- **보고**: Audio In 트랙에 일반 clip 3개(순서대로 1번, 2번, 3번)가 있고, 3번 clip에 punch-repeat로 Take A/B/C/D 4개가 녹음된 상태. Take B가 active일 때 1번 clip을 이동하면 Take B를 제외한 Take A/C/D가 1번과 함께 이동한다. 2번 clip을 이동해도 동일하게 Take A/C/D가 따라 움직인다. 반면 3번 clip(active Take B)을 이동하면 Take A/B/C/D가 모두 함께 움직이며, 이 경우는 정상 동작이다.
- **기대 동작**: Take clip은 **해당 원본/대표 clip의 위치가 변경될 때만** 함께 이동해야 한다. 1번·2번 같은 일반 clip 이동은 3번 clip의 Take A/C/D 위치에 영향을 주면 안 된다.
- **원인 분석**: UI는 [ui-tracks.jsx](ui-tracks.jsx)에서 보이는 clip만 드래그 대상으로 잡는다. `takeId === null`인 일반/base clip과 현재 active take clip만 메인 레인에 표시되고, inactive take clip은 직접 선택되지 않는다. 그러나 실제 커밋은 [app.jsx](app.jsx) `handleMoveClip` → [audio-engine.js](audio-engine.js) `moveClip`으로 들어가며, `moveClip`은 이동한 clip이 일반 clip인지 active take clip인지 구분하지 않고 이동 후 항상 `_shiftInactiveTakeClips(t, delta)`를 호출한다.
- **문제 지점**: [audio-engine.js](audio-engine.js)의 `_shiftInactiveTakeClips(track, delta)`는 현재 active take가 아닌 **모든 take clip**을 같은 delta로 이동시킨다. 따라서 active가 Take B일 때 1번 또는 2번 일반 clip을 이동해도, 함수가 Take A/C/D 전체를 같이 밀어버린다. 3번 active Take B 이동에서는 B가 직접 이동되고 A/C/D가 보조 함수로 이동하므로 정상처럼 보인다.
- **영향 범위**: 단일 드래그뿐 아니라 `moveClipsByResolved`, `moveClipsBy`, `nudgeClip`도 같은 보조 함수를 호출하므로 다중 선택 이동과 키보드 nudge에서도 같은 유형의 동반 이동이 발생할 수 있다.
- **수정 방향(메모)**: `_shiftInactiveTakeClips`를 무조건 호출하지 말고, 이동 대상이 active take clip이거나 해당 take region의 대표/원본 clip일 때만 호출하도록 조건을 좁혀야 한다. 일반 `takeId=null` clip 이동은 take sibling 이동을 트리거하면 안 된다.
- **시험 확인**: T-1.43.7-1 사용자 확인 완료. 1번·2번 일반 clip 이동 시 Take A/B/C/D는 제자리에 남고, 3번 active Take B clip 이동 시 Take A/B/C/D가 함께 이동하는 정상 동작 확인.

---

## B-Native-RelPath — 네이티브 엔진이 bounce WAV를 상대경로로 찾다 "File not found"

- **상태**: ✅ 수정(v1.43.5, 2026-07-21) · 사용자 시험 대기(T-1.43.2-1 ⑤)
- **보고**: 2026-07-21(T-1.43.2-1, v1.43.4). 오디오는 정상 로드되나 재시작 시 터미널: `[AudioEngine Err] File not found: CCC_untitled Audio/Bounces/….wav`(bounce 2개). 원본 스템·Audio In은 로그 없음.
- **원인**: 수집 소스 `filePath`는 상대경로. 렌더러 재연결은 `resolveSourcePath`로 절대경로를 만들어 읽지만, **네이티브 브리지 `loadTrack`은 `track.filePath`(상대) 그대로** 넘김([audio-bridge.js](audio-bridge.js) 2곳). 네이티브는 경로로 열고 cwd=앱 루트라 상대경로 실패. Audio In은 baked→temp WAV(절대)라 무사, un-edited bounce/파일 트랙만 해당.
- **수정**: 재연결 때 해석한 절대경로를 `addFileBuffer(absPath)`로 넘겨 `track._nativePath`(런타임, 미직렬화)로 stamp([app.jsx](app.jsx)·[audio-engine.js](audio-engine.js)), 브리지가 `track._nativePath || track.filePath`로 네이티브에 전달([audio-bridge.js](audio-bridge.js)). 저장 filePath는 상대 유지(이식성). 상세 [앱개발.md](앱개발.md) v1.43.5.

---

## B-Boot-NoAudio — 재시작 자동복원 시 "NO AUDIO"(수집 오디오 미재연결)

- **상태**: ✅ 진짜 원인 수정(v1.43.4, 2026-07-21) · 사용자 시험 대기(T-1.43.2-1 ①). (v1.43.3은 부분 방어선)
- **보고**: 2026-07-21(T-1.43.0-1 재시험). Save As 저장 후 재시작 시 **bounce/수집 트랙만 NO AUDIO**(원본 스템=절대경로는 정상). 터미널 로그: `read-audio-file: Invalid audio file path`(6회, [electron/main.js](electron/main.js):48 `assertFilePath`). "불러오기"로 한 번 열면 이후 재시작은 정상.
- **원인(로그로 확정)**: `assertFilePath`는 **절대경로 아니면 거부**. 부팅 재연결이 상대경로를 **해석 못한 채**(base=null) 넘겨 발생. base가 null인 이유 = **스냅샷의 `projectPath`가 null**. 그 근본은 `saveProject`(Save As) 순서 버그: `onProjectPathChange(경로)`를 **먼저**, `onRenameProject`(→`renameProject`가 `setProjectPath(null)`)를 **나중에** 불러 **방금 저장한 projectPath를 null로 덮음** → 1.5초 autosave가 스냅샷 projectPath를 null로 클로버. File>Open(`loadProjectJson`)은 순서가 반대(rename 먼저·pathChange 나중)라 멀쩡했다.
- **수정**: ① **핵심** [app.jsx](app.jsx) `saveProject`에서 `onProjectPathChange`를 **`onRenameProject` 뒤로** 이동(v1.43.4) → projectPath 상태·스냅샷이 올바른 절대경로 유지. ② (방어선, v1.43.3) 부팅 재연결이 스냅샷 `projectPath`를 명시로 넘김 + `bootReconnectedRef` 1회 실행. 상세 [앱개발.md](앱개발.md) v1.43.4·v1.43.3.

---

## B-Reopen-DupTracks — 재시작→열기 시 트랙 전체가 2배로 복제됨

- **상태**: ✅ 수정(v1.43.2, 2026-07-21) · **사용자 재시험서 복제 사라짐 확인**(그림4, 2026-07-21) — 전체 재시험은 T-1.43.2-1로 계속.
- **보고**: 2026-07-21(T-1.43.0-1 시험 중). Save As 저장 → 앱 재시작 → 그 상태에서 File>Open → 트랙이 전부 2배로 복제("옛 사본 + 현재 사본"). 오디오 폴더+WAV는 정상 생성됨(수집은 성공), 열기 자체는 오디오 로드됨.
- **원인**: 부팅 시 autosave 스냅샷을 `importProject`로 복원하고 `reconnectProjectAudio`가 오디오를 **비동기 재연결**하는데, 그게 도는 도중 File>Open을 하면 **두 재연결 루프가 동시**에 같은 트랙을 훑는다. `importProject`가 json 트랙 id를 보존하므로 부팅본·열기본이 같은 id를 만들고, 한 루프가 트랙을 재연결(`needsAudio=false`)하면 다른 루프의 `addFileBuffer`가 placeholder를 못 찾아(`t.needsAudio && …` 실패) **새 트랙을 추가** → 전체 복제.
- **수정**: ① [audio-engine.js](audio-engine.js) `addFileBuffer` — `reconnectTrackId`가 있는데 placeholder 미발견 시 `return null`(새 트랙 생성 안 함). ② [app.jsx](app.jsx) `reconnectProjectAudio` — 재진입 티켓(`reconnectSeqRef`)으로 await마다 낡은 루프 중단. 상세 [앱개발.md](앱개발.md) v1.43.2.
- **관찰**: 재시작 직후 "NO AUDIO"(그림2)가 열지 않아도 결국 해소되는지는 T-1.43.2-1 ③에서 확인(부팅 재연결 base/타이밍 별도 가능성).

---

## B-Punch-NoRegion — Punch ON인데 Repeat 구간이 없으면 1차 녹음이 통째로 사라짐

- **상태**: ✅ 수정(v1.43.1, 2026-07-21) · 사용자 시험 대기(T-1.43.1-1)
- **보고**: 2026-07-21. Audio In 트랙 1차 녹음(약 0:13~0:38) 후, **Repeat 구간을 지정하지 않은 채** Punch를 켜고 30초 부근에서 ~3초 녹음 → **1차 녹음 전체가 사라지고** 새 3초 클립만 남음(스크린샷 2장).
- **원인**: Punch는 `DAW.loopRange`(Repeat 구간)를 [in→out]으로 재사용한다. 구간이 없으면 `region=false` → Punch 버튼이 켜져 있어도 `punch`/`loopPunch`가 false가 되어 **일반 `attachRecording`** 으로 강등된다. 일반 녹음은 **v1.40.2부터 "트랙 전체 교체"** 라 기존 오디오가 지워진다. Punch 툴팁이 "fall back to normal"이라 폴백을 의도된 동작으로 문서화했으나, 그 "normal"이 v1.40.2에서 비파괴→파괴로 바뀌면서 **데이터 유실 함정**이 됐다.
- **수정(사용자 결정: 모달 대신 버튼 게이팅)**: [app.jsx](app.jsx) Punch 버튼을 `disabled={!punchRegion}`으로 두어 **유효 Repeat 구간이 없으면 아예 켤 수 없게** 하고, 무효-구간 툴팁을 "To use Punch, set a Repeat region first…"로 교체. `MenuTransport` 본문에 `if (!punchRegion && isPunchOn()) setPunchOn(false)` auto-heal을 넣어 **구간을 나중에 지워도(`setLoopRange(null)`) Punch가 자동 off** → "구간 없이 Punch ON 고착" 원천 봉쇄. 게이트 조건은 "구간 있으면"(Repeat ON/OFF 무관)이라 단일 Punch·Loop-Punch 모두 유지. 상세 [앱개발.md](앱개발.md) v1.43.1.
