# FocusDAW — Stem Studio · 앱 개발 문서

> 이 문서는 **진행 중/향후 과제만** 유지합니다. 완료된 버전별 구현 기록(v1.20.12 이하 전체)은 [앱개발-아카이브.md](앱개발-아카이브.md)로 이관했습니다.
> **현재 버전**: v1.20.12 · **완료**: Phase 1–4(단일 Audio In 녹음까지) · **다음 작업**: Phase 5 — Audio In 클립 편집.

---

## [Phase 4 점검] 단일 Audio In 녹음 — 구현 현황 및 남은 작업 — 2026-07-13

> [개발-프로세스.md](개발-프로세스.md) **Phase 4(단일 Audio In 녹음)** 스펙을 현재 코드와 1:1 대조한 결과다. Phase 4는 v1.18~v1.20에서 사실상 구현되었으나 개발-프로세스.md에는 여전히 "다음 작업"으로 남아 있어(상태 라인 없음) **문서-코드 불일치** 상태다.

### 반영 완료 (핵심 녹음 파이프라인)

| Phase 4 요구 | 상태 | 근거 |
|---|---|---|
| Settings `Device Setup`(Audio Devices) 섹션 | ✅ | v1.19.0 ([ui-dialogs.jsx](ui-dialogs.jsx)) |
| 입력 장치 선택(마이크/USB/인터페이스) | ✅ | v1.19.0 |
| 입력 채널 선택(Mono 1/2, Stereo 1-2) | ✅ | v1.19.4 `buildInputPortOptions` |
| Sample rate / buffer 설정 | ✅ | v1.19.0 |
| 선택 장치를 Audio In 기본 입력으로 사용 | ✅ | `audioInputSettingsForTrack` |
| `+ Audio In` 트랙 생성 | ✅ | [ui-tracks.jsx](ui-tracks.jsx) |
| 트랙별 input gain / ARM / MON | ✅ | [ui-tracks.jsx](ui-tracks.jsx) |
| JUCE `AudioDeviceManager` 입력 설정 확장 | ✅ | [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) |
| 입력 미터 (+ 오버로드 표시) | ✅ | `armedInputLevel` |
| Limiter 기본 On, ceiling −1.0 dBFS | ✅ | [ui-tracks.jsx](ui-tracks.jsx) / [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) |
| writer thread + FIFO 스트리밍 기록 | ✅ | `ThreadedWriter`(32768) |
| start / stop / **cancel** Recording | ✅ | [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp), [audio-bridge.js](audio-bridge.js) |
| 녹음 완료 후 source/clip 추가 | ✅ | `attachRecording` |
| 장치/시작/확정 오류 사용자 표시 | ✅ | [app.jsx](app.jsx) `toggleRecording` alert |
| **limiter gain reduction(GR) 미터** | ✅ | **v1.20.6에서 구현**(메인 헤더 + 믹서) |

### 남은 작업 / 보류 (사용자 협의 반영)

| 항목 | 상태 | 사유·결정 |
|---|---|---|
| **① Input source 선택**(mic/line/Hi-Z) | ⛔ Skip | WASAPI가 앱에 input source를 노출하지 않는 한계. per-source 선택은 **ASIO 드라이버가 있어야** 가능 → 사용자 협의로 **skip**. 인터페이스 구분은 Settings의 입력 장치 선택으로 대체. |
| **② Input latency 표시** | ✅ 완료 | **v1.20.7**에서 Settings Audio Devices의 Active 줄 아래 `Latency: in ≈ X ms · out ≈ Y ms · round-trip ≈ Z ms (driver estimate)` 근사 표시로 구현(아래 v1.20.7 참조). WASAPI 특성상 드라이버 보고 기반 **근사치**임을 문구로 명시. |
| **③ `.wav.part`→`.wav` 확정 스테이징** | ⏸ 보류 | 현재는 최종 `.wav`에 직접 스트리밍 기록하고 **정상 Stop=헤더 확정 / Cancel=파일 삭제**로 안전. `.part` 확장자는 오직 **크래시·정전 등 비정상 종료 시 반쪽 `.wav` orphan**을 "미완성"으로 구분/정리하기 위한 안전장치. 작업원칙 4번(녹음 WAV `Unused` 관리 후 정리 명령)·**Phase 7 정리 도구**와 함께 다루는 것이 자연스러워 보류. |
| **④ GR 미터** | ✅ 완료 | **v1.20.6**에서 구현(위 표 참조). |

### 완료 기준 중 미검증

- "30분 이상 녹음에서 드롭아웃·메모리 증가 없음"은 스트리밍 기록(`ThreadedWriter`+FIFO) 설계상 충족하나, **장시간 실측 시험 기록은 없음**(필요 시 별도 스트레스 시험 권장).

### 권고

- [개발-프로세스.md](개발-프로세스.md) Phase 4 섹션에 `상태: 완료(핵심 파이프라인)` 표기를 추가하고, ①③은 별도 후속(①은 skip, ③은 Phase 7로 이관)으로 명시하면 문서-코드 정합이 맞는다. (②는 v1.20.7, ④는 v1.20.6에서 구현 완료)
- ✅ **반영 완료(2026-07-14)**: [개발-프로세스.md](개발-프로세스.md)에 Phase 4 `상태` 라인 추가 + ①skip·③Phase 7 이관 주석 + "다음 작업 = Phase 5" 표기로 문서-코드 정합을 맞춤. 다음 작업은 **Phase 5 — Audio In 클립 편집**.

## Phase 5 착수 전 문의·결정 정리 — 2026-07-14

> Phase 5(Audio In 클립 편집) 착수 전 사용자 문의에 대한 답변·결정을 누적 기록. (번호 체계: 개발 Phase 5 = 제안.md Phase 3 = 제안.md §6 Audio In 편집 + §7 재생 엔진.)

### Q1. 클립 편집에 crossfade 구현 계획이 있는가?

- **현황(코드)**: 클립 편집 crossfade **미구현**. 문서상 계획만 존재. (검색에 잡히는 crossfade는 SoundTouch 타임스트레치 내부 overlap crossfade로 별개 DSP.)
- **계획상 위치**: crossfade는 **개발 Phase 7(Comp/Consolidate)** 소속 — "Comp 구간 경계에 equal-power crossfade 자동 생성"([제안.md](제안.md) §6 "Take 선택과 Comp", [개발-프로세스.md](개발-프로세스.md) Phase 7). **개발 Phase 5에는 crossfade 없음**, 대신 클립 경계 **micro fade 3~5ms**(click/pop 방지용 fade in/out, crossfade 아님)만 있음([개발-프로세스.md](개발-프로세스.md) Phase 5).
- **제약**: [제안.md](제안.md) §6 시간축 이동은 **v1에서 같은 트랙 내 클립 겹침 금지**(겹친 녹음은 Take Lane으로 표현)로 규정 → 일반 DAW식 "겹친 두 클립 수동 crossfade" 편집은 현재 로드맵에 없음.
- **결정 지점**: (a) 로드맵 유지 = Phase 5는 micro fade만, crossfade는 Phase 7 Comp에서 처리(권장, 겹침 금지 규칙과 정합) / (b) Phase 5에 수동 클립 crossfade(겹침 허용) 추가 = 겹침 금지·Take Lane 규칙과 충돌하므로 데이터 모델·재생 엔진 설계 변경 필요. → **결정 대기.**

### Q2. 음정 튜닝 기능이 있는가? (일반 기능 문의)

- **① 전체 키 변경(트랜스포즈) → 있음**: **Vari Key** — Key 자동 감지(`detectedKey`) 후 반음 단위 오프셋으로 전곡 재생 피치 이동([audio-engine.js](audio-engine.js) `keyShift`/`setKey`/`setKeyShift`, `variKey`). SoundTouch RateTransposer로 재생·Export 반영. 관련: Vari BPM(템포), Keep pitch/preservePitch(템포만 변경·피치 유지). Key 설정창 개편은 v1.10.x.
- **② 오토튠식 음정 보정(피치 커렉션) → 없음**: per-note auto-tune/Melodyne류 기능은 코드에 없고 로드맵([제안.md](제안.md)·[개발-프로세스.md](개발-프로세스.md))에도 없음(로드맵은 파일 트랙·Audio In 녹음·클립·Take·Comp 범위). SoundTouch는 신호 전체 균일 transpose만 하며 음 단위 검출·재합성은 미지원. 추가 시 로드맵 밖 신규 기능(음정 검출 + PSOLA/phase vocoder 등 재합성)으로 규모 큼.
- **정리**: 키/피치 트랜스포즈(Vari Key)는 있음, 음정 보정(auto-tune)은 없음.

### Q3. clip 복사/붙여넣기 기능이 있는가?

- **현황**: 없음(코드·로드맵 모두). 현재 클립 편집은 split/join(`handleSplit`/`handleJoin`)·clip volume 기초뿐([app.jsx](app.jsx)). [제안.md](제안.md)·[개발-프로세스.md](개발-프로세스.md)에 clip copy/paste 언급 없음(문서의 "복사"=Save As 파일 복사, "복제"=피해야 할 버퍼 복제로 다른 맥락). Phase 5 명시 범위(이동·trim·delete·deleteRange·clip gain·micro fade)에도 copy/paste 없음.
- **추가 난이도 낮음**: Phase 1 source/clip 모델 덕분에 clip 복사 = **같은 `sourceId` 참조하는 새 clip을 다른 `start`에 배치**(오디오 복제 없이 비파괴). 붙여넣기 = playhead/대상 위치 삽입. Phase 5에 이동/trim/delete와 함께 얹기 좋은 후보.
- **결정 지점**: Phase 5 범위에 **clip 복사/붙여넣기(+duplicate) 추가 여부** → 결정 대기.

### Q4. Punch Recording(원하는 구간만 다시 녹음)이 계획에 있는가?

- **현황**: 없음(코드·로드맵 모두 별도 기능 미명시). 현재 녹음은 playhead→곡 끝(또는 수동 정지)까지 기록. ([app.jsx](app.jsx)의 "punch"는 BPM 표시 애니메이션으로 무관. 문서의 "재녹음"은 트랙 전체 교체 맥락이지 구간 punch 아님.)
- **기반 요소 존재**: ① 구간 `loopRange` 이미 있음(구간 반복 재생용). ② 개발 Phase 6(루프 Take 녹음)의 `startRecording({ trackId, start, end, loopEnabled, countIn })`에 구간(start/end) 파라미터가 계획됨(단 회차별 Take/루프용이지 "구간 교체" punch와는 목적 다름). ③ Phase 5 클립 모델(비파괴 배치/trim)로 구간 클립 교체가 용이.
- **추가 시 형태**: pre-roll 재생 → in 지점 자동 punch-in → out 지점 자동 punch-out → 해당 구간 클립만 배치/교체(비파괴). Phase 4/5/6 연계 신규 항목.
- **결정 지점**: Punch Recording을 신규 로드맵 항목으로 추가할지 → 결정 대기.

### Q5. Auto-tune 설계 방향(별도 창 + 피아노 롤로 구간별 음 올림/내림) 검토 — Q2 연장

사용자 구상: 추후 **별도 window**에 **피아노 롤**을 배치하고, 그에 맞춰 **특정 구간의 음을 올리거나 내리는** 수동 피치 편집(Melodyne류 그래픽 보정). 실현 가능성 평가:

- **구조·UI·아키텍처 → 무리 없음**: 독립 Electron 창 + BroadcastChannel 동기화 패턴 확립됨([mixer-app.jsx](mixer-app.jsx)·[advanced-pan-app.jsx](advanced-pan-app.jsx)·[advanced-eq-app.jsx](advanced-eq-app.jsx)·[advanced-ambience-app.jsx](advanced-ambience-app.jsx)). 비파괴 편집은 Phase 1 source/clip 모델에 "피치 편집 메타데이터 + 렌더 캐시 source"로 정합.
- **핵심 리스크(DSP·범위)**:
  1. **모노포닉 한정 권장**(보컬/리드/베이스). 폴리포닉은 연구급 난이도로 비권장.
  2. **피치 검출(f0 tracking)** 선행 필요(YIN/pYIN 등) — 피아노 롤에 현재 음 표시용.
  3. **피치 시프트 품질**이 최대 리스크 — SoundTouch는 전체 균일 transpose용이라 보컬 구간 보정 품질(포먼트 보존) 부족. Melodyne급은 포먼트 보존 PSOLA/phase-vocoder 또는 전용 라이브러리 필요.
  4. **오프라인 렌더 방식 권장**("편집 → 새 WAV 렌더 → 클립 교체") — 웹/네이티브 두 엔진은 결과 WAV만 재생하므로 [작업원칙 6](개발-프로세스.md) 충돌 원천 회피. 실시간 시변 피치의 이중 엔진 동기화는 부담·위험 큼.
- **규모·위치**: 로드맵 밖 **대형 신규 모듈**. Phase 5(클립)·Phase 7(Comp) 이후 또는 독립 진행. "별도 창 + 피아노 롤 + 오프라인 렌더 + 모노포닉 한정"이면 단계적 구현 가능. 관건 = 피치 시프트 품질(알고리즘/라이브러리) + 모노포닉 범위 확정.
- **결론**: 구조적으로 무리 없음. 착수 전 결정 필요 = ① 모노포닉 범위, ② 피치 엔진(라이브러리/알고리즘), ③ 오프라인 렌더 확정.

---

## 릴리즈 전 남은 작업

1. **최종 배포 빌드 생성**
   - Windows 설치본/포터블 빌드가 필요하면 `npm run dist:win` 실행.
   - 네이티브 엔진 포함 여부, `bin/FocusDAW-AudioEngine.exe`, Help 이미지, ffmpeg 경로가 패키징 결과에 포함되는지 확인.

2. **최종 스모크 테스트**
   - 앱 실행/종료, 프로젝트 신규 생성, 오디오 파일 import, 최근 프로젝트 복원.
   - 재생/정지/seek/repeat, Solo/Mute, 트랙 볼륨/팬, Mixer/Advanced Pan/Advanced Effects 창 열기.
   - Vari BPM, Vari Key, Fade in/out, Volume Automation이 재생과 Export에서 모두 반영되는지 확인.
   - MP3/WAV Export 완료 및 결과 파일 재생 확인.

3. **시험 문서 확인**
   - 남은 수동 시험 항목이 있으면 [시험.md](시험.md)에만 유지.
   - 완료된 시험은 [시험-아카이브.md](시험-아카이브.md)로 이동.

---

## 향후 개발 후보

### 우선순위 높음

1. **클립 편집 UI 재개 여부 결정**
   - 엔진의 `splitClip`/`joinClips`와 관련 코드(`ToolBar`, `handleSplit/Join`, 단축키)는 남아 있으나 화면에서는 숨김 상태.
   - 재개 시 가위/잇기/선택 툴바, 클립 인스펙터, 클립 이동/재정렬 UI를 함께 설계.

2. **성능 개선 Phase 6 고도화**
   - 대용량/다트랙 프로젝트에서 peak 캐시, Web Worker 디코딩, automation 곡선 memoize, 스크롤/줌 성능 추가 개선.
   - 장시간 세션의 메모리 사용량과 Export 캐시 무효화 정책 점검.

### 중간 우선순위

3. **트랙 재정렬 / 클립 타임라인 이동**
   - 트랙 헤더 드래그 순서 변경.
   - 선택 툴 기반 클립 드래그 이동 및 정렬 보조 기능 검토.

4. **자동 업데이트 + CI 릴리즈 파이프라인**
   - `electron-updater` + GitHub Releases 연동.
   - `electron-builder --publish` 및 릴리즈 아티팩트 자동 생성 검토.

5. **리버브 IR 개선**
   - 절차적 노이즈 IR을 실측 IR 파일 로딩/프리셋 방식으로 확장.
   - Ambience 프리셋과 네이티브/웹 fallback 경로의 사운드 일치성 확인.

### 검토 항목

6. **EQ 밴드 표현 통일**
   - OUTPUT FX 트랙의 3밴드(Low/Mid/High)와 Mixer Master/Advanced EQ의 9밴드 표현 차이를 유지할지, 조작 경험을 통일할지 검토.

7. **VST3/고급 미터링 장기 검토**
   - VST3 플러그인 로딩, True Peak Meter, 고급 LUFS Meter, 외부 플러그인 체인 지원 가능성 조사.

8. **Cloud Sync 장기 검토**
   - `.focus` 프로젝트 파일과 원본 Stem 파일을 Google Drive 등 클라우드 저장소와 동기화하는 방식 검토.

---

## 보류 항목

- **클립 편집 UI(1-D)**: 현재 미사용 방침. 엔진 코드는 유지하되 화면 노출은 보류.
- **변동 BPM / Tempo Map 분석**: 현재 전체 곡 비례 스케일링 구조에서는 필수 아님. 비트 그리드/워프 편집을 도입할 때 재검토.
- **AudioWorklet 기반 실시간 DSP 재시도**: 현재 네이티브 JUCE 엔진과 WebAudio fallback이 동작하므로 즉시 착수하지 않음.
