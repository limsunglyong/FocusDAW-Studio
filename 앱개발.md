# FocusDAW — Stem Studio · 앱 개발 문서

> 이 문서는 **최근 변경(현재 버전 2건) + 상시 개발 노트 + 남은 항목 및 계획**만 유지합니다. 완료된 버전별 구현 기록(**v1.34.4 이하 전체** + Phase 6 착수 전 조사 + 옛 세션 요약)은 [앱개발-아카이브.md](앱개발-아카이브.md)로 이관했습니다.
> **현재 버전**: v2.4.7 · **완료**: Phase 1–7 · 보컬 채널 스트립 A~F · 사용자 메뉴얼 전면 개정(v1.45.0) · v1 계열 마감(v1.46.3) · **Pitch Editor Stage A(골격)·B(피치 트래킹)·C(노트 세그멘테이션) 종결** · **에디터 UI 완성**(트랜스포트·줌·레이아웃·탐색·클립 트랜스포트·건반 프리뷰) · **Undo/Redo 성능 623 ms → 1.1 ms**. **다음은 Pitch Editor Stage D — 노트 편집**(설계 [피치-에디터-설계.md](피치-에디터-설계.md) §8 · 창 로컬 Undo 스택 §11-2). **T-1.46.3-2는 2026-08-31 사용자 판정으로 close** → **T-2.4.1-1 · T-2.4.2-1 · T-2.4.2-2 2026-08-31 전건 통과**(Copy to track 소리 · 지운 클립 부활 · 그 수정의 무회귀 — Merge 결과가 되돌아가지 않는 것 포함) → [시험-아카이브.md](시험-아카이브.md). **T-2.4.3-1도 2026-08-31 통과.** **T-2.4.4-1·2 · T-2.4.5-1은 2026-09-02 전건 통과**(최대화 시 31초~5분 → **1초 미만**) → [시험-아카이브.md](시험-아카이브.md). **남은 대기 시험은 재시험하는 T-2.4.0-1·2·3(Stage C — 창 스로틀 버그로 첫 시험이 오염돼 판정 불가) · T-2.0.2-2(참고치가 무의미해져 재측정)**이다. ⚠️ **v2.4.7에서 배운 것: 합성 신호가 통과해도 실녹음은 다른 이야기다.** 합성으로 완벽했던 v2.4.6이 실제 녹음에서는 오류를 절반만 줄였고, 그 시험 음원은 **애초에 단성이 아니라 2~3성 합창**이었다. **Pitch Editor(와 Stage E)의 대상은 단성 테이크다** — 합창은 단성 추적기의 원리적 한계 밖이고, 벗어난 프레임의 중앙 신뢰도가 0.81이라 **신뢰도로도 걸러지지 않는다.** ⚠️ **정답표가 없는 실녹음도 계측할 수 있다** — "곡선이 음에서 벗어났다 돌아온다"를 *자기 국소중앙값에서 6반음 이상 벗어난 구간*으로 세면 버전 간 비교가 된다(114 → 68 → 45). ⚠️ **v2.4.6에서 배운 것: 절대 임계로 상대 질문을 판정하면 안 된다.** YIN이 "cmnd가 0.15 미만인 첫 딥"으로 주기를 고르는데, 진짜 딥은 0.12~0.20 사이를 오간다 — 0.03 차이로 한 옥타브가 갈렸다. 임계를 조정하는 것이 아니라 **약수 자리를 함께 보는 상대 판정**이 답이었다. ⚠️ **그리고: 고칠 방법이 둘이면 계측해서 고른다.** 내가 먼저 권했던 사후 보정안은 진짜 옥타브 도약을 뭉갰다(5개 중 3개) — 계측이 아니었으면 그걸 넣을 뻔했다. ⚠️ **v2.4.4에서 배운 것: 계산이 도는 창과 사용자가 보는 창이 다르면, 창을 가리는 것만으로 계산이 멈춘다.** Chromium은 완전히 덮인 창의 타이머를 초당 1회(5분 뒤 분당 1회)로 스로틀하고, 피치 분석은 **스튜디오 창**에서 도는데 Pitch Editor는 그 위에 뜨는 자식 창이다. 모든 창에 `backgroundThrottling: false`, 그리고 긴 계산의 슬라이스 양보는 `setTimeout`이 아니라 **`MessageChannel`**(스로틀 대상이 아니고 4 ms 클램프도 없다). ⚠️ **덤으로 배운 것: 테마 토큰 두 개가 같은 색일 수 있다** — Navy는 `--amber`와 `--surface3`가 둘 다 `#18B7BE`다. 새 색 조합은 반드시 10개 테마에서 **계산해서** 고를 것(v2.4.4 절의 하네스). ⚠️ **v2.4.3에서 배운 것: 하드코딩 색은 CSS만 훑으면 못 찾는다** — 캔버스에 그리는 색은 JS 문자열이라 v1.44.21의 일괄 정리를 통과해 버렸다. 새 캔버스 코드는 반드시 토큰을 `getComputedStyle`로 해석할 것. ⚠️ **v2.4.1에서 배운 것: 마스터(OUTPUT FX) 미터는 네이티브 레벨이 0이면 웹 엔진 값으로 폴백하므로 단독으로는 "엔진에 오디오가 갔다"의 증거가 못 된다. 반면 트랙 미터(`getTrackLevel`)는 폴백이 없어 네이티브의 직접 증거다 — 두 미터를 함께 보면 원인이 갈린다(판별표: v2.4.1 절).**
> ✅ "선택 해제 후 undo 무반응"은 **v1.23.2에서 원인 확정·수정**(OUTPUT FX 레인 클릭이 no-op undo 항목을 쌓음). 임시 진단 코드는 전부 제거됨.

> 📌 **상시 지침 — UI 문구는 영어로 쓴다.** 메뉴 이름·버튼 라벨·패널 설명·힌트·툴팁 전부. 코드 주석·개발 문서·사용자와의 대화는 한국어 그대로다. 근거: v1.46.2에서 잔여 한글을 전수 영문화해 실구동 한글 0건을 확인했고, 새 화면에서 한글이 섞이면 그 작업이 되돌려진다(v2.3.1 사용자 확정).

> 📌 **다음 세션 이어받기 (2026-09-02 마감 시점)**
>
> **오늘 한 일** — 사용자가 Stage C 시험(T-2.4.0-1·2·3) 중 보고한 것 가운데 **원인이 확정된 2건을 수정(v2.4.4)**: ① **창을 가리면 분석이 초당 1회로 기어가던 버그**(Chromium occlusion 스로틀 — 계산이 스튜디오 창에서 돌고 자식 창이 그것을 덮었다), ② **분석 중 중앙 오버레이**(진행 표시가 접히는 사이드 패널 안에만 있었다). **둘 다 같은 날 사용자 시험 통과** — 최대화 시 31초~5분이 **1초 미만**이 됐고, NOTES 세 설정이 모두 1초 미만으로 수렴해 **NOTES가 분석과 무관하다는 것이 실측으로 확인**됐다. 이어서 그 시험 중 나온 질문에서 **v2.4.5(Analyze 단축키 Ctrl+R)** 를 붙였다.
>
> **🔴 내일 첫 작업 = T-2.4.0-1(Stage C 노트 정확도) 결과 받기.** 남은 대기 시험은 그것과 T-2.0.2-2뿐이다. T-2.4.4-1·2 · T-2.4.5-1 · T-2.4.0-2·3 · T-2.4.7-1 전건 통과. v2.4.7까지 커밋됨(feature/v2).
>
> **Stage C 재시험 시 유의** — 이미 알려진 결함 2건은 다시 보고받지 않는다: ① 깊은 비브라토(±70¢ 이상)가 한 음을 쪼갬([버그.md](버그.md) `B-Vibrato-Fragment`, **미수정 · 트레이드오프 결정 필요**) ② 합창 녹음(`B-Chorus-Polyphonic`, 원리적 한계). 시험용 합성 신호는 `node tools/make-pitch-test-audio.js`(정답표 포함, 프로젝트 BPM 105). Stage C 시험은 **최대화 상태에서 잰 시간이 전부 오염됐고** 노트 정확도도 옥타브 오류가 섞인 채 봤기 때문에 **판정 불가로 두고 다시 받는다.**
>
> **아직 손대지 않은 사용자 요청 2건**(오늘 논의만 하고 착수 안 함) —
> - ⬜ **옥타브 오류 자동 교정.** 사용자 보고: 마지막 노트가 `F2`로 표시되나 실제로는 F4/F3. 곡선 자체가 F2로 내려가 있다(= 라벨만의 문제가 아니다). 지금 스무딩은 반경 2프레임(≈53 ms)이라 **수백 ms 이어지는 옥타브 오류를 못 잡는다.** 제안한 수: `_pitchAnalysisFinish`에 **옥타브 연속성 패스**(각 voiced 프레임을 ±150 ms 국소 중앙값에 가장 가까운 옥타브로 스냅). **합성 24건 하네스로 회귀 확인 가능.**
> - ⬜ **후보 노트를 사용자가 고르게 하기**(사용자 제안, 내가 동의). 다만 **"임의의 후보 목록"이 아니라 "옥타브 후보"** 로 좁히는 것을 권함 — 트래커 오류의 대부분이 ±12/±24이고, YIN의 cmnd 배열에 τ·2τ·3τ 딥이 **이미 있어 후보를 공짜로 뽑는다.** ⚠️ **설계상 못 박을 것: 옥타브를 고치면 라벨(`target`)만이 아니라 그 구간의 `f0`도 함께 ×2 해야 한다.** 안 그러면 Stage E(TD-PSOLA)가 틀린 원음 피치를 기준으로 시프트한다. Stage D에 얹는 것이 자연스럽다.
>
> ---
>
> 📌 **이전 이어받기 (2026-08-31 마감 시점)**
>
> **오늘 한 일** — Pitch Editor **Stage C(노트 세그멘테이션) 구현**(v2.4.0)과, 그 시험 중 사용자가 찾아낸 **버그 3건 수정**(v2.4.1~v2.4.3). 버그 쪽은 **전건 사용자 시험 통과**했고, **Stage C 시험(T-2.4.0-1·2·3)만 남았다 — 사용자가 면밀히 볼 필요가 있어 2026-09-01로 미뤘다.**
>
> **🔴 내일 첫 작업 = T-2.4.0-1·2·3 결과 받기.** 그 결과에 따라 갈린다 —
> - **통과** → Stage D(노트 편집) 착수. 아래 "Stage D 착수 지점" 참조.
> - **노트가 부른 음표와 어긋남** → 임계 조정. 손댈 자리는 [pitch-editor-app.jsx](pitch-editor-app.jsx)의 `PE_HYST_ST`(0.6반음) · `minNoteSec` 계수(0.75) · `holdCount`(minNoteSec/2)뿐이고, **세 값 모두 §12-1에 근거가 적혀 있다.** 합성 신호 시험 24건이 있으므로 조정 후 그것부터 다시 돌릴 것(아래 하네스 항목).
>
> **Stage C가 무엇을 하는가(요약)** — Analyze가 그린 red 곡선을 사람이 부른 음표 단위로 잘라 **amber 블록**으로 얹는다. 과다 노트를 막는 것은 최소 길이가 아니라 **경계의 시간 이력**이다(임계를 넘고 `minNoteSec`의 절반 이상 유지해야 경계로 인정 — 비브라토는 앞을 늘 만족하고 뒤를 결코 만족하지 못한다). 그 `minNoteSec`을 박에서 뽑는 것이 BPM의 역할이다. 블록 안에는 음이름 + (넓으면) 센트를 쓰고, 조성 밖 음은 점선 테두리다. 상세 [피치-에디터-설계.md](피치-에디터-설계.md) §12 · 이 문서 v2.4.0.
>
> **Stage D 착수 지점** — Stage C가 세운 블록을 드래그·스냅·분할/병합으로 고치는 단계다.
> - 블록 **선택**(클릭 · Ctrl/Shift+클릭)과 상태줄의 선택 정보는 **v2.4.0에 이미 있다.** 드래그가 얹힐 자리가 있다.
> - 노트에는 `target` · `strength` · `keepVibrato`가 **처음부터 실려 있다**(설계 §4). Stage C는 `target = 검출 음정의 반올림`으로 둘 뿐이므로, 편집은 이 값을 움직이는 일이다.
> - **창 로컬 Undo 스택**이 §11-2에 확정돼 있다(로컬 스택이 앞단, 비면 스튜디오로 위임, 드래그 한 동작 = 항목 하나, Apply는 스튜디오 스택에 1건).
> - ⚠️ **먼저 정할 것 — 노트 id 정책.** 지금 id는 **한 번의 세그멘테이션 안에서만 유일**하다. v2.4.0은 재분할 때 선택을 비우는 것으로 넘겼지만, Stage D에서 **편집 상태를 재분할·재분석 뒤에도 유지**하려면 id가 안정적이어야 한다. 설계 §4의 `clip.pitch.notes`가 **저장 단위가 되는 시점**이므로, 착수 전에 이것부터 결정할 것.
>
> **소리에 손대는 것은 Stage E(TD-PSOLA 렌더·프린트)** 다. Stage C·D는 화면만 바꾸므로 v2.3.3 같은 위험이 없다.
>
> ---
>
> ⚠️ **오늘 값비싸게 배운 것 3가지 — 다음에 같은 함정을 밟지 말 것**
>
> 1. **브리지 래퍼 없이는 네이티브가 모른다(v2.4.1).** [audio-bridge.js](audio-bridge.js) 끝의 `Proxy`는 `Bridge`에 없는 이름을 **조용히 `LocalDAW`로 위임**한다. 오디오를 바꾸는 엔진 메서드에 래퍼를 안 만들면 **화면은 맞는데 소리만 없고, 재시작하면 낫는다.** `copyClipToTrack`이 v2.1.0부터 넉 달간 그랬다. 새 변경 메서드를 만들면 반드시 래퍼 + `syncTrackToNative`. ⚠️ **효과가 다른 트랙에 떨어지면 그 트랙을 실을 것**(형제 래퍼는 전부 `trackId`를 싣는다 — 이것이 누락의 원인이었다).
> 2. **마스터 미터는 증거가 아니다(v2.4.1).** `getMasterLevel`은 네이티브 레벨이 0이면 **웹 엔진 값으로 폴백**하고, 웹은 `setOutputMuted`로 스피커에서만 꺼져 있다(그 게인이 `masterAnalyser` 뒤에 있다). 그래서 **소리가 없어도 미터는 춤춘다.** 폴백이 없는 **트랙 미터(`getTrackLevel`)** 가 네이티브의 직접 증거이고, 두 미터를 함께 보면 원인이 갈린다(판별표: v2.4.1 절).
> 3. **하드코딩 색은 CSS만 훑으면 못 찾는다(v2.4.3).** 캔버스에 그리는 색은 **JS 문자열**이라 v1.44.21의 일괄 정리를 그대로 통과했다. 새 캔버스 코드는 토큰을 `getComputedStyle`로 해석할 것(예: `--clip-edge`).
>
> ⚠️ **검증은 엔진 경계에서 한다(v2.3.3에서 배운 것, 오늘 두 번 더 확인됨).** 웹 계층만 계측하면 위 1·2번 부류는 전부 통과처럼 보인다. **프리로드에서 `WebSocket`을 가짜 엔진 소켓으로 바꿔 `DAW.isNative`를 켜고 나가는 명령을 붙잡는다.**
>
> 📌 **오늘 쓴 하네스(전부 임시 파일로 만들어 쓰고 지웠다 — 필요하면 같은 방식으로 다시 만들 것)**
> - **세그멘테이션 단위 시험** — `pitch-editor-app.jsx`에서 §12 코드 블록만 떼어 `vm`으로 돌린다. 합성 피치 곡선으로 24건(그리드 산수 · 비브라토 · 음 변화 · 조각 병합 · 숨/자음 경계 · 밀도 상한 · 속도). **임계를 만지면 이것부터 돌릴 것.**
> - **Pitch Editor 화면** — `pitch-editor.html`에 스크립트를 하나 끼워 **`BroadcastChannel`을 페이지 안에서 가로채면**, 스튜디오·프로젝트·오디오 없이 창 전체를 실제로 띄울 수 있다(합성 클립 + 합성 분석 결과를 먹인다).
> - **엔진 경계** — 위 가짜 WebSocket. `copyClipToTrack`이 명령 0건임을 잡아낸 것이 이것이다.
> - **저장/재열기** — `audio-engine.js`만 로드해 `exportProject → importProject → addFileBuffer(reconnectTrackId)`를 돌리면 "지운 클립 부활"류를 단계별로 볼 수 있다. **저장·import는 멀쩡하고 오디오 재접속에서 터지는** 경우가 있으므로 단계를 나눠 찍을 것.
> - **테마 대비** — `studio.html` / `pitch-editor.html`을 띄워 10개 테마를 순회하며 토큰을 해석해 **합성 대비를 계산**한다. 눈대중보다 빠르고 정확하다.
>
> **미결로 남긴 것**
> - ⬜ **Shift+Mute 이상**(File Tracks) — 한 세션에서 관찰됐으나 **재현되지 않는다.** 순수 렌더러 경로라 v2.4.1의 무음 버그와 원인을 공유하지 않는다. **사용자 결정: 원인이 분명하지 않으면 손대지 않는다.** 재발 시 확보할 것은 [버그.md](버그.md) `B-ShiftMute-NoRepro`에 적어 뒀다.
> - ⬜ **`getMasterLevel`의 웹 폴백** — `getMasterBandLevels`는 이미 걷어낸 것과 같은 종류다. 걷어내면 미터가 정직해지지만 핸드오버 전·유휴 구간 표시가 함께 바뀌므로 별도 판단이 필요하다.
> - ⬜ **`.audio-input-port-select option:checked`** — `--cream` on `--surface3`가 Deep Navy에서 2.3:1. 증상 보고는 없다.
> - ⬜ **T-2.0.2-2**(긴 클립 분석 속도·무회귀) — 언제든 가능.

---

## v2.4.7 — 숨 구간 유령 노트 제거 (유성 신뢰도 게이트) · 실녹음 계측 (2026-09-02)

> 📌 **`PITCH_MIN_CONF` 하나가 이 절 전체의 스위치다.** `0.5` = 켜짐(현재), `0` = v2.4.6 동작으로 **정확히** 복귀(게이트 줄을 지운 빌드와 프레임 단위로 동일함을 확인했다). 다른 변경도 재빌드도 필요 없다 — [audio-engine.js](audio-engine.js)는 [studio.html](studio.html)이 직접 로드한다. 2026-09-02에 사용자 요청으로 한 번 `0`으로 내렸다가 되돌렸고, 비교 시험 절차는 [시험.md](시험.md) T-2.4.6-2에 남아 있다.

> 사용자가 v2.4.6을 **실제 녹음**으로 시험하고 8건을 보고했다. 두 종류였다 — **옥타브 하강 5건**과 **숨 쉬는 구간의 유령 노트 3건**. **버전 판정**: 분석 결함 완화 → **Patch +1, v2.4.6 → v2.4.7.** **renderer 재빌드 불필요.**
>
> ⚠️ **그리고 시험 음원이 단성이 아니었다** — 사용자 확인: "**한 명이 아니라 2~3명 이상의 여성 합창**". 이것이 남은 오류의 대부분을 설명한다.

### ① 숨 구간 유령 노트 — 유성 판정이 너무 관대했다

유성 판정이 **RMS 게이트(peak × 0.02)** 와 `_yinFrame`의 폴백(`cmnd ≤ 0.6`)뿐이었다. 숨소리·자음은 RMS를 넘고, 폴백은 **conf 0.4까지 통과**시키므로 "자신 없는 추정"이 그대로 노트가 됐다. `PITCH_MIN_CONF = 0.5`를 두어 그 아래는 **무성으로 남긴다** — 숨에 대한 정직한 답은 "음이 없다"이다.

### ② 실녹음에서 무엇이 얼마나 좋아졌나 (정답 없이 재는 법)

실제 녹음에는 정답표가 없다. 그래서 사용자의 불평 자체를 지표로 삼았다 — **"곡선이 음에서 벗어났다 돌아온다"** = 자기 **국소중앙값(±0.6 s)에서 6반음 이상 벗어난 3프레임 이상 구간**.

| 버전 | 벗어난 구간 | 아래로 | 위로 |
|---|---|---|---|
| v2.4.5 | 114 | 86 | 28 |
| v2.4.6 (하위배수 검사) | 68 | 42 | 26 |
| **v2.4.7 (+ 유성 게이트)** | **45** | 35 | **10** |

합성 파일은 전부 **곡선 40/40 · 옥타브 오류 0** 유지, 구간 A 8/8, 구간 C 순서(1/8<1/16<1/32) 유지. 유성 비율은 합성 solo에서 80% → 79%로 **비용이 사실상 0**이다.

⚠️ **`RATIO` 상한안은 계측하고 기각했다.** v2.4.6의 하위배수 검사가 너무 관대한 것 아닌가 싶어 `cmnd < thresh × 상한` 조건을 얹어 봤는데, 위로 튀는 것은 줄지만(26 → 6) **아래로 벗어나는 것이 35 → 60으로 악화**된다. 무제한이 총량에서 낫다. **처음 든 의심이 틀렸음을 계측이 알려준 두 번째 사례다.**

### ③ 사용자 A/B 시험 결과 (2026-09-02, T-2.4.7-1 통과)

사용자가 v2.4.6과 v2.4.7을 같은 클립으로 각각 분석해 화면을 4장 비교했다.

| | 솔로 테이크 | 화음(합창) 테이크 |
|---|---|---|
| v2.4.6 | 스파이크 40~50개, **곡선이 파묻혀 읽을 수 없음** | 스파이크 다수 |
| v2.4.7 | **10개 이하, 선율선이 드러남** | 눈에 띄게 감소하나 잔여 있음 |
| 계측(벗어난 구간) | **56 → 24** | **68 → 45** |

⚠️ **가장 걱정한 부작용(작게 부른 진짜 음이 사라짐)은 없었다.** 솔로 테이크에서 게이트가 382프레임을 버리고 그중 **71%가 음 안쪽**이라 처음엔 위험해 보였는데, 구멍 길이를 재 보니 **80%가 1~3프레임**으로 `PE_GAP_SEC`(40 ms) 아래였다 — `peVoicedRuns`가 그대로 메운다. **노트 블록 수는 늘지 않고 줄었다**(1/16: 106→95, 1/32: 170→141). 통째로 사라진 구간은 딱 둘(`0:16.83` A#4 117 ms · `0:22.86` A#5 75 ms)이고, **사용자가 직접 들어보고 "음절이 바뀌면서 음이 거의 없는 구간"으로 확인**했다 — 올바른 제거였다.

⚠️ **남은 24건의 원인도 규명했고, 고치지 않기로 했다.** 전부 YIN의 **폴백 경로**(cmnd가 0.15 밑으로 내려가는 딥이 아예 없는 프레임)로 들어간다. 배수검사 자체는 작동한다 — 원본 출력은 `8.16s → F4`, `24.94s → A#4`, `27.07s → A#4`(cmnd 0.067, 매우 확신)로 **맞다.** 이웃이 틀려서 **중앙값 스무딩이 덮어쓰는** 것이다. 그래서 스무딩을 고치려 했으나 —

| 스무딩 | 솔로 | 화음 |
|---|---|---|
| 현재(중앙값 r=2) | 24 | 45 |
| 아예 끔 | 22 | 46 |
| 신뢰도 가중 | 23 | 45 |

**차이가 없다. 이웃 자체가 틀리므로 스무딩은 지렛대가 아니다 — 손대지 않는다.** (계측이 짐작을 막은 세 번째 사례다.)

### ④ 남은 것은 고칠 수 없다 — 합창이기 때문이다

남은 35건은 임계 조정으로 사라지지 않는다. **근거: 신뢰도로 걸러지지 않는다** — 벗어난 프레임의 **중앙 신뢰도가 0.81**로 정상 프레임(0.91)과 거의 같다. conf 하한을 0.4로 둬도 나쁜 프레임의 **21%** 만 지워진다. 즉 **YIN이 자신 있게 다른 성부를 짚고 있다.**

YIN은 단성 추적기다 — 신호에 주기가 하나라고 가정한다. 2~3성이면 차분함수에 경쟁하는 주기가 여럿 생기고 어느 하나를 고르는 것이 원래 임의적이다. **이는 분석만의 문제가 아니라 Stage E(음정 보정)의 문제이기도 하다** — 합창을 보정하면 모든 성부가 함께 밀리므로 애초에 원하는 결과가 나오지 않는다.

**→ Pitch Editor의 대상은 단성 보컬 테이크다. 앞으로의 시험도 단성 녹음으로 한다**([버그.md](버그.md) `B-Chorus-Polyphonic`에 한계와 기준선을 기록).

---

## v2.4.6 — 옥타브 오검출 수정 (YIN 하위배수 검사) + 시험용 합성 신호 (2026-09-02)

> 사용자가 T-2.4.0-1 시험 중 **재현 파일과 함께** 보고: "약 26~28.5초 구간에서 F4가 단순히 작아지는 듯한데 중간에 한 번 F3로 바뀐다. **실제 다른 보컬 녹음 분석에서도 자주 나타나는 증상.**" **버전 판정**: 분석 결함 수정 → **Patch +1, v2.4.5 → v2.4.6.** **renderer 재빌드 불필요**([audio-engine.js](audio-engine.js)는 [studio.html](studio.html)이 직접 로드한다).

### ① 시험용 합성 신호 — 정답을 아는 보컬

먼저 **[tools/make-pitch-test-audio.js](tools/make-pitch-test-audio.js)** 를 만들었다. 실제 녹음은 정답을 모르니 "블록이 부른 음표와 맞는가"를 판정할 수 없었다. 105 BPM · 7개 구간(레가토 스케일 · 비브라토 · 8/16/32분음표 · 낮은 음+감쇠 · 글리산도 · 2성·3성 하모니)에 **정답표(md) + 기계용 정답(json)** 을 같이 낸다.

- **사인파가 아니다.** 포먼트 공명으로 배음을 빚고 지터·셰이머·숨소리를 얹었다. 사인파는 실전보다 훨씬 쉬워서, 사인파로 통과한 임계값은 실제 노래에서 무너진다.
- **레가토 구간이 핵심이다.** 음 사이에 무음이 없으므로 세그멘테이션이 **음정 계단으로** 잘라야 한다 — 무음으로 끊으면 어떤 알고리즘이든 통과한다.
- **105 BPM인 이유**: minNoteSec이 1/8·1/16·1/32에서 200·107·60 ms로 **전부 갈린다.** 그래야 MIN 설정별 개수 차이를 정답으로 볼 수 있다.
- WAV은 크므로 `.gitignore`(`test-audio/`), **생성기만 커밋**한다.

⚠️ **이 신호가 곧바로 결함 2건을 잡았다** — 아래 ②와, 별건인 [버그.md](버그.md) `B-Vibrato-Fragment`(깊은 비브라토가 한 음을 여러 조각으로 쪼갬).

### ② 옥타브 오검출 🔴 — 절대 임계로 상대 질문을 판정하고 있었다

사용자가 그 신호에 **노이즈를 섞어** 재현시켰다(`Pitch-Test-Solo(2).wav`). 계측 결과 **27.14~27.46 s, 연속 25프레임(270 ms)** 이 정확히 −12반음이었다.

**두 가지가 기존 방어를 무력화한다** —
- **신뢰도가 높다.** 틀린 구간의 conf가 0.85~0.93으로 정상 구간과 같다. **conf로는 못 거른다.**
- **스무딩 사정거리 밖이다.** 오류가 270 ms인데 중앙값 스무딩은 반경 2프레임(53 ms)이다.

**원인은 `_yinFrame`의 `if (cmnd[tau] < thresh)` 한 줄이다.** 진짜 주기의 딥은 **사라진 적이 없고**, 고정 임계 0.15 바로 양옆에서 오락가락한다 — 27.08 s에 0.1216(정답), 27.28 s에 0.1523(오답), 27.40 s에 0.2019(오답), 27.49 s에 0.1129(정답). **차이가 0.03**이다. 임계를 넘는 순간 주사가 진짜 주기를 건너뛰고 2τ를 집는다. **실제 녹음에서 자주 나오는 이유가 이것이다 — 0.03은 어떤 녹음에서든 프레임 간 요동 범위 안이다.**

**수정**: 딥을 고른 뒤 그 τ의 **정확한 약수(τ/2·τ/3·τ/4)** 자리를 보고, 거의 비슷한 깊이의 딥이 있으면 그쪽이 진짜 주기이므로 **가장 짧은 것(=가장 높은 음)** 을 택한다. 임계를 조정하는 것이 아니라 **상대 질문을 상대적으로 판정**한다.

| 파일 | 수정 전 옥타브 오류 프레임 | 수정 후 |
|---|---|---|
| `Pitch-Test-Solo(2).wav` (재현) | **32 / 1576** | **0** ✅ |
| `pitch-test-solo.wav` · `pitch-test-full.wav` (회귀) | 0 | 0 ✅ |

곡선 정확도 40/40 유지 · 구간 A 8/8 유지 · 구간 C 순서 유지 · 성능 607→638 ms(47.4초 클립).

⚠️ **`RATIO = 2.5`는 계측해서 골랐다.** 반대 방향(옥타브 위로 잘못 올라감)이 유일한 위험이라 따로 쓸었다 — 기본파를 60/30/10/0%로 줄이고 짝수 배음을 지배시킨 F3~F4에 −60/−32 dB 잡음, 비율 1.9~5.0. **새로 생기는 오류 0건.** YIN은 스펙트럼이 아니라 **파형 주기**를 보므로 기본파를 없앤다고 τ/2에 딥이 생기지 않는다.

⚠️ **내가 앞서 권했던 "옥타브 연속성 패스"(사후 국소 중앙값 스냅)는 계측해 보고 기각했다.** 같은 케이스를 고치기는 하지만 **추적기의 옥타브 도약과 가수의 옥타브 도약을 구분하지 못한다** — 300 ms 음 레가토 `C4 C5 C4 C5 C4`에서 **진짜 도약 5개 중 3개를 뭉갰다.** 창을 좁히면 이번엔 오류 구간이 스스로 다수파가 되어 원래 버그를 못 고친다. **추측으로 고를 뻔한 것을 계측이 막았다.**

**덤**: 하모니에서 유령 옥타브가 줄었다 — `F3+A3+C4` F2→**F3**, `G3+B3+D4` G2→**G3**.

---

## v2.4.5 — Pitch Editor `Analyze` 단축키 (Ctrl+R) (2026-09-02)

> 사용자가 T-2.4.4-2를 시험하다 던진 질문에서 나왔다: "**사이드 패널을 접으면 Re-analyze 버튼이 안 보이는데 어떻게 누르나요?**" — 못 누른다. 내가 쓴 시험 절차가 틀렸고, 그 질문이 **실제 빈틈**을 드러냈다. **버전 판정**: 기능 추가 → **Patch +1, v2.4.4 → v2.4.5.** **renderer 재빌드만.**
>
> **v2.4.4의 오버레이는 "패널이 접혀 있어도 진행이 보이게" 하려고 넣은 것인데, 정작 접힌 상태에서는 분석을 시작할 수가 없었다** — 반쪽이었다. 이 창의 단축키는 Space·Ctrl+Z·Ctrl+Y·Esc뿐이었다.

`Ctrl`/`Cmd`+`R`을 [pitch-editor-app.jsx](pitch-editor-app.jsx)의 keydown 핸들러에 Ctrl+Z/Y 옆에 붙였다. `runAnalyze`가 이미 `busy`와 클립 유무를 막고 있으므로 겹쳐 걸리지 않는다. 발견 가능하도록 **버튼 툴팁**(`… (Ctrl+R)`)과 **상태줄**(`press Analyze to detect pitch` → `press Ctrl+R to detect pitch`)에도 실었다.

⚠️ **Ctrl+R을 쓸 수 있는 이유** — 보통 이 조합은 새로고침이라 위험하지만, 이 앱은 [electron/main.js](electron/main.js)에서 `Menu.setApplicationMenu(null)`로 기본 메뉴를 없앴고 **기본 reload 액셀러레이터도 함께 사라졌다.** 스튜디오 쪽도 Ctrl+R은 비어 있다(스튜디오는 맨글자 s/c/j = 툴, Ctrl+letter = 명령 규칙).

**검증**(Electron 실구동 하네스, `sendInputEvent`로 실제 키 입력):
- 패널을 접은 상태에서 `Ctrl+R` → 분석 요청 **1건 발생 + 오버레이 표시** ✅
- 분석 중 `Ctrl+R` 재입력 → 요청 **1건 유지**(무시됨) ✅
- `Ctrl` 없이 `R`만 → 요청 **1건 유지**(무반응) ✅
- **페이지 새로고침 0회** ✅ (위 액셀러레이터 부재가 실제로 맞는지 확인한 것)


---

## v2.4.4 — 창을 가리면 분석이 초당 1회로 기어가던 버그 + 분석 오버레이 (2026-09-02)

> 사용자 보고: "**Pitch Editor를 최대화하고 Analyze하면 30초 클립이 31초~5분 걸리는데, 도중에 최대화 버튼을 다시 누르면 그 즉시 끝난다.**" 그리고 "**분석 중임을 화면 중앙에 표시해 달라 — 멈춘 것처럼 보인다.**" **버전 판정**: 성능 버그 수정 + UI 추가 → **Patch +1, v2.4.3 → v2.4.4.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### ① 창을 가리면 분석이 초당 1회로 기어간다 🔴

**증상이 창 크기와 묶여 있던 이유는 계산이 무거워서가 아니라, 계산하는 창이 "숨겨진 창"으로 판정됐기 때문이다.**

경로:
1. **분석은 Pitch Editor 창이 아니라 스튜디오(메인) 창에서 돈다.** 에디터는 `REQUEST_PITCH_ANALYZE`를 보낼 뿐이고, `analyzeClipPitchAsync`를 부르는 것은 [app.jsx](app.jsx)다.
2. 그 슬라이스 루프는 **`setTimeout(…, 0)`으로만 전진**했다(90 ms 계산 → 양보 → 반복).
3. Pitch Editor·믹서·보컬 스트립은 전부 `parent: mainWindow`인 **자식 창**이라 항상 메인 창 **위**에 뜬다.
4. 그래서 **Pitch Editor를 최대화하면 메인 창이 완전히 가려진다.** Chromium은 완전히 덮인 창을 *occluded = hidden*으로 보고 **그 페이지의 타이머를 초당 1회로 스로틀**한다. 5분을 넘기면 *intensive throttling*으로 **분당 1회**가 된다.
5. [electron/main.js](electron/main.js)의 `buildWebPreferences()`에 **`backgroundThrottling: false`가 없었다**(기본값 `true`).

**산수가 증상과 맞는다** — 실계산 3.5초 = 슬라이스 약 40개. 스로틀이 걸리면 40초. 사용자 실측 **31초**. 그리고 최대화/복원을 누르면 창 경계가 바뀌어 **occlusion이 재계산되고 스로틀이 풀리므로 남은 계산이 그 자리에서 끝난다** — 이것이 "누르자마자 완료"다.

**수정 2단**(둘 다 넣었다):
- **`backgroundThrottling: false`** — 창 수준의 수정. 이건 Pitch Editor만의 문제가 아니었다. **믹서·스트립 창을 최대화해도 스튜디오의 미터·플레이헤드가 같이 얼어붙는다.** DAW는 자기 위성 창이 앞에 있어도 계속 돌아야 하므로 전 창에 적용했다.
- **슬라이스 양보를 `setTimeout` → `MessageChannel`로 교체**([audio-engine.js](audio-engine.js) `analyzeClipPitchAsync`). MessagePort 태스크는 hidden 타이머 스로틀 대상이 아니므로 **플래그가 없는 렌더러에서도 분석은 정직하게 돈다.** 덤으로 `setTimeout(0)`의 **중첩 5단계 초과 4 ms 클램프**도 사라진다(90 ms마다 4 ms씩 내던 세금).

⚠️ **NOTES(division)는 분석과 아무 관계가 없다 — 사용자 판단이 맞았다.** 요청은 `{trackId, clipId}`만 싣고 `opts`는 `null`이며, division은 분석이 **끝난 뒤** 세그멘테이션에만 쓰인다. "1/32라서 5분"은 인과가 아니라 **창 최대화와 겹친 우연**이다. 다만 최대화 상태에서 1/16 = 14초 · 1/8 = 31초로 갈린 것은 **아직 설명되지 않았다** — occlusion 판정 시점 차이로 보이나 추정이며, **수정 후 두 값이 같아지는지가 이 가설의 검증**이다(T-2.4.4-1).

### ② 분석 중 표시가 사이드 패널 안에만 있었다

진행률이 **`ANALYSIS` 버튼 라벨 하나**뿐이었고, 그 영역 전체가 `{sideOpen && …}`로 감싸여 있었다 — **패널을 접으면 진행 표시가 0건**이다. ①번과 겹쳐 "멈춘 것 같다"가 됐다.

롤 위에 중앙 오버레이(`.pe-analyzing`)를 얹었다. **`pointer-events:none`이다** — 분석은 의도적으로 비차단이므로(슬라이스로 도는 이유가 그것이다) 오버레이가 모달처럼 굴어 그 설계를 뒤집으면 안 된다. 실측으로 확인했다: 카드 중앙의 클릭 대상은 오버레이가 아니라 롤이다.

⚠️ **색은 전부 계측해서 골랐다 — v2.4.3의 교훈을 그대로 적용.** 처음에 자연스러워 보인 조합이 실제로는:
- **진행 바 `--amber` on `--surface3`가 Navy에서 대비 1.00** — 두 토큰이 **같은 색**(`#18B7BE`)이다. Solar도 2.23. 그대로 갔으면 **두 테마에서 진행 바가 보이지 않았다.** → `--amber-deep` on `--bg`로 교체(10개 테마 최저 3.38).
- **스피너 링**은 `--amber`가 Ivory에서 2.78, `--amber-deep`이 Blue에서 2.22 — **어느 액센트도 10개를 통과하지 못한다.** → 링은 `--cream`(최저 5.61), 액센트는 3:1을 넘기는 진행 바가 담당.
- **카드가 배경에서 떠 보이지 않는 문제** — `--surface`와 `--bg2`는 어두운 테마에서 대비가 1.05 수준이다(Slate는 `#111827` vs `#0f172a`). **테마 설계상 불가피**하므로 기존 패널과 같이 테두리+그림자가 맡고, 중립 스크림 `rgba(0,0,0,.30)`을 깔아 롤과 분리했다(앱이 `--outfx-bg`에서 이미 쓰는 관용구).

### 검증

- **분석 하네스**(합성 30초 · 비브라토 4음): MessagePort 양보 **4회 · `setTimeout` 0회**, 진행 콜백 단조증가·total 도달, 검출 음정 A3/F4/C4/G3 **전부 ±1센트**. 스케줄러만 갈고 분석은 그대로임을 확인.
- **테마 대비 하네스**: `pitch-editor.html`에서 토큰을 직접 파싱해 오버레이가 쌓는 실제 조합(스크림→카드→내용)을 10개 테마에서 계산 — **전건 통과**. 위 3건이 여기서 잡혔다.
- **Electron 실구동 하네스**: preload에서 `BroadcastChannel`을 가로채 스튜디오·프로젝트·오디오 없이 창을 띄우고 Analyze를 눌러 오버레이 상태를 확인 + 10개 테마 스크린샷. 진행 41% → 바 폭 70.5 px(172 × 0.41 정확), 카드 중앙 정렬, `pointer-events:none`, 중앙 클릭 대상이 롤.
- ⬜ **실측 미검증 = 스로틀 해제 그 자체.** Chromium의 occlusion 스로틀은 하네스로 재현할 수 없어 **사용자 시험(T-2.4.4-1)이 유일한 검증**이다. 만약 최대화 상태에서 스튜디오 미터가 여전히 얼어붙으면 다음 수는 `app.commandLine.appendSwitch('disable-features', 'CalculateNativeWinOcclusion')`이다(occlusion 판정 자체를 끈다 — 가려진 창도 계속 그리므로 GPU 비용이 붙어 먼저 쓰지 않았다).


---

## v2.4.3 — 테마 대비 결함 2건 (클립 경계선 · Pitch Editor 드롭다운) (2026-08-31)

> 사용자 보고: ① "**Classical Ivory에서 클립의 경계선이 거의 보이지 않는다**(Sage Mist도 유사)" ② "**Pitch Editor의 Notes 드롭다운 글자가 거의 보이지 않는다**(이번엔 어두운 테마들)". **버전 판정**: UI 결함 수정 → **Patch +1, v2.4.2 → v2.4.3.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**
>
> 두 건은 **정반대 방향으로 같은 실수**다 — 한 테마에서 고른 색을 다른 테마에 그대로 들고 간 것.

### ① 클립 경계선 — 캔버스에 하드코딩된 크림색 🔴

[ui-tracks.jsx](ui-tracks.jsx) 파형 캔버스가 경계선을 **`"rgba(232,212,170,.30)"` 문자열**로 그리고 있었다. 어두운 테마의 크림색이라, **밝은 배경 위에 옅은 크림을 얹는** 두 밝은 테마(Classical Ivory · Sage Mist)에서는 사실상 사라진다.

**v1.44.21에서 52곳을 쓸어낸 바로 그 부류인데 이것만 살아남았다** — 색이 CSS가 아니라 **JS 문자열** 안에 있었기 때문이다. CSS만 훑는 점검에는 걸리지 않는다.

**수정**: `--clip-edge` 토큰을 신설해 밝은 테마 2곳에서 재정의한다. 캔버스는 `var()`를 쓸 수 없으므로 [ui-tracks.jsx](ui-tracks.jsx)가 **draw마다 한 번** 해석한다(클립마다가 아니라 — 이 함수는 스크롤 프레임마다 돈다).

| | 값 | 근거 |
|---|---|---|
| 기본(어두운 테마 8개) | `rgba(232,212,170,.30)` | **종전 값 그대로** — 어두운 테마의 보이기는 손대지 않는다 |
| Classical Ivory | `rgba(95,75,42,.45)` | 그 테마 자신의 `--line-strong` 색상(95,75,42)을 진하게 |
| Sage Mist | `rgba(63,73,69,.45)` | 동일 |

### ② Pitch Editor 드롭다운 — 닫힌 면만 칠하고 열린 목록을 잊었다 🔴

v2.4.0에서 넣은 `.pe-select`는 **닫힌 면만** 스타일했다. 열린 목록의 `<option>`은 select의 글자색(`--cream-2` = 어두운 테마에서 거의 흰색)을 **상속**하는데, 팝업은 `color-scheme`을 따르지 않고 밝게 떠서 **흰 배경 위 크림 글자**가 됐다.

**해법은 앱 안에 이미 있었다** — [studio.html](studio.html)의 `.audio-input-port-select`가 같은 문제를 `option{background:var(--bg);color:var(--cream)}`으로 이미 풀어 뒀다. 같은 방식을 그대로 채택했다.

**`option:checked`는 일부러 칠하지 않았다.** 사용자 스크린샷에서 **선택된 행은 OS 하이라이트로 이미 잘 읽히고 있었고**(상속된 글자색을 덮는다), 후보 색을 전부 재 보니 **손대는 쪽이 더 나빴다** — `--surface3` 쌍은 Deep Navy에서 2.3:1, amber 쌍은 Solar Flare에서 1.7:1까지 떨어진다. 고칠 곳은 선택 안 된 행이지 선택된 행이 아니었다.

닫힌 면의 글자도 `--cream-2` → **`--cream`**, 배경은 형제 버튼(`.pe-btn`)과 같은 **패널 위 오버레이**로 되돌렸다(`--surface2`로 두면 Deep Navy에서 3.7:1까지 내려간다).

### 검증 — 10개 테마 전부를 눈이 아니라 숫자로

각 테마의 토큰을 실제로 해석해 **합성 후 명도 대비**를 계산했다.

| | 최저 대비 | 어느 테마 |
|---|---|---|
| 클립 경계선 (`--clip-edge`를 `--bg2` 위에 합성) | **1.94** | Classical Ivory |
| 드롭다운 닫힌 면 · 열린 목록 글자 | **8.71** | Solar Flare |

밝은 테마의 경계선은 수정 전 사실상 1.0(구분 불가)에서 **1.94**로, 드롭다운 글자는 **모든 테마에서 8.7:1 이상**(본문 텍스트 기준 4.5:1을 크게 상회)이 됐다. 어두운 테마의 경계선 대비는 종전 값을 그대로 써 **변하지 않았다**(2.04~2.24). Deep Navy에서 실제 화면 캡처로 닫힌 면이 또렷하게 읽히는 것도 확인했다.

> ⬜ **남겨 둔 것**: `.audio-input-port-select option:checked`도 같은 약한 쌍(`--cream` on `--surface3`)이라 Deep Navy에서 2.3:1이다. **이번 보고 범위 밖이고 증상 보고도 없어 손대지 않았다.**

### 버전
- [package.json](package.json)·[version.js](version.js) **2.4.2 → 2.4.3**(Patch +1).

---

## v2.4.2 — 지운 녹음 클립이 재실행 때 되살아나던 버그 (2026-08-31)

> 사용자 보고(T-2.4.1-1 시험 중): "Audio In 1에 붙였던 클립을 **삭제하고 앱을 종료**했는데, 재실행하면 **훨씬 이전에 녹음했던 클립이 살아나 있고 소리도 난다.** 그 클립을 지우고 다시 종료·재실행해도 **여전히 돌아온다.**" **버전 판정**: 버그 수정 → **Patch +1, v2.4.1 → v2.4.2.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**
>
> ✅ **v2.4.1 수정 자체는 통과했다** — 같은 시험의 2번(복사한 클립이 재시작 없이 들림)이 정상이었다. 이 항목은 그 뒤에 드러난 **별개의 복원 경로 문제**다.

### 원인 — "비어 있음"과 "길이를 모름"을 같은 것으로 읽었다 🔴

두 코드가 각자는 옳은데 맞물리면서 문제가 됐다.

**ⓐ `deleteClip`은 마지막 클립을 지울 때 빈자리를 남긴다.** 트랙이 클립 0개가 되지 않도록 **길이 0짜리 placeholder 클립**을 start 0에 하나 남긴다("keep at least an empty placeholder clip"). 그 자체로는 문제가 없다 — 길이가 0이므로 소리도 파형도 없다.

**ⓑ `_assignDecodedToTrack`은 외톨이 클립을 버퍼 전체로 늘린다.** 오디오 재접속 시, **start 0에 클립이 하나뿐이고 그것이 primary 소스를 가리키면** 방금 디코드한 버퍼의 길이로 늘린다. 저장된 길이가 낡았을 수 있는 "클립 1개 == 파일 전체" 경우를 고치기 위한 규칙이다.

**ⓐ가 남긴 placeholder는 ⓑ의 조건과 정확히 일치한다** — 외톨이 · start 0 · primary 소스. 그래서 재열기 때마다 **빈자리가 예전 녹음 전체로 부풀려졌다.** 사용자가 몇 번을 지우든 다음 실행에서 돌아온 이유다(보고 5번).

### 어디서 되살아나는가 — 저장도 import도 무죄였다

엔진만으로 저장→재열기→오디오 재접속을 재현해 단계별로 찍었다.

| 단계 | 클립 |
|---|---|
| ① 8초 녹음 직후 | `start 0 · dur 8.00` |
| ② 클립 삭제 직후 | `start 0 · dur 0.00` (placeholder) |
| **저장된 `.focus` 내용** | `dur 0` — **저장은 정상** |
| ④ 재열기(오디오 재접속 전) | `dur 0` — **import도 정상** |
| **⑤ 오디오 재접속 후** | **`dur 8.00`** 🔴 |

즉 디스크에 잘못 쓰인 것이 아니라 **부팅 시 오디오를 다시 붙이는 마지막 단계에서** 만들어졌다. 저장 파일만 들여다봤다면 못 찾았을 자리다.

### 수정 — 늘리기에서 "의도적으로 비운 트랙"만 제외

```js
const emptied = !!lone && !(lone.duration > 0) && this._clipEditable(track);
if (lone && !emptied && lone.start === 0 && (!lone.sourceId || lone.sourceId === primaryId)) {
```

**`duration <= 0`만으로 판정하지 않고 `_clipEditable`을 함께 본 이유**: 소스 길이를 0으로 저장한 **옛 스템**은 이 늘리기에 정당하게 기대고 있다. 스템은 clip-editable이 아니고, `deleteClip`이 손댈 수 있는 것은 **audioIn·bounce뿐**이므로, "의도적으로 비운 placeholder"를 가질 수 있는 트랙도 그 둘뿐이다. 두 경우가 kind로 깨끗이 갈린다.

⚠️ **이 코드는 v1.37.0에서 한 번 데인 자리다**("재열기하면 원래 clip 1개만 남아있음" — Consolidate 결과를 되돌려 놓던 버그). 그래서 조건을 넓히지 않고 **한 갈래만** 막았다.

### 검증 — 이 규칙이 지키던 경우들을 함께 고정

수정 전/후를 같은 하네스로 돌려 **바뀐 것이 (f)·(g) 둘뿐임**을 확인했다.

| 케이스 | 수정 전 | 수정 후 |
|---|---|---|
| (a) 손대지 않은 8초 녹음 | 8.00 | 8.00 |
| (b) 3초로 trim한 클립 | 3.00 | 3.00 |
| (c) split한 2클립 | 유지 | 유지 |
| (d) 녹음 트랙의 낡은 저장 길이 | 5.00 | 5.00 |
| (e) **길이 0인 옛 스템** | 8.00으로 교정 | **8.00으로 교정(유지)** |
| **(f) 비운 녹음 트랙** | **8.00으로 부활** 🔴 | **0.00 (빈 채로)** |
| **(g) 그 상태로 두 번째 재열기** | **8.00으로 부활** 🔴 | **0.00 (빈 채로)** |

> 📌 **(d)는 수정 전에도 5.00이었다** — 제 변경과 무관한 기존 동작이다. Audio In 트랙에서 녹음은 `sources[1]`에 들어가므로 `lone.sourceId === primaryId` 가드에 걸려 **늘리기가 애초에 적용되지 않는다.** 대조 실행으로 확인했고, 회귀로 오인하지 않도록 시험에 그대로 고정해 뒀다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.4.1 → 2.4.2**(Patch +1).

---

## v2.4.1 — Copy to track한 클립이 소리가 나지 않던 버그 (2026-08-31)

> 사용자 보고: "Bounce 트랙을 쪼개 클립 하나를 **Audio In 1 트랙으로 복사**한 뒤 재생하면 **OUTPUT FX 레벨미터는 움직이는데 소리가 들리지 않는다.** 앱을 껐다 켜면 정상." **버전 판정**: 버그 수정 → **Patch +1, v2.4.0 → v2.4.1.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### 원인 — 브리지 Proxy를 통과해 버린 유일한 클립 명령 🔴

[audio-bridge.js](audio-bridge.js)는 마지막에 **`Proxy`로 래핑되지 않은 메서드를 그대로 `LocalDAW`로 흘려보낸다.** 클립 편집 명령은 전부 래퍼를 갖고 그 안에서 `syncTrackToNative()`를 부르는데, **`copyClipToTrack`(v2.1.0)에만 그 래퍼가 없었다.** 오디오를 바꾸는 엔진 메서드 30개를 전수 조사한 결과 **누락은 이것 하나**였다.

**왜 이것만 빠졌는가** — 다른 래퍼는 전부 `syncTrackToNative(트랙 = trackId)`로, **호출된 트랙 자신**을 다시 싣는다. Copy to track은 **효과가 다른 트랙(`destTrackId`)에 떨어지는 유일한 클립 명령**이다. 형태가 형제들과 달라서 같은 자리에 놓이지 못했고, 그래서 눈에 띄지 않았다.

### 왜 "미터는 움직이는데 소리만 없었나" — 이 조합이 진단을 어렵게 했다

핸드오버가 끝나면 브리지는 `LocalDAW.setOutputMuted(true)`로 **웹 엔진을 스피커에서만 끄고** 나머지는 계속 돌린다(상태·오토메이션·플레이헤드용). 그런데 `setOutputMuted`가 줄이는 `masterOutputGain`은 **`masterAnalyser` 뒤에** 있고([audio-engine.js](audio-engine.js) `masterComp → masterAnalyser → masterOutputGain`), 브리지의 마스터 미터는 네이티브 레벨이 0에 가까우면 **웹 엔진 값으로 폴백**한다.

```js
return nativeLevel > 0.0001 ? nativeLevel : LocalDAW.getMasterLevel();
```

그래서 화면은 **소리가 죽은 웹 엔진의 레벨**을 보여 주고 있었다. 미터가 춤추는데 스피커는 조용한, 정확히 보고하신 그림이다. **미터를 근거로 "엔진에 오디오가 갔다"고 읽으면 안 된다** — 이 폴백이 있는 한 마스터 미터는 네이티브의 증거가 아니다.

나머지 증상도 같은 뿌리에서 나온다. **파형·Pitch Editor Analyze가 멀쩡했던 것**은 둘 다 렌더러 모델(`_rawBufferForSource`)만 읽기 때문이고, **재시작하면 정상**인 것은 프로젝트를 다시 열 때 전 트랙이 네이티브로 다시 실리기 때문이다 — 저장된 데이터는 처음부터 멀쩡했고 **런타임 상태만 어긋나 있었다.**


### 미터로 원인을 가르는 법 — **마스터 미터와 트랙 미터는 신뢰도가 다르다**

"미터를 보지 말라"는 것은 틀린 정리다(사용자 지적 2026-08-31). 무효인 것은 **마스터 미터 하나만 보고 "엔진에 오디오가 갔다"고 읽는 추론**뿐이고, **두 미터를 함께 보면 오히려 원인이 갈린다.** 둘의 폴백 정책이 다르기 때문이다.

| 브리지 함수 | 네이티브 값이 0일 때 | 신뢰도 |
|---|---|---|
| `getMasterLevel` / `getMasterStereoLevels` (OUTPUT FX 미터) | **웹 엔진 값으로 폴백** | 🔴 모호 — 소리가 죽어도 움직인다 |
| `getTrackLevel` (트랙 헤더 · 믹서 채널) | `nativeState.trackLevels[id] || 0` — **폴백 없음** | ✅ 네이티브의 직접 증거 |
| `getMasterBandLevels` | 네이티브 데이터가 있으면 항상 그것 | ✅ (**같은 함정을 이미 한 번 고친 자리** — 코드 주석에 "the old any-nonzero check fell back to the muted web engine during silence"로 남아 있다) |

그래서 **재생 중 소리가 없을 때** 두 미터의 조합이 곧 진단이다.

| OUTPUT FX 미터 | 그 트랙의 미터 | 판정 |
|---|---|---|
| 움직임 | **0** | **네이티브가 그 트랙의 오디오를 모른다** — v2.4.1 버그의 서명. 마스터 미터는 폴백으로 웹 값을 보여주는 중이다 |
| 움직임 | 움직임 | 네이티브는 정상 재생 중인데 **소리가 장치로 나가지 않는다** — 출력 장치 분리/무음 장치([memory] `focusdaw-audio-device`, v1.43.21 폴백이 못 잡는 "열리지만 소리 없는" 장치) 또는 마스터 뮤트·볼륨 |
| 0 | 0 | 웹 엔진조차 신호가 없다 — 트랜스포트가 안 돌거나, 전 트랙 뮤트/솔로 배제이거나, 오디오가 아예 안 실렸다. **v2.4.1 버그는 여기 해당하지 않는다**(그 버그에서는 렌더러 모델이 멀쩡하므로 마스터 미터가 반드시 움직인다) |

즉 **"미터가 안 움직이고 무음"과 "미터가 움직이고 무음"은 서로 다른 원인**이며, 후자 안에서 다시 둘을 가르는 것이 **해당 트랙의 미터**다. 관찰할 때 마스터 미터만 적어 두면 이 구분이 사라진다.

> ⬜ **남겨 둔 판단**: `getMasterLevel`의 폴백은 `getMasterBandLevels`가 이미 걷어낸 것과 같은 종류다. 걷어내면 미터가 정직해지지만 **핸드오버 전·네이티브 유휴 구간의 표시가 함께 바뀌므로** 별도 판단이 필요하다. 이번 수정 범위 밖으로 둔다.

### 수정

`copyClipToTrack` 래퍼를 추가해 **복사 대상 트랙**을 다시 싣는다. 새 장치가 아니라 split/trim/move가 이미 쓰는 경로 그대로이며, 그 안의 `ensureSongLengthConsistent()`가 프로젝트 길이 변화까지 함께 처리한다.

```js
copyClipToTrack(trackId, clipId, destTrackId, atStart) {
  const id = LocalDAW.copyClipToTrack(trackId, clipId, destTrackId, atStart);
  if (id && this.isNative) syncTrackToNative(LocalDAW.tracks.find(t => t.id === destTrackId));
  return id;
}
```

### 검증 — 엔진 경계에서 관찰

v2.3.4에서 정한 방식대로 **웹 계층이 아니라 엔진 경계**를 봤다(WebSocket을 가짜 엔진 소켓으로 바꿔 `isNative`를 켜고 나가는 명령을 전부 붙잡는다). 사용자 절차와 같은 모양 — bounce 트랙 + 빈 Audio In 트랙 → split → Copy to track:

| 조작 | v2.4.0 | v2.4.1 |
|---|---|---|
| bounce 트랙 추가 | `loadTrack` | `loadTrack` |
| **Copy to track** | **명령 0건** 🔴 | **`loadTrack` + 런타임 파라미터** |
| 그 클립을 이동(대조군) | `loadTrack` | `loadTrack` |

v2.4.0에서는 렌더러에 baked 버퍼까지 생겼는데도 **엔진으로 한 건도 나가지 않았다.** 수정 후 나가는 `loadTrack`의 대상이 **복사 대상 트랙(dst)이고 원본(src)은 건드리지 않는 것**까지 확인했다 — 형제 래퍼를 그대로 베꼈다면 여기서 엉뚱한 트랙을 실었을 자리다.

### 남겨 둔 것 — Shift+Mute 이상 (재현 안 됨)

같은 세션에서 **File Tracks의 Shift+Mute가 동작하지 않았다**는 보고가 함께 있었다. 로직을 다시 읽었으나 **네이티브와 무관한 순수 렌더러 경로**이고 깨질 지점을 찾지 못했으며, **현재 재현되지 않는다**. **사용자 결정(2026-08-31): 원인이 분명하지 않은 상태에서 손대지 않는다** — 짐작으로 고치면 다른 문제를 만든다. [버그.md](버그.md)에 관찰 항목으로 남겼다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.4.0 → 2.4.1**(Patch +1).

---

## v2.4.0 — Pitch Editor Stage C: 노트 세그멘테이션 (2026-08-31)

> 사용자 요구 2건: ① "**Analyze 이후 ref UI처럼 블록 사각형으로 편집할 노트를 만들되, 현재 BPM을 참조해 과다한 Note가 생성되지 않도록 하는 Rule이 필요**" ② "**Note 블록 사각형 안에 Analyze된 Key를 표시**". **버전 판정**: 신규 기능 → **Minor +1, v2.3.4 → v2.4.0.** **renderer 재빌드만 · 네이티브 재빌드 불필요.** 설계 [피치-에디터-설계.md](피치-에디터-설계.md) §12.

Stage B가 그린 **피치 곡선**을 사람이 부른 **음표 단위**로 잘라 amber 노트 블록으로 얹는다. **오디오는 한 바이트도 건드리지 않는다** — 화면과 판정만 바뀐다(소리에 손대는 것은 Stage E).

### ① 과다 노트를 막는 것은 "최소 길이"가 아니라 **시간 이력**이었다 🔴

설계 §5-3의 최소 길이 **60 ms 고정**은 **신호만 보고 정한 값**이다. 실제로 과분할을 만드는 원인은 길이가 아니라 **비브라토**다 — 한 음을 붙들고 있는 가수는 반음 경계를 **초당 대여섯 번 넘나든다**. 임계(±0.6반음)만으로 자르면 그때마다 경계가 서고, 한 음이 서너 조각이 된다.

그래서 경계 판정을 **두 조건의 곱**으로 바꿨다.

1. 피치가 노트의 **running reference**에서 `hyst`(0.6반음)보다 멀어지고,
2. **그 상태를 `minNoteSec`의 절반 이상 유지**해야 경계로 인정한다.

**비브라토는 1을 늘 만족하고 2를 결코 만족하지 못한다** — 이 구분이 규칙의 전부다. reference는 노트 **안쪽 프레임에서만** 천천히 따라가므로(α=0.08) 포르타멘토는 흡수하고, 벗어난 프레임이 reference를 끌고 가 경계가 영영 안 서는 일도 없다.

**실측 대조**(±0.5반음 비브라토를 건 5음, 96 BPM): 옛 60 ms 고정 규칙 **13개** → 새 규칙 **5개**(부른 그대로). 음정 판정도 `C4 D4 E4 D4 C4`로 정확했다.

### ② 최소 길이는 **박에서** 나온다 (사용자 요구 ①)

"이보다 짧은 음은 없다"의 기준은 초가 아니라 **분해능**이다. 60 BPM에서 너무 거칠고 180 BPM에서 너무 고운 상수를 쓰는 대신, 곡을 따라가게 했다.

```js
gridSec    = (60 / bpm) * (4 / division);          // division 기본 16 = 1/16음표
minNoteSec = clamp(gridSec * 0.75, 0.06, 0.20);
```

| BPM | 1/16음표 | `minNoteSec` |
|---|---|---|
| 60 | 250 ms | 188 ms |
| 96 | 156 ms | **117 ms** |
| 120 | 125 ms | 94 ms |
| 180 | 83 ms | 63 ms |

하한 **60 ms**는 검출기 자신의 한계이고, 상한 **200 ms**는 느린 곡이 진짜 빠른 패시지를 삼키지 않게 하는 안전선이다(1/8 @96 BPM은 산식으로 234 ms지만 이 상한에 걸려 200 ms가 된다 — 의도된 동작).

**짧은 조각은 지우지 않고 병합한다.** 지우면 그 구간이 **노트 없는 구멍**이 되어 Stage E에서 보정되지 않는다. `minNoteSec` 미만 조각은 **음정이 가까운 이웃**에 흡수시키고, 병합이 런을 비우는 일은 없다(마지막 하나는 길이와 무관하게 남는다). 검출기 하한(60 ms)에도 못 미치는 런만 애초에 노트가 아니었던 것으로 보고 버린다.

**밀도 상한은 마지막 방어선**이다 — 노트 수가 `ceil(클립길이/gridSec) × 1.2`를 넘으면 임계를 1.5배로 올려 다시 자른다(최대 2회). 실측해 보면 **대개 발동하지 않는다**(난수 피치 3초 → 12개, 상한 24). 병합 규칙이 이미 일을 하고 있고, 상한은 신호가 병적일 때만 필요하다.

**BPM 미측정은 정상 상태다**(v1.46.0에서 확정 — 이 앱은 BPM을 추측하지 않는다). 없으면 **고정 120 ms**를 쓰고, **어느 쪽을 쓰는지 상태줄이 밝힌다**: `Min note 117 ms (1/16 @ 96 BPM)` / `Min note 120 ms (no project BPM)`. 우측 패널의 `Note resolution`(1/8·1/16·1/32)은 **BPM이 없으면 비활성**이다. ⚠️ 기준은 **`projectBpm`이지 `playbackBpm`이 아니다** — Vari BPM은 재생 속도만 바꾸고 분석 대상은 원본이므로, 늘어난 템포로 만든 그리드는 모든 노트의 길이를 잘못 잰다(§3 원본 도메인 규칙).

**그리드를 바꿔도 재분석하지 않는다.** 비싼 것은 검출이고 그것은 이미 끝났으므로, 세그멘테이션만 다시 돈다 — **5분 테이크(27,900 프레임) 재분할 7 ms**.

### ③ 블록 안에 음이름 (사용자 요구 ②)

블록이 어느 음인지는 **블록 자신이 말해야 한다** — 줌 아웃 상태에서 좌측 건반과 눈으로 줄을 맞추는 것은 어렵다. 블록 내부 좌측에 **음이름 + 옥타브**(`A3`), 넓으면 **센트 편차**까지(`F#4 +25¢`). 임계는 참조 디자인의 declutter 규격 그대로 — 이름은 `rowH ≥ 12 && w > 30`, 센트는 `w > 62`, 그보다 좁으면 아무것도 그리지 않고 라벨은 블록에 **클립**한다(넘치는 글자가 라벨 없는 것보다 나쁘다).

> **용어**: 요구 ②의 "Analyze된 Key"는 **그 노트의 음이름**으로 해석했다. 프로젝트 단위 **조성**은 이미 상태줄 우측에 `Key C`로 나와 있어 블록마다 반복할 값이 아니다. 대신 조성이 있으면 **스케일 밖 음은 테두리를 점선**으로 구분해 Stage D의 `Snap all to key`가 무엇을 건드릴지 미리 보인다. **선택은 색(cream 링), 조성은 점선** — 두 표시가 겹칠 수 있으므로 채널을 나눴다.

블록은 **amber**, 곡선은 **red**다(v2.2.1에서 선점해 둔 대로). 신뢰도가 낮은 노트는 흐리게 — 곡선이 이미 따르던 규칙을 블록도 따른다.

### ④ 선택 · 상태줄

롤에서 **노트를 클릭하면 선택**되고(Ctrl/Shift+클릭은 추가·해제), **빈 곳 클릭은 종전대로 seek**다. 블록은 Stage D가 편집할 대상이므로 트랜스포트를 움직이지 않고 집을 수 있어야 한다. 상태줄 좌측은 선택된 노트의 정보를 보여준다(`F#4 · 0:01.98 → 0:02.40 · 0.42 s · +25¢ · 75% conf`). **노트 id는 한 번의 세그멘테이션 안에서만 유일**하므로, 재분석·그리드 변경이 일어나면 선택은 비운다.

### 구조 — 세그멘테이션은 **창 안**에 둔다

분석 결과 배열은 채널을 타고 이미 창에 와 있고, 그리드를 바꿀 때마다 **왕복 없이 즉시** 다시 잘라야 한다. Stage E의 프린트는 설계 §6대로 **완성된 notes를 엔진에 넘기는** 방향이므로, 이 배치가 그 흐름과도 맞는다. 엔진 쪽 변경은 [audio-engine.js](audio-engine.js) `clipAudioInfo`가 `tempo.projectBpm`을 실어 보내는 **한 줄뿐**이다.

### 검증

**규칙 자체는 합성 신호로 24건 전건 통과**(그리드 산수 7 · 비브라토 3 · 음 변화 1 · 조각 병합 2 · 숨 경계 2 · 자음 1 · 밀도 상한 3 · 그리드 변경 1 · 라벨 2 · 조성 4 · 속도 1). 요지:

| 시나리오 | 결과 |
|---|---|
| ±0.5반음 비브라토 5음 | **5개**(옛 규칙 13개) · 음정 정확 |
| 무음 없이 이어 부른 C→G→C | **3개**(진짜 음 변화는 자른다) |
| 0.5 s 음 사이의 40 ms 스파이크 | **1개** · 구간은 구멍 없이 덮임 |
| 같은 음, 사이에 300 ms 숨 | **2개**(숨은 경계다) |
| 같은 음, 사이에 20 ms 무성(자음) | **1개**(자음은 경계가 아니다) |
| 난수 피치 3초 | 12개(상한 24 — 병합만으로 충분, 상한 미발동) |
| 5분 테이크 재분할 | **7 ms** |

**화면은 Electron 실구동으로 확인했다.** v2.3.4에서 배운 대로 **진짜 경계를 가짜로 바꿔 끼우는** 방식이다 — 그때는 엔진 소켓이었고 여기서는 **BroadcastChannel**이다. 창은 스튜디오와 이 채널로만 이야기하므로, 채널을 페이지 안에서 가로채면 프로젝트·오디오 없이 창 전체를 실제로 띄울 수 있다. 합성 클립(4 s · 96 BPM · Key C · 여덟 음, 다섯 번째는 일부러 스케일 밖 F#)으로:

- 노트 **8개** = 부른 8음 · 캔버스에 amber 블록 실제 렌더(16,737 px) · 콘솔 오류 **0건**
- 라벨 `C4 +10¢ / D4 +35¢ / E4 +19¢ / F4 +3¢ / F#4 +25¢ / G4 +36¢ / F4 +4¢ / E4 +7¢` — **F#4만 점선 테두리**
- 블록 클릭 → 상태줄 `F#4 · 0:01.98 → 0:02.40 · 0.42 s · +25¢ · 75% conf`
- 상태줄 `Min note 117 ms (1/16 @ 96 BPM)` · 패널 `8 notes · shortest 117 ms`
- `1/8`로 바꾸면 **재분석 없이** `Min note 200 ms (1/8 @ 96 BPM)`(234 ms가 상한에 걸린 값)
- **BPM을 null로 둔 프로젝트**: 선택기 **비활성** · `Min note 120 ms (no project BPM)` · 노트는 정상 생성

### 버전
- [package.json](package.json)·[version.js](version.js) **2.3.4 → 2.4.0**(Minor +1).

---

## v2.3.4 — v2.3.3 회귀: Undo/Redo가 소리에 닿지 않던 문제 (2026-08-28)

> 사용자 보고: "Mute는 버튼으로는 동작하지만 **Undo/Redo로는 전혀 동작하지 않는다**. Automation도 재생 중 포인트를 바꾸면 적용되는데 Undo/Redo는 소리에 반영되지 않는다." **버전 판정**: 회귀 수정 → **Patch +1, v2.3.3 → v2.3.4.** **renderer 재빌드만.**
>
> 사용자의 진단이 정확했다 — **"이전에 시간이 걸리던 문제를 대충 알 수 있을 듯"**. 느렸던 그 전체 재적재가, 실은 **파라미터를 엔진에 전달하던 유일한 통로**였다.

### 원인 — 파라미터 전달이 재적재에 얹혀 있었다 🔴

네이티브 엔진이 mute·solo·volume·pan·sends·automation을 알게 되는 경로는 둘뿐이다.

1. `Bridge.setTrackParam()` — 버튼을 누를 때 (그래서 **버튼은 잘 동작했다**)
2. `syncTrackToNative()`의 마지막 줄 `sendTrackRuntimeStateToNative(track)`

그런데 **`applySnapshot`은 둘 다 지나가지 않는다.** 값을 LocalDAW 트랙 객체에 `Object.assign(t.params, …)`로 직접 써넣을 뿐이다. v2.3.2까지는 undo가 **모든 트랙을 재적재**했으므로 2번이 딸려 실행됐고, 그래서 우연히 동작했다 — 1초를 치르면서. v2.3.3에서 그 재적재를 걷어내자 **파라미터가 엔진에 도달할 방법이 사라졌다.** 화면(웹 엔진 `_applyMix`)은 맞고 소리(네이티브)만 옛것이 되는, 정확히 보고하신 증상이다.

**이것은 제가 v2.3.3에서 만든 회귀입니다.** 성능 수정 자체는 유효했지만, 걷어낸 코드가 **부수적으로 하고 있던 일**을 확인하지 않았다.

### 수정 — 파라미터를 재적재에서 떼어내 따로 보낸다

트랙마다 서명을 **두 갈래**로 나눴다. 비용이 완전히 다른 두 가지를 하나로 묶고 있었던 것이 문제의 뿌리다.

| 서명 | 담는 것 | 달라졌을 때 하는 일 | 비용 |
|---|---|---|---|
| `audio` | filePath · baked 여부 · 버퍼 동일성 · startSeconds · songLength | `syncTrackToNative` (파일/임시 WAV 재적재) | 큼 |
| `params` | volume · pan · mute · solo · reverb · echo · autoOn · autoCurve · automation · vocalFx | `sendTrackRuntimeStateToNative` (작은 명령 몇 개) | 무시 가능 |

`params`만 바뀌면 재적재 없이 값만 보낸다. **바뀐 트랙에만** 보내므로, 재생 중 Ctrl+Z가 멀쩡한 트랙들에 같은 값을 다시 앉히지도 않는다.

### 검증 — 가짜 엔진 소켓을 물려 실제로 나가는 명령을 관찰

이번 회귀는 "웹 계층만 계측했다"는 구멍에서 나왔으므로, 하네스를 **엔진 경계까지** 늘렸다: 프리로드에서 `WebSocket`을 가짜 소켓으로 바꿔 브리지가 **네이티브 경로**를 타게 하고(`DAW.isNative = true`), 엔진으로 나가는 명령을 전부 붙잡았다.

| 시나리오 | 결과 |
|---|---|
| 버튼으로 Mute | `setTrackParam mute=true` 1건 (종전대로) |
| **그 Mute를 Undo** | **`setTrackParam mute=false` 1건** · `clearTracks` 0 · `loadTrack` **0** |
| Automation 포인트 변경 | `setTrackAutomation` 2건 |
| **그 변경을 Undo** | **`setTrackAutomation` 1건**(해당 트랙만) · `loadTrack` **0** |
| 클립 이동을 Undo | `loadTrack` 발생 — 프로젝트 길이가 바뀌면 `songLength` 때문에 전 트랙 재적재가 **필요**하다 |

즉 **소리에 닿으면서도** v2.3.3의 성능 이득(웹 계층 623 ms → 1.1 ms, 재적재 0건)은 그대로다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.3.3 → 2.3.4**(Patch +1).

---

## v2.3.3 — Undo/Redo가 1초 걸리던 원인 + 보완 3건 (2026-08-28)

> 사용자 지적: "**Undo/Redo 동작이 매우 느리다** — 스템 5개 + Audio In 1개 프로젝트에서 Solo/Mute 같은 간단한 동작도 1초쯤 걸린다." 및 시험 보완 3건. **버전 판정**: 버그·성능 수정 → **Patch +1, v2.3.2 → v2.3.3.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### ④ 🔴 Undo/Redo 지연 — 매번 프로젝트 전체를 다시 계산하고 엔진을 다시 실었다

재현부터 했다. 스템 5개(3분) + 클립 2개짜리 Audio In 트랙에서 **파라미터만 바뀐 스냅샷 왕복**을 실측:

| | v2.3.2 | v2.3.3 |
|---|---|---|
| `getSnapshot` | 1.0 ms | 0.3 ms |
| `applySnapshot` | **622.3 ms** | **0.8 ms** |
| 합계 | **623.3 ms** | **1.1 ms** (566배) |

원인은 세 겹이었다.

**ⓐ 피크를 매번 다시 계산했다 (~420 ms).** `applySnapshot`은 `st.buffer`가 있으면 무조건 `computePeakLevels`를 돌렸다 — 오디오 전체를 **3회 스캔**하는 작업을, **모든 트랙에 대해 매 Undo마다**. 그런데 **스냅샷은 AudioBuffer를 참조로 들고 있다**(undo 스택은 메모리에 있으니 그래도 된다) — 즉 **같은 객체 = 같은 샘플 = 이미 계산된 피크가 그대로 유효**하다. 게다가 클립 편집 트랙에서는 이 피크를 몇 줄 뒤 `_ensureBaked`가 **버리고 다시 계산**했다(이중 낭비). 수정 = 버퍼 객체가 실제로 바뀌었을 때만, 그리고 베이크가 뒤따르지 않을 때만 계산한다.

**ⓑ 클립 레이아웃을 매번 다시 렌더했다 (~200 ms/트랙).** Mute를 되돌리는데도 Audio In 트랙 전체를 다시 베이크했다. 베이크는 **입력의 순수 함수**이므로, 그 입력을 서명으로 만들어 **결과를 캐시**했다 — 서명이 같으면 이전에 만든 버퍼가 곧 정답이다. 서명은 `_renderClips` 결과(클립의 sourceId·start·offset·sourceOffset·duration·gain)와 **원본 버퍼의 객체 동일성**으로 이루어진다. 길이·샘플레이트 같은 값으로 비교하지 않는 이유: **같은 길이의 다른 테이크는 같은 오디오가 아니다.** 캐시 적중 시에는 `audioRev`를 올리지도, 재생 중인 소스를 다시 시작하지도 않는다 — 아무것도 바뀌지 않았는데 소리를 끊는 셈이 되기 때문이다.

**ⓒ 네이티브 엔진에서 프로젝트를 통째로 헐고 다시 실었다.** 브리지의 `applySnapshot`은 `clearTracks` → 전 트랙 재적재였다. Mute 하나를 되돌리는 데 **클립 편집 트랙마다 임시 WAV를 디스크에 쓰고**(`bufferToWav`), 스템 5개를 다시 읽게 하고, 그동안 `nativeOutputActive=false`라 **출력이 웹 엔진으로 떨어졌다** — 재생 중 Undo가 소리를 건드리던 이유다. 수정 = **트랙별 서명을 스냅샷 전후로 비교해 실제로 달라진 것만** 다시 보낸다. 새 장치를 만든 게 아니라 **이미 있던 경로를 쓴다**: `syncTrackToNative`는 split/trim/move가 이미 단건으로 쓰는 함수이고, `removeTrack`도 이미 있는 명령이다. 프로젝트 단위 상태(tempo·master·loop)는 명령 몇 개라 항상 보낸다. **`songLength`가 모든 서명에 들어 있으므로 프로젝트 길이가 바뀌면 전 트랙이 다시 나간다** — 각 트랙이 자기 `songLength`로 루프하는 엔진 특성상 반드시 그래야 한다.

**정확성 검증이 이 수정의 핵심이다.** 캐시가 틀린 결과를 재사용하면 "화면은 맞는데 소리는 옛것"이 된다. 베이크 결과를 **샘플 값으로 지문화**해 실구동 확인했다: 무변경→같은 버퍼 객체·같은 오디오 · 스냅샷 왕복→재사용·동일 · **클립 이동→재베이크(길이 6.0→7.5 s, 값 변화)** · 그 이동을 Undo→**원본과 완전 일치** · **gain 변경→재베이크**, 되돌리면 일치 · **trim→재베이크**(6.0→5.0 s) · **같은 길이의 다른 원본 버퍼로 교체→재베이크**(문자열 서명이었다면 놓쳤을 경우), 되돌리면 일치.

### ① Shift+Mute가 트랙 선택까지 건드리던 문제

트랙 헤더의 mousedown이 **트랙 선택 핸들러**이고 거기서 Shift는 "선택 확장"을 뜻한다. 일괄 Mute가 같은 수식키를 쓰면서, 한 번의 Shift+클릭이 **두 제스처를 동시에** 실행하고 있었다. 수정 = Shift가 눌린 Mute 클릭만 헤더로 전파되지 않게 한다(`MuteBtn`에 `onMouseDown`을 추가). **Shift 없는 Mute 클릭은 종전대로 트랙을 선택한다** — 바꾼 것은 겹친 제스처 하나뿐이다.

### ② Pitch Editor — 피아노롤 가로 스크롤바 제거

위쪽 **CLIP MAP 띠**가 이미 "클립 어디를 보고 있는지"를 보여주고 드래그로 이동까지 시킨다. 같은 말을 두 번 하면서 롤의 높이를 10px 먹고 있었다.

### ③ Pitch Editor — 열 때 재생을 멈춘다

이 창의 트랜스포트는 **클립 범위**인데 스튜디오가 그 밑에서 딴 데를 재생하고 있으면 에디터가 고장 난 것처럼 읽힌다. **창이 클립과 트랜스포트 상태를 처음 알게 된 순간 딱 한 번** 멈추고(그 뒤로 여기서 재생하는 것은 자유), 정지는 ■와 같은 경로를 타 **클립 시작에 선다**. ⚠️ **녹음 중에는 무시한다** — `transportStop`은 녹음을 종료시키므로, 창을 열었다고 테이크가 끊기면 안 된다. 에디터가 스스로 내는 정지에는 `fromEditor` 표식이 붙고, 스튜디오가 그 표식이 붙은 정지만 녹음·카운트인 중에 버린다.

### 검증 — Electron 실구동

성능·정확성은 위에 적었고, 나머지는 실구동 스모크로 확인했다: **트랙 1을 선택한 상태에서 Shift+Mute → 3트랙 전부 Mute, 선택 표시(box-shadow) 전후 완전 동일** · 피아노롤에 `.pe-hbar` 없음 · 세로 스크롤바가 롤 바닥까지 닿음 · **재생 중 창을 열면 `REQUEST_STOP{fromEditor:true}` 1건 발생하고 재생이 멈춤** · 그 뒤 ▶로 재생하면 **추가 정지 0건** · 콘솔 오류 0건.

> ⚠️ 하네스에는 네이티브 엔진이 없으므로 위 623→1.1 ms는 **웹 계층만의 수치**다. ⓒ(네이티브 선택적 재동기화)의 실제 효과와 **재생 중 Undo의 무결성**은 **사용자 시험(T-2.3.3-3)** 에서 확인한다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.3.2 → 2.3.3**(Patch +1).

---

## v2.3.2 — Shift+Mute가 bounce 트랙을 건너뛰던 문제 (2026-08-28)

> 사용자 보고: "file tracks의 mute 버튼 Shift+클릭이 stem은 일괄 mute되는데 **merge되어 bounce된 트랙에는 적용되지 않는다**." **버전 판정**: 버그 수정 → **Patch +1, v2.3.1 → v2.3.2.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### 원인 — 그룹의 소속 기준과 일괄 동작의 대상 기준이 서로 달랐다 🔴

`File Tracks` 그룹은 **stem · bounce · `kind`가 없던 옛 트랙**을 담는다([app.jsx](app.jsx#L1710)):

```js
DAW.tracks.filter((t) => !t.kind || t.kind === "file" || t.kind === "bounce")
```

그런데 일괄 Mute는 **`kind === "file"` 하나만** 골랐다. 그래서 화면에는 같은 그룹 안에 나란히 있는 트랙들이, 동작에서는 서로 다른 집합으로 갈렸다 — **bounce 트랙은 그룹 안에 앉아 있으면서 일괄 Mute만 비껴갔다.** 같은 어긋남이 **`kind`가 없는 옛 프로젝트 트랙**에도 있었다(그룹에는 들어오지만 일괄 Mute에서 빠진다).

어긋난 자리는 **다섯 곳**이었고, 세 곳은 "대상 선별", 두 곳은 "Shift가 먹히는 트랙" 쪽이었다 — 즉 **bounce 트랙의 Mute를 Shift+클릭하면 일괄 동작 자체가 시작되지도 않았다.**

| 위치 | 하던 일 |
|---|---|
| [app.jsx](app.jsx) `muteAllFileTracks` | 일괄 대상 선별(트랙 헤더 경로) |
| [app.jsx](app.jsx) `MUTE_ALL_FILES` | 일괄 대상 선별(믹서 창에서 온 요청) |
| [mixer-app.jsx](mixer-app.jsx) `muteAllFileTracks` | 믹서 창의 낙관적 즉시 반영 |
| [ui-tracks.jsx](ui-tracks.jsx) MuteBtn | Shift가 먹히는 트랙 판정(트랙 헤더) |
| [ui-mixer.jsx](ui-mixer.jsx) MuteBtn | Shift가 먹히는 트랙 판정(믹서 채널) |

### 수정 — 판정을 한 곳으로

다섯 곳이 각자 조건을 들고 있었던 것이 문제의 본질이므로, **그룹의 정의 자체를 공유 술어로 뽑아** 여섯 곳(그룹 필터 포함)이 **같은 함수**를 부르게 했다. [ui-kit.jsx](ui-kit.jsx)에 둔 이유는 그 파일이 스튜디오·믹서 두 창에서 **가장 먼저 로드되는 공용 스코프**이기 때문이다(창들이 하나의 전역 스코프를 공유한다 — 상시 노트).

```js
function isFileGroupTrack(t) { return !!t && (!t.kind || t.kind === "file" || t.kind === "bounce"); }
```

`nonFileTracks`(그룹의 여집합)는 이미 `t.kind && t.kind !== "file" && t.kind !== "bounce"` 라 정확히 반대이므로 손대지 않았다. 전수 확인 결과 코드베이스에 남은 `kind === "file"` 비교는 **이 술어 한 줄뿐**이다(나머지 `item.kind !== "file"`은 드래그&드롭의 `DataTransferItem`으로 무관).

### 검증 — 실제 스튜디오 창 구동

studio.html을 띄워 `DAW.tracks`를 합성 트랙 5종(stem×2 · bounce · `kind` 없음 · audioIn)으로 바꾸고 **실제 채널로 `MUTE_ALL_FILES`를 보내 app.jsx의 진짜 핸들러를 태웠다.**

- 술어: file ✓ · bounce ✓ · legacy(kind 없음) ✓ · audioIn ✗ · null ✗
- 결과: **`stem1 · stem2 · bounce1 · legacy1` 이 mute되고 `vox`(audioIn)는 그대로.**

> ⚠️ 하네스가 만든 합성 트랙에는 `clips`·`meter` 같은 필드가 없어 콘솔에 그 필드를 읽는 오류 3건이 남는다 — **하네스의 산물이지 앱의 결함이 아니다.** 실제 Shift+클릭 제스처(트랙 헤더·믹서 창 양쪽)는 **사용자 시험(T-2.3.2-1)** 에서 확인한다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.3.1 → 2.3.2**(Patch +1).

---

## v2.3.1 — Pitch Editor 시험 보완 5건 (2026-08-28)

> T-2.3.0-1·2 통과 후의 보완 요청. **버전 판정**: 문구·색·동작 수정 → **Patch +1, v2.3.0 → v2.3.1.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### ①~③ UI 문구 — **전면 영문화가 지침으로 확정됐다**

우측 패널 안내를 한글로 쓴 것에 대해 사용자가 **"이후 UI에도 기본적인 메뉴 이름·설명은 모두 영어"** 를 지침으로 확정했다. 앱은 이미 v1.46.2에서 잔여 한글을 전수 영문화해 **실구동 한글 0건**을 확인한 전례가 있고, 새 화면에서 한글이 다시 섞이면 그 작업이 되돌려진다. (코드 주석·개발 문서는 계속 한국어다 — 지침은 **화면에 렌더되는 문자열**에만 적용된다.)

- `VIEW` 안내를 영문으로 교체하고, **`Alt`+wheel을 "음정 줌"이 아니라 "Key zoom"** 으로 바로잡았다(v2.2.1에서 라벨을 KEY로 바꾸면서 안내만 옛 이름으로 남아 있었다).
- **`KEYBOARD` 구역 삭제**(사용자 요청). 다만 "건반을 누르면 소리가 난다"는 발견 수단이 통째로 사라지므로, **상태줄에 `click a key to hear it`** 한 줄을 넣었다 — 참조 디자인이 상호작용 안내에 배정한 자리가 바로 상태줄이다.

### ④ 스크롤바 손잡이가 밝은 테마에서 보이지 않던 문제 🔴

참조 디자인 §3e가 지정한 `--surface3`를 그대로 썼는데, 그 토큰은 **밝은 테마에서 트랙 색과 거의 같다.** 실측: **Classical Ivory 1.07:1 · Sage 1.23:1 · lime 1.31 · default 1.40** — 마우스를 올리기 전에는 손잡이의 위치를 알 수 없다(보고 그대로).

10개 테마 × 후보 6종을 **실구동 계측**해서 골랐다. UI 컨트롤의 기준선인 **3:1을 모든 테마에서 넘는 후보는 `--cream-2` 하나뿐**이었고(최저 **3.95:1**, Ivory), 이 토큰은 팔레트가 이미 "보조 텍스트·아이콘"에 배정한 컨트롤 색이라 의미도 맞는다.

| 후보 | 최저 대비(테마) |
|---|---|
| `--surface3` (참조 디자인 원안) | **1.07** (Ivory) |
| `color-mix(--cream 38%, --surface3)` | 2.05 (Ivory) |
| `color-mix(--cream 55%, --surface3)` | 2.89 (Ivory) |
| `color-mix(--cream-2 80%, --surface3)` | 2.91 (Ivory) |
| **`--cream-2`** ← 채택 | **3.95** (Ivory) |

**곁다리 수정**: 손잡이 hover를 `--amber-deep` → `--amber`로 바꿨다. 어두운 테마에서 `--amber-deep`은 새 손잡이보다 **어두워서**, 마우스를 올리면 바가 **흐려지는 것처럼** 보였을 것이다.

### ⑤ 클립 끝에서 자동 정지 🔴

`CLIP` 루프를 끄면 재생이 **클립 밖으로 계속 흘러갔다**. 롤은 클립 가장자리에서 끝나므로 화면에 따라갈 것이 없고, 한 클립을 편집하는 중에 트랜스포트만 달아나는 상태가 된다.

**단, 이 창이 시작한 재생만 멈춘다** ⚠️ 에디터를 열어 둔 채 **스튜디오에서 곡 전체를 들어 보는 것을 클립 가장자리에서 잘라 버리면 안 된다.** 소유권은 이 창의 ▶를 누를 때 주장하고, 관측된 재생→정지 전이에서 놓는다 — 스튜디오에서 누른 Play는 이 창의 깃발을 달지 못한다. `CLIP` 루프가 켜져 있으면 건너뛴다(엔진이 같은 경계에서 되감으며, 그게 토글의 목적이다).

정지는 **■와 같은 경로**를 탄다. 같은 상태에 이르는 두 길이 플레이헤드를 서로 다른 자리에 세워두면 안 된다 — 엔진의 `stop()`은 오프셋을 곡의 0으로 되돌리므로, 두 경로 모두 그 뒤에 클립 시작으로 seek한다.

### 검증 — Electron 실구동

패널 구역 `["VIEW","ANALYSIS","CORRECTION"]` · **렌더된 DOM 전체 + 모든 `title` 속성에서 한글 0건**(TreeWalker 전수 스캔) · 손잡이 대비 **default 10.61 · Ivory 3.95 · Sage 8.37 · Slate 12.02** · **클립 끝 통과 시 `REQUEST_STOP` 1건 + 클립 시작 seek, 통과 전 0건** · **스튜디오가 시작한 재생은 클립 끝을 지나도 정지 0건·계속 재생** · **`CLIP` 켠 상태에서는 정지 0건** · 콘솔 오류 0건.

> ⚠️ 계측 자체의 함정 하나: Chromium은 `color-mix()`를 **`color(srgb 0.5 …)` 형식(0~1 실수)** 으로 직렬화한다. 처음 계측 스크립트가 이 값을 0~255로 읽어 **대비비가 완전히 틀렸고**(Ivory가 13.65:1로 나왔다), 파서를 고친 뒤에야 위 표의 실제 값이 나왔다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.3.0 → 2.3.1**(Patch +1).

---

## v2.3.0 — Pitch Editor UI 재구성 (2026-08-28)

> 사용자 요청 5건(2026-08-28) + 참조 디자인(`_refer/design_handoff_pitch_editor`) 반영. 판정·계획은 설계 [피치-에디터-설계.md](피치-에디터-설계.md) §11. **버전 판정**: 화면 구조 변경 + 기능 추가 → **Minor +1, v2.2.1 → v2.3.0.** **renderer 재빌드만 · 네이티브 재빌드 불필요.**
>
> **Stage C 착수 전에 한 이유**: 노트 블록이 올라오면 화면이 더 붐비고, 같은 화면을 두 번 시험하게 된다.

### ① 레이아웃 — 트랜스포트 바 / 개요 띠 / 롤 + 우측 패널 / 상태줄

툴바 한 줄에 트랜스포트 · 클립명 · 줌 2그룹 · 버튼 4개가 몰려 있었고, 창을 좁히면 `flex-wrap`이 두 줄을 만들어 **피아노롤 높이를 먹었다.** Stage C~E의 Strength·Vibrato·프리셋까지 들어오면 감당이 안 된다. → 트랜스포트 줄에는 **트랜스포트와 신원(클립명·시간)만** 남기고, 줌·분석·보정은 **오른쪽 패널**로 옮겼다.

패널은 **고정 300px + 접기**다(비율 아님, 사용자 승인 ⓑ). 30%로 두면 27인치에서 500px짜리 여백이 생기고 노트북에서는 롤이 눌리는데, 패널에 들어가는 것은 **폭이 늘어나도 좋아질 게 없는 컨트롤**이다.

**구현 중 발견한 결함** 🔴 — 개요 띠가 46px가 아니라 **416px로 부풀어 롤의 높이를 빼앗았다.** 원인은 플렉스의 **자동 최소 크기**: 플렉스 아이템의 `min-height`는 기본값이 `auto`(= 콘텐츠 크기)이므로, 안에 든 캔버스가 보고하는 고유 높이가 `flex:0 0 46px`를 **위로 밀어 올렸다.** 게다가 캔버스 높이를 부모에서 측정하는 구조라 **되먹임 고리**가 된다. 수정 = 띠에 `min-height:0` + `overflow:hidden`, 캔버스는 `position:absolute; inset:0`. 이로써 띠의 크기는 **레이아웃의 성질이지 그려진 내용의 성질이 아니게** 됐다.

### ② 클립 개요 띠 (요청 ④의 절반)

트랜스포트 줄 아래 46px. 클립 전체 파형 + **현재 보고 있는 구간을 밝은 상자**로(바깥을 어둡게 덮는 방식 — 눈이 밝은 쪽을 "지금 보는 곳"으로 읽는다) + 플레이헤드 + `Analyze` 후에는 **음정이 검출된 구간의 붉은 리본**(어디를 확대할 가치가 있는지 한눈에). 상자 바깥 클릭 = 건너뛰기, 상자 드래그 = 스크롤, 가장자리 드래그 = 확대/축소.

피크는 **이미 받아둔 4000버킷 배열**이라 추가 분석이 없다. 다만 화면 1픽셀마다 **그 구간에 걸친 버킷들의 극값**을 취한다 — 하나씩 골라 쓰면(샘플링) 4000개를 700px에 넣을 때 여섯 중 다섯을 버려 **짧고 센 소리가 통째로 사라진다.**

### ③ 상하·좌우 이동 (요청 ④)

정확히 말하면 **좌우는 되고 있었다** — 확대 상태에서 그냥 휠을 굴리면 스크롤됐다. 다만 **스크롤바도 지도도 없어서 화면에 그렇다고 말해주는 것이 없었고**, 위아래는 정말로 없었다(음정 축은 중심 기준 줌만 있었다).

- **음정 범위를 정수 두 개에서 실수 범위로** 바꿨다. 정수였던 이유는 연산이 "중심 기준 줌"뿐이었기 때문인데, 반음 단위로 이동하면 한 번에 한 행(~15px)씩 튀어 스크롤이 아니라 덜컹거림으로 읽힌다.
- **양축 스크롤바**(참조 디자인 §3e 규격). DOM 요소로 뒀다 — 잡아서 끄는 10px 띠는 캔버스 픽셀보다 요소로 다루는 편이 히트 테스트도 스타일도 쉽다.
- **휠 규약을 참조본에 맞춤**: 그냥=위아래 · Shift=좌우 · Ctrl=시간 줌 · Ctrl+Shift=음정 줌. **Alt+휠(v2.2.0의 음정 줌)은 남겼다** — 이미 가르친 습관이고 유지 비용이 0이다. 두 줌 모두 **커서 기준**이고, 스크롤은 **픽셀 1:1**이다.
- **가운데 버튼 드래그로 상하좌우 이동.** ⚠️ 왼쪽 버튼을 쓸 수 없는 이유는 **롤 전체가 이미 seek 면**이기 때문이다. Stage D에서 pan 툴이 들어오면 그쪽으로 통합한다.

### ④ 클립 범위 트랜스포트 (요청 ②)

스튜디오에 **이미 동작하는 루프**(Repeat 구간, 웹·네이티브 양쪽이 존중)가 있어 **두 번째 루프 장치를 만들지 않고 빌려 쓴다** — 나란히 두면 서로 맞춰야 할 것이 둘이 된다. 채널에 `REQUEST_LOOP_RANGE` 추가.

- `CLIP` 토글 ON = 반복 구간이 `[clip.start, clip.start+duration]`. **끄거나 창을 닫으면 사용자의 원래 구간을 되돌려준다**(사용자 승인 ⓒ). 저장 위치는 punch가 쓰는 `prevRepeatRef` 옆의 `peLoopPrevRef` — 같은 모양, 다른 주인(punch는 정지 시 복구, 이쪽은 해제 시 복구).
- **녹음 중에는 점유를 거부한다** 🔴 punch가 **바로 이 구간**을 [in, out]으로 읽기 때문에, 진행 중인 테이크 밑에서 구간을 옮기면 **무엇을 대체하는 중인지가 재정의된다.**
- **Undo 스냅샷을 쌓지 않는다** — `loopRange`/`repeatPlayEnabled`는 트랜스포트 상태이고 `getSnapshot()`이 둘 다 담지 않으므로, 쌓아 봐야 현재와 동일한 항목이 하나 늘 뿐이다(ui-mixer의 루프 드래그가 이미 따르는 근거).
- **▶는 클립에서 시작한다**: 재생 위치가 클립 밖이면 먼저 클립 시작으로 옮긴다. 이미 클립 안이면 건드리지 않는다 — 아니면 중간부터 이어 듣는 것이 불가능해진다. **■는 곡의 0초가 아니라 클립 시작으로** 돌아온다.

### ⑤ 건반 클릭 = 피아노 소리 (요청 A-2)

배음 4개를 감쇠시키는 합성음이다(샘플 없이 몇 줄, 목적은 그럴듯한 피아노가 아니라 **음정 기준**). 창이 **자기 `AudioContext`** 를 연다 — 스튜디오가 이미 웹 엔진 컨텍스트를 네이티브와 공존시키고 있어 안전하다.

⚠️ **다만 출력 장치는 상속되지 않는다.** 맞추지 않으면 재생은 인터페이스로, 프리뷰는 기본 장치로 나간다 → `focusdaw-audio-device`를 읽어 `setSinkId`로 **같은 장치에 맞춘다**(audio-engine의 `setOutputDevice`가 같은 이유로 하는 라벨 매칭과 동일).

**재생 중에는 그 순간 부른 음의 건반에 불이 들어온다.** 곡선의 정확도를 눈과 귀로 동시에 확인하는 수단이며, 사용자가 T-2.0.2-1 ④를 "개발 중 지속 확인"으로 남긴 것에 대한 대응이다.

### 검증 — Electron 실구동, 13개 항목 통과

레이아웃(개요 46px · 롤 539px · 패널 300px · 네 구역) · **Ctrl+휠 시간 줌**(손잡이 100%→54.9%) · **Ctrl+Shift+휠 음정 줌**(100%→55.8%) · **그냥 휠 상하 스크롤**(21.2%→44.2%) · **Shift+휠 좌우 스크롤**(21.3%→41.4%) · **개요 상자 바깥 클릭 = 건너뛰기**(41.4%→0%) · **가운데 버튼 드래그가 양축을 함께 이동** · **스크롤바 손잡이 드래그가 축별로 이동** · **건반 클릭 = 건반 점등(amber 795px) + seek 0회** · **`CLIP` ON = `REQUEST_LOOP_RANGE{30,50}` 1건, OFF = `restore` 1건** · **▶ = `SEEK:30` → `PLAY_PAUSE`** · **■ = `STOP` → `SEEK:30`** · **패널 접기 시 롤 884→1184px** · 콘솔 오류 0건.

> ⚠️ 하네스의 스튜디오 측은 채널 계약만 흉내 낸 스탠드인이다. 따라서 **app.jsx의 `REQUEST_LOOP_RANGE` 처리(실제 반복 재생·구간 복구·녹음 중 거부)와 프리뷰 음의 실제 출력 장치**는 **사용자 시험(T-2.3.0-2)** 에서 확인한다. 특히 프리뷰 음은 네이티브 엔진이 장치를 배타 모드로 잡고 있을 때 어떻게 되는지 실기기 확인이 필요하다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.2.1 → 2.3.0**(Minor +1 — 구조 변경 + 기능 추가).

---

## v2.2.1 — Pitch Editor 시험 보완 3건 + 곡선 색 선점 (2026-08-28)

> 사용자 시험 피드백(T-2.2.0-2 · T-2.0.2-1, 2026-08-28)의 보완 요청과 문의. **버전 판정**: 라벨·색·키 배선 — 기능 추가나 구조 변경이 아니므로 **Patch +1, v2.2.0 → v2.2.1**. **renderer 재빌드만 · 네이티브 재빌드 불필요.**
>
> 같은 피드백에 들어 있던 **UI 재구성 5건은 v2.3.0으로 분리**했다(설계 [피치-에디터-설계.md](피치-에디터-설계.md) §11). 이번 버전은 "지금 바로 불편한 것"만 걷어낸다.

### ① 줌 라벨 `PITCH` → `KEY`

사용자 지적: 창 전체의 주제가 pitch인데 세로 줌 그룹까지 `PITCH`라고 적혀 있어 **"오디오의 음정을 바꾸는 버튼"으로 읽힌다.** 이 그룹이 조정하는 것은 건반 축이다. 참조 디자인도 두 축을 `TIME` / `KEY`로 표기한다(handoff README §Group D) → **`KEY`로 확정**(사용자 승인). 툴팁도 "Key zoom — vertical"로 맞췄다.

### ② 건반 음이름 대비 🔴 — 원인은 글자가 아니라 **건반을 텍스트 색으로 칠한 것**

증상은 "글씨가 흐리다"였지만, 실제로는 세 가지가 겹쳐 있었다.

- ⓐ **건반 면을 `--cream` / `--bg`로 칠하고 있었다.** 이 둘은 **텍스트 색**이다. 어두운 테마에서는 우연히 그럴듯했지만, **밝은 테마(ivory·sage)에서 `--cream`은 거의 검정**(ivory `#3b3327`)이라 **"흰 건반"이 새까맣게** 칠해졌다. 실측에서도 v2.2.0의 ivory 건반 면이 `rgb(59,51,39)`, sage가 `rgb(25,28,26)`으로 나온다.
- ⓑ 음이름을 **`--faint`** — 팔레트에서 **가장 흐린 색** — 으로 그렸다.
- ⓒ **8px · weight 400 고정.** 이 크기의 가는 획은 사실상 전부 안티에일리어싱이라 **명목 색에 도달하지 못한다**: v2.2.0 기본 테마의 가장 진한 글자 픽셀이 `rgb(111,102,89)`로, 지정한 `--faint`(`#5f574a`)보다 눈에 띄게 옅다.

수정 = 참조 디자인 §3c를 따라 **건반 전용 토큰 4종**(`--key-white` / `--key-white-2` / `--key-black` / `--key-ink`)을 신설하고, 흰 건반은 가로 그라디언트 면 + 폭 전체, 검은 건반은 62% 폭, 음이름은 **어두운 잉크 · weight 700 · 행 높이에 따라 7~9px · 건반 오른쪽 끝에 우측 정렬**. 토큰은 테마 블록 **바깥**에 따로 뒀다 — 위쪽 테마 블록들은 vocal-strip.html과 **글자 그대로 복사 동기화**되는 영역이고, 이 4종은 이 창에만 있기 때문이다. 피아노 건반은 어느 테마에서도 피아노 건반이므로 색상은 재해석하지 않고 테마에 맞춰 **틴트만** 했다.

### ③ 창 안에서 Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y

사용자 문의: "왜 스튜디오 화면을 선택한 다음 undo를 해야 하나?" → **설계 의도가 아니라 단순 미배선이었다.** 다른 위성 창은 전부 넘기고 있다([vocal-strip-app.jsx](vocal-strip-app.jsx#L495) · [advanced-eq-app.jsx](advanced-eq-app.jsx#L323) · [mixer-app.jsx](mixer-app.jsx#L201) → [app.jsx](app.jsx#L2536) `REQUEST_UNDO`). 이 창의 키 핸들러에는 Escape와 Space뿐이었는데, Stage A·B의 피치 에디터는 **자기 편집이 하나도 없는 읽기 전용 창**이라 아직 아무도 요구하지 않았을 뿐이다.

**사용자 결정 — Stage D부터는 창 로컬 스택을 갖는다**(설계 §11-2에 기록): 피치 조정을 취소하려고 스튜디오로 창을 옮기는 것은 혼동이자 작업성 저하다. 구조는 **로컬 스택이 앞단**이고, 비면 스튜디오로 넘긴다.

### ④ 검출 피치 곡선 amber → red (Stage C 대비)

Stage C가 **amber 노트 블록**을 이 곡선 위에 얹는다. 곡선이 같은 색이면 둘이 동시에 켜지는 순간 읽을 수 없다 — 참조 디자인도 **노트 amber / 검출 곡선 red**로 갈라 놓았다. 나중에 바꾸면 Stage C 시험이 색 문제와 세그멘테이션 문제를 동시에 안게 되므로 **지금 선점**했다. 플레이헤드는 amber 그대로 뒀다(스튜디오 눈금자와 같은 색 — 이번 스코프 밖).

### 검증 — Electron 실구동 계측

건반 대비는 **창을 실제로 띄워 캔버스 픽셀을 읽어** 측정했다(글자 획의 가장 진한 픽셀 대 건반 면, WCAG 대비비). 테마 7종:

| 테마 | v2.2.0 | v2.2.1 |
|---|---|---|
| default | 4.55:1 | **6.88:1** |
| ivory | 5.92:1 *(건반 면 `rgb(59,51,39)` — 흰 건반이 검정)* | **8.96:1** *(면 `rgb(231,223,202)`)* |
| blue | 4.37:1 | **10.76:1** |
| slate | 5.48:1 | **11.30:1** |
| sage | 9.08:1 *(면 `rgb(25,28,26)` — 흰 건반이 검정)* | **14.60:1** |
| navy | 5.67:1 | **12.99:1** |
| lime | 5.11:1 | **11.76:1** |

숫자보다 중요한 것은 **ivory·sage에서 건반 흑백이 뒤집혀 있었다**는 사실이다(대비비만 보면 통과처럼 보인다). 또 v2.2.1의 가장 진한 글자 픽셀이 **지정한 잉크 색과 정확히 일치**한다(default `rgb(74,64,51)` = `--key-ink` `#4a4033`) — 획이 굵어져 안티에일리어싱에 먹히지 않는다는 뜻이다.

스모크: 줌 라벨 `["TIME","KEY"]` · Ctrl+Z→`REQUEST_UNDO` · Ctrl+Shift+Z→`REQUEST_REDO` · Ctrl+Y→`REQUEST_REDO` · 합성 분석 주입 후 롤에서 **`--red` 픽셀 1600개 / `--amber` 픽셀 0개** · 콘솔 오류 0건.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.2.0 → 2.2.1**(Patch +1).

---

## v2.2.0 — Pitch Editor 트랜스포트 · 플레이헤드 · 줌 (2026-08-26)

> 사용자 문의(2026-08-26): "pitch editor 쪽에도 play/stop, playbar 표시, time zoom in/out, vertical in/out을 구현하는 데 어려움이 있을까요?" **버전 판정**: 기능 추가 → **Minor +1, v2.1.0 → v2.2.0**. **renderer 재빌드만 · 네이티브 재빌드 불필요.**
>
> **Stage B 시험(T-2.0.2-1) 전에 넣었다.** 곡선이 부른 음정과 맞는지 판단하려면 **들으면서 플레이바를 봐야** 하고, Stage C·D의 노트 편집은 줌 없이는 시험 자체가 성립하지 않는다.

### 조사 — 필요한 조각이 이미 다 있었다

| 요청 | 기존 상태 |
|---|---|
| Play / Stop | `transportRef`에 `transportPlayPause`·`transportStop`이 있고 공용 채널에 `REQUEST_PLAY_PAUSE`가 이미 연결됨(보컬 스트립이 사용 중) |
| 플레이헤드 | 이미 `LEVEL_METERS`로 방송되지만 **믹서/Advanced 창이 열려 있을 때만**(`needsLevels` 게이팅) |
| seek | `guardedUserSeek`가 이미 있음(녹음 중 seek 차단 포함) |
| 줌 | 창 내부 상태 — 엔진 무관 |

### 구현

**① 채널에 트랜스포트 4종 추가** ([app.jsx](app.jsx))
- `REQUEST_STOP` · `REQUEST_SEEK` · `REQUEST_TRANSPORT`(폴링 응답 `TRANSPORT_STATE`).
- **플레이헤드는 방송이 아니라 폴링(~30fps)으로 받는다.** 스튜디오는 이 창이 열렸는지 모르고, 매 프레임 도는 `LEVEL_METERS` 피드는 믹서/Advanced 창 가시성에 묶여 있다. 숫자 두 개를 주고받는 편이 프레임 루프에 창 하나를 더 가르치는 것보다 싸다 — 보컬 스트립이 GR 미터에서 내린 것과 같은 결론.
- **seek는 `guardedUserSeek` 클로저가 아니라 `transportRef`를 통해 부른다** 🔴 이 effect는 매 렌더 다시 돌지 않으므로 클로저를 잡으면 **stale 참조**가 된다(메뉴 핸들러가 이미 겪은 함정). 같은 이유로 다른 트랜스포트 호출도 전부 ref를 거치고 있었다. 그리고 `DAW.seek`가 아니라 `guardedUserSeek`인 이유는 **녹음 중 플레이헤드 이동을 거부하는 경로**이기 때문 — 별창이 메인 타임라인보다 날것의 seek를 가져서는 안 된다.

**② 창** ([pitch-editor-app.jsx](pitch-editor-app.jsx))
- 툴바 왼쪽에 **▶/⏸ · ■ · 시간 표시**. 이 창은 들으면서 쓰는 곳이라 손이 이미 가 있는 자리에 둔다. **Space**로도 재생/정지(누르기 전에 포커스를 떼어, 포커스가 걸린 툴바 버튼이 같이 눌리는 것을 막는다).
- **플레이헤드는 캔버스가 아니라 DOM 오버레이.** 초당 30번 움직이는 선 하나 때문에 격자·파형·곡선을 다시 그리는 것은 순수한 낭비다.
- **클릭 seek**: 롤을 클릭하면 그 시각으로 이동. **건반 거터(왼쪽 54px)는 seek 면이 아니다.**
- **시간 줌**: 버튼(−/맞춤/+) + **Ctrl+휠**. 휠 줌은 **커서 위치를 기준**으로 한다 — 왼쪽 끝 기준이면 확대할 때마다 보던 곳이 달아나서, 이 차이가 "쓸 만함"과 "화면 쫓아다니기"를 가른다.
- **세로(음정) 줌**: 버튼 + **Alt/Shift+휠**. 맞춤 버튼은 분석 결과가 있으면 **실제로 부른 음역에 맞춘다**(위아래 4반음 여유).
- 자잘하지만 필요한 것들: 눈금 간격이 줌을 따라간다(고정 1초 격자는 축소하면 벽, 확대하면 텅 빈다) · 파형 피크를 4000버킷(엔진 상한)으로 요청해 확대해도 뭉개지지 않게 · 재생 중 플레이헤드가 화면을 벗어나면 따라가되 **전체 보기일 때는 따라가지 않는다**(따라갈 것이 없고 사용자와 싸우게 된다).

**③ 발견한 버그 — 클립 밖에서 시간 표시가 깨짐** 🔴
시험 중에 드러났다. 표시는 **클립 상대 시간**인데, 트랜스포트가 클립보다 **앞**에 있으면 음수가 되고 ui-kit의 `fmtTime`은 음수를 다루지 않아 **`-1:-5.-15`** 같은 문자열이 나왔다. 수정 = 클립 밖이면 표시할 클립 위치가 없으므로 **`--:--`**, 그리고 **플레이헤드 선도 그리지 않는다**(둘이 같은 조건이라 서로 어긋날 수 없다).

### 검증 — Electron 2창 실구동, 16개 단언 전부 통과

에디터의 Play/Stop이 스튜디오 트랜스포트를 움직임 · 버튼이 Pause로 바뀜 · **클립 밖에서 표시 `--:--` + 선 없음** · 클립 안으로 seek하면 **클립 시간 표시 + 선 표시** · **롤 클릭 seek가 클립시간→곡시간으로 정확히 변환**(클립 5 s + 30 s = 곡 35.00 s) · 건반 거터 클릭은 seek 안 함 · TIME + 가 다시 그림 · **맞춤이 원래 화면과 픽셀 단위로 동일** · PITCH + 가 다시 그림 · 새 뷰 코드에서도 분석 정상 완료.

> ⚠️ 시험 하네스의 스튜디오 측은 **트랜스포트 계약만 흉내 낸 스탠드인**이다(오디오 장치를 열지 않고 결정론적으로 구동하기 위함). app.jsx의 네 케이스는 `transportRef`로 넘기는 배선이라 실제 재생 동작은 **사용자 시험(T-2.2.0-1)** 에서 확인한다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.1.0 → 2.2.0**(Minor +1 — 기능 추가).

---

## v2.1.0 — 클립을 다른 트랙으로 복사 (Copy to track) (2026-08-26)

> 사용자 문의(2026-08-26): "Audio Input 트랙은 다른 트랙에서 클립을 copy 해 올 수 없는데, bounce 클립을 Audio Input 트랙으로 복사해오는 기능 추가가 어려울까요?" **버전 판정**: 기능 추가 → **Minor +1, v2.0.2 → v2.1.0**. **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### 조사 — 엔진은 이미 하고 있었다

착수 전에 실제로 돌려 확인했다. [audio-engine.js](audio-engine.js) `pasteClip`은 클립보드의 **소스 메타데이터와 raw 버퍼를 대상 트랙에 심어 주므로**, bounce → Audio In 붙여넣기가 **이미 완전히 동작**한다(대상 트랙이 오디오를 해석하는 것까지 확인). UI에서도 bounce 트랙은 Audio In과 동일한 클립 편집 props를 받는다([app.jsx](app.jsx)).

**막고 있던 것은 "어느 트랙에 붙일지 지목할 방법"뿐이었다.** [app.jsx](app.jsx) `pasteTargetTrackId()`는 ① 선택된 클립의 트랙 → ② 마지막으로 만진 클립 트랙 → ③ 편집 가능한 트랙이 **정확히 하나뿐일 때만** 그 트랙, 순으로 대상을 고른다. 그런데 bounce 클립을 복사하면 ①②가 모두 **원래의 bounce 트랙**을 가리키므로 **Ctrl+V가 제자리에 다시 붙는다.** Audio In 트랙을 대상으로 만들려면 그 트랙의 클립을 먼저 클릭해야 하는데, **빈 Audio In 트랙에는 클릭할 클립이 없다** → 키보드로는 지목 불가. 유일하게 뚫려 있던 길은 대상 트랙의 빈 레인 우클릭 → `Paste at playhead`였다.

### 구현 — 우클릭 "Copy to track" (사용자 결정)

- **엔진** [audio-engine.js](audio-engine.js) `copyClipToTrack(trackId, clipId, destTrackId, atStart)` + `copyToTrackTargets(trackId)`.
  - 소스 반출 배관은 `pasteClip`과 같지만 **`_clipboard`를 건드리지 않는다** — 이 메뉴를 쓴다고 사용자의 Ctrl+C 내용이 날아가면 안 된다.
  - **위치는 원래 타임라인 위치 그대로.** 이 명령의 목적이 "바운스를 그 바운스가 나온 보컬 자리에 맞춰 얹는 것"이기 때문이다. 대상이 이미 차 있으면 기존 `_clampStartNoOverlap`이 오른쪽으로 밀어 겹침 불변식을 지킨다.
  - **떨어뜨리는 필드 3종**: ⓐ `takeId` — 대상 트랙의 테이크 목록은 그 id를 모르므로, 들고 가면 **존재하지 않는 레인에 속해 영원히 선택할 수 없는 클립**이 된다. ⓑ `recordedStart` / `recordedOffsetMs` — 이 사본은 녹음된 적이 없으므로 "Recording Offset Cal."이 이것으로 보정을 제안하면 안 된다. ⓒ `pitch` — 오디오는 따라간다(보정된 클립이면 `sourceId`가 이미 프린트본을 가리킨다) 그러나 노트 목록은 그것을 측정한 클립의 것이다(설계 §10-2의 split/Merge 규칙과 동일).
  - 대상 목록은 **클립 편집이 가능한 트랙**(audioIn/bounce)에서 자기 자신을 뺀 것. 스템(file)은 대상이 될 수 없다 — 다만 **출발지로는 가능**하다(읽기 전용인 것은 목적지로서지 출발지로서가 아니다).
- **UI** [ui-tracks.jsx](ui-tracks.jsx): 클립 우클릭 메뉴에 `COPY TO TRACK` 제목 행 + 대상 트랙 목록(이름 + `AUDIO IN`/`BOUNCE` 배지). 서브메뉴 기구를 새로 만들지 않고 **평평한 목록**으로 둔 이유는, 한 프로젝트의 클립 편집 가능 트랙이 몇 개 되지 않기 때문이다. 메뉴 컴포넌트에는 클릭 불가한 `heading` 항목 타입만 추가했다.
- **[app.jsx](app.jsx)** `handleCopyClipToTrack`: `pushUndo` → 실패 시 `cancelUndo`(v1.23.1 규칙), 성공하면 **붙은 자리에서 새 클립을 선택**해 어디로 갔는지 눈에 보이게 한다.

### 검증 — 엔진 단언 25건 전부 통과 (Electron 렌더러, 실제 AudioBuffer)

대상 목록(자기 트랙·스템 제외, 이름/kind 포함) · 새 클립 id 반환 · 대상 트랙 +1 · **출발 트랙 무변경** · **사용자 클립보드 보존** · 같은 타임라인 위치 유지 · 소스 메타/raw 오디오 반출 · takeId·recordedStart·recordedOffsetMs·pitch 제거 · **자리가 차 있으면 밀림, 겹침 0** · 스템으로 복사 거부 · 같은 트랙 거부 · 없는 클립/대상 거부 · **스템에서 Audio In으로 복사는 허용**.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.0.2 → 2.1.0**(Minor +1 — 기능 추가).

---

## v2.0.2 — Pitch Editor Stage B: 피치 트래킹 (2026-08-26)

> 설계 [피치-에디터-설계.md](피치-에디터-설계.md) §5-2 Stage B. **renderer 재빌드만 · 네이티브 재빌드 불필요.** Stage A와 마찬가지로 **오디오를 건드리지 않는다** — 검출해서 보여주기만 한다(노트 분할은 Stage C, 편집은 D, 렌더는 E).

### ① 검출기 — YIN, 12 kHz로 데시메이션해서

[audio-engine.js](audio-engine.js) `analyzeClipPitch` / `analyzeClipPitchAsync`.

- **왜 데시메이션인가**: 보컬 기본주파수는 위로 1.1 kHz(C6)를 넘지 않는다. 48 kHz 원본을 그대로 쓸 이유가 없고, YIN의 비용인 차분함수는 **창 길이와 지연 범위 양쪽에 비례**하므로 4배 데시메이션은 비용을 **약 16배** 줄인다. 33탭 윈도우드 sinc로 앤티앨리어싱한 뒤 4배 솎는다 — 이걸 빼면 6 kHz 위 에너지가 접혀 내려와 **차분함수가 읽는 바로 그 대역을 오염**시킨다.
- **시간 도메인 차분(FFT 자기상관 아님)**: 여기서 지연 범위는 11~184 샘플뿐이라 직접 합이 프레임당 ~9만 곱셈-덧셈이다. 교과서적 "fast YIN"이 쓰는 **2048점 FFT 3회보다 오히려 싸다.**
- CMND(누적 평균 정규화) → 임계 0.15 아래 첫 골 → **포물선 보간**. 보간이 없으면 f0가 지연 격자에 양자화되는데, 12 kHz에서 C6 부근은 격자 간격이 이미 1.5반음쯤 된다.
- **후처리**: 중앙값 필터를 **반음(로그) 도메인**에서 반경 2(≈53 ms)로 적용. 옥타브 오류는 로그 도메인에서 정확히 ±12라 중앙값이 깨끗이 걸러낸다. Hz로 하면 큰 저음이 평균을 끌고 다닌다.
- **무음 게이트**: 프레임 RMS가 피크의 2% 아래면 차분함수를 **아예 돌리지 않는다.** 보컬 테이크는 대부분 노래하고 있지 않으므로, 이게 여기서 얻을 수 있는 가장 큰 속도 이득이다(실측 5분 테이크에서 32%가 게이트에 걸려 계산 생략).

### ② 실측과 판정 — Web Worker는 쓰지 않는다 (설계 §5-1의 미결 해소)

설계는 "5분 보컬 실측 후 Worker 필요 여부 판정"으로 남겨 뒀다. 실측했다.

| 항목 | 값 |
|---|---|
| 5분 테이크(노래 2 s / 쉼 1 s) | **28,120 프레임 · 4.2 s** = 실시간의 **1.4%** |
| 무음 게이트 통과 | 68% (32%는 계산 생략) |
| 30초 클립 환산 | 약 0.4 s |

**판정: Worker 없이 청크 분할.** 4.2초는 "느려서" 문제가 아니라 **그동안 창이 얼어붙는 것**이 문제다. Worker는 번들을 하나 더 만들어 빌드·패키징·동기화해야 하고 데시메이션된 버퍼를 전송해야 하는데, **이벤트 루프에 양보하는 것만으로 같은 문제가 사라진다.** `analyzeClipPitchAsync`가 90 ms 슬라이스로 돌며 `onProgress`를 보고한다. 슬라이스 사이는 `setTimeout(0)` — 마이크로태스크로 양보하면 렌더링이 굶어서 슬라이스의 의미가 없다.

- 동기(`analyzeClipPitch`)와 비동기 경로는 **setup / step / finish** 를 공유해 알고리즘이 한 벌뿐이다. 시험에서 두 경로의 결과가 **부동소수점 단위로 동일**함을 확인했다.

### ③ 창 — Analyze 버튼과 피치 곡선

- [app.jsx](app.jsx) `REQUEST_PITCH_ANALYZE` → 슬라이스 실행 → `PITCH_ANALYZE_PROGRESS` / `PITCH_ANALYSIS`. **읽기 전용이라 `pushUndo`를 부르지 않는다** — 모델을 안 바꾸는데 스냅샷을 쌓으면 그 항목이 다음 Ctrl+Z를 먹는다(상시 노트 "Undo 스냅샷 정합성").
- 곡선은 **유성 구간마다 끊어서** 그린다. 숨소리 구간을 이어 버리면 없는 음정을 지어내는 셈이다. 불투명도는 그 구간의 평균 신뢰도 — 흔들린 검출은 흔들려 보인다.
- 곡선 좌표는 프레임의 **중앙 시각**(`k*hop + win/2`)에 놓는다. 왼쪽 끝에 놓으면 창 길이의 절반(21 ms)만큼 통째로 밀린다.
- **분석 결과는 저장하지 않는다.** 프레임당 f0를 프로젝트 파일에 넣으면 5분 클립 하나가 수만 개 숫자가 된다. 설계 §2대로 `clip.pitch.analysis`에는 **파라미터만** 두고 곡선은 그때그때 다시 계산한다.
- 스튜디오가 상태를 재방송하면(undo·redo 등) **곡선을 버린다** — 클립이 잘리거나 되돌려졌을 수 있고, 그러면 옛 오디오에서 잰 곡선이 새 파형 위에 얹혀 조용히 거짓말을 한다.

### 검증

- **합성 신호 정확도**(배음 6개를 가진 모음 유사음): A3·C4·E4·C5 네 음의 중앙값 오차 **최대 0.005 반음**. 무음 구간은 unvoiced.
- **옥타브 견고성**: 배음이 풍부한 A2(110 Hz)를 옥타브 위로 읽지 않음(오차 0.000).
- **비브라토 보존**: ±0.5반음 5.5 Hz 비브라토가 평활화 후에도 **0.83반음 진폭**으로 살아남음(음악적 내용이지 오류가 아니므로 지워지면 안 된다).
- **왕복 시험**(엔진을 가진 스튜디오 페이지 + 진짜 에디터 창, Electron 2창): 30개 음표 **전부 검출**, 오차 0.005반음, 쉼표 구간 unvoiced, 곡선이 실제로 캔버스에 그려짐(픽셀 해시 변화), 푸터가 유성 비율 보고.
- **응답성**: 1.1초짜리 분석이 도는 동안 스튜디오 페이지가 **13회 왕복 응답** — 얼지 않는다는 직접 증거.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.0.1 → 2.0.2**(Patch +1 — 보컬 스트립 스테이지들이 v1.44.x 안에서 올라간 것과 같은 방식).

---

## v2.0.1 — Pitch Editor 테마 전환 시 피아노롤이 옛 색을 유지하던 버그 (2026-08-26)

> 사용자 시험 결과: **T-2.0.0-2 통과**, **T-2.0.0-1은 ④ 테마 항목만 결함**(나머지 ①②③⑤⑥⑦ 정상) → [시험-아카이브.md](시험-아카이브.md). 이 버전은 그 한 건. **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### 창을 열어 둔 채 테마를 바꾸면 창 외곽만 바뀌고 피아노롤은 그대로였다 🔴

**보고** — Pitch Editor를 열어 둔 채 테마를 변경하면 **창 외곽(타이틀바·툴바·상태줄)은 즉시 바뀌는데 가운데 피아노롤은 옛 색 그대로**였다. 창을 닫았다 다시 열어야 반영됐다.

**원인 (두 가지가 겹침, 확정)**

1. **캔버스는 CSS 변수 변경으로 다시 칠해지지 않는다.** 창 외곽은 DOM 요소라 `data-theme`이 바뀌는 순간 브라우저가 알아서 재계산하지만, 피아노롤은 `<canvas>` — **이미 칠해진 픽셀**이다. 색은 그리는 시점에 `getComputedStyle`로 한 번 읽을 뿐이므로, **다시 그리라고 하지 않으면 영원히 그때 팔레트로 남는다.** 그리기 effect의 의존성이 `[size, info]`뿐이라 테마 변경은 재그리기를 유발하지 못했다. 창을 다시 열면 고쳐진 이유가 이것이다 — 그때는 새로 그리니까.
2. **고쳤어도 한 박자 늦게 그려질 뻔했다.** `data-theme` 속성을 부모(`PitchEditorApp`)의 effect에서 붙이고 있었는데, **React는 자식 effect를 부모 effect보다 먼저 실행**한다. 즉 의존성만 추가했다면 피아노롤이 **아직 옛 `data-theme`이 걸린 상태에서** 변수를 읽어, 한 단계 뒤처진 색으로 칠했을 것이다.

**수정**
- 그리기 effect 의존성에 **`theme` 추가**. 그 값은 그리기 코드 안에서 쓰이지 않지만 **CSS 변수가 의존하는 것이 바로 그 값**이므로, "안 쓰는 의존성"으로 보고 나중에 지우지 않도록 주석을 달았다.
- `data-theme` 속성을 **effect가 아니라 테마가 도착한 그 자리에서 동기적으로** 적용(`peApplyThemeAttr`). 이로써 자식이 언제 그리든 이미 올바른 팔레트를 읽는다. 기존 effect는 최초 마운트용 안전망으로만 남겼다.

**검증** — 회귀 시험을 자동화했다(Electron 실구동): 창을 열고 클립을 주입한 뒤 `THEME_CHANGED`를 브로드캐스트하고 **캔버스 픽셀 해시**를 비교한다. `ivory·blue·lime` 3종에서 ⓐ `data-theme` 반영 ⓑ 창 외곽 재도색 ⓒ **피아노롤 재도색**을 각각 확인하고, default로 되돌리면 **최초와 정확히 같은 해시**가 나오는 것까지 확인 — 11개 단언 통과. 수정 전 코드로 되돌려 같은 시험을 돌리면 **ⓐⓑ는 통과하고 ⓒ만 실패**(해시 동일)하여, 보고된 증상을 그대로 재현한다.

### 버전
- [package.json](package.json)·[version.js](version.js) **2.0.0 → 2.0.1**(Patch +1 — 버그 수정).

---

## v2.0.0 — Pitch Editor Stage A: 골격 (2026-08-25)

> 설계 [피치-에디터-설계.md](피치-에디터-설계.md) §8 Stage A. **renderer 재빌드만 · 네이티브 재빌드 불필요**(네이티브 소스 무변경). **버전 판정**: 사용자 요구에 따른 대형 신규 기능 → **Major +1, v1.46.3 → v2.0.0**. 보컬 채널 스트립이 Stage A에서 v1.44.0을 가져간 것과 같은 방식으로, 이후 Stage B~F는 2.0.x로 올라간다.
>
> **Stage A의 범위는 "오디오를 건드리지 않는 것"이다.** 창이 클립 하나를 열어 파형을 피아노롤 위에 그리고, 그 클립에 대해 아는 것을 보고한다. 분석(B)·노트(C·D)·렌더(E)는 각 스테이지에서 붙으며, 툴바 버튼은 **비활성 상태로 미리 놓여** 기능의 형태만 보여준다(반쯤 연결된 버튼을 사용자 앞에 두지 않는다).

### ① 진입점 — 클립 우클릭 메뉴 (사용자 결정)

[ui-tracks.jsx](ui-tracks.jsx) `clipMenuItems`에 **"Pitch Editor..."** 를 추가했다. 트랙 헤더가 아니라 클립 메뉴인 이유는 두 가지다. ⓐ 피치 편집 대상은 트랙이 아니라 **클립 하나의 오디오**다. ⓑ 244px 헤더에는 가로 여유가 없다 — 보컬 스트립의 FX 컨트롤이 텍스트 버튼에서 아이콘으로 줄어든 것이 바로 이 때문이다. 게이팅은 스트립과 동일하게 `kind === audioIn | bounce`.

### ② 별창 — `pitch-editor.html` + `pitch-editor-app.jsx`

- [electron/main.js](electron/main.js) `open-pitch-editor` IPC + BrowserWindow(1040×660, 최소 720×480). 보컬 스트립과 **같은 패턴**: hide-on-close, 위치·크기 기억, 앱 종료 시 강제 close.
- 상태는 기존 **`focusdaw-advanced-effects-sync` 채널**을 그대로 탄다 → 테마·프로젝트명·undo/redo 브로드캐스트를 새로 만들지 않고 재사용. 테마 전환도 `focusdaw-theme-sync`로 즉시 따라온다.
- 피아노롤은 **캔버스**로 그린다. Stage C는 음절마다 노트 블록을, Stage B는 5 ms마다 피치 점을 놓으므로 5분 보컬이면 DOM 요소가 수천 개가 된다. 색은 전부 CSS 변수에서 읽어 테마를 따라간다(하드코딩 액센트 색 금지 — v1.44.21).
- **Vari Key 배지**: 이조가 걸린 상태에서는 화면의 음이름과 들리는 소리가 어긋나므로, 하단에 "VARI KEY +2 — editing the original pitch"를 띄운다. 끄라고 강요하지 않는다(설계 §3).

### ③ 엔진 — `clipAudioInfo(trackId, clipId, buckets)`

창이 필요한 것(버킷별 min/max 피크 + 메타데이터)만 돌려준다. 샘플을 BroadcastChannel로 넘기지 않는다.

**`track.buffer`가 아니라 raw 소스 버퍼를 읽는다** — 이게 이 함수의 핵심이다. baked 버퍼는 `track._layoutBaked`가 참일 때 **타임라인 위치로 인덱싱**되며, 이 이원성은 이 코드베이스에서 가장 자주 재발한 버그다(상시 노트 "버퍼 인덱싱 이원성"). raw 소스에는 그런 모호함이 없다: `clip.sourceOffset`이 곧 이 클립이 소스의 어디서 시작하는지다. **피치 분석과 PSOLA 렌더가 같은 도메인을 읽어야 하므로 둘 다 여기를 통한다.** De-noise(`denoiseClip`)가 이미 같은 선택을 했다.

### ④ 저장 포맷 — `_normalizeClip`에 `pitch` 추가 🔴

`_normalizeClip`은 **화이트리스트**다. 여기 없는 필드는 로드·정규화 때마다 조용히 버려지므로, `clip.pitch`를 명시적으로 넣지 않으면 **편집 상태가 재열기에서 전부 사라진다.** 그래서 아직 아무도 쓰지 않는 단계인 Stage A에서 미리 넣었다. 구조는 설계 §2의 `{ baseSourceId, printedSourceId, notes[], analysis }`이며, **`baseSourceId`가 원본을 계속 가리키는 덕에 재편집이 보정본 위에 쌓이지 않는다**.

### 검증

- **렌더러 스모크**(Electron 실구동, 합성 클립 주입): 창 마운트 ✅ · 피아노롤 캔버스 1024×482 실제 렌더(픽셀 표본 75색) ✅ · 툴바 4개 버튼 전부 비활성 ✅ · Vari Key 배지 문구 ✅ · 시간 포맷 `0:12.50` ✅.
- **엔진 단언 15건 전부 통과**(Electron 렌더러에서 실제 AudioBuffer로): `_normalizeClip`이 `pitch`를 보존/미보유 시 null · `clipAudioInfo`의 duration·sampleRate·start·fileName·버킷 수 · **소스 구간 정확성**(offset 1 s 클립에서 2~3 s의 버스트가 클립 후반부에만 나타남) · 없는 트랙/클립은 null.
- **전역 스코프 충돌 검사**(상시 노트): `pitch-editor.html`은 ui-kit + 이 앱만 로드하므로 그 쌍을 대조 — 최초에 **`fmtTime` 충돌 1건 발견**. ui-kit 쪽이 `function` 선언이라 이쪽 `const`와 만나면 "already declared"로 **창 전체가 죽는다**. `peFmtTime`으로 바꾸고 ui-kit 구현을 재사용해 해소, 재검사 0건.
- 패키징: `pitch-editor.html`·`pitch-editor-app.jsx`를 [package.json](package.json) `build.files`에 등재했다 — v1.45.0에서 `vocal-strip.html`이 빠져 **설치본에서 스트립 창이 안 뜨던 버그**와 같은 함정.

### 버전
- [package.json](package.json)·[version.js](version.js) **1.46.3 → 2.0.0**.

---

## v1.46.3 — v1 계열 최종 릴리즈 (문구 정합 + 재빌드) (2026-08-25)

> **목적**: v1.46.2까지의 시험이 전부 통과하고 대기 항목이 없어([시험.md](시험.md)), v1 계열을 **배포 가능한 상태로 확정**하고 v2.0.0(Pitch Editor)으로 넘어가기 위한 마무리 릴리즈. **renderer 재빌드만 · 네이티브 재빌드 불필요**(네이티브 소스 무변경).

### ① 남아 있던 `NO AUDIO` 문구 정합 (마지막 1건)

트랙 헤더 소스 칩은 v1.44.20에서 `AUDIO`→**`SOURCE`**, `NO AUDIO`→**`NO SRC`** 로 바뀌었고, 인앱 메뉴얼([ui-help.jsx](ui-help.jsx))은 v1.45.0 개정에서 전부 `NO SRC`로 따라갔다. 그런데 **"오디오 파일을 찾을 수 없음" 모달**([app.jsx](app.jsx):4700)만 옛 라벨을 그대로 설명하고 있었다 — 정작 이 모달은 **트랙이 실제로 `NO SRC` 칩을 달고 있을 때 뜨는 창**이라, 화면과 안내가 어긋나는 유일한 자리였다.

- 수정: `Those tracks show <b>NO AUDIO</b> …` → `Those tracks show <b>NO SRC</b> …`
- 확인: 재빌드된 `build/app.js`에서 `NO AUDIO` **0건** / `NO SRC` 1건. [ui-help.jsx](ui-help.jsx)에 남은 `NO AUDIO` 1건은 **v1.43.x 릴리즈 노트 본문**(과거 증상 기록)이라 그대로 둔다.
- 이로써 앱개발.md "알려진 버그 / 한계"의 📝 **도움말 문구 미추종** 항목은 **해소**된다.

### ② 메뉴얼 재생성 · 재빌드

- `npm run build:renderers` + `npm run build:manual` 실행. 마지막 커밋([c4035c7](#)) 의 **Merge Tracks 트랙 다중 선택(Ctrl/Shift+클릭) 안내**가 v1.46.2 인스톨러(2026-08-19 10:35 빌드) 이후 커밋(11:00)이라 **설치본에 빠져 있었는데**, 이번 재빌드로 포함된다.
- 인스톨러 출력은 `dist/release-1.46.3/`으로 지정해 v1.45.0 때와 같은 **잠긴 `dist/win-unpacked.tmp` EBUSY를 회피**한다.

### 산출물 · 검증

- **`dist/release-1.46.3/FocusDAW-Studio-Setup-1.46.3.exe`** (NSIS, x64, 179 MB) + `.blockmap` + `latest.yml`. exe 메타데이터 `ProductVersion/FileVersion 1.46.3` 확인.
- asar 대조(930 항목):

| 확인 항목 | 결과 |
|---|---|
| `vocal-strip.html`(과거 누락 이력) | ✅ 포함 |
| `bin/FocusDAW-AudioEngine.exe` | ✅ 포함(asarUnpack) |
| 메뉴얼 그림 `screens-v2` / `live-screens` | ✅ **72장 / 50장** |
| `build/app.js`의 `NO AUDIO` | ✅ **0건**(`NO SRC` 1건) |
| `version.js` | ✅ `1.46.3` |
| Merge Tracks 다중 선택 표(c4035c7) | ✅ **국문·영문 모두 포함** |

- ⚠️ 검증 함정 하나: `build/*.js`는 esbuild가 **비ASCII를 `원` 형태(대문자 hex)로 이스케이프**한다. 번들에서 한글 문구를 확인할 때 원문으로 grep하면 **없는 것처럼 보인다** — 이스케이프 형태로 찾아야 한다.
- `manual/사용자 메뉴얼.html`(단독 HTML)은 `build.files`에 없어 **의도적으로 미패키징**이다. 인앱 메뉴얼은 `build/ui-help.js`가 그리므로 정상.

### 버전
- [package.json](package.json)·[version.js](version.js) **1.46.2 → 1.46.3**(Patch +1 — 문구 수정).

### 남은 것
- **설치본 스모크 테스트**([시험.md](시험.md) T-1.46.3-1) — 사용자 실행 필요(개발 앱을 닫고 설치).
- 통과 후 `feature/v2` → `main` 병합 + 태그. 현재 `main`은 **86 커밋 뒤처져 있다**(v1.44 이전).
- 잠긴 `dist/win-unpacked.tmp` 정리(v1.45.0 잔재).

---

## v1.46.2 — Flatten이 무관한 클립을 붙이던 버그 + UI 영문화·Merge 버튼 대비 (2026-08-18)

> 사용자 시험 결과: T-1.46.1-1·T-1.46.1-2·T-1.46.0-3·T-1.46.0-5·T-1.44.29-1 **통과**(→ [시험-아카이브.md](시험-아카이브.md)). T-1.46.0-4에서 **결함 1건**, 추가로 **UI 요청 2건**. **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### ① Flatten Comp가 punch와 무관한 이웃 클립까지 하나로 합치던 버그 🔴

**재현(사용자 보고)** — punch take와 아무 상관 없는 **4·5번째 클립이 서로 맞닿아 있었는데**, Flatten Comp를 실행하니 **두 클립이 하나로 합쳐졌다**(그 외 기능은 정상).

**원인 (확정)** — `canFlattenComp`가 "**연속된 클립은 한 run**"이라는 규칙으로 **렌더 대상 전체**를 훑어 run을 만들고, run 하나를 **새 오디오 파일 하나로 렌더**했다. 이 규칙은 comp 조각(같은 구간을 나눠 가진 take 조각들)을 이어 붙이려고 만든 것인데, **take와 무관한 일반 클립(takeId=null)** 도 옆에 붙어 있으면 같은 run에 들어갔다. 사용자의 4·5번 클립은 복사/이동으로 정확히 맞닿아 있어(1:04.781) 한 run이 됐고, flatten이 둘을 한 파일로 합쳐 버렸다.

**수정** — flatten이 **커밋해야 할 take 재료만** 건드리도록 범위를 좁혔다.
- `canFlattenComp`가 `passthrough`(takeId=null인 일반 클립)와 `runs`(take 클립)를 **분리 반환**. 일반 클립은 **그대로 통과**(위치·소스·id 유지, 재렌더 없음).
- run은 **같은 Take Group 안에서만** 이어진다 — 서로 다른 클립의 take가 맞닿아 있어도 융합되지 않는다.
- `flattenComp`는 passthrough를 먼저 담고 run만 확정한다.

**검증** — 보고 배치를 그대로 재현(일반 클립 5개 중 4·5번이 맞닿음 + punch 그룹 2개): flatten 후 **클립 수 7 → 7 유지**, 4번(54.641–64.781)·5번(64.781–74.920)이 **각각 독립 유지**, 두 구간을 걸치는 클립 없음, punch 구간 2곳은 일반 클립으로 확정, **재렌더 0회**(단일 take run은 비트 동일). 회귀로 **진짜 comp(한 그룹 안에서 두 take를 swipe로 나눈 경우)는 여전히 한 클립으로 합쳐지고 새 소스로 렌더**되는 것도 확인.

### ② UI 한글 잔여 문구 영문화 (요청 1)

- **Merge Tracks 창**의 안내문(바운스는 원본 BPM·Key로 렌더링) → 영문.
- 앱 전체를 훑어 나머지도 정리: **Vari BPM / Vari Key 스위치 툴팁**, Key 패널의 **원곡 Key 목록 툴팁**, 트랙 헤더의 **FX 배지 툴팁**, **Automation 초기화 / 트랙 삭제 확인창**(제목·본문·버튼), **Advanced Ambience**의 룸 프리셋 설명 5종·"효과 없음(Dry)"·"사용자 설정"·그래프 캡션.
- 유일하게 남긴 한글은 **데모 곡/폴더 이름(`그 여름의 우리 - focustone`)** — 실제 파일 경로이자 곡 제목이라 번역 대상이 아니다.
- 검증: 실구동에서 빈 세션 · 편집 화면 · Project/Edit/Advanced Effects/Settings/Help 메뉴 · BPM/Key 패널 · Merge 대화창까지 열어 **표시 텍스트와 tooltip을 전수 스캔 → 한글 0건**(곡 제목 제외).

### ③ FILE TRACKS의 `MERGE TRACKS...` 버튼 대비 반전 (요청 2)

**원인 (확정)** — 기존 배색은 활성 = `--amber-soft`(액센트 17% 반투명) 배경 + `--amber` 글자, 비활성 = 흰색 3.5% 배경 + `--dim` 글자였다. 헤더 띠가 `surface2` 기반이라 **액센트와 surface2가 가까운 테마에서 뒤집힌다**: Ocean은 활성 **1.43:1** / 비활성 **2.10:1**, Modern Blue는 2.06 / 2.84 → **활성이 비활성보다 흐리게** 보였다(사용자 지적과 일치).

**수정** — 활성은 **액센트로 꽉 채운 칩**(`--amber` 배경 + `--accent-fg` 글자 + 은은한 글로우), 비활성은 **배경에 녹아드는 외곽선만**(투명 배경 + `color-mix(--muted 70%, --cream 30%)` 글자 + 옅은 테두리).
- 결과: 활성 라벨 대비 **Ocean 7.08:1 · Modern Blue 5.80:1**(실측 픽셀 기준), 10종 테마 전부 4.7:1 이상. 비활성은 배경과 1.4~3.3:1로 조용히 가라앉는다.
- 함정 하나: `background` **단축 속성**에 `transition`을 걸고 `transparent ↔ var(--amber)`를 오가게 했더니 Chromium이 **보간 값(완전 투명)에 고착**돼 인라인 스타일은 `var(--amber)`인데 화면은 칠해지지 않았다. **longhand(`backgroundColor`) + transition 제거**로 해결(계산값 `rgb(24,183,190)` 확인).

### 버전
- [package.json](package.json)·[version.js](version.js) **1.46.1 → 1.46.2**(Patch +1).

---

## v1.46.1 — T-1.46.0-3 결함 수정(두 번째 punch가 앞 클립의 take를 빼앗음) + 안내창 영문화 (2026-08-18)

> 사용자 시험 결과: T-1.44.27-1·T-1.44.28-1·T-1.46.0-2·T-1.46.0-6 **통과**(→ [시험-아카이브.md](시험-아카이브.md)), T-1.46.0-1은 **안내창 영문화 요청**, T-1.46.0-3에서 **결함 발견**. 이 버전은 그 두 가지. **renderer 재빌드만 · 네이티브 재빌드 불필요.**

### ① 두 번째 punch 녹음이 앞 클립의 take를 메인 트랙으로 떨어뜨리던 버그 🔴

**재현(사용자 보고)** — Audio In 트랙에 클립 4개(녹음 1개 + 복사/이동) → 44초 부근 punch 녹음으로 take 6개(A~F) → 1분 16초 부근 두 번째 punch 녹음으로 take 3개(A~C). 결과: **첫 그룹의 Take E가 레인에서 사라지고 그 오디오가 메인 트랙 클립으로 붙어** 있으며, 클립이 6개가 아니라 **7개**.

**원인 (두 가지가 겹침, 확정)**
1. **`_sliceActiveAtRegion`이 두 종류의 클립을 한 배열(`outside`)에 담고 있었다** — ⓐ punch 구간이 **자른 클립의 앞/뒤 조각**과 ⓑ **구간과 무관하게 그냥 바깥에 있던 클립**. `attachLoopPunchRecording`은 "구간 밖은 모든 패스가 공유하는 base"라는 규칙으로 **이 배열 전체를 `takeId=null`로 detach**한다. 트랙 단위 테이크 시절에는 맞는 규칙이었지만, 클립별 그룹에서는 **다른 클립(첫 punch 그룹)의 선택된 테이크까지 detach**해 버렸다 → 그 테이크는 클립을 잃고 하이진에서 삭제 → 오디오는 base 클립으로 메인 레인에 남음(= 사라진 Take E, 그리고 +1 클립).
2. **`_groupSelection`의 fallback이 "그룹의 마지막 테이크"** 였다. `activeTakeId`는 트랙에 하나뿐이라 두 번째 punch가 그것을 가져가면 첫 그룹은 fallback으로 떨어지는데, 마지막 테이크는 보통 **꼬리 partial 테이크**다 → 스크린샷의 `Take E (partial) active`처럼 **선택이 제멋대로 partial로 바뀜**.

**수정**
- `_sliceActiveAtRegion`이 **`outside`(구간이 건드리지 않은 라이브 클립)와 `remnants`(잘린 조각)를 분리 반환**. loop-punch는 **`remnants`만 base로 detach**하고 `outside`는 takeId를 유지 → 다른 클립의 테이크는 손대지 않는다. 단일 punch는 둘 다 원래대로 takeId 유지.
- `_groupSelection` fallback을 **"마지막 완전(non-partial) 테이크"** 로 변경 — 녹음 자체가 고르는 규칙(`lastFull || partial`)과 같아졌다.
- `_applyPinnedSelections(track, pinned, newTakeId)` 신설: 두 punch 경로가 **작업 전 `_effectiveComp`(= 지금 각 클립이 무엇을 재생 중인지)를 캡처**해 두었다가, 녹음 후 그것을 comp에 **고정(pin)** 하고 새 테이크가 자기 그룹만 차지하게 한다. 이로써 "단일 그룹일 때 사용자가 명시적으로 고른 테이크(=activeTakeId에만 있던 선택)"가 다른 자리에 punch를 해도 유지된다.

**검증** — 사용자 보고 시나리오를 그대로 시뮬레이션(4클립 → punch 6패스 → punch 3패스): 첫 그룹 **6개 테이크 전원 유지**, base 클립 **4개 그대로**(누출 0), 보이는 클립 **5 → 6**(예상대로 +1), 첫 그룹 선택이 **Take E 유지**(partial로 안 바뀜), 두 그룹의 선택 공존. 추가로 "단일 그룹에서 Take B를 고른 뒤 다른 자리에 punch" → **Take B 유지 + 새 클립은 새로 녹음한 테이크 재생**. Electron 실구동에서도 동일 결과(그룹 [6,3] · base 4 · 클립 5→6 · `g0:Take E` / `g1:Take B`). 기존 시험 2종(테이크 그룹 23단언 / Merge 8단언) 무회귀.

### ② BPM 안내창 영문화 (T-1.46.0-1 요청)
- `toggleVariBpm`의 안내 모달을 **영문**으로 교체("Project BPM required" + Detect / TAP / Type it in). 흐려진 스위치의 툴팁도 영문으로. 실구동에서 한글 0건 확인.

### 버전
- [package.json](package.json)·[version.js](version.js) **1.46.0 → 1.46.1**(Patch +1 — 버그 수정 + 문구 변경).

---

## v1.46.0 — 수정요청 6건 (2026-08-18) · ✅ **구현 완료 · 사용자 시험 대기**

> 출처: [수정요청.md](수정요청.md) v1.45.0 항목 1~6. **버전 판정**: 3번(테이크 모델을 트랙 단위 → 클립(구간) 단위로 재편)이 **큰 구조 변경**이므로 Minor +1 → **v1.45.0 → v1.46.0**([package.json](package.json)·[version.js](version.js) 동시 갱신).
> **사용자 승인**: 2026-08-18, 6건 전부 진행 + 3번은 "새 모델 일괄 적용"(기존 프로젝트도 열자마자 클립별 선택).
> **재빌드: renderer(`npm run build:renderers`) + `npm run build:manual`.** **네이티브 재빌드 불필요** — 네이티브 소스는 한 줄도 고치지 않았고, 브리지가 보내는 값만 바로잡았다(근거는 1번 항목).
> **검증**: 엔진 로직 시뮬레이션 2건(테이크 그룹 21개 단언 / Merge 옵션 8개 단언) 전건 통과 + Electron 실구동(Vari BPM 모달·Ocean 대비·클립별 테이크 레인 스크린샷) 확인. 상세는 각 항목 끝의 "검증".

### 1. BPM 미측정 상태(`---`)에서 Vari BPM을 켜면 재생 속도가 변하는 문제

**원인 규명 (확정).** 웹 엔진은 무죄다 — [audio-engine.js](audio-engine.js) `_projectRate()`는 `projectBpm`이 없으면 **항상 1.0**을 돌려준다(:1945). 속도가 변하는 것은 **네이티브 엔진**이며, 원인은 "측정 안 됨"이라는 상태가 네이티브로 전달되지 않는 데 있다.

| 지점 | 내용 |
|---|---|
| [AudioEngine.h](juce_skeleton/Source/AudioEngine.h):2110 | 네이티브의 기본값은 `projectBpm = 120.0`, `playbackBpm = 120.0`. **"미측정"을 표현할 값이 없다.** |
| [audio-bridge.js](audio-bridge.js):1310 | `syncTempoKeyToNative()`가 `if (t.projectBpm) …` / `if (t.playbackBpm) …` — **null이면 아예 보내지 않는다.** |
| [audio-bridge.js](audio-bridge.js):933 `clearTracks()` | New Project는 master만 재푸시하고 **tempo/key는 재푸시하지 않는다.** |

→ 시나리오: A 프로젝트에서 BPM 100 측정 후 재생 BPM 128로 조정 → 네이티브는 `100/128`을 기억 → **New Project 또는 BPM 없는 프로젝트 열기** → 표시기는 `---`인데 네이티브에는 옛 값이 남아 있음 → Vari BPM ON → `updateDspParams()`([AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp):1441)가 `variBpm && projectBpm > 0 && playbackBpm > 0`을 통과해 **1.28배로 재생**. 느려지는 경우는 그 반대 조합.
**같은 구멍이 Key에도 있다** — `keyShift`/`variKey`도 clearTracks에서 재푸시되지 않으므로 이전 프로젝트의 이조가 남는다. 아래 (a)가 두 경우를 함께 막는다.

**(a) 근본 수정 — 네이티브 상태 동기화** ([audio-bridge.js](audio-bridge.js), 네이티브 무변경)
- `syncTempoKeyToNative()`가 **항상 명시값을 전송**하도록 조건부 push 제거: `setProjectBpm(t.projectBpm || 120)`, `setPlaybackBpm(t.playbackBpm || t.projectBpm || 120)` → 미측정 프로젝트는 비율 1.0으로 확정. 중립값을 `0`이 아니라 `120`으로 둔 이유는 비-JUCE 경로가 `playbackBpm / projectBpm`을 그대로 나누기 때문(0 나눗셈 방지). 상수 `NEUTRAL_BPM` + 헬퍼 `pushNeutralTempoToNative()` 신설.
- **Key도 같은 조건부 push였다** — `setDetectedKey` / `setKey`를 무조건 전송(없으면 빈 문자열)으로 바꿔 이전 프로젝트의 키 문자열이 남지 않게 했다. (`keyShift` / `variKey`는 원래 무조건 전송.)
- `Bridge.clearTracks()` · `clearTracksKeepMaster()` · `removeTrack()`(마지막 트랙 삭제 분기)에서 **`syncTempoKeyToNative()` 호출 추가** → New Project / Delete all tracks / 트랙 전부 삭제 후 네이티브 tempo·key가 UI(`---`)와 일치.
- `Bridge.setVariBpm(on)`: `projectBpm`이 없으면 **중립 BPM을 먼저 push**하고, 네이티브에는 요청값이 아니라 **엔진이 실제로 채택한 값(`res`)** 을 보낸다(거부됐는데 `on:true`가 가는 일 차단).

**(b) 요청받은 UX — 엔진 차원 거부 + 안내 모달** (요청서의 두 대책을 모두 반영)
- [audio-engine.js](audio-engine.js) `setVariBpm(on)`: `on=true`이고 `projectBpm`이 없으면 **켜지 않고 `false` 반환** → 메뉴·복원·스냅샷 등 모든 진입점이 한 번에 안전.
- `importProject` / `applySnapshot`도 `variBpm: !!jt.variBpm && !!projectBpm`으로 정리 — BPM 없이 저장된 프로젝트가 스위치 ON 상태로 열리지 않는다.
- [app.jsx](app.jsx) `toggleVariBpm`: 켜려는데 `projectBpm`이 없으면 **BPM 측정 안내 모달**. 새 모달을 만들지 않고 **기존 `showAppNotice`** 를 사용 — 배경/테두리/글자색이 전부 `var(--bg)`·`var(--line-strong)`·`var(--cream-2)` 등 **테마 변수로만** 그려져 10종 테마에 자동으로 맞는다(요청서의 "color theme에 맞게" 충족, Ocean 실캡처로 확인). 문구는 Detect / TAP / 직접 입력+APPLY 3가지 방법 안내. `tone: "warning"`을 아이콘 분기에 추가.
- `VariBpmSwitch`는 `---`일 때 **opacity 0.45로 흐리게 + 툴팁 교체**하되 클릭은 받는다(클릭해야 안내가 뜨므로).

**검증** — Electron 실구동: Demo session(BPM 미측정) → Vari BPM 클릭 시 `variBpm=false` 유지 + 모달 표시, 스위치 opacity `0.45` 확인. BPM 120 적용 후에는 정상 ON(`rate=1`). Ocean 테마에서 모달이 네이비/틸로 렌더되는 스크린샷 확인.

### 2. File Track Merge 결과가 BPM/Key의 영향을 받던 문제

**원인 규명 (확정).** `mergeTracks()`([audio-engine.js](audio-engine.js):1657)는 `renderMix()`를 **Export와 같은 옵션 없이** 호출한다. 그래서 `renderMix`는
- `renderRate = options.applyTempo === false ? 1 : this._projectRate()` (:4352) → **Vari BPM ON이면 템포가 구워진다**,
- `exportSemis = this._exportKeyShiftSemis()` (:4499) → **Vari Key ON이면 이조가 구워진다**.

→ Vari BPM/Key를 켠 채 Merge하면 바운스 트랙의 **길이·피치가 원본 트랙과 어긋나** 타임라인에서 정렬이 깨진다. (1번의 네이티브 오동작과 겹치면 "BPM을 재지도 않았는데 Merge 결과가 다르다"로 보인다.)

**구현**
- `mergeTracks()`가 `renderMix`에 **`applyTempo: false`, `applyKey: false`** 를 넘긴다. 두 값은 `...options` **뒤에** 두어 호출자가 실수로 덮어쓸 수 없게 했다.
- `renderMix`에 `applyKey` 옵션 신설: `const exportSemis = options.applyKey === false ? 0 : this._exportKeyShiftSemis();` (Export 경로는 옵션을 안 넘기므로 기본값 유지 → **무회귀**).
- 규칙 확정: **타임라인에 놓이는 오디오(Merge/Bounce, Consolidate, Flatten)는 항상 원본 BPM·Key로 렌더**하고, **Vari BPM/Key는 실시간 재생과 Export에만 적용**한다. Consolidate·Flatten은 `_renderClipsToSource`가 raw 버퍼를 그대로 이어 붙이는 구조라 이미 원본 기준 — **수정 불필요(코드 확인 완료)**.
- Merge Tracks 대화창에 안내 한 줄 추가 + 메뉴얼 5장(ko/en)에 같은 규칙을 note로 기재.

**검증** — 시뮬레이션: Vari BPM 120→140 + Vari Key +3을 켠 상태에서 `mergeTracks()`가 넘기는 옵션이 `applyTempo:false, applyKey:false`이고 호출자의 `applyTempo:true`도 무시됨을 확인(8개 단언 통과). 같은 상태에서 `_projectRate()=1.1667`, `_exportKeyShiftSemis()=3`(= Export는 그대로 적용)도 함께 확인.

### 3. Audio In 트랙 — 클립별 Take 개별 선택 (테이크 모델 재편)

**현재 구조와 문제 (확정).** 테이크는 **트랙 단위 레인**이다.
- `track.activeTakeId` 하나로 트랙 전체의 라이브 레인이 정해지고(`_isActiveClip`, :403), **다른 take 태그가 붙은 클립은 전부 숨겨진다** → 클립 1(테이크 1개) · 클립 2(4개) · 클립 3(3개)일 때 클립 2의 Take C를 고르면 **클립 1·3은 소리도 파형도 사라진다.**
- 테이크 번호도 트랙 전역 A·B·C…(`_normalizeTrackLayout` 하이진, :531)라 3개 클립의 테이크가 A~H로 뒤섞인다.
- Punch 녹음은 새 클립을 **기존 active take에 합류**시켜(`attachPunchRecording`, :1064) 한 테이크가 여러 구간에 걸치는 구조를 만든다 — 사용자가 본 "정리되면서 문제가 생김"의 실체.
- 구간별 선택을 표현하는 그릇은 이미 있다(`track.comp` = `{start,end,takeId}` 세그먼트, `_renderClips` :430). 다만 ① 시드가 **take 전체 구간 하나**(`_takeRegion`, :413)라 한 구간을 swipe하면 나머지 구간이 비고, ② 레인 클릭(`setActiveTake`)은 **comp를 통째로 버리며**(:1265), ③ 메인 타임라인 표시는 comp를 모르고 `activeTakeId`만 본다([ui-tracks.jsx](ui-tracks.jsx):1171).

**구현 — "Take Group(클립별 테이크 묶음)" 도입**

핵심 아이디어: **그룹은 저장하지 않고 클립에서 유도**한다. 파일 포맷·undo 스냅샷·네이티브 동기화에 새 필드가 하나도 늘지 않으므로, 기존 프로젝트도 열자마자 올바르게 그룹화된다.

| 신설/변경 | 내용 |
|---|---|
| `_takeGroups(track, clips?)` | take 클립의 **타임라인 구간이 겹치는 것끼리** 묶어 그룹(=클립) 배열을 만든다. 그룹은 시간순, 그룹 안의 테이크는 **녹음 순서**. `clips` 인자는 이동 보정용(아래 참고). |
| `_groupSelection(track, g)` | 그 그룹이 지금 재생하는 테이크 = ① comp에서 그룹 구간을 **가장 넓게 덮는** 테이크 → ② `activeTakeId`가 이 그룹 소속이면 그것 → ③ 그룹의 **마지막(최신)** 테이크. |
| `_selectedTakeIdSet` / `getSelectedTakeIds` | 그룹마다 하나씩, "지금 맨 위에 있는" 테이크 id 집합. UI가 메인 레인을 그릴 때 쓴다. |
| `_effectiveComp(track)` | 저장된 comp + **comp가 언급하지 않은 그룹은 그 그룹의 선택으로 채운** 세그먼트 목록. 한 구간을 swipe해도 나머지 구간이 비지 않는 이유. |
| `_isActiveClip` | `activeTakeId` 비교 → **그룹 선택 집합 조회**로 교체. 이 함수 하나가 bake·재생·Export·겹침 판정·슬라이스의 공통 관문이라, 여기만 고치면 전 경로가 클립별 선택을 따른다. |
| `_renderClips` | `track.comp` → `_effectiveComp(track)` 사용. |
| `setActiveTake` | 그룹이 2개 이상이면 **그 테이크가 속한 그룹의 세그먼트만** 교체(다른 클립의 선택 유지). 결과가 이전과 같으면 `false`를 반환해 **빈 undo 항목이 쌓이지 않게** 한다. 그룹이 1개면 기존 동작(`activeTakeId` + `comp=null`) 그대로. |
| `setCompRegion` | 시드를 `_effectiveComp()`로 바꾸고, swipe 범위를 **그 테이크가 속한 그룹 구간으로 클램프**(옆 클립 구간을 빼앗아 무음으로 만들던 문제 차단). |
| `clearComp` | 그룹이 2개 이상이면 comp를 **버리지 않고** "그룹당 1세그먼트"로 축약 — swipe로 나눈 것만 지우고 **클립별 테이크 선택은 보존**. |
| `_normalizeTrackLayout` | 테이크 번호를 **그룹별 A·B·C…** 로 부여. comp 하이진은 삭제된 테이크의 세그먼트를 트랙 전역 fallback으로 **재지정하던 것을 폐기(drop)** 로 변경 — fallback 테이크는 그 구간에 오디오가 없어 무음이 되기 때문이고, 빈 자리는 `_effectiveComp`가 그 구간의 선택으로 메운다. |
| `deleteTake` | 지운 것이 `activeTakeId`였을 때 **같은 그룹의 다른 테이크**로 넘긴다(트랙 전역 최신 테이크로 튀지 않도록). 그룹 판정은 클립을 지우기 **전에** 읽는다. |
| `attachPunchRecording` | punch가 active take의 **그룹과 겹칠 때만** 그 테이크에 합류. 다른 자리면 새 그룹의 새 테이크가 된다 — 한 테이크가 여러 클립에 걸치던 원인 제거. |
| `_shiftInactiveTakeClips` | "테이크 클립을 옮기면 다른 테이크도 같이 이동"을 **같은 그룹 안으로 한정**(예전 규칙은 다른 클립의 테이크까지 끌고 갔다). 이동이 이미 적용된 뒤 호출되므로 **이동 전 위치로 그룹을 계산**한다(`_takeGroups`의 `clips` 인자). |
| `canFlattenComp` | `groupCount`를 함께 반환 → 확인창이 "몇 개가 버려지는지"를 정확히 표시(`takeCount − groupCount`). Flatten 자체는 이미 `_renderClips` + 구간 끊김 단위 run 처리라 **로직 변경 없이** 클립별 선택이 각각 확정된다. |
| `getComp` | 저장된 comp → **effective comp** 반환. 세그먼트 수 > 그룹 수면 "실제 swipe 컴프"라는 뜻이라, UI가 단순 선택과 컴프 모드를 구분할 수 있다. |

**UI**([ui-tracks.jsx](ui-tracks.jsx))
- 메인 레인이 `DAW.getSelectedTakeIds()`로 그려져 **선택된 테이크가 맨 위 타임라인에 표시**되고, 화면 = 재생 = Export가 일치한다(예전엔 comp가 있어도 `activeTakeId`만 봤다).
- 테이크 레인 목록을 **`CLIP n` 머리글 + 구간 시간**으로 그룹화. 배지도 `3 clips · 8 takes`로 표기.
- 클립 위 테이크 배지는 **그 클립 기준 위치**를 보여 준다(`Take C · 3/4`, 예전엔 트랙 전체 기준이라 의미가 없었다).
- 레인 dim/하이라이트(컴프 표현)는 **실제 swipe가 있을 때만** 켜지므로, 클립별 테이크를 고르는 것만으로 UI가 컴프 모드로 바뀌지 않는다. `Clear` 버튼도 그때만 노출.

**⚠️ 사용자에게 알린 영향 (요청서 3번 마지막 줄에 대한 답, 승인 후 진행)**
1. **기존 저장 프로젝트의 소리가 달라질 수 있다.** 옛 모델에서 "다른 구간의 테이크"는 들리지 않는 것이 정상이었는데, 새 모델에서는 각 구간이 자기 테이크를 되찾아 **전에 안 들리던 구간이 다시 들린다.** (사용자 결정: **새 모델 일괄 적용.**)
2. **테이크 이름이 바뀐다.** 트랙 전역 A~H → 클립별 A·B·C…. 오디오 파일에는 영향 없음.
3. **그룹 경계는 "구간 겹침"으로 판정**한다. 일부러 반쯤 겹치게 둔 테이크는 한 그룹이 된다(그 경우는 swipe 컴프로 조정).

**검증** — 요청서의 시나리오(클립 1개/4개/3개 테이크)를 그대로 시뮬레이션해 **21개 단언 전건 통과**: 그룹 3개·크기 1/4/3, 그룹마다 Take A부터 번호, 세 클립이 동시에 렌더, 클립 2의 Take C 선택 후에도 클립 1·3 유지, 세 선택 공존, 한 클립 swipe가 다른 클립을 비우지 않음, 구간을 넘긴 swipe가 옆 클립을 침범하지 않음, `Clear` 후 클립별 선택 보존. **회귀**: 단일 구간 트랙은 그룹 1개 · Take A/B/C 번호 · `activeTakeId` + `comp=null` 동작이 **v1.45.0과 동일**, 테이크 없는 트랙은 전 클립 렌더. Electron 실구동으로 `CLIP 1(1 take) / CLIP 2(4 takes) / CLIP 3(3 takes)` 레인과 메인 레인 3클립 동시 표시, 클립 2 선택 변경 시 나머지 유지 스크린샷 확인.

### 4. Ocean 테마 — 인앱 메뉴얼 표의 좌측 열 대비 부족

**원인 규명 (확정).** [ui-help.jsx](ui-help.jsx):291 `.manual-th { background: var(--surface2); color: var(--amber); }`. Ocean(`data-theme="navy"`)에서는 `--surface2:#178CA4`, `--amber:#18B7BE`로 **둘 다 밝은 청록** → **명암비 약 1.6:1**(WCAG 최소 4.5:1에 크게 미달). 배포용 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html)은 자체 종이 테마라 무관하며, **인앱 메뉴얼([help.html](help.html) + ui-help.jsx)만 해당**.

**구현** — 테마별 예외가 아니라 **모든 테마에서 성립하는 공식**으로 고쳤다.
- `.manual-th` = 배경 `color-mix(in srgb, var(--surface2) 30%, var(--bg) 70%)` + 글자 **`var(--cream)`**(액센트 대신 본문 글자색). 배경만 어둡게 하면 Ocean은 4.9:1로 살아나지만 **Ivory가 2.4:1로 여전히 미달**이라(액센트 자체가 밝은 배경과 대비가 낮음), 좌측 열은 액센트를 포기하고 본문색으로 갔다 → **10종 테마 전부 ≥7:1**(Ocean 실측 19.6:1). 굵기(600)로 우측 설명 열(`--cream-2`)과 계속 구분된다.
- `.manual-code`는 강조 요소라 액센트를 유지하되 배경을 `color-mix(… 15%, var(--bg) 85%)`로 더 눌렀다 → Ocean **1.6 → 6.2:1**, 어두운 테마 전부 ≥4.5:1. (Ivory는 팔레트 자체의 한계로 낮게 남지만 v1.45.0과 동일 수준이라 회귀 아님.)
- 10종 테마 팔레트를 파싱해 명암비를 **수치로 계산**하고, Ocean에서 옛 규칙/새 규칙 표를 나란히 렌더한 스크린샷으로 육안 확인. 배포용 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html)은 자체 종이 테마라 영향 없음(내용 동기화 목적으로 재생성).

### 5. 메뉴얼 "2. 시작과 프로젝트" — 개발용 실행 안내 삭제

- [ui-help.jsx](ui-help.jsx) 2장 **"앱 실행 / `npm start`"**(ko·en)를 **"앱 설치와 실행"** 으로 교체: 설치 파일 실행 → 바탕화면·시작 메뉴 아이콘 → 첫 실행 시 최근 세션 자동 복원 → 소리가 안 나면 Settings의 출력/입력 장치 확인(12장 링크). 개발 환경 언급 삭제.
- 메뉴얼 전체를 훑어 남은 개발자 전용 문구를 찾았고, **14장 "MP3 저장이 실패할 때"의 `npm install` 안내**가 하나 더 있었다 → 설치본 사용자가 할 수 있는 조치(쓰기 권한·파일 점유·디스크 여유 → 그래도 안 되면 WAV로 먼저 저장 후 재설치)로 재작성(ko·en). 검증: 생성된 메뉴얼에 `npm start` **0건**, `npm install` **0건**.
- `npm run build:manual`로 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html) 재생성.

### 6. 메뉴얼 "7. Key 표시 및 설정" — 두 번째 그림 설명 오류 (사용자 지적이 맞음)

**확인 결과.** 하단 Key 리스트는 [app.jsx](app.jsx):959–975의 **읽기 전용 `<select>`** 로, `KEY_OPTIONS.major` 12개 + `minor` 12개 = **모든 조성 24개를 항상 표시**하고 감지된 원곡 Key만 앰버로 강조한다. **신뢰도 순 후보 목록이 아니다**(선택도 불가 — 주석에 "selection reverts, Apply is the only way"). 따라서 현재 설명("신뢰도가 높은 으뜸음 및 조성 후보들을 목록으로 제안")은 **틀렸다.**

**구현** — ko/en 캡션을 사실대로 교체: "하단 리스트는 **참고용 전체 조성 목록(장조 12 · 단조 12)** 이며, 감지된 원곡 Key가 강조 표시됩니다. **이 목록에서는 선택할 수 없고**, 키 변경은 +/− 오프셋 + **APPLY**로만 적용됩니다." 바로 위 ② Key Detection 본문도 "감지된 대표 키값이 목록에 표시" → "**결과는 하나의 원곡 Key**이며 원Key 표시창에 나타나고 목록에서 강조된다"로 정정(영문 `candidate keys`도 동일 취지로 수정).

### 그 밖에 함께 반영한 것
- **메뉴얼 4장(테이크 고르기와 컴프)** 을 새 모델에 맞춰 개정(ko·en): 클립별 테이크 관리 · `CLIP n` 머리글 · 클립마다 Take A부터 · `Clear`의 의미 · Flatten이 클립별 선택을 각각 확정.
- **메뉴얼 6장**: Vari BPM은 프로젝트 BPM이 있어야 켤 수 있다는 note(ko·en).
- **메뉴얼 5장**: 바운스는 항상 원본 BPM·Key로 렌더된다는 note(ko·en).
- **릴리즈 노트**를 `v1.40.0 - v1.46.0`(2026-08-18)로 갱신하고 이번 4건을 맨 앞에 추가.

### 시험
- [시험.md](시험.md)에 `T-1.46.0-1 ~ 6` 추가. 사용자 시험 대기.

---

## v1.45.0 — 사용자 메뉴얼 전면 개정 + HTML 메뉴얼 자동 생성 (2026-08-11)

> **재빌드: renderer([ui-help.jsx](ui-help.jsx) esbuild) + version metadata + `npm run build:manual`.** **엔진·네이티브 무변경.** 사용자 요청(2026-08-11): 앱 완성 단계이므로 인앱 메뉴얼과 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html)을 신 버전 내용으로 갱신. 스크린샷은 `manual/2nd manual revision/`에 제공받고, 반영 후 원본 삭제.

### 버전
- [package.json](package.json) `version`과 [version.js](version.js) `APP_VERSION`을 **1.44.29 → 1.45.0**으로 올림(두 곳 동시 — version.js 주석의 릴리즈 규칙대로).

### 메뉴얼 내용 — 구 버전 대비 새로 들어간 것
개정 전 메뉴얼은 **녹음 이후에 들어온 기능이 거의 비어 있었다**(HTML 쪽은 아예 13장 구성으로 녹음 장 자체가 없었음). 이번에 아래를 채웠다.

| 장 | 새로 반영한 내용 |
|---|---|
| 1. 앱 개요 | 번호가 매겨진 **화면 구성도(①~⑯)**, Project/Edit/Advanced Effects/Help **메뉴 4종 스샷**, **트랙 크기 S·M·L** 비교, **FILE TRACKS 그룹 접기** |
| 2. 시작과 프로젝트 | **Save As(프로젝트 이식성 — `<Project> Audio` 폴더 + 상대 경로)**, **Open ▸ 최근 목록(RECENT PROJECT 자동저장 / RECENT SAVED)**, **Clean Up Unused Recordings** |
| 3. 오디오 가져오기 | `NO AUDIO` → 실제 UI 문구인 **`NO SRC`**로 정정, 드롭 재연결 안내 |
| 4. 녹음 · 클립 편집 | 장 제목부터 확장. **구간 반복 = 테이크 쌓기**, **테이크 레인 · swipe 컴프 · Flatten Comp**, **Punch(단일 / Loop-Punch Comp)**, **클립 편집 전체**(선택·이동·트림·다중선택·nudge·Split·Merge·Copy/Paste/Duplicate), **클립별 볼륨(cut-only)**, **보컬 채널 스트립 6모듈**, **Broadband De-noise 3단계 절차** |
| 5. 타임라인과 트랙 | **트랜스포트 8버튼 표**(메트로놈·프리롤·Punch 활성 조건 포함), **FILE TRACKS 그룹**(일괄 뮤트 `M4` 배지 · 적용 제외 대상 · MUTE Clr), **Merge Tracks → Bounce 트랙** |
| 6. BPM | **TAP 사용법**을 문단 + 스샷으로 보강 |
| 9. 믹서와 마스터 | **Audio In 채널 스트립**(ARM/MON/LIM·입력 포트·GAIN·IN/GR 미터), **GRAPHIC EQ·FFT ↔ LEVEL METER 전환**, **OUTPUT FX 트랙 헤더**(MUTE Clr · EFFECT · R/D/S/W/E 배지) |
| 11. 내보내기 | 신 Export 창 반영(**File name** 행 추가, AUDIO INFO(TAGS) 구성) |
| 12. 설정 | **테마 10종 목록**, **Audio Input Texture**, **Recording offset(Auto / 수동 / Offset Cal.)**, **드라이버 모드 비교표(지연 ↔ 안정성)**, 활성 장치·지연 추정 표시 |
| 13. 단축키 | **도구 S/C/J**, **클립 편집 단축키**(Esc·Del·Ctrl+C/V/D), **nudge 3단계(1/10/100 ms)**, ←/→의 이중 역할 설명 |
| 14. 문제 해결 | 무음+미터 동작(출력 장치 해제), NO SRC, **녹음 타이밍 어긋남**, **드롭아웃**, **Record/Punch/메트로놈 비활성 이유**, **폴더 용량 증가** |

- 근거는 스샷이 아니라 **코드에서 확인**했다: 단축키/도구는 [app.jsx](app.jsx)의 키 핸들러(3254~3352), 프리롤 `PREROLL_CHOICES=[0,2,4,8]`·Punch 규칙은 같은 파일 440~530·3759~3821, 클립 편집 가능 조건(`audioIn`/`bounce`)과 FX 버튼은 [ui-tracks.jsx](ui-tracks.jsx):760·993, Merge 옵션은 [app.jsx](app.jsx):4515~4566, 스트립 모듈 번호(03=Equalizer)는 [vocal-strip-app.jsx](vocal-strip-app.jsx):764.

### 스크린샷
- 제공받은 **72장**을 ASCII 이름으로 정규화해 **[manual/screens-v2/](manual/screens-v2/)** 로 복사(`01-01-main-screen.png` 처럼 `장-번호-의미` 규칙). 원본은 한글+공백 이름이라 `img src`에 그대로 쓰기 부적합했다.
- **부족분 5장은 사용자에게 요청해 추가로 받음**: Export 창, Merge 버튼 활성 상태, Merge Tracks 대화창, Clean Up 대화창, 보컬 스트립 03 Equalizer.
- **162개 figure 중 146개가 신규 스샷**. 남은 16개는 고급 이펙트 3창(Ambience / Spatial Field / Equalizer)으로, 해당 창은 변경이 없어 **사용자 승인 하에 기존 `live-screens/` 이미지를 재사용**.
- 반영 완료 후 `manual/2nd manual revision/`의 이미지 72장을 **삭제**(삭제 전 md5로 screens-v2 사본과 전량 일치 확인). 폴더 자체는 비어 있는 상태로 남겨 둠.

### HTML 메뉴얼을 생성물로 전환 — [scripts/build-manual-html.js](scripts/build-manual-html.js)
- **문제**: 인앱 메뉴얼([ui-help.jsx](ui-help.jsx))과 [manual/사용자 메뉴얼.html](manual/사용자%20메뉴얼.html)이 **같은 내용을 두 벌로 손수 유지**하는 구조였고, 실제로 크게 어긋나 있었다(JSX는 14장 + 녹음 장 있음 / HTML은 13장 + 녹음 장 없음).
- **해결**: HTML을 **ui-help.jsx에서 생성**하도록 바꿨다. 스크립트가 `<section className="manual-section">` 블록을 뽑아 `{lang === "ko" ? ... : ...}`를 `div.lang-ko` / `div.lang-en`로 가르고, `manual-*` className을 HTML 페이지의 자체 클래스로 매핑한다(`manual-figure`→`figure`, `manual-note`→`note`, 나머지 대부분은 **클래스 제거 후 소자 그대로**). 이미지 경로는 `manual/` 접두어를 떼어 `manual/` 폴더 기준 상대 경로로 바꾼다.
- **`<head>`는 기존 파일에서 그대로 보존**한다 — 페이지 CSS/테마는 손으로 관리하는 자산이라 생성 대상이 아니다. 바뀌는 것은 `<body>`(헤더·목차·본문·푸터)뿐.
- **매핑되지 않은 className을 만나면 즉시 throw**한다. 조용히 클래스를 흘려보내면 스타일이 빠진 채로 배포되므로, 새 클래스를 쓰면 스크립트도 같이 고치도록 강제한다.
- CRLF 함정: 두 파일 모두 디스크상 **CRLF**라 `"\n              ) : (\n"` 같은 구분자 탐색이 처음에 전부 실패했다. 읽는 즉시 LF로 정규화하고 LF로 쓴다.
- 실행: `npm run build:manual`([package.json](package.json)에 스크립트 추가). **`build:renderers`에는 일부러 넣지 않았다** — `npm start`마다 1,800줄짜리 산출물이 다시 쓰이면 git diff가 계속 더러워진다.
- 결과: **14장 · 164 figure · 약 185 KB**. 검증으로 잔여 JSX 흔적(`className` / `{lang` / `</>`) 0건, `img src` 전량 존재 확인, `div/section/table/tr/th/td/p/h3` 태그 균형 일치.

### 릴리즈 노트
- [ui-help.jsx](ui-help.jsx)의 `RELEASE_NOTES`를 `v1.40.0 - v1.45.0`(2026-08-11)로 갱신. 메뉴얼 개정 항목과 함께, 그동안 노트에 빠져 있던 **Recording offset · 오디오 장치 설정 표시 개선 · Audio Input Texture · mp3 Demo session**을 추가했다.

### 윈도우 설치 빌드 + `build.files` 누락 2건 수정 🔴
`npm run dist:win`을 돌리기 전에 [package.json](package.json)의 `build.files` 화이트리스트를 점검했는데, **패키징에서 빠진 파일이 2건** 있었다. `files`는 화이트리스트라 여기 없으면 asar에 안 들어간다.

| 누락 | 영향 | 확인 방법 |
|---|---|---|
| **`vocal-strip.html`** | 🔴 **기존 릴리즈부터 있던 버그.** [electron/main.js](electron/main.js):1508·1531이 이 파일을 `loadFile`하므로, **설치본에서는 FX 버튼을 눌러도 보컬 채널 스트립 창이 뜨지 않는다.** `build/vocal-strip-app.js`는 `build/*.js`로 들어가 있었지만 **호스트 HTML이 없었다.** | `npx asar list dist/release-1.44.25/.../app.asar`에 `vocal-strip.html` **0건**(다른 5개 html은 모두 존재) |
| **`manual/screens-v2/**/*`** | 이번 메뉴얼 개정분. 추가하지 않았다면 설치본 인앱 메뉴얼의 **그림 148개가 전부 깨진 채** 배포될 뻔했다(`manual/live-screens/**/*`만 등재돼 있었음) | 신규 경로라 기존 `files` 항목에 걸리지 않음 |

- 두 항목을 `files`에 추가하고, 일관성을 위해 `vocal-strip-app.jsx`도 다른 `*-app.jsx`들과 함께 등재했다(실제 로드는 `build/*.js`가 하므로 동작에는 무관).
- `manual/live-screens/**/*`는 **유지**한다 — 고급 이펙트 3창 그림 16개가 아직 이 폴더를 참조한다.

### 빌드 실행 — EBUSY 회피
- 첫 `npm run dist:win`이 `dist\win-unpacked.tmp\resources\default_app.asar` **EBUSY(파일 잠김)** 으로 실패했다. 30초간 재시도해도 잠금이 풀리지 않았다.
- 실행 중이던 프로세스를 확인해 보니 electron 6개 + `FocusDAW-AudioEngine.exe`가 떠 있었지만 **전부 `node_modules/electron/dist/`에서 실행된 개발 인스턴스**였고 `win-unpacked.tmp`를 참조하지 않았다(`Win32_Process`의 CommandLine으로 확인). 즉 **사용자가 쓰고 있던 앱을 죽일 이유가 없었다.**
- 그래서 잠긴 폴더와 싸우는 대신 **출력 폴더를 새로 지정**해 우회했다: `npx electron-builder --win -c.directories.output=dist/release-1.45.0`. `win-unpacked.tmp`도 새 출력 폴더 아래에 생기므로 잠긴 경로를 아예 건드리지 않는다. 기존 `dist/release-1.43.22`·`release-1.44.25` 관례와도 맞는다.
- 이후 `dist/win-unpacked.tmp`는 잠긴 채 남아 있다. **다음 빌드 전에 지우거나**, 같은 방식으로 버전별 출력 폴더를 쓰면 된다.

### 산출물 · 검증
- **`dist/release-1.45.0/FocusDAW-Studio-Setup-1.45.0.exe`** (NSIS, x64, 171 MB) + `.blockmap` + `latest.yml`(electron-updater 배포용).
- exe 메타데이터 `ProductVersion 1.45.0.0` / `FileVersion 1.45.0` 확인.
- asar 검증: `vocal-strip.html` **1건**(수정 확인), `screens-v2` PNG **72장**, `live-screens` PNG 50장 포함.
- 패키징된 `build/ui-help.js`를 asar에서 꺼내 **참조 이미지 80개(KO/EN 공용분 중복 제거 후) 전부가 asar 안에 존재**함을 대조 확인 — 누락 0건.
- ⚠️ **설치 후 스모크 테스트는 아직 안 했다.** 개발 인스턴스가 오디오 엔진 포트 8082를 점유 중이라 패키지본을 같이 띄우면 충돌할 수 있어 실행을 보류했다. 개발 앱을 닫은 뒤 설치해 **FX 버튼(보컬 스트립 창) → Help ▸ Manual 그림 표시** 두 가지를 우선 확인할 것.

### 남은 것
- 고급 이펙트 3창 스샷은 현재 구 테마(`live-screens/`)다. 창 UI를 손보게 되면 그때 새로 찍어 교체하면 된다.
- 메뉴얼 본문을 고칠 때는 **[ui-help.jsx](ui-help.jsx)만 고치고 `npm run build:manual`을 돌린다.** HTML을 직접 편집하면 다음 생성 때 덮어써진다.
- 잠긴 `dist/win-unpacked.tmp` 정리.
- 설치본 스모크 테스트(위 ⚠️).

---

## v1.44.29 — mp3 Demo session 첫 재생 볼륨 점프 수정 (2026-08-10)

> **재빌드: [audio-bridge.js](audio-bridge.js) 플레인 로드 + version metadata.** **renderer/네이티브 재빌드 불필요.** 사용자 보고: 빈 프로젝트에서 Demo session 로드 후 첫 재생 시 작게 시작하다가 약 6초 부근에서 정상 볼륨으로 커짐. 앱 종료 후 재실행해도 첫 재생마다 100% 재현.

- **원인**: Demo mp3는 web engine에서 먼저 디코딩되어 50% 볼륨으로 재생되고, 네이티브 엔진은 temp WAV를 비동기로 로드한다. 로드 완료 시점에 output handover가 일어나는데, 네이티브 트랙 설치 후 볼륨 파라미터가 기본값(1.0)으로 노출될 수 있어 web 0.5 → native 1.0으로 청감상 볼륨이 튀었다. 이후 재생은 이미 native가 활성화되어 같은 레벨로 유지되므로 점프가 사라진 것처럼 보였다.
- **수정**: `trackLoaded` 이벤트를 받을 때, pending load를 해제하고 native output으로 전환하기 **전에** 해당 트랙의 현재 runtime state(`volume`, `pan`, `mute`, `solo`, `reverb`, `echo`, automation, vocal strip)를 다시 네이티브에 전송한다. 기존 `syncTrackToNative`의 파라미터 전송도 공용 `sendTrackRuntimeStateToNative`로 정리했다.
- **효과**: 첫 재생 중 web → native handover가 발생해도 데모 트랙 볼륨이 50%로 유지되어 6초 부근 볼륨 상승이 없어야 한다.

---

## v1.44.28 — mp3 Demo session 재실행 NO SRC 수정 (2026-08-10)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js) 플레인 로드 + version metadata.** **네이티브 재빌드 불필요.** 사용자 보고: Demo session을 열고 앱 종료 후 재실행하면 각 트랙이 `NO SRC`로 표시되고 파형이 표시되지 않음.

- **원인**: v1.44.27의 mp3 데모 로드는 `DAW.addFileBuffer`에 `filePath` 없이 mp3 bytes만 넘겼다. 현재 세션은 정상 재생되지만 autosave/exportProject에는 `fileName`만 남고 `filePath`가 비어, 재실행 import 시 일반 오디오 placeholder로 복원되어 `needsAudio=true`가 됐다. 그런데 `filePath`가 없으므로 `reconnectProjectAudio`도 읽을 파일을 찾지 못해 `NO SRC`가 유지됐다.
- **수정**: Demo session 로드 시 내장 mp3의 실제 `filePath`를 함께 저장한다. 재실행 복원에서는 `needsAudio` 트랙이 내장 데모 파일명과 일치하면, 저장된 `filePath`가 없는 v1.44.27 autosave도 [electron/main.js](electron/main.js)의 `get-demo-session` 목록으로 자동 재연결한다.
- **네이티브 안정성 유지**: 데모 mp3 경로는 저장/재연결 식별용으로 남기되, bridge에는 `_forceNativeTemp` 플래그를 추가해 네이티브가 mp3/asar 경로를 직접 읽지 않고 기존처럼 디코딩된 버퍼를 temp WAV로 받아 재생하게 했다.
- 관련 파일: [app.jsx](app.jsx), [audio-engine.js](audio-engine.js), [audio-bridge.js](audio-bridge.js), [package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js), [시험.md](시험.md).

---

## v1.44.27 — Demo session을 mp3 스템으로 전환 (2026-08-10)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + Electron main/preload([electron/main.js](electron/main.js), [electron/preload.js](electron/preload.js)) + [audio-engine.js](audio-engine.js) 플레인 로드 + version metadata.** **네이티브 재빌드 불필요.** 사용자 확인 질문: `C:\Users\INNO-A-1223\Downloads\그 여름의 우리 - focustone`의 mp3 트랙을 Demo session으로 사용할 수 있는지.

- **가능하며, 구현 완료**: 다운로드 폴더의 mp3를 직접 참조하지 않고 프로젝트 내부 [Demos/그 여름의 우리 - focustone](Demos/그 여름의 우리 - focustone)으로 복사해 내장 데모 자산으로 만들었다. 설치본에도 포함되도록 [package.json](package.json) `build.files`에 `Demos/**/*`를 추가했다.
- **5트랙 구성 유지**: 이전 요청의 "5가지 이하 악기" 조건에 맞춰 보컬 2개와 퍼커션을 제외하고 `2 Drums.mp3`, `3 Bass.mp3`, `4 Guitar.mp3`, `5 Keyboard.mp3`, `7 Synth.mp3`만 기본 Demo session으로 사용한다.
- **로드 경로**: [electron/main.js](electron/main.js)에 `get-demo-session` IPC를 추가해 앱 내부 데모 파일 목록을 반환하고, [electron/preload.js](electron/preload.js)에 노출했다. [app.jsx](app.jsx)의 `loadDemo`는 Electron에서 이 목록을 읽어 mp3를 디코딩하고, 실패 시 v1.44.26의 합성 데모로 fallback한다.
- **네이티브 안정성**: 데모 mp3를 파일 경로로 직접 네이티브에 넘기지 않고, `DAW.addFileBuffer`로 메모리 디코딩 후 네이티브에는 기존 temp WAV 경로로 sync되게 했다. `asar` 내부 mp3 경로를 네이티브가 직접 읽지 않아도 된다.
- **기본 볼륨 50%**: 데모 mp3 로드 시 `params: { volume: 0.5 }`를 전달하도록 [audio-engine.js](audio-engine.js)의 `addFileBuffer`가 선택적 params를 새 트랙에 반영한다.

---

## v1.44.26 — Demo session 교체 (2026-08-10)

> **재빌드: renderer.** **네이티브 재빌드 불필요.** 사용자 요청(2026-08-10): 현재 Demo session을 10~20초 길이의 간단한 MIDI풍 음악으로 교체하고, Piano/Guitar/Bass/Drum/Synth 등 5가지 이하 악기로 구성하며, 각 트랙 소리는 최대 볼륨 50% 이하로 낮춘다.

- **데모 길이 교체**: `DURATION`을 8초에서 **16초**로 변경. 4초 섹션 4개(C–G–Am–F)로 구성해 요구 범위(10~20초)에 맞췄다.
- **악기 구성 교체**: 기존 4트랙(Drums/Bass/Keys/Lead)을 **5트랙(Piano, Guitar, Bass, Drums, Synth)** 으로 교체. 피아노 코드, 기타 아르페지오, 베이스 루트/5도, 기본 드럼 패턴, 신스 멜로디가 서로 다른 파형/리듬으로 들리도록 합성 로직을 정리했다.
- **기본 볼륨 50% 적용**: 데모 트랙 생성 경로(`init`, `addDemoTracks`)에서 `params: { volume: 0.5 }`를 전달한다. 이를 위해 `_addTrack`에 선택적 `params` 오버라이드를 추가했으며, 일반 import/녹음 트랙의 기본 볼륨은 기존처럼 1.0으로 유지된다.
- 관련 파일: [audio-engine.js](audio-engine.js), [package.json](package.json), [version.js](version.js), [수정요청.md](수정요청.md), [시험.md](시험.md).

---

## v1.44.25 — 스트립 A/B를 트랙 헤더 행 우측으로 재배치 (2026-08-04)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx)).** **엔진·네이티브 무변경.** 사용자 지정(2026-08-04, 스크린샷으로 위치 지정): v1.44.24에서 타이틀바로 옮겼던 A/B 버튼을 **트랙 이름 행의 우측**으로 재배치.

- 위치: 트랙 이름(`Audio In 1 …` + AUDIO IN 배지)과 `Project: …`가 있는 행의 **오른쪽 끝**. 이 행이 이미 `justifyContent: space-between`이라 두 번째 자식으로 넣는 것만으로 **우측 정렬**되고, `alignSelf: center`로 이름/프로젝트 두 줄의 세로 중앙에 맞춘다.
- 타이틀바에서 뺀 이유: 창 크롬 자리는 **창 설정**처럼 읽히는데 이 스위치는 **해당 트랙의 스트립** on/off다. 트랙 이름 바로 옆이 의미가 더 분명하다. (드래그 영역에 버튼을 두느라 필요했던 `no-drag` 처리도 함께 사라짐 — 타이틀바에 남은 `no-drag`는 창 컨트롤 것.)
- 크기도 타이틀바용 축소본(24px)에서 **원래 크기(패딩 8/15, 아이콘 15)** 로 복원.

---

## v1.44.24 — 스트립 A/B를 타이틀바로 이동 + PRESET Reset 추가 (2026-08-04)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx)).** **엔진·네이티브 무변경.** 사용자 요청(2026-08-04).

- **마스터 A/B(STRIP ACTIVE / Bypassed)를 PRESET 행 → 창 상단 우측(타이틀바, 창 컨트롤 왼쪽)으로 이동**. 스트립 **전체**를 지배하는 스위치라 프리셋 행의 한 컨트롤처럼 보이는 것보다 창 수준 스위치로 읽히는 편이 맞다. 타이틀바는 `WebkitAppRegion: "drag"`이므로 버튼에 **`no-drag`를 주지 않으면 클릭이 드래그에 먹힌다**(창 이동만 되고 토글 안 됨) — 적용함. 트랙이 없을 때(빈 상태)는 표시하지 않음.
- **PRESET 행에 `Reset` 추가**: HPF·Gate·EQ·Comp·De-esser를 **전부 기본값으로 되돌리고 모듈을 끈다**. 값은 하드코딩이 아니라 **`defaultVocalFx()`에서 읽으므로**, 나중에 파라미터가 늘어도 Reset이 자동으로 완전해진다. `grab()`으로 **Undo 1스텝**, 활성 프리셋 표시도 해제.
  - **⚠️ Reset은 `enabled`(A/B 상태)를 건드리지 않는다**(사용자 요구). bypass는 "편집 중인 값"이 아니라 **듣고 있는 방식**이라, 초기화가 오디오 경로를 말없이 바꾸면 안 된다. `vocalEnabled` 키를 아예 전송하지 않는 것으로 보장.
  - Reset 후 `userOffRef` 플래그도 초기화 → 노브를 만지면 해당 모듈이 자동 활성(신규 상태와 동일 동작).

---

## v1.44.23 — 보컬 프리셋이 HPF·De-esser까지 세팅 (2026-08-04)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx)).** **엔진·네이티브 무변경.** 사용자 점검 요청(2026-08-04): "프리셋이 모든 파라미터를 조정하는가?"

### 점검 결과 — 절반만 조정하고 있었다
프리셋은 **Stage B(EQ+Comp만 있던 시절)에 만들어진 뒤 갱신되지 않았다**(코드 주석에도 `only EQ + Comp are wired`로 남아 있었음). Stage D·E에서 HPF/Gate/De-esser가 들어왔는데 프리셋은 그대로여서, "Podcast"를 눌러도 **HPF·Gate·De-esser는 이전 상태 그대로** 남았다 — 특히 HPF가 핵심인 Podcast에서 어긋남이 컸다.

| 모듈 | 변경 전 | 변경 후 |
|---|---|---|
| EQ(9밴드) · Compressor | ✅ | ✅ |
| **HPF** | ❌ | ✅ **추가** |
| **De-esser** | ❌ | ✅ **추가** |
| **Noise Gate** | ❌ | ❌ **의도적 제외**(사용자 결정) |

### Noise Gate를 프리셋에서 뺀 이유 (사용자 결정 2026-08-04)
게이트의 적정 임계값은 **녹음마다 노이즈 플로어가 달라** 고정값이 무의미하다 — 너무 낮으면 아무 일도 안 하고, 너무 높으면 **말끝이 잘린다**. 그래서 프리셋은 게이트를 **켜지도 끄지도 않고** 사용자 설정을 그대로 둔다(`userOffRef`의 `gate` 플래그도 보존).

### 프리셋 값
| 프리셋 | HPF | De-esser (freq / thresh / amount) |
|---|---|---|
| Clean Lead | 80 Hz | 7.0k / −24 / 40% |
| Warm Pop | 75 Hz | 6.8k / −24 / 50% |
| Bright Air | 85 Hz | 7.2k / −26 / **65%** (4.8k~15k를 올리므로 치찰음 억제 강화) |
| Podcast | **100 Hz** (럼블 컷 최강) | 6.5k / −25 / 55% |

`applyPreset`이 `vocalHpfOn/Freq`·`vocalDeEssOn/Freq/Threshold/Amount`를 추가로 전송하고, PRESET 라벨 툴팁에 "게이트는 사용자 설정 유지"를 명시.

---

## v1.44.22 — 보컬 스트립 창 영문화 + De-noise 안내문 정리 (2026-08-04)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx)·[app.jsx](app.jsx)).** **엔진·네이티브 무변경.** 사용자 요청(2026-08-04).

- **창 내 한글 문구 전부 영문화(23곳)**: 카드 부제(HPF/Gate/EQ/Comp/De-esser/De-noise/Spectrum), 노브·페이더 툴팁, ON/OFF 토글 툴팁, 빈 상태 안내(오디오 없음 / 보컬 트랙 없음), De-noise 버튼 라벨(`Learn Noise` / `✓ Noise learned` / `Apply De-noise` / `Processing…`), 하단 상태바.
- **[app.jsx](app.jsx)의 `DENOISE_RESULT` 메시지 10건도 영문화** — 이 문자열들은 **스트립 창 안에 표시**되므로 같이 처리해야 창이 일관된다(학습 완료/구간 미지정/구간 짧음/클립 불일치/적용 완료 등).
- **De-noise 조작 설명 정리**: 색이 3가지(`--muted` 본문 + `--cream-2` 볼드 + `--faint` 꼬리말)로 섞여 있던 것을 **단일 색(`--muted`)으로 통일**하고 볼드 오버라이드 제거. 꼬리말 "— 그 구간이 있는 클립이 대상이 됩니다."는 **삭제**(동작 자체는 그대로).

---

## v1.44.21 — 하드코딩 액센트 색을 테마 변수로 일괄 교체 (2026-08-04)

> **재빌드: renderer 전체 + [studio.html](studio.html)·[mixer.html](mixer.html)(플레인 CSS).** **엔진·네이티브 무변경.** 오래 미뤄둔 "액센트 색 38곳" 백로그를 처리. 실제 스캔 결과 **52곳**이었다.

### 문제
`rgba(232,176,75,…)`는 **기본 테마(Warm Analog)의 앰버 리터럴**인데, 테마 10종은 각각 `--amber`를 다른 색(파랑/보라/초록…)으로 재정의한다. 따라서 테마를 바꿔도 이 리터럴을 쓴 요소만 **노란색으로 남았다**(v1.40.4에서 Repeat/Punch 경로만 부분 수정).

### 수정 — 52곳 일괄 치환
- 규칙: `rgba(232,176,75,A)` → **`color-mix(in srgb,var(--amber) N%,transparent)`** (N = A×100). 알파 없는 형태는 `var(--amber)`.
- 분포: [ui-tracks.jsx](ui-tracks.jsx) 17 · [app.jsx](app.jsx) 10 · [ui-mixer.jsx](ui-mixer.jsx) 10 · [ui-dialogs.jsx](ui-dialogs.jsx) 4 · [studio.html](studio.html) 4 · [mixer.html](mixer.html) 4 · [ui-kit.jsx](ui-kit.jsx) 1 · [mixer-app.jsx](mixer-app.jsx) 1 · [variations.html](variations.html) 1.
- **의도적으로 남긴 9곳**: 각 HTML `:root`의 **테마 정의 8곳**(`--amber:#e8b04b; … --amber-soft:rgba(232,176,75,.16)` — 기본 테마를 정의하는 자리라 여기서 리터럴이 맞다) + [app.jsx](app.jsx)의 **설명 주석 1곳**. 이 8곳을 바꿨다면 기본 테마 자체가 깨졌다.
- 확인 사항: 각 테마가 `--amber`뿐 아니라 **`--amber-soft`도 자체 색으로 재정의**하고 있어 `var(--amber-soft)` 사용처는 원래부터 테마 안전이었다.
- `#e8b04b`로 남은 곳은 **데이터라서 정상**: 앨범 커버 프리셋 팔레트("Amber Glow"), THEMES 목록의 Warm Analog 스와치 정의, 디자인 시안 페이지.

### 검증
- 전 렌더러 빌드 통과. `color-mix()`를 SVG `fill` 속성에 쓰는 패턴은 **기존 코드에 선례**가 있고(ui-mixer 노브, Stage C 스펙트럼 — 사용자 표시 확인 완료) `var()`도 SVG 속성에서 이미 널리 쓰인다. **T-1.44.21-1 사용자 시험 통과(2026-08-04)** — 이 작업의 완료 조건이던 테마별 육안 확인 완료.

---

## v1.44.20 — 보컬 채널 스트립 Stage F: 광대역 De-noise 오프라인 프린트 (2026-08-03) — **스트립 완결**

> **재빌드: renderer([audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)·[app.jsx](app.jsx)·[vocal-strip-app.jsx](vocal-strip-app.jsx)).** **네이티브 무변경**(순수 JS 오프라인 처리). 사용자 결정(2026-08-03, 설계 §9 미결 해소): **엔진 = 자체 STFT + 프로파일 학습**(ffmpeg afftdn 아님 — afftdn은 사용자 선택 구간 프로파일을 못 씀), **범위 = 선택한 클립**.

### 동작 (설계 §6)
1. 대상 트랙의 **FX 버튼으로 스트립 창**을 연다(창 자체가 그 트랙 맥락)
2. 목소리 없는 **무음 구간을 Repeat 구간**으로 지정 → 카드 06의 **[노이즈 학습]** → 그 구간 스펙트럼이 노이즈 지문, **구간이 걸친 클립이 대상**으로 확정
3. **[De-noise 적용]** → 그 클립 소스 전체에 STFT 감산 → **새 WAV로 프린트**, 클립이 새 소스를 가리킴

> **⚠️ 초기 설계에서 바꾼 점(사용자 보고 2026-08-04)**: 처음엔 "타임라인에서 클립을 선택 → Repeat 구간 지정"으로 안내했는데, **Repeat 구간을 드래그하면 레인 바깥 클릭으로 판정돼 클립 선택이 해제**되어 안내대로 따라 할 수 없었다. 스트립 창은 이미 한 트랙에 스코프되어 있으므로 **클립 선택 의존을 없애고, Repeat 구간이 곧 대상 클립을 지목**하도록 변경. 학습한 프로파일이 `clipId`를 함께 보관하므로 **적용 시점에는 선택도 구간도 필요 없다**.

**비파괴**: 결과는 원본과 **길이가 같은 새 소스**라 clip의 `sourceOffset`/`duration`이 그대로 유효하고 `clip.sourceId`만 재지정된다 → 원본 녹음 WAV 불변, Undo로 복원. 프린트 파일은 기존 consolidate 배관(`_pendingConsolidations` → `persistConsolidatedSources` → `save-consolidated-audio`)을 타고 `<Project> Audio/Consolidated/`에 `... (De-noised).wav`로 기록되어 **재열기 복원도 자동**.

### 구현
- **엔진** [audio-engine.js](audio-engine.js): `_hann`(주기 Hann), `_ifft`(켤레 트릭 — 기존 `fft()` 재사용, 역FFT 별도 구현 없음), `_learnProfileFromChannel`(구간 프레임 평균 magnitude = 지문), `_denoiseChannel`(75% 오버랩 STFT 감산: `mag−over×prof`를 **스펙트럼 플로어** `floorGain×mag`로 하한 → musical noise 억제, 위상 보존, 켤레 대칭 미러링으로 실수 역변환 보장, Σw² 정규화 OLA). 공개 API `learnDenoiseProfile`/`denoiseProfileInfo`/`denoiseClip`. `amount`(0~1) → 과감산 1~3배 + 플로어 −6~−24 dB.
- **⚠️ 동반 수정 — `_isTrivialLayout`**: trivial 고속경로는 **primary 소스의 raw를 그대로 반환**하는데, clip의 `sourceId`가 primary인지 검사하지 않았다. De-noise는 길이·오프셋이 같은 **비-primary 소스**를 가리키므로 그대로 두면 통과해 **원본(노이즈 있는) 오디오가 재생**된다 → `clip.sourceId !== sources[0].id`면 trivial 아님으로 판정(레거시 `sourceId` 없는 클립은 종전대로 trivial). Take 레인 가드와 같은 부류의 함정.
- **브리지** [audio-bridge.js](audio-bridge.js): `denoiseClip` 래퍼(재베이크된 오디오를 `syncTrackToNative`로 푸시). 나머지는 DAWProxy 패스스루.
- **메인** [app.jsx](app.jsx): `REQUEST_DENOISE_LEARN`/`REQUEST_DENOISE_APPLY` → 대상 클립(선택 클립, 없으면 클립이 하나뿐인 트랙의 그 클립) + `DAW.loopRange`로 해석 → 학습/적용, 적용은 `pushUndo`(실패 시 `cancelUndo`) + `persistConsolidated()` + `DENOISE_RESULT` 회신.
- **UI** [vocal-strip-app.jsx](vocal-strip-app.jsx): 카드 06 신설(3단계 안내 + Amount 노브 + [노이즈 학습]/[De-noise 적용] + 성공/실패 메시지). 실시간 인서트가 아니므로 `OFFLINE` 태그.

### 검증
- **DSP 수치 검증**(실제 파일에서 함수 추출해 node 실행): ① 영(zero) 프로파일 왕복이 **투명**(max |err| 1.19e-7 → OLA·역FFT·Σw² 정규화 정확) ② 사인+히스에서 무음부 노이즈 **16.7 dB 감소** ③ 톤 레벨 오차 **0.05 dB**(신호 보존) ④ NaN 없음·피크 유계 ⑤ 최소 설정은 큰 신호에 무영향. 렌더러 빌드 통과. ⚠️ T-1.44.20-1 사용자 시험 대기.

### 동반 UI 정리 — 트랙 헤더 (사용자 요청 2026-08-03)

**발단**: Bounce 트랙에 FX 버튼이 생기면서 **BPM/AUDIO/삭제 버튼이 오른쪽으로 밀려** 일반 file 트랙과 정렬이 어긋남. 원인은 폭 부족 — 헤더는 `HEADER_W = 274px`이고 **File Tracks 그룹 안의 트랙은 `indent=15`만큼 더 좁다**([app.jsx](app.jsx) `headerIndent={15}`). 좌우 패딩까지 빼면 가용 236px인데, AUTO 행 소비가 `AUTO(~62) + FX(~34) + VRB/ECHO/BPM 슬롯(~66, 꺼져도 `visibility:hidden`으로 **항상 예약**) + AUDIO(~40) + −(22) + gap(24) ≈ 248px`로 이미 초과 → `flex:1` 스페이서가 0으로 찌부러짐. (구 주석의 "244px"는 낡은 값이라 실제는 274px.)

**적용한 정리** — 모두 [ui-tracks.jsx](ui-tracks.jsx):
1. **FX 버튼: 텍스트 → 22×22 정사각 아이콘**(삭제 버튼과 동일 규격, 스트립 창 타이틀과 같은 `wave` 아이콘). 활성 시 앰버 채움·툴팁 유지. **≈ +12px**
2. **상단 B·S·M 행 gap**: `compact?6:8` → **4px 고정**(전 크기). 절약분은 전부 트랙 이름(`flex:1`)으로 감. **≈ +16px(이름 영역)**
3. **AUTO·FX 행 gap**: 6 → **4px 고정**. **≈ +10px**
4. **헤더 좌우 패딩**: S 10 / M·L 11 → **8px 고정**(세로 패딩은 크기별 유지). **≈ +6px**
5. **소스 칩 라벨**: `AUDIO` → **`SOURCE`**, `NO AUDIO` → **`NO SRC`**. 이 칩은 하드코딩이 아니라 `track.type`(`"audio"`)을 `.chip`의 `text-transform:uppercase`로 표시하던 것이라, **`audio`일 때만** `source`로 바꾸고 데모 트랙 타입(drums/bass/keys/lead, `addDemoTracks` 경유)은 그대로 뒀다. (`NO SRC`는 글자가 짧아져 누락 상태에서 ~10px 추가 절약.)
6. **스페이서 `<div flex:1/>` → 칩의 `marginLeft:auto`**. git blame 확인 결과 이 스페이서는 **최초 커밋(2026-06-07)**의 2구역 행(`AUTO … | 칩·삭제`)에서 유래했고, 배지 그룹은 한 달 뒤(2026-07-11)에 그 **왼쪽**으로 끼어든 것 — 즉 "배지를 중앙 고정"하려는 3구역 정렬 의도는 **없었다**(배지의 위치 고정은 `visibility:hidden` 슬롯 예약이 담당하는 별개 장치). 스페이서도 flex 아이템이라 **행 gap을 양쪽으로 2번** 먹어, 행이 꽉 차면 BPM↔칩이 8px가 됐다. 정렬은 동일하게 두고 gap 1회만 소비 → **+4px**.

→ AUTO 행: 가용 236→242px, 소비 ~248→~216px로 **25px 이상 여유 확보**. 사용자 확인 완료(2026-08-04).

**미채택(보류)**: VRB/ECHO/BPM 슬롯 예약을 조건부로 바꾸면 평상시 66px를 더 회수할 수 있으나 "값이 켜질 때 다른 요소가 밀리지 않게" 한 v1.16.0 의도와 상충. 헤더 폭 확대(274→290)는 `HEADER_W`가 플레이헤드 위치 계산 등 여러 곳에 얽혀 파급이 큼.

**⚠️ 후속 필요**: 화면 표기를 `NO SRC`로 바꿨으므로 **도움말 문구의 "NO AUDIO"**([app.jsx](app.jsx) 사용자 메뉴얼 섹션)도 맞춰야 한다 — **사용자 결정(2026-08-03): 도움말은 추후 일괄 변경**.

### 보컬 채널 스트립 — 전 단계 완료
A·B(셸/EQ/Comp) · C(스펙트럼) · D(HPF+Gate) · E(De-esser) · **F(De-noise)** + GR 미터. 설계 문서의 계획이 모두 구현됨.

---

## v1.44.19 — GR 미터 무반응 수정 (스트립 폴링이 isPlaying에 고착) (2026-08-03)

> **재빌드: renderer([app.jsx](app.jsx)·[vocal-strip-app.jsx](vocal-strip-app.jsx)).** **엔진·네이티브 무변경.** 사용자 보고(2026-08-03): 과장 설정(De-esser Freq 2kHz·Thresh −48·Amount 93%)에도 GR 미터 무반응.

### 원인
- 스트립의 GR 폴링 effect가 `isPlaying`에 게이트돼 있었는데, **스트립 창만 열려 있으면 재생 상태를 갱신하는 `LEVEL_METERS`가 오지 않는다**(그 브로드캐스트는 믹서/Advanced Pan 창이 열렸을 때만 발생). 스트립의 `isPlaying`은 INIT/SYNC(ready·undo/redo)에서만 갱신 → 재생을 눌러도 false로 고착 → `if(!isPlaying) return`으로 폴링이 아예 시작되지 않아 미터가 0에 멈춤.

### 수정
- [vocal-strip-app.jsx](vocal-strip-app.jsx): GR 폴링을 **isPlaying과 무관하게 항상 실행**(마운트 1회, ~25fps). 네이티브 `getTrackVocalGr`가 정지 시 0을 반환하므로 무조건 폴링해도 안전.
- [app.jsx](app.jsx): `VOCAL_METERS` 응답에 `isPlaying`을 실어 보냄 → 스트립 재생 상태 표시(하단바)도 믹서 없이 정확.
- [version.js](version.js)·[package.json](package.json)·[ui-help.jsx](ui-help.jsx) `1.44.19`.

### 검증
- 렌더러 빌드 통과. ⚠️ T-1.44.18-1 재시험(미터 반응).

---

## v1.44.18 — 스트립 Gate/Comp/De-esser 실시간 GR 미터 (2026-08-03)

> **재빌드: renderer([app.jsx](app.jsx)·[audio-bridge.js](audio-bridge.js)·[vocal-strip-app.jsx](vocal-strip-app.jsx)) + 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)·[WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp)) → `bin/` 교체.** 사용자 요청(2026-08-03, T-1.44.17-1 시험 중): De-esser 효과가 미묘해 동작 확인이 어려움 → **Gate·Comp·De-esser 각각의 감쇠량(dB)을 실시간 막대 미터로 표시**. 기존 Comp 미터는 가짜 고정값(38%)이었던 것을 실측으로 교체.

### 구현 (네이티브 → 브리지 → 메인 → 스트립)
- **네이티브** [AudioEngine.h](juce_skeleton/Source/AudioEngine.h): `TrackAudioSource`에 `gateGrDb`/`compGrDb`/`deEssGrDb` atomic. `getNextAudioBlock`에서 블록당 계산 — Gate=블록 최소 게인의 dB, Comp=comp 전/후 피크 magnitude 비(makeup 제외), De-esser=블록 최소 g의 dB. 모듈 off/스트립 bypass/정지 시 0. `AudioEngine::getTrackVocalGr(id, gate, comp, deEss)`(engineMutex 락, 정지 시 0 — `getTrackMagnitude` 패턴). [WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp) `levels` 이벤트에 `"vocalGr":{id:[g,c,d]}` 추가.
- **브리지** [audio-bridge.js](audio-bridge.js): `levels`에서 `vocalGr` → `nativeState.trackVocalGr`, `getTrackVocalGr(id)` getter(웹 fallback 0).
- **메인** [app.jsx](app.jsx): 스트립의 `REQUEST_VOCAL_METERS {id}` → `VOCAL_METERS {id, gate, comp, deEss}` 응답.
- **스트립** [vocal-strip-app.jsx](vocal-strip-app.jsx): 재생 중 ~25fps 폴링, `GrMeter` 컴포넌트(우→좌 채움, amber→red)로 Gate/Comp/De-esser 카드에 표시. 정지 시 0으로 감쇠.

> 미터는 **네이티브 전용**(Gate/De-esser가 네이티브에서만 가청이므로) — 웹 fallback·정지 시 0.

### 검증
- 렌더러·네이티브 빌드 통과 → exe에 `vocalGr` 브로드캐스트 확인, `bin/` 교체. ⚠️ T-1.44.18-1 사용자 시험 대기(치찰음/큰 신호 재생 시 미터 반응).

---

## v1.44.17 — 보컬 채널 스트립 Stage E: De-esser (2026-08-03)

> **재빌드: renderer([audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)·[vocal-strip-app.jsx](vocal-strip-app.jsx)) + 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)) → `bin/FocusDAW-AudioEngine.exe` 교체.** 인서트 체인 맨 뒤(컴프 뒤, 설계 §9)에 스플릿밴드 De-esser 추가. 자리표시자였던 카드 05 기능화. 인서트 순서: **HPF → Gate → EQ → Comp → De-esser**.

### 범위 결정 (Stage D와 동일: 네이티브 권위)
- **De-esser**: **네이티브 완전 구현**(재생·Export 권위). 웹 fallback은 미적용(게이트와 동일 — 동적 사이드체인은 WebAudio 노드로 부적합). 파라미터는 저장/복원·네이티브 전달.

### 구현 (스플릿밴드)
- **네이티브** [AudioEngine.h](juce_skeleton/Source/AudioEngine.h): `TrackAudioSource`에 `vocalDeEssHpf`(치찰음 대역 사이드체인 HPF, makeHighPass Q=0.707)·`deEssBuffer`(선할당 스크래치)·`deEssEnv` 추가. `getNextAudioBlock`에서 컴프 뒤에: 블록 복사 → HPF로 치찰음 대역 분리 → 대역 엔벌로프(0.5ms attack / 40ms release) → 임계값 초과 시 `redDb = min(overshoot,24)×amount` 감쇠 → `output = signal − hf×(1−g)`(위상 무관·정확). `updateVocalDeEssCoefficients()`·prepare/reset 반영. [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) setTrackParam 양 루프+loadTrack에 `vocalDeEss*` 키.
- **브리지** [audio-bridge.js](audio-bridge.js): `sendVocalFxToNative`에 De-esser 키(재접속 재동기화).
- **웹** [audio-engine.js](audio-engine.js): `_setVocalFxParam`에 `vocalDeEss*` 키(모델 저장). 오디오 처리는 네이티브 담당(웹 인서트 노드 미추가).
- **UI** [vocal-strip-app.jsx](vocal-strip-app.jsx): 카드 05를 토글+노브(Freq/Thresh/Amount)로 기능화(auto-arm). De-esser는 동적이라 Spectrum 미표시.

### 검증
- 렌더러 빌드 통과. 네이티브 증분 빌드 통과 → exe에 `vocalDeEss*` 심볼 확인, `bin/` 교체. ⚠️ T-1.44.17-1 사용자 시험 대기.

### 남은 스트립 단계
- **Stage F(광대역 De-noise 오프라인 프린트)** — 마지막 단계. UI/엔진 신규(노이즈 프로파일 학습 → STFT 감산 또는 ffmpeg afftdn → 새 WAV 비파괴 프린트).

---

## v1.44.16 — 스트립 Noise Gate 노브 1행 고정 (2026-08-03)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx)).** **엔진·네이티브 무변경.** 사용자 요청(2026-08-03, T-1.44.15-1 시험 후): Noise Gate의 4개 노브가 창 폭에 따라 2행으로 접히던 문제 → **항상 1행**으로 고정.

### 수정
- HPF/Gate 카드의 그리드를 `1fr/1fr` → `minmax(0,0.8fr)/minmax(0,1.2fr)`로 바꿔 **Gate 열을 넓힘**.
- Gate 노브 행을 `flexWrap: nowrap` + `overflowX: auto`로 두고 노브 크기 48→**42**로 축소 → 창을 줄여도 **2행 reflow 없이 1행 유지**, 극단적으로 좁을 때만 가로 스크롤.
- [version.js](version.js)·[package.json](package.json)·[ui-help.jsx](ui-help.jsx) 버전 `1.44.16`. (순수 레이아웃 수정 — 별도 시험 없이 육안 확인.)

---

## v1.44.15 — 보컬 채널 스트립 Stage D: HPF + Noise Gate (2026-08-03)

> **재빌드: renderer([audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)·[vocal-strip-app.jsx](vocal-strip-app.jsx)) + 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)) → `bin/FocusDAW-AudioEngine.exe` 교체.** 인서트 체인 앞단에 **HPF → Gate**를 추가(순서: HPF → Gate → EQ → Comp, 설계 §2). 자리표시자(SOON)였던 카드 01·02를 기능화.

### 범위 결정 (설계 §4-1 + 네이티브 권위)
- **HPF**: **웹+네이티브 완전 구현**(12 dB/oct highpass). 재생·Export 모두 반영.
- **Noise Gate**: **네이티브 완전 구현**(재생·Export 권위). **웹 fallback은 게이트 미적용** — WebAudio에 게이트 노드가 없어(설계가 근사/생략 허용) 네이티브가 기본 엔진이므로 그대로 둠. 파라미터는 저장/복원되고 네이티브로 전달됨.

### 구현
- **웹** [audio-engine.js](audio-engine.js): `_addTrack` 인서트에 `vocalHpf`(BiquadFilter highpass, off=10 Hz≈투명) 추가 → `fxIn→hpf→eq…→comp→makeup→fader`. `_applyVocalFx`가 hpf.on 시 corner를 `hpf.freq`로 이동. `_setVocalFxParam`에 `vocalHpf*`/`vocalGate*` 키. **Export 오프라인 체인**에도 HPF 복제(게이트는 네이티브 Export 담당).
- **네이티브** [AudioEngine.h](juce_skeleton/Source/AudioEngine.h): `TrackDef`·`TrackAudioSource`에 HPF/Gate 필드+atomic. `TrackAudioSource::getNextAudioBlock`의 인서트에서 **① HPF(`dsp::IIR` highpass, makeHighPass Q=0.707) → ② Gate(임계값 아래 다운워드 익스팬더, 스테레오 링크, attack/release 스무딩 `gateGain` 유지) → ③ EQ → ④ Comp** 순 처리. `updateVocalHpfCoefficients()` 추가, prepare/reset 반영. [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) `setTrackParam` 양 루프(TrackDef+juceTracks)와 `loadTrack` 적용에 새 키.
- **브리지** [audio-bridge.js](audio-bridge.js): `sendVocalFxToNative`에 HPF/Gate 키 추가(네이티브 재접속/재시작 시 초기화 방지). 신규 키는 `setTrackParam`이 이미 스칼라로 포워딩.
- **UI** [vocal-strip-app.jsx](vocal-strip-app.jsx): 카드 01(HPF: Freq)·02(Gate: Thresh/Ratio/Attack/Release)를 토글+노브로 기능화(EQ/Comp와 동일 auto-arm), **한 줄에 좌우 2열 배치**. 게이트는 네이티브에서 가청.
- **Spectrum 반영**: POST 곡선에 **HPF 저역 롤오프**를 추가(RBJ highpass Q=0.707, 가청 필터와 동일) → HPF 조절 시 스펙트럼에 즉시 반영. **Noise Gate는 레벨 기반 동적 이펙트라 고정 주파수 곡선이 없어 스펙트럼에 표시하지 않음**(컴프레서와 동일 사유, 소리로 확인).

### 검증
- 렌더러 빌드 통과. 네이티브 증분 빌드 통과(C4819 경고만) → exe에 `vocalHpf*`/`vocalGate*` 심볼 확인, `bin/` 교체. ⚠️ T-1.44.15-1 사용자 시험 대기.

### 남은 스트립 단계
- **Stage E(De-esser) → F(광대역 De-noise)**. UI 카드 05(De-esser)는 아직 자리표시자.

---

## v1.44.14 — 보컬 채널 스트립 Stage C: 적용 전/후 FFT 스펙트럼 (2026-08-03)

> **재빌드: renderer([audio-engine.js](audio-engine.js)·[app.jsx](app.jsx)·[vocal-strip-app.jsx](vocal-strip-app.jsx)).** **네이티브 무변경.** 보컬 스트립 Spectrum 카드(그동안 SOON 자리표시자)를 실제 차트로 구현. 사용자 결정(2026-08-03): **POST 곡선 = 측정한 PRE에 EQ 응답을 실시간 적용**(오프라인 컴프 실측 대신 — 가볍고 페이더 드래그에 즉시 반응).

### 구현
- **엔진** [audio-engine.js](audio-engine.js) `computeTrackSpectrum(trackId)`: 선택 보컬 트랙 버퍼의 **정적 FFT**(기존 `computeSpectrum` 패턴 재사용, 트랙별 캐시 `trackId:length:sr`). **FFT 크기 N=4096**(마스터 EQ의 2048보다 큼 — 저역 해상도 ~11.7 Hz/bin@48k로 60/150 Hz 보컬 밴드 가독성↑, 정적 1회 계산이라 비용 무시 가능). 실시간 analyser가 아니라 버퍼를 읽으므로 **네이티브/웹 출력 무관하게 동작**. `[{f, db, n}]` 반환(db raw — 스트립이 EQ dB를 더한 뒤 정규화).
- **메인** [app.jsx](app.jsx): 스트립의 `REQUEST_TRACK_SPECTRUM {id}`에 `TRACK_SPECTRUM {id, pts}`로 응답(advanced-effects-sync 채널).
- **스트립** [vocal-strip-app.jsx](vocal-strip-app.jsx): Spectrum 카드를 SVG 차트로 교체. **PRE**(측정, 흐린 muted 선+면) + **POST**(PRE + EQ 응답, 앰버 선). POST의 EQ 응답은 **RBJ peaking Q=1.1**(웹 BiquadFilter·네이티브 `makePeakFilter(sr,f,1.1,gain)`와 동일 설계)로 계산해 가청 EQ와 일치. geq 드래그 시 즉시 갱신. 로그 주파수 X축·밴드 그리드/라벨. 스트립/EQ off면 PRE만, makeup(comp on)은 POST에 브로드밴드 오프셋으로 반영. 트랙 선택·INIT/SYNC마다 스펙트럼 재요청(녹음·편집·undo/redo 반영). 오디오 없으면 안내 문구.

### 검증
- `node scripts/build-renderers.js` 빌드 통과. ⚠️ T-1.44.14-1 사용자 시험 대기.

### 남은 스트립 단계
- **Stage D(HPF + Noise Gate) → E(De-esser) → F(광대역 De-noise)**. UI 카드는 이미 자리표시자(SOON)로 존재.

---

## v1.44.13 — 화살표 키 play bar seek 버그 수정 (forward 튐 + backward 고착) (2026-08-03)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-bridge.js](audio-bridge.js)(플레인 로드) + version metadata.** **엔진·네이티브 무변경.** 별도 PC(v1.43.x 베이스)에서 먼저 규명·수정한 뒤 v1.44.12 위에 재적용한 것(핸드오프: [playbar-seek-수정내역-2026-08-03.md](playbar-seek-수정내역-2026-08-03.md)). 네이티브 연결 상태에서 ① →(forward) 홀드 시 순차 증가 못 하고 튐(예 01:01→01:05→01:02) ② ←(backward)가 아예 이동 안 됨. 두 증상은 원인이 다르다.

### 원인 ① forward 튐 — 네이티브 seek `ack` 프레임이 로컬 offset을 되돌림
- `seek()`는 `nativeState.offset`을 권위 있게 갱신하는데, 네이티브가 **seek 명령마다 `ack:true` playbackPosition을 되쏜다**([WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp)). stale 방어선이 `!msg.ack`만 버리고 ack는 통과시켜, 키 홀드로 seek가 연발될 때 **한 박자 늦은 ack(옛 위치)** 가 offset을 과거 값으로 덮어씀 → 다음 넛지가 되돌아간 값 기준 → 튐.

### 원인 ② backward 고착 — 비동기 `getPlayhead()` 표류
- 화살표 seek가 매 반복마다 `getPlayhead()`를 다시 읽어 `ceil(cur)-1`로 계산. 네이티브 `getPlayhead()`는 비동기(`offset+elapsed`)라 seek 사이 살짝 앞으로 흘러, **backward는 `ceil(cur)-1`이 계속 같은 정수로 계산 → 제자리**. forward(`floor(cur)+1`)는 전진해 안 보임(비대칭).

### 수정 내용
- [audio-bridge.js](audio-bridge.js) `handleNativeMessage`: seek 방어선에서 `!msg.ack &&` 제거 → seek 직후 500ms 창에서 **ack 프레임도 드롭**(offset의 유일 권위는 `seek()`).
- [app.jsx](app.jsx) 화살표 seek: 매번 `getPlayhead()`를 읽지 않고 **직전 명령 목표(`seekNudgeRef={at,target}`)를 기준으로 연쇄**. 400ms 이내 연속(홀드/연타)이면 이전 target에서 한 칸 이동해 **양방향 결정적**, 400ms 넘게 쉬면 실제 playhead 재독. `[0, duration]` 클램프.
- [version.js](version.js)·[package.json](package.json)·[ui-help.jsx](ui-help.jsx) 버전 `1.44.13`.

### 검증
- `node scripts/build-renderers.js` 빌드 통과. **T-1.43.24-1 사용자 시험 통과(2026-08-03, v1.43.24/25 기준)** — 동일 수정이라 v1.44.13에도 유효.

---

# 2026-07-26 작업 요약 (v1.44.2 ~ v1.44.12)

> 하루 동안 **11개 증분**. 큰 줄기는 셋 — ① **보컬 채널 스트립 조작성**(사용자 보고 3건) ② **Export 100% 멈춤/엔진 크래시 완전 규명**(5개 증분에 걸친 추적) ③ **테마 색상 정합**. **시험은 전건 통과**했고 대기 중인 시험 항목은 없다 → [시험.md](시험.md) · [시험-아카이브.md](시험-아카이브.md).

| 버전 | 내용 | 계기 | 범위 |
|------|------|------|------|
| v1.44.2 | STRIP ACTIVE만으로 EQ/Comp 조작 가능 + 모듈 자동 ARM | 사용자 보고 | renderer |
| v1.44.3 | 이펙트별 ON/OFF 버튼(카드 우측 상단) + 명시적 OFF 유지 | 사용자 요청 | renderer |
| v1.44.4 | Compressor 노브 마우스 휠 조절(파라미터별 step) | 사용자 요청 | renderer |
| v1.44.5 | Export 스레드 예외 가드 + `std::set_terminate` 진단 + 렌더러 무한대기 차단 | 크래시 보고 | **네이티브**+bridge |
| v1.44.6 | 스트립 창 Undo/Redo 약 1초 지연 해소(스냅샷 프리뷰 선전송) | 시험 미통과 | renderer |
| v1.44.7 | SoundTouch `Excessive samplerate` 클램프 + 장치 0 방어 + 입구 클램프 | 진단 로그 | **네이티브** |
| v1.44.8 | 전이값 산수 규명(96k×96k÷44.1k) + transport 이중 prepare | 클램프 로그 | **네이티브** |
| v1.44.9 | SoundTouch에 파일 레이트 직접 주입 — 전이값 원천 차단 | 로그 잔존 | **네이티브** |
| v1.44.10 | STRIP ACTIVE를 테마 highlight(`--amber`)로, Bypassed는 disabled 표현 | 사용자 요청 | renderer |
| v1.44.11 | Antique Olive highlight `#694d00`→`#d6a63a`(대비 1.73→6.09:1) + `--on-amber` 토큰 | 사용자 보고 | 테마 CSS 7종 |
| v1.44.12 | 이펙트 ON 버튼도 `--amber`로 통일(OFF 유지) | 사용자 요청 | renderer |

### 핵심 성과 1 — Export 100% 멈춤 / 엔진 종료코드 3221226505 종결
- **증상**: 스트립을 건 상태로 Export하면 진행바가 100%에서 멈추고 엔진이 죽음. 간헐적(1회차 실패·2회차 성공).
- **추적**: 크래시 덤프를 직접 파싱 → **0xC0000409 · param 7 = FAST_FAIL_FATAL_APP_EXIT = `abort()`**(접근위반 0xC0000005가 아님) → 스택이 `_CxxThrowException`→언와인드→`abort`이므로 **예외가 스레드를 탈출해 `std::terminate`** 로 확정. PDB가 없어 심볼을 못 푸는 상황이라 **로그 계측(v1.44.5)** 으로 전환 → 다음 재현에서 곧바로 `Export threw: Error: Excessive samplerate` 확보 → 클램프 로그(v1.44.7)로 **실제 값 208980 Hz** 확보 → **96000×96000÷44100 = 208979.59** 로 산수까지 일치(사용자 장치 96 kHz 확인).
- **근본 원인**: JUCE `AudioTransportSource::prepareToPlay`가 **하위 체인을 먼저 준비하고 리샘플링 ratio를 나중에 갱신**해, 레이트 변경 직후 첫 prepare에 `새레이트 × (파일레이트 ÷ 이전레이트)` **전이값**이 내려간다. SoundTouch는 192 kHz 초과에 **예외를 던진다**. 발생 지점이 **Export 종료 후 복원 구간**이라 렌더는 끝났는데 완료 신호만 못 나갔다.
- **해소**: SoundTouch에 **파일 레이트를 직접 주입**(v1.44.9)해 전이값을 아예 쓰지 않음 + 클램프/예외 가드/진단 핸들러는 안전망으로 유지. 상세는 아래 v1.44.5~v1.44.9 절.
- **재발 방지 자산**: 엔진의 어떤 스레드에서 예외가 터져도 이제 stderr에 **예외 이름이 남는다**(`std::set_terminate`).

### 핵심 성과 2 — 보컬 채널 스트립 Stage B 종결
- Stage A+B(9밴드 EQ + Compressor)의 **사용자 시험 15건 전부 통과**(T-1.44.0-1~7 · 1-1·2 · 2-1·2 · 3-1·2 · 4-1 · 7-1 · 10-1 · 11-1).
- 조작 규칙이 정리됨: 잠금은 **마스터 STRIP ACTIVE로만**, 꺼진 모듈은 흐리게 표시 후 **터치 시 자동 ARM**, 단 **사용자가 직접 끈 모듈은 자동으로 켜지지 않음**.
- 상태 색은 전부 **테마 highlight(`--amber`)** 로 통일. 의미 색(EQ 부스트/컷 green·red, Makeup 게인)은 예외로 유지.

### 핵심 성과 3 — 테마 토큰 정비
- Antique Olive의 액센트가 **대비 1.73:1**로 사실상 보이지 않던 문제를 실측으로 확인·수정. 테마가 정의된 **7개 html + 테마 선택창 스와치**를 동시에 갱신(한 곳만 고치면 창마다 색이 갈림).
- **`--on-amber`** 토큰 신설 — `--amber` 배경 위 글자색. 기본값 `var(--mixer-bar-fg)`(late-bound), `solar`만 어둡게 재정의. 이유는 solar의 `--mixer-bar-fg`가 **어두운 크롬 바 전용 밝은 크림**이라 밝아진 골드 위에서 1.70:1이 되기 때문.

### 남은 작업(다음 세션)
- **스트립 Stage C(전/후 FFT 스펙트럼) → D(HPF+Noise Gate) → E(De-esser) → F(오프라인 광대역 De-noise)** — 설계 [보컬-채널-스트립-설계.md](보컬-채널-스트립-설계.md).
- 릴리즈 준비(배포 빌드 + 스모크 테스트), 잔여 폴리시(하드코딩 액센트 색 38곳).
- 장기 관찰 항목 T-1.20.8-1(앱 종료 시 엔진 비정상 종료 코드)은 계속 유지 — 오늘 건(0xC0000409)과는 **다른 코드**(0xC0000005)임에 유의.

---

## v1.44.12 — 이펙트별 ON 버튼도 테마 highlight 색으로 (2026-07-26)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 요청: 각 이펙트 카드의 ON 버튼도 STRIP ACTIVE와 **같은 색**으로(OFF는 현행 유지).

### 변경
- `tagStyle`의 켜짐 상태를 `--green` → **`--amber`** (글자·테두리·20% 틴트 배경·`--amber-soft` 글로우). 이제 카드 좌측 dot(원래부터 `--amber`)·우측 ON 태그·마스터 STRIP ACTIVE가 **한 색으로 통일**되고 테마를 따라간다.
- **OFF는 그대로** — 중립 배경(`--bg`)·흐린 글자(`--faint`)·글로우 없음.
- 남은 `--green` 사용처 2곳은 **상태가 아니라 의미** 표시라 유지: EQ 밴드 값의 부스트(green)/컷(red) 색, Comp Makeup 노브의 게인 색.

### 검증
- 렌더러 빌드 통과. ⚠️ **T-1.44.10-1 시험에 포함해 함께 확인**(테마 전환 시 ON 태그 색 동기).

---

## v1.44.11 — Antique Olive 테마 highlight 색 상향 (2026-07-26)

> **재빌드: 테마 CSS 7개 html + renderer([ui-tracks.jsx](ui-tracks.jsx)·[vocal-strip-app.jsx](vocal-strip-app.jsx)·[ui-dialogs.jsx](ui-dialogs.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 보고: **Antique Olive(`solar`) 테마에서 highlight가 눈에 띄지 않는다 — 훨씬 밝게.**

### 원인(실측)
- `solar`의 `--amber`는 **`#694d00`**(짙은 올리브)인데 배경은 `#1f3130`. **명도 대비 1.73:1** — 어두운 배경 위 어두운 액센트라 사실상 보이지 않는다. v1.44.10에서 STRIP ACTIVE를 테마 highlight로 바꾸면서 이 테마의 약점이 그대로 드러난 것.

### 변경
- `solar`의 highlight를 **`#d6a63a`**(밝은 앤티크 골드)로, `--amber-deep:#a87e22`, `--amber-soft:rgba(214,166,58,.20)`. **대비 1.73 → 6.09:1**(surface `#374F4E` 위에서도 3.92:1). 테마 성격("aged olive · warm sand")은 유지.
- 테마가 정의된 **7개 html 전부**(studio·mixer·vocal-strip·advanced-pan/eq/ambience·help)와 **테마 선택창 스와치**([ui-dialogs.jsx](ui-dialogs.jsx) `accent`)를 함께 갱신 — 한 곳만 고치면 창마다 색이 갈린다.
- **`--on-amber` 토큰 신설** — `--amber` 배경 위에 얹히는 글자색. 기본값은 `var(--mixer-bar-fg)`(late-bound라 테마별 값이 그대로 따라옴)이고, `solar`만 **`#2a1e02`** 로 재정의. 이유: solar의 `--mixer-bar-fg`는 **어두운 올리브 타이틀바용 밝은 크림(`#f1dec0`)** 이라, 밝아진 골드 위에 얹으면 **1.70:1**로 읽을 수 없다. 사용처는 트랙 헤더 **FX 버튼**([ui-tracks.jsx](ui-tracks.jsx))과 스트립 창 **활성 프리셋 칩**([vocal-strip-app.jsx](vocal-strip-app.jsx)) → 이제 `#2a1e02`로 **7.30:1**.
- 타이틀바/메뉴바/믹서 바의 `#694d00` 배경은 **그대로 둔다** — 그건 highlight가 아니라 크롬 표면이고 밝은 크림 글자와 짝이 맞다.

### 검증
- 대비 실측(WCAG 상대휘도 계산)으로 확인, 렌더러 빌드 통과. ⚠️ **T-1.44.11-1 시험 대기.**

---

## v1.44.10 — STRIP ACTIVE 버튼을 테마 highlight 색으로 (2026-07-26)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** T-1.44.0-7 통과 후 사용자 후속 요청: STRIP ACTIVE를 **고정 green이 아니라 각 컬러 테마의 highlight 색**으로, **Bypassed는 disabled처럼**.

### 변경
- 마스터 A/B 버튼의 켜짐 상태를 `--green` → **`--amber`** 로 교체. `--amber`는 [vocal-strip.html](vocal-strip.html)에서 테마마다 재정의되는 **highlight 색**(gold·blue·pink·orange·lime·white·teal 등)이라 테마를 그대로 따라간다. 배경은 20% 틴트 + `--amber-soft` 글로우, 글자는 `--amber` — 프리셋 칩(단색 채움)과 시각적으로 구분된다.
- **Bypassed = 비활성 표현**: 배경 `--bg`, 테두리 `--line`, 글자 `--faint`, 글로우 제거, opacity 0.65.
- 각 이펙트 카드의 ON/OFF 태그는 **green 유지**(모듈 단위 상태와 스트립 단위 A/B를 색으로 구분). 통일이 필요하면 후속에서 처리.

### 검증
- 렌더러 빌드 통과. ⚠️ **T-1.44.10-1 시험 대기**(테마 몇 종에서 색 반영·Bypassed 표현 확인).

---

## v1.44.9 — SoundTouch에 파일 레이트를 직접 알려 전이값을 원천 차단 (2026-07-26)

> **재빌드: 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h), 컴파일 성공 · bin/dist 교체 완료 2441216 bytes) + version metadata.** **렌더러 무변경.** 사용자 환경 확인: **출력 Realtek digital output · 96 kHz · buffer 256** — v1.44.8에서 계산한 조합(96k 장치 + 96k 소스 + 44.1k Export)과 정확히 일치. (최초 빌드 시 앱이 실행 중이라 bin 복사가 막혔고, 앱 종료 후 artefacts에서 복사해 마무리.)

### v1.44.8이 부족했던 이유
- v1.44.8의 이중 prepare는 **최종 상태만** 바로잡았다. **첫 번째 prepare는 여전히 전이값(208980 Hz)을 SoundTouch까지 내려보낸다** — 그래서 크래시는 사라졌지만(클램프) 로그는 계속 찍혔다. 사용자 로그 `transitional rate (208980 Hz) — clamped to 192000`이 그 증거.

### 수정 — 값을 고치지 말고, 애초에 올바른 값을 알려준다
- `SoundTouchAudioSource`는 **리샘플러 앞단**에 있으므로 항상 **파일의 샘플레이트 도메인**에서 오디오를 본다. 즉 prepare로 흘러드는 값(리샘플러가 곱한 `rate × ratio`)이 아니라 **파일 레이트가 언제나 정답**이다.
- `setSourceSampleRate(fileSampleRate)`를 추가하고 `TrackAudioSource` 생성자에서 즉시 지정 → `prepareToPlay`는 **전이값을 아예 쓰지 않는다**. 전이 상태든 아니든 항상 정확한 레이트.
- 기존 클램프는 override가 비정상일 때만 쓰이는 **최후 방어**로 남기고(절대 throw하지 않음), 로그는 제거(정상 경로에서 더 이상 발생하지 않음).

### 검증
- 컴파일 성공 · **bin/dist 교체 완료**(2441216 bytes). **Electron 부팅 스모크 통과** — 엔진 spawn 정상, 40초 관찰 동안 `Excessive samplerate`·`transitional rate`·크래시 없음(종료코드 143은 스모크 종료용 kill). ⚠️ **T-1.44.0-3·T-1.44.7-1 실 Export 재시험 대기**(96 kHz 설정 유지). 기대: 폴백 배너·해당 로그 모두 없음.

---

## v1.44.8 — Excessive samplerate의 정확한 산수 규명 + 전이값 자체를 제거 (2026-07-26)

> **재빌드: 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h), build_native.bat 통과·bin 교체 2441728 bytes) + version metadata.** v1.44.7의 클램프가 실제로 작동해 사용자 로그에 **`out-of-range rate (208980 Hz)`** 가 찍혔고, **그 숫자가 원인을 정확히 특정해 줬다.**

### 208980 Hz가 말해준 것 — v1.44.7의 "장치가 0을 보고" 가설은 틀렸다
- 값이 **유한**하므로 무한대 ratio(장치 0 Hz 보고)가 아니다. `장치레이트 × 소스레이트 ÷ Export레이트`로 정확히 재현된다: **96000 × 96000 ÷ 44100 = 208979.59**(C++ 기본 출력 정밀도 6자리 → `208980`으로 인쇄). 즉 **96 kHz 장치 + 96 kHz 소스(장치 레이트로 기록된 녹음/바운스) + 44.1 kHz Export** 조합.
- 발생 시점은 **Export 종료 후 복원 구간**이다(`prepareToPlay(originalBlockSize, originalSampleRate)`). 그래서 **렌더는 100%까지 정상으로 끝난 뒤** 죽었고, 진행바가 100%에서 멈춘 것이다. 렌더 자체는 오염되지 않았다(Export 시작 시 ratio는 1.0이라 44100 그대로 내려간다).
- 메커니즘은 v1.44.7에 적은 그대로 **JUCE의 순서 문제**다 — `AudioTransportSource::prepareToPlay`는 **하위 체인을 먼저 준비하고 ratio를 나중에 갱신**하므로, 첫 호출에는 `새레이트 × (파일레이트 ÷ 이전레이트)`라는 **전이값**이 내려간다. 값이 비정상인 게 아니라 **한 박자 낡은 것**이다.

### 수정
- **전이값 자체를 없앰**([AudioEngine.h](juce_skeleton/Source/AudioEngine.h) `TrackAudioSource::prepareToPlay`) — `transportSource->prepareToPlay(spb, sr)`를 **의도적으로 두 번** 호출한다. 첫 호출은 **ratio를 맞추기 위한 것**이고, 두 번째가 진짜 레이트를 전파한다. prepare는 값싸고 멱등이며, 그래프는 이미 Export마다 트랙을 여러 번 prepare한다.
- **클램프는 안전망으로 유지**하되 로그를 **소스당 1회**로 줄이고 문구를 "전이값을 클램프했으며 다음 prepare가 실제 레이트를 세팅하면 무해"로 바꿨다(정상 동작을 오류처럼 보이게 하지 않기 위함).
- v1.44.7의 장치 0 방어와 입구 클램프는 **그대로 둔다** — 이번 건의 원인은 아니었지만 실제로 가능한 경로이고 비용이 없다.

### 검증
- `build_native.bat` 컴파일 성공·bin 교체(2441728 bytes). ⚠️ **T-1.44.0-3·T-1.44.7-1 재시험 대기 — 앱 재시작 필수.** 기대: 폴백 배너 없이 네이티브로 완료, `Excessive samplerate`·`transitional rate` 로그 모두 없음.

---

## v1.44.7 — Export 크래시 진짜 원인: SoundTouch "Excessive samplerate" (2026-07-26)

> **재빌드: 네이티브([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp), build_native.bat 통과·bin 교체 2441728 bytes) + version metadata.** **렌더러 무변경.** v1.44.5의 진단 로그가 한 번에 범인을 찍었다 — 사용자 로그: `[AudioEngine Err] [AudioEngine] Export threw: Error: Excessive samplerate`. (이때 앱은 웹 오디오 폴백으로 정상 Export 완료 — v1.44.5 가드가 의도대로 동작.)

### 원인 — 전송 리샘플링 비율이 오염되면 SoundTouch가 예외를 던진다
- 그 문자열은 [soundtouch/TDStretch.cpp](juce_skeleton/Source/soundtouch/TDStretch.cpp)의 `if (aSampleRate > 192000) ST_THROW_RT_ERROR("Error: Excessive samplerate")` 뿐이다. 즉 **SoundTouch에 192 kHz 초과 샘플레이트가 들어갔다.**
- `SoundTouchAudioSource::prepareToPlay`가 받는 값은 **장치 샘플레이트가 아니다.** JUCE `AudioTransportSource`가 파일 레이트 보정을 위해 끼워 넣는 `ResamplingAudioSource`는 내부 소스에 **`sampleRate * ratio`** 를 넘기고, 게다가 `AudioTransportSource::prepareToPlay`는 **먼저 하위 그래프를 준비한 뒤에** ratio를 갱신하므로 그 순간의 ratio는 **직전 prepare 기준**이다.
- `ratio = sourceSampleRate / 마지막_prepare_레이트`. 그래서 **단 한 번이라도 0 Hz로 prepare되면 ratio가 무한대가 되고, 그 다음 prepare에서 inf가 그대로 흘러들어와** 예외가 터진다. 0 Hz가 나오는 실제 경로: **Export 종료 후 복원 구간**에서 `currentDevice->getCurrentSampleRate()`를 **검증 없이 그대로 사용**했다([AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)) — 장치가 닫혔거나 전환 중이면 0을 반환한다. 그래서 **크래시가 항상이 아니라 간헐적**이었고, "1회차 실패·2회차 성공" 패턴과도 맞는다.

### 수정 — 3중 방어(원인 차단 + 경계 방어)
1. **원인 차단**([AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) export 복원부) — 장치가 보고한 rate/blockSize가 **0 이하면 무시하고 안전 기본값(44100/512)** 을 쓴다. 비정상 보고 시 로그 출력. 이제 무한대 ratio 자체가 생기지 않는다.
2. **입구 클램프**(exportMix 진입) — 클라이언트가 보낸 `sampleRate`가 NaN·0·범위 밖이면 44100으로 보정하고 로그. 깨진 파일 생성과 그래프 오염을 동시에 막는다.
3. **경계 방어**([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)) — `SoundTouchAudioSource::prepareToPlay`는 비정상 레이트를 **4k~192k로 클램프**(비유한값은 44100)하고 로그만 남긴다. `TrackAudioSource::prepareToPlay`도 레이트·블록 크기를 정규화해 transport로 넘긴다. **한 블록의 피치 정확도를 잃는 것이 Export 전체를 잃는 것보다 낫다**는 판단.

### 검증
- `build_native.bat` 컴파일 성공·bin 교체 확인(2441728 bytes). ⚠️ **T-1.44.0-3 재시험 대기** — **앱을 재시작해야 새 엔진이 뜬다**(구 엔진 프로세스는 메모리에 남아 있음).

---

## v1.44.6 — 스트립 창 Undo/Redo가 약 1초 늦게 반영되던 문제 (2026-07-26)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 보고(T-1.44.2-2): "스트립 창의 페이더/노브가 Ctrl+Z를 누르면 **약 1초 후에** 변경된다."

### 원인
- 스트립 창의 Ctrl+Z는 `REQUEST_UNDO`를 보내고, 메인 창이 `undo()` → **`sendInit()`** 순서로 처리했다([app.jsx](app.jsx)). 문제는 그 사이의 `DAW.applySnapshot()`이다 — 로컬 클립 **re-bake**(`_ensureBaked`)와 **네이티브 전체 재동기화**(`resetNativeProjectState` + 트랙 재로드 + master/tempo 재푸시)를 **동기로** 수행하므로 프로젝트가 크면 수백 ms~1초가 걸린다.
- 그 작업이 끝나야 `sendInit()`이 실행되므로 스트립 창의 페이더는 **키를 누른 뒤 1초쯤 지나서** 움직였다. 값 자체는 정확했고 지연만 있었다.
- 부수적으로 드러난 구멍: **메인 창/믹서 창에서 누른 Ctrl+Z는 advanced 채널로 아무 것도 broadcast하지 않았다** → 그 경로로 undo하면 스트립 창은 아예 갱신되지 않고 이전 값을 계속 표시.

### 수정 — 스냅샷 프리뷰 선전송 + undo/redo 자체가 broadcast
- 되돌릴 스냅샷에는 **이미 undo 이후의 정확한 params가 들어 있다.** 그래서 `applySnapshot` **이전에** 그 스냅샷의 트랙/params를 `SYNC_STATE`로 먼저 쏜다 → 창은 **키 입력 즉시** 반영. 무거운 작업이 끝난 뒤 `sendInit()`이 권위 있는 상태로 한 번 더 확정한다(값이 같아 깜빡임 없음).
- 이 두 호출을 메시지 핸들러가 아니라 **`undo()`/`redo()` 안에** 넣었다(`advancedSyncRef`로 채널 접근). 그 결과 **스트립 창·믹서 창·메인 창 키보드** 어느 경로로 undo/redo해도 advanced 창이 동일하게 따라온다.

### 검증
- 렌더러 빌드 통과. ⚠️ **T-1.44.2-2 재시험 대기**(Ctrl+Z 즉시 반영 + 메인 창에서 누른 Ctrl+Z도 스트립 창에 반영).

---

## v1.44.5 — Export 100% 멈춤 / 엔진 종료코드 3221226505 — 원인 규명 + 예외 가드 (2026-07-26)

> **재빌드: 네이티브([Main.cpp](juce_skeleton/Source/Main.cpp)·[WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp), build_native.bat 통과·bin 교체 2440704 bytes) + [audio-bridge.js](audio-bridge.js) + version metadata.** 사용자 보고(T-1.44.0-3 2차): Export가 **100%에서 멈추고** `[AudioEngine] Process exited with code 3221226505`.

### 원인 규명 — 크래시 덤프 분석
- `%LOCALAPPDATA%\CrashDumps\FocusDAW-AudioEngine.exe.14624.dmp`(2026-07-26 00:34)를 미니덤프 파서로 직접 해석.
- **예외코드 0xC0000409, 파라미터 0x7 = `FAST_FAIL_FATAL_APP_EXIT`, 발생 위치 `ucrtbase.dll`** → 접근위반(0xC0000005, T-1.20.8-1의 그 코드)이 **아니라 `abort()`**.
- 폴트 스레드 스택: `VCRUNTIME140!_CxxThrowException` → `VCRUNTIME140_1!__CxxFrameHandler4`(언와인드) → `ucrtbase!abort` → fastfail. 즉 **던져진 C++ 예외를 아무도 잡지 않아 `std::terminate` → `abort()`**.
- 이유: **export는 `std::thread(...).detach()`로 도는데 그 안에 try/catch가 없었다**([WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp) `cmd == "export"`). 스레드 진입 함수를 예외가 빠져나가면 규격상 곧바로 terminate다. 렌더링이 이미 100%까지 끝난 뒤라 **`exportDone`이 나가지 못해 진행바가 100%에서 영원히 대기**한 것.
- ⚠️ **어떤 예외인지는 아직 미확정** — Release 빌드에 PDB가 없어 스택의 코드 주소를 함수 이름으로 풀 수 없다. 그래서 이번 증분은 **다음 재현이 스스로 이름을 말하게 만드는 것**을 목표로 했다.

### 수정
1. **export 스레드 예외 가드**([WebSocketServer.cpp](juce_skeleton/Source/WebSocketServer.cpp)) — 스레드 본문을 `try/catch(const std::exception&)/catch(...)`로 감싸 **엔진을 죽이지 않고** `[AudioEngine] Export threw: <what>`을 stderr로 남기고 `exportError` 이벤트를 보낸다.
2. **전역 terminate 핸들러**([Main.cpp](juce_skeleton/Source/Main.cpp)) — `std::set_terminate`로 `logAndAbortOnTerminate()` 설치(JUCE·standalone 양쪽 진입점). 진행 중인 예외를 `rethrow`해 **타입/메시지(`what()`)를 로그로 남긴 뒤** abort. export 외 다른 스레드에서 터져도 이제 원인이 로그에 남는다.
3. **렌더러 무한 대기 차단**([audio-bridge.js](audio-bridge.js) `switchToLocal`) — 네이티브 연결이 끊길 때 `window._activeExport`가 남아 있으면 **즉시 reject**("네이티브 오디오 엔진이 Export 중 종료되었습니다"). 엔진이 죽어도 진행바가 100%에 박제되지 않는다.

### 검증
- `build_native.bat` 컴파일 성공·bin 교체 확인. `node --check audio-bridge.js` 통과, 렌더러 빌드 통과. ⚠️ **T-1.44.5-1·T-1.44.0-3 실 DAW 재시험 대기** — 재현 시 로그의 예외 이름을 확보해 진짜 원인을 잡는다.

---

## v1.44.4 — Compressor 노브 마우스 휠 조절 (2026-07-26)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 요청: **Compressor 노브도 마우스 스크롤로 값 변경**(EQ 페이더는 v1.44.0 후속에서 이미 지원).

### 변경
- `Knob`에 **휠 조절** 추가. EQ 레일과 같은 이유로 **네이티브 non-passive `wheel` 리스너**를 쓴다(React `onWheel`은 passive라 `preventDefault`가 안 먹어 패널이 같이 스크롤됨). 최신 props는 **ref로 조회**해 한 번만 설치한 리스너가 항상 현재 값을 본다.
- 파라미터별 **step**(노치 1회): Thresh **1 dB** · Ratio **0.1** · Attack **1 ms** · Release **5 ms** · Makeup **0.1 dB** — 각 노브의 표시 자릿수와 맞춰 한 노치가 항상 눈에 보이게 했다.
- `snap()` 헬퍼로 step 격자에 스냅(+`toFixed(4)`로 0.1 누적 시 부동소수 오차 제거). 값이 안 바뀌면(경계) `onGrab`을 부르지 않아 **빈 Undo 스텝이 쌓이지 않는다**.
- 툴팁("드래그 · 더블클릭=중앙값 · 휠=미세 조절")과 Compressor 카드 sub 문구 갱신. 미구현 모듈(HPF·Gate·De-Esser)의 disabled 노브는 휠을 무시하고 **패널 스크롤을 그대로 통과**시킨다.

### 검증
- `node scripts/build-renderers.js` 빌드 통과. ⚠️ **T-1.44.4-1 실 DAW 시험 대기.**

---

## v1.44.3 — 이펙트별 ON/OFF 버튼 (카드 우측 상단) (2026-07-26)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 요청: 채널 스트립 창에서 **각 이펙트 카드 우측 상단 버튼으로 개별 on/off**.

### 변경
- 카드 헤드 우측의 상태 태그가 **읽기 전용 `<span>`(ON/BYP)** 이었던 것을 **토글 버튼(ON/OFF)** 으로 교체. 좌측 dot(◯)과 **같은 핸들러**를 공유하므로 어느 쪽을 눌러도 동일하게 동작하고 표시도 함께 바뀐다.
- 켜짐 = green 배경/테두리 + 소프트 글로우, 꺼짐 = 중립. 문구는 요청대로 **ON / OFF**(기존 "BYP" 폐기). 툴팁: 켜기/끄기, 스트립 Bypassed 시 "STRIP ACTIVE를 먼저 켜세요".
- 마스터 스트립이 Bypassed면 종전 dot와 같이 **비활성**(개별 on/off는 스트립이 켜진 상태에서만 의미가 있으므로 의존관계를 UI로 드러냄).
- 미구현 모듈(HPF·Noise Gate·De-Esser)은 계속 **SOON 태그**(토글 없음).

### 자동 ARM(v1.44.2)과의 정합
- **명시적 OFF는 유지된다.** `userOffRef`로 "사용자가 직접 끈 모듈"을 기억해, 그 모듈은 페이더/노브를 움직여도 **자동으로 다시 켜지지 않는다**. 이게 없으면 모듈 단위 A/B 비교 중 페이더를 살짝만 건드려도 OFF가 풀려 버린다.
- 플래그는 사용자가 다시 ON을 누르거나 **프리셋을 적용하면 해제**된다(프리셋은 두 모듈을 모두 켬).

### 검증
- `node scripts/build-renderers.js` 빌드 통과. ⚠️ **T-1.44.3-1 실 DAW 시험 대기.**

---

## v1.44.2 — 스트립 활성화 후 프리셋 없이는 아무것도 조절 안 되던 문제 수정 (2026-07-26)

> **재빌드: renderer([vocal-strip-app.jsx](vocal-strip-app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 보고: 채널 스트립 창에서 **STRIP ACTIVE를 눌러도 preset을 고르기 전까지 EQ를 비롯해 아무것도 조정할 수 없음.**

### 원인
- `defaultVocalFx()`에서 각 모듈은 `eq.on=false` · `comp.on=false`로 시작하는데, 컨트롤 활성 조건이 **`!strip || !모듈.on`** 이었다([vocal-strip-app.jsx](vocal-strip-app.jsx)). 즉 마스터 스위치(STRIP ACTIVE)만으로는 부족하고 **모듈 dot를 따로 켜야** 페이더/노브가 살아났다.
- 그런데 모듈 dot을 켜 주는 경로가 사실상 **프리셋뿐**이었다(`applyPreset`이 `vocalEqOn`·`vocalCompOn`을 1로 세팅). 카드 헤드의 작은 dot(◯)이 유일한 수동 경로였으나 발견이 어려워 "프리셋을 안 고르면 아무것도 안 된다"로 드러났다.

### 수정 — 마스터만 켜면 조절 가능 + 컨트롤 터치 시 모듈 자동 ARM
- **잠금 조건을 마스터 스위치로만 한정**: `eqLocked/compLocked = !strip`. 모듈이 꺼져 있으면 **잠그지 않고 흐리게만**(opacity 0.72) 표시해 "꺼져 있지만 만질 수 있다"를 나타낸다(스트립 자체가 Bypassed면 종전대로 0.4 + 조작 불가).
- **자동 ARM**: 꺼진 모듈의 페이더/노브를 움직이면 **같은 편집 안에서 모듈을 ON으로 전환**(`setBand`가 `vocalEqOn`, `setComp`가 `vocalCompOn`을 함께 전송). mousedown에서 이미 `grab()`이 실행되므로 **Undo 1스텝으로 묶인다**.
- 드래그 핸들러는 mousedown 시점 콜백을 캡처하므로 모듈 on/off는 **`vfxRef`(매 렌더 갱신)로 조회** — state 클로저를 쓰면 드래그 내내 stale 값이라 매 move마다 ARM 명령이 중복 전송된다.
- 모듈 dot(◯) 수동 토글·프리셋 동작은 종전과 동일(회귀 없음).

### 검증
- `node scripts/build-renderers.js` 빌드 통과. ⚠️ **T-1.44.2-1 실 DAW 시험 대기.**

---

## v1.44.1 — 프로젝트 이름 변경이 오디오 경로를 깨뜨리던 버그 수정 (2026-07-25)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 보고: 저장된 프로젝트(`untitled_ch2`)에서 **이름 변경 → Export → 종료 → 재시작** 후 `[AudioEngine] File not found: untitled_ch2 Audio/Bounces/… .wav`로 바운스/녹음 오디오가 사라짐. 원래 프로젝트는 정상이었음.

### 원인
- `renameProject`([app.jsx](app.jsx))가 이름 변경 시 **`setProjectPath(null)`** 을 호출했다("rename ⇒ unsaved" 의도). 그런데 Save As 수집 이후 소스 경로는 `<Project> Audio/…` **상대경로**로 저장되고, 이 상대경로는 **projectPath(=.focus 위치)를 기준**으로만 절대경로로 풀린다.
- 이름 변경으로 projectPath가 null이 되면 **1.5초 오토세이브가 projectPath=null을 그대로 영속화**([loadRecentProject](app.jsx#L139)가 `json.projectPath`로 복원). 재시작 시 base가 없어 `untitled_ch2 Audio/Bounces/…`가 절대경로로 안 풀리고 네이티브가 상대문자열 그대로 받아 "File not found". (코드 주석에도 이 함정이 기술돼 있었으나 헤더 rename 경로는 방어되지 않았음.)

### 수정 — 표시 이름과 저장 폴더 식별자 분리 (사용자 제안 채택)
- `renameProject`에서 **`setProjectPath(null)` 제거** → 이름 변경은 **표시 이름만** 바꾸고 저장된 .focus/폴더(projectPath)는 유지. 상대 오디오 경로가 계속 정상 해석되고, 오토세이브도 유효 경로를 영속화해 **재시작 후 오디오 정상**.
- 동작 변화: 이름 변경 후 **제자리 Save는 같은 .focus에 새 이름을 기록**(파일명은 유지), **Save As는 종전대로 새 폴더로 분기**. 즉 표시 이름 ≠ 폴더/파일 이름 허용(디커플링).
- `saveProject`의 stale 주석(“renameProject가 projectPath를 null로 만든다”) 갱신.
- **후속(같은 디커플링 완성): 이름 변경 후 Save 시 이름이 이전 파일명으로 되돌아가던 문제 수정.** 원인 = 저장 경로가 **제자리(projectPath 재사용)** 인데도 ① 메인 프로세스 `stampProjectNameFromPath`가 json.projectName을 **파일명으로 덮어쓰고** ② 렌더러가 `projectNameFromPath(result.path)`로 표시 이름을 파일명에 맞춰 되돌렸다. 수정: 렌더러가 **제자리 저장(reusedPath)** 을 판별해 ⓐ `saveProject(..., { stampName:false })`로 메인 stamping 생략(json의 표시 이름 유지) ⓑ 표시 이름 파일명 동기화를 **Save As/최초 저장(새 파일)에서만** 수행. [electron/preload.js](electron/preload.js) 시그니처에 opts 추가, [electron/main.js](electron/main.js) save-project가 `opts.stampName!==false`일 때만 stamp.

### 복구 안내(이미 깨진 프로젝트)
- 이미 projectPath=null로 영속화된 프로젝트는 **File ▸ Open으로 `.focus`를 직접 열면** projectPath가 다시 잡혀 `… Audio/Bounces/…`가 정상 해석된다(바운스 파일은 폴더에 그대로 있음).

### 검증
- `node scripts/build-renderers.js` 빌드 통과. ✅ **T-1.44.1-1·2 사용자 시험 통과(2026-07-25)** — 이름 변경→Export→재시작 후 오디오 정상 유지, 제자리 Save/Save As 모두 정상 → [시험-아카이브.md](시험-아카이브.md).

---

## v1.44.0 — 보컬 채널 스트립 Stage B (9밴드 EQ + Compressor, 별도 창·풀스택) (2026-07-25)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx)·신규 [vocal-strip-app.jsx](vocal-strip-app.jsx)·[ui-help.jsx](ui-help.jsx) esbuild + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js) 플레인 로드) + 네이티브 리빌드([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp), build_native.bat 통과·bin 교체) + version metadata.** [보컬-채널-스트립-설계.md](보컬-채널-스트립-설계.md)의 Stage B를 A(골격) 흡수해 착수. 사용자 결정(2026-07-25): **UI=별도 창**, **첫 증분=풀스택(웹+네이티브 리빌드)**.

### 범위 (이번 증분)
- **인서트 체인(pre-fader)**: 9밴드 그래픽 EQ + Compressor. **HPF·Noise Gate·De-esser·전/후 FFT 스펙트럼은 다음 단계(설계 §9 C–E)로 남김.**
- 대상: `kind === audioIn/bounce`(보컬) 트랙만. 스템(`file`)엔 노출·적용 안 함(구조적 kind 게이팅).

### 데이터 모델 ([audio-engine.js](audio-engine.js))
- `track.params.vocalFx = defaultVocalFx()` 신설(enabled·hpf·gate·eq·comp·deEss). **통째 직렬화라 저장 자동.** `normalizeVocalFx()`로 구프로젝트/부분저장을 기본값 위에 딥머지 하이드레이트(importProject + snapshot restore 양쪽).

### 웹 엔진 (fallback·파형/스펙트럼 권위) ([audio-engine.js](audio-engine.js))
- 트랙마다 인서트 노드(`fxIn` → EQ 9× `BiquadFilter(peaking)` → `DynamicsCompressor` → makeup gain → `fader`) 상시 구성. **소스 연결을 `fader`→`fxIn`으로 변경**(실시간 2곳). 비활성/비보컬은 중립(EQ 0dB·comp 1:1·makeup 0dB)이라 소리 영향 없음.
- `_applyVocalFx(track)`가 `vocalFx`를 노드에 반영(enabled=A/B 마스터, eq.on/comp.on 개별). `setTrackParam`이 `vocal*` 평탄화 키를 `_setVocalFxParam`으로 디스패치.
- **Export**: 오프라인 그래프(`exportMix`)에 동일 인서트 복제 → Export == 재생.

### 네이티브 엔진 (가청 권위) ([AudioEngine.h](juce_skeleton/Source/AudioEngine.h)·[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp))
- `TrackInfo`에 vocalFx 필드 + `TrackAudioSource`에 `ProcessorDuplicator<IIR::Filter>` ×9(마스터 EQ 패턴 재사용) + `dsp::Compressor<float>` + atomics. `getNextAudioBlock`에서 **게이트 직후·echo/reverb 센드 전** 인서트 적용(EQ 계수는 `vocalEqDirty` 플래그로 오디오 스레드 재계산). 오프라인 Export도 같은 경로라 자동 반영.
- `setTrackParam`이 `vocal*` 키를 TrackInfo(비동기 로드 대비) + 라이브 juceTracks 양쪽에 반영. 설치 동기 블록에서 TrackInfo→소스 복사.

### 브리지 ([audio-bridge.js](audio-bridge.js))
- `setTrackParam`의 스칼라 패스스루가 `vocal*` 키를 native에 자동 전달(변경 불필요). **네이티브 (재)연결 재동기화에 `sendVocalFxToNative(track)` 추가** — 없으면 재시작·재접속 때 보컬 FX 초기화(reverb/echo와 동일 함정, 메모리 [[native-has-no-importproject-sync-via-commands]]).

### UI — 별도 창 ([vocal-strip.html](vocal-strip.html)·[vocal-strip-app.jsx](vocal-strip-app.jsx)·[electron/main.js](electron/main.js)·[electron/preload.js](electron/preload.js)·[ui-tracks.jsx](ui-tracks.jsx))
- 전용 BrowserWindow(`open-vocal-strip` IPC, trackId를 쿼리로 전달). Advanced 창 프레임/테마 CSS 재사용. **기존 `focusdaw-advanced-effects-sync` 채널의 `SET_TRACK_PARAM`을 재사용** → studio가 `DAW`(=브리지 프록시)로 라우팅해 엔진+네이티브+저장까지 자동(스튜디오측 신규 핸들러 불요). INIT_STATE에 `kind` 추가.
- **디자인: 목업([_refer/Vocal Channel Strip 2.dc.html](<_refer/Vocal Channel Strip 2.dc.html>)) 레이아웃 이식**(DCLogic 런타임은 참고만, React+앱 테마 변수로 재구현). 구성(위→아래, 설계 §7): 타이틀바 → 트랙명(Michroma)+kind 배지+트랙 선택 → **PRESET 행 + 마스터 A/B**(전원 아이콘) → Spectrum(Stage C 플레이스홀더) → 01 HPF | 02 Noise Gate 2열(Stage D 플레이스홀더·비활성) → **03 그래픽 EQ(수직 페이더 슬라이더, 실동작)** → **04 Compressor(원형 노브×5 + GR 바, 실동작)** → 05 De-esser(Stage E 플레이스홀더). 프리셋 Clean Lead/Warm Pop/Bright Air/Podcast. 카드=해칭 그라디언트, 섹션 헤더=dot 토글+mono 인덱스+ON/BYP 태그, 미구현 섹션은 `SOON` 태그+dim 프리뷰. 하드코딩 색 대신 테마 변수. (Pitch/Echo/Output 섹션은 설계 §7대로 미포함.)
- 보컬 트랙 헤더의 **AUTO 버튼 오른쪽**에 **FX 버튼**(strip enabled면 앰버 하이라이트) → `electronAPI.openVocalStrip(track.id)`. (초기엔 MON/LIM 클러스터에 뒀으나 input gain 표시창과 겹쳐 이동.)

### 검증
- `node scripts/build-renderers.js` 전체 빌드 통과(vocal-strip-app.js 포함). `node --check` audio-engine·audio-bridge·electron/main·preload 통과. **네이티브 build_native.bat 컴파일 성공·bin 교체 확인**(2437632 bytes). Electron 부팅 스모크(엔진 spawn·렌더러 예외 없음).
- **시험 결과(2026-07-25)**: T-1.44.0-1(FX 노출·kind 게이팅)·2(EQ/Comp 가청)·4(저장/재열기·구프로젝트) **통과**. T-1.44.0-3(Export)은 **청음 결과 정상 반영 확인**, 단 1회차 진행률 100%에서 완료 신호가 안 오고 2회차 정상 저장되는 현상 관찰(아래 후속 참고). T-1.44.0-6(undo)은 아래 후속에서 **원인 수정**.

### 후속 수정 (시험 피드백 반영, 2026-07-25)
- **undo/redo 미동작 근본 수정** — `getSnapshot()`이 `params:{...t.params}` **얕은 복사**라 스냅샷의 `vocalFx`가 라이브 트랙과 **같은 참조**였고, `_setVocalFxParam`이 그 객체를 **제자리 변경**해 undo 스냅샷이 오염됐다. [audio-engine.js](audio-engine.js) getSnapshot에서 `vocalFx`를 **깊은 복사**로 스냅샷 → undo가 엔진을 실제 되돌림. 창 쪽은 INIT/SYNC 수신 시 `setVfx`를 **중첩 setState 밖**(targetIdRef)에서 갱신하도록 고쳐 undo/redo가 창 UI에도 반영.
- **트랙명 라이브 반영** — 메인 헤더에서 트랙 이름 변경 시 [app.jsx](app.jsx)가 트랙명/kind 시그니처 변화에 `SYNC_STATE`를 advanced-effects 채널로 브로드캐스트 → 스트립 창 제목/헤더 즉시 갱신.
- **UI 조정** — 상단 트랙 선택 드롭다운 제거(FX 버튼이 트랙별로 열림). A/B 버튼 색 반전(STRIP ACTIVE=green 강조 / Bypassed=중립), 문구 `BYPASSED (A/B)`→`Bypassed`. 수직 스크롤바를 메인 앱과 동일 스타일로([vocal-strip.html](vocal-strip.html)). EQ 밴드 **마우스 휠=0.1dB/노치**(React wheel이 passive라 네이티브 non-passive 리스너로 preventDefault).
- **컬러 테마 라이브 반영** — INIT_STATE는 창 열 때의 테마만 전달해 앱 테마 변경 시 스트립 색이 고정됐다. [vocal-strip-app.jsx](vocal-strip-app.jsx)가 메인 앱의 `focusdaw-theme-sync` 채널(THEME_CHANGED)을 구독해 테마 변경을 즉시 반영(믹서 창과 동일 경로).
- **관찰(미결) — Export 1회차 완료 신호 누락 + 네이티브 종료코드 `3221226505`(0xC0000409, STACK_BUFFER_OVERRUN)**: 종료코드는 `Killing JUCE Audio Engine process...`(종료 시점) **직후** 찍혀, 기존 장기 관찰 항목 [시험.md](시험.md) T-1.20.8-1(앱 종료 시 엔진 비정상 종료코드 재발)과 정황 일치. 인서트 DSP는 마스터 EQ와 동일 패턴(오디오 스레드 계수 재계산·표준 dsp process)이라 정적 검토상 오버런 없음. **스트립 활성 상태 Export에서 재현되는지 별도 확인 필요**(재현 시 네이티브 Export 경로 조사).

---

## v1.43.23 — take 개수 배지 클릭 시 play bar가 이동하던 버그 수정 (2026-07-23)

> **재빌드: renderer([ui-tracks.jsx](ui-tracks.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 요청(2026-07-23): Audio input 트랙 좌측상단의 take 개수 버튼("2 takes")을 누르면 play bar가 클릭 지점으로 원치 않게 이동. take 개수 버튼을 누를 때는 play bar에 영향이 없어야 함.

### 원인
- take 배지 버튼([ui-tracks.jsx](ui-tracks.jsx))은 `onClick`에서 `e.stopPropagation()`을 했으나, play bar seek는 **부모 레인 div의 `onMouseDown`**(`laneClick` → `onSeek(sec)`)에서 발생한다.
- `mousedown`은 `click`보다 먼저 발생하므로, 버튼을 누르는 순간 mousedown이 부모 레인으로 버블링되어 이미 seek가 실행된다. 그 뒤에 오는 `onClick`의 `stopPropagation`은 지나간 mousedown을 취소할 수 없어 무력.

### 수정 내용
- [ui-tracks.jsx](ui-tracks.jsx): take 배지 버튼에 `onMouseDown={(e) => e.stopPropagation()}` 추가 → mousedown이 부모 레인의 seek(및 트랙 선택) 핸들러에 도달하지 못하게 차단. 버튼의 toggle 동작(`onClick`)은 그대로 유지. 부수효과로 배지 클릭이 트랙 선택을 바꾸지 않게 되는데, 토글 버튼 성격상 바람직.
- [version.js](version.js)·[package.json](package.json)·[ui-help.jsx](ui-help.jsx)(릴리즈 노트 range·fix 항목) 버전 `1.43.23`.

### 검증
- `node scripts/build-renderers.js` 빌드 통과. 정적 분석으로 mousedown 버블링 차단 확인. ⚠️ T-1.43.23-1 사용자 시험 대기.

---

## v1.43.22 — 메뉴 핸들러 stale 클로저 수정 (저장된 프로젝트에서 메뉴 Save가 Save As처럼 동작) (2026-07-23)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** T-1.43.18-3(미저장 프로젝트 안내) 시험 중 사용자가 보고: 저장된 프로젝트를 File▸Open으로 열고 아무 편집 없이 **메뉴 ▸ Save Project**를 누르면 Save As처럼 저장 다이얼로그가 뜨고, 터미널에 `[collect] failed ... Invalid source audio path`가 찍혔다. Clean Up Unused Recordings도 "먼저 저장" 안내가 계속 떴다. **키보드 Ctrl+S는 정상**이라 "키보드와 메뉴가 다르게 동작"으로 좁혀졌다.

### 원인
- `App`이 렌더 시점에 `const H = handlersRef.current`로 메뉴 핸들러 객체를 캡처했다. 그런데 `Studio`가 핸들러를 등록하는 `useEffect`는 **렌더 커밋 이후** 실행되고, ref 갱신(`handlersRef.current = h`)은 리렌더를 유발하지 않는다. 그래서 프로젝트 로드로 `saveProject`가 새 `projectPath`를 담아 재생성돼도, 렌더에서 캡처된 `H`는 **한 세대 뒤처진 초기 마운트 `saveProject`(projectPath=null, projectName="untitled")**를 가리켰다.
- 키보드 Ctrl+S는 keydown `useEffect` deps에 `saveProject`가 있어 클로저가 최신이라 정상. **메뉴만 stale** → 저장된 프로젝트인데 `projectPath=null`로 동작해 Save As 다이얼로그 + collect가 상대 source 경로를 null 기준으로 풀어 `Invalid source audio path` 실패, Clean Up 게이팅(`!projectPath`)도 오작동.
- 사용자 로그로 확정: `[SaveDBG] saveProject ... projectPath="…dfghjkl.focus"`(Ctrl+S, 정상) 직후 `[SaveDBG] saveProject ... projectPath=null projectName="untitled"`(메뉴, stale) 2회 호출.

### 수정 내용
- [app.jsx](app.jsx): `const H = handlersRef;`로 바꾸고 모든 MenuBar 핸들러를 **호출 시점에** `H.current.onXxx`로 ref를 읽도록 변경 → 메뉴가 키보드와 동일하게 항상 최신 핸들러 사용. (신규 메뉴 항목 추가 시에도 이 call-time-ref 패턴 유지.)
- [app.jsx](app.jsx) `collectProjectAudioForSave`: 상대 source 경로를 `projectPath || targetPath` 기준으로 해석(방어) — `projectPath` 상태가 순간적으로 비어도 저장 대상 폴더 기준으로 풀린다.
- 버전 `1.43.22`.

### 검증
- `node scripts/build-renderers.js` 전체 빌드 통과. 사용자 재확인: File▸Open → 메뉴 Save Project 시 다이얼로그 없이 제자리 저장, Clean Up 정상. 상세 메모리 [[menu-handlers-stale-ref]].

---

## v1.43.21 — 출력 장치 사용 불가 시 기본 장치 자동 폴백 + 테마 경고 (2026-07-22)

> **재빌드: renderer([audio-bridge.js](audio-bridge.js) 플레인 로드 + [app.jsx](app.jsx) esbuild) + version metadata.** **엔진·네이티브 무변경.** 사용자 요청(2026-07-22): 저장된 출력 장치가 사라지면(예: USB 스피커 분리) 앱이 조용히 무음이 되던 문제 — ① 기본 장치로 **자동 폴백**, ② **앱 테마 경고창**(윈도우 기본 창 아님)으로 알림.

### 배경 (진단)
- 증상 = **전 트랙 무음인데 레벨 미터는 움직임** → 신호는 엔진을 통과하는데 출력 장치만 죽음. 원인은 localStorage `focusdaw-audio-device`에 저장된 출력 장치(당시 분리된 Jabra SPEAK 510 USB)를 네이티브가 열지 못한 것. 기존 `resetSavedAudioDeviceOnLaunchFailure`는 **저장 키만 지우고**(다음 실행부터 기본) 현재 세션 폴백·사용자 알림이 없었다. 상세 진단은 메모리 [[silent-output-disconnected-device]].

### 수정 내용
- [audio-bridge.js](audio-bridge.js) `resetSavedAudioDeviceOnLaunchFailure`:
  - 저장 장치 이름을 지우기 전에 캡처.
  - **현재 세션 폴백**: `LocalDAW.setOutputDevice("")`(웹) + native면 `setAudioDevice("","")`(빈 type/name = 플랫폼 기본 출력) 즉시 재적용 → 재시작 없이 소리 복구.
  - **UI 알림**: `window.dispatchEvent("focusdaw-output-device-fallback", {name})` (기존 `audioInputLost` 패턴 재사용).
  - 트리거: 시작 시 저장 장치 재적용 실패(`audioDeviceChanged`/`audioInputChanged` ok:false, `startupDeviceReapply`)에서 호출되는 기존 choke point 그대로 사용.
- [app.jsx](app.jsx): `focusdaw-output-device-fallback` 리스너 추가 → **앱 테마 모달**(`showAppNotice`, 컬러 테마 CSS 변수 사용, 네이티브 alert 아님)로 "저장된 출력 장치 '(이름)'을 열 수 없어 기본 출력 장치로 전환했습니다. Settings ▸ Device Setup에서 다시 선택하세요." 표시.
- 버전 `1.43.21`.

### 한계
- **장치 분리/모드 거부**(native가 열기 실패로 보고)는 감지·폴백·경고된다. 그러나 **장치는 열리지만 소리가 안 나는 경우**(예: 스피커 없는 HDMI 모니터를 정상 오픈)는 native가 실패로 보고하지 않아 자동 감지 대상이 아니다(사용자가 Settings에서 변경).

### 검증
- `node --check audio-bridge.js` 통과, `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`).
- ⚠️ 실 DAW 시험: 출력 장치를 분리한 채 앱 실행 → 기본 장치로 소리 나고 테마 경고창이 뜨는지.

---

## v1.43.20 — 클립별 볼륨(clip gain) 라인 UI 신설 (2026-07-22)

> **재빌드: renderer([audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js) 플레인 로드 + [ui-tracks.jsx](ui-tracks.jsx)·[app.jsx](app.jsx) esbuild) + version metadata.** **네이티브 무변경.** 사용자 요청(2026-07-22): Audio In/Bounce 트랙의 나눠진 클립별로 볼륨을 조절. 클립(라운드 사각형) 위의 **수평 라인을 드래그**하는 방식. **v1.43.20은 시험 대기.**

### 결정 — 이전 "구현 안 함"을 뒤집음
- 2026-07-20에 "clip 단위 volume UI → 트랙 automation으로 대체(구현 안 함)"으로 종결했으나, 사용자 요청으로 **클립 gain 라인 UI를 실제 구현**한다. 역할 분담: **클립별 볼륨 = clip gain 라인**(신규) / 트랙 전체 볼륨 곡선 = 트랙 automation(기존) / 트랙 출력 게인 = 트랙 volume 슬라이더(기존).
- 동작 = **컷 전용(단순)**(사용자 선택): 라인 상단 = 0 dB(unity·최대), 아래로 내릴수록 감쇠, 하단 = 음소거(-∞). 부스트 없음.

### 구현 (엔진 배관은 이미 존재 — `clip.gain`)
- [audio-engine.js](audio-engine.js) `setClipGain(trackId, clipId, gain)`: `clip.gain`을 [0,1]로 클램프해 설정하고 `_ensureBaked`. **gain≠1이면 `_isTrivialLayout`이 non-trivial로 판정 → bake**되어 재생·Export·피크·네이티브 WAV에 모두 반영. `clip.gain`은 정적(baked), `clip.params.volume`/automation은 동적 커브로 **이중 적용 방지 설계**(기존).
- [audio-bridge.js](audio-bridge.js) `setClipGain` 래퍼: 웹 엔진 적용 후 native면 `syncTrackToNative`.
- [app.jsx](app.jsx) `handleSetClipGain`: 드래그당 **undo 1개**(변화 없으면 `cancelUndo`). 두 `TrackRow` 렌더 지점에 `onSetClipGain` 전달.
- [ui-tracks.jsx](ui-tracks.jsx): 클립 박스 안에 **드래그 가능한 수평 볼륨 라인**(select 모드, editable 트랙). `startClipDrag`에 세로 드래그 `gain` 모드 추가 — 드래그 중에는 ghost 라인 + dB 라벨만 표시하고 mouse-up에 1회 커밋(move/trim과 동일 granularity, 매 mousemove bake 방지). 라인은 **선택된 클립 + 이미 감쇠된 클립**에 표시(trim 핸들 피해 좌우 8px inset).

### 검증
- `node --check`(engine·bridge) 통과, `node scripts/build-renderers.js` 전체 빌드 통과.
- ⚠️ **실 DAW 시험 대기**: 클립 라인 드래그로 볼륨 감쇠 → 파형 축소 + 재생/Export 반영, 저장/재열기 후 유지, undo/redo.

---

## v1.43.19 — temp 위치를 Documents로 확정 + 시험 통과 확정 빌드 (2026-07-22)

> **재빌드: Electron main([electron/main.js](electron/main.js)) + version metadata([package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)).** **렌더러·엔진 무변경.** v1.43.18에서 개발한 Clean Up Unused Recordings + temp 수명주기를 사용자 시험으로 확인하고, temp 위치를 사용자 결정대로 `Documents\FocusDAW\Temp\`로 확정한 버전 확정 빌드.

### 사용자 시험 확인 (2026-07-22)
- **미저장 산출물 = temp**: 녹음/bounce/consolidate가 stem 폴더가 아니라 `Documents\FocusDAW\Temp\` 아래에 생성됨(stem 폴더 깨끗 유지). ✅
- **저장 없이 종료→재실행**: 녹음/bounce 트랙 정상 복원, 터미널 에러 없음(dedup/heal + 시작 스크래치 정리가 카테고리 하위폴더 보존). ✅
- **저장 시 temp 원본 삭제**: `<Project> Audio\`로 수집 후 temp 원본 영구 삭제(`fs.unlinkSync`). **휴지통 미경유는 의도된 동작** — 수집된 사본이 프로젝트 폴더에 있어 복구 대상 아님. (휴지통 이동은 Clean Up Unused Recordings 전용, `shell.trashItem`.) ✅

### 수정 내용
- [electron/main.js](electron/main.js) `appTempDir()` 위치를 `%TEMP%/temporary/` → **`Documents\FocusDAW\Temp\`**(`app.getPath('documents')`)로 변경. `assertTempWavPath`/`isUnderTempDir`를 공용 `isInside(parent,child)`로 재작성해 **appTempDir OR os.tmpdir** 둘 다 허용(네이티브 os.tmpdir export 경로 계속 통과).
- 버전 `1.43.19`.

### 검증
- `node --check electron/main.js` 통과.

---

## v1.43.18 — Clean Up Unused Recordings + temp 자동 정리 + temp 폴더 그룹화 (2026-07-22)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + Electron main/preload([electron/main.js](electron/main.js), [electron/preload.js](electron/preload.js)) + version metadata([package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)).** **엔진·네이티브 무변경.** 사용자 요청(2026-07-22): Phase 7 잔여 "Clean Up Unused Recordings" 정리 도구 + 작업 중 생성된 temp WAV 자동 정리, 그리고 temp 폴더를 `%TEMP%/temporary/` 아래로 그룹화(식별 용이). 시험은 이후 진행.

### 정책 결정 (사용자, 2026-07-22)
- **삭제 방식**: OS 휴지통 이동(`shell.trashItem`, 복구 가능).
- **Undo 참조 파일**: 현재 undo/redo 스택 스냅샷이 참조하는 WAV는 **정리 대상에서 제외**(되돌리면 되살아나므로).
- **temp 자동 정리 시점**: **저장 시 수집된 temp 원본만** 삭제(앱 시작/종료 정리는 하지 않음).

### 수정 내용
- **Clean Up Unused Recordings** (Project 메뉴)
  - [electron/main.js](electron/main.js) `scan-unused-recordings` IPC: `<Project> Audio/{Recordings,Bounces,Consolidated}/`의 `.wav`/`.wav.part` 중 참조되지 않은 파일을 스캔(삭제는 안 함). Windows 대소문자·구분자 정규화로 참조 파일 오탐 방지.
  - `trash-files` IPC: 확장자 검증 후 `shell.trashItem`으로 휴지통 이동. `{trashed, failed}` 반환.
  - [app.jsx](app.jsx): `collectReferencedPaths`가 **현재 트랙 + 모든 undo/redo 스냅샷 소스**의 절대경로를 모아 스캔에 전달(undo로 되살아날 파일 보호). 결과를 앱 테마 모달(파일명·카테고리·용량·총량, Cancel / Move to Recycle Bin)로 표시. 미저장 프로젝트는 "먼저 저장" 안내.
- **저장 시 temp 원본 자동 삭제**
  - [electron/main.js](electron/main.js) `collect-project-audio`: 파일을 프로젝트 폴더로 복사한 뒤 **원본이 OS temp 안(`isUnderTempDir`)일 때만** 삭제. 사용자 실제 파일(import 스템의 Bounces, Save As 시 이전 프로젝트 Audio 폴더)은 보존.
- **temp 폴더를 찾기 쉬운 고정 위치로 이동 + 미저장 산출물 temp 강제**
  - [electron/main.js](electron/main.js) `appTempDir()` 헬퍼 신설. **위치 = `Documents\FocusDAW\Temp\`**(사용자 결정 2026-07-22: `%TEMP%`가 탐색기로 접근 번거로워 문서 하위 고정 위치로). Recordings/Consolidated/Bounces temp fallback과 모든 per-op export/decode 중간물(`tempFilePath`·mp3·tempo)을 이 아래로 이동.
  - `appTempDir()`가 더 이상 `os.tmpdir()` 하위가 아니므로 `assertTempWavPath`(보안 검증)와 `isUnderTempDir`(수집 후 삭제 판정)를 **appTempDir OR os.tmpdir 둘 다 허용**으로 갱신(공용 `isInside(parent,child)` 헬퍼). 네이티브 엔진이 export를 os.tmpdir에 바로 쓰는 경로도 계속 통과.
  - ⚠️ 이전 초기 빌드가 쓰던 `%TEMP%/temporary/`는 이 변경 후 방치됨 — 미저장 프로젝트를 저장하면 오디오가 `<Project> Audio/`로 수집되고, 남은 `%TEMP%/temporary/`는 사용자가 1회 수동 삭제하면 됨.
  - ⚠️ **초회 시험에서 드러난 버그 수정 ①**: `save-bounce-audio`/`save-consolidated-audio`/`prepare-recording-path`가 **미저장 상태에서 import 스템의 `sourcePath` 폴더 옆**에 `Recordings`/`Bounces`/`Consolidated`를 만들던 분기를 제거했다. 이 파일들은 temp가 아니라 스템 폴더에 남아 저장 후에도 `isUnderTempDir` 판정에서 빠져 정리되지 않았다. 이제 **저장 여부만으로 결정**: `projectPath` 있으면 `<Project> Audio/<cat>/`, 없으면 `%TEMP%/temporary/FocusDAW <cat>/`. → 미저장 산출물은 항상 temp에 스테이징되고, 저장 시 collect가 프로젝트 폴더로 옮기며 temp 원본을 삭제한다. (bounce도 저장 시 스템 폴더가 아니라 `<Project> Audio/Bounces`로 통일.)
  - ⚠️ **초회 시험에서 드러난 버그 수정 ② (재열기 시 temp 경로 `read-audio-file` ENOENT)**: Audio In 트랙의 **primary source와 Take source가 같은 녹음 WAV 하나를 공유**하는데, `collect-project-audio`가 첫 소스를 복사하며 **temp 원본을 삭제**해 두 번째 소스 수집이 `missing`으로 실패, `sources[1].filePath`가 temp 경로로 굳었다(재열기 시 그 temp를 읽다 ENOENT). **수정**: collect가 **물리 파일당 한 번만 수집하고 그 파일을 참조하는 모든 소스를 같은 대상 경로로 매핑**(정규화 경로 dedup `Map`). + **heal**: 소스 파일이 없어도 같은 이름 파일이 대상 카테고리 폴더에 이미 있으면 채택 → **v1.43.18 초기 빌드로 잘못 저장된 프로젝트도 재저장 시 자동 치유**. ([app.jsx](app.jsx) `setSourcePath`는 primary일 때 `track.filePath`도 갱신하므로 primary는 원래 정상이었다.)
  - **세션 스크래치 자동 정리** (`cleanupTempScratch`): `%TEMP%/temporary/` **루트에 직접 쌓이는 loose 파일**(baked 네이티브 WAV `write-temp-audio` + 디코드/Export/mp3 중간물)을 **시작 시**(이전 세션 잔여 제거) + **종료 시**(엔진 정지 후 이번 세션분 제거) 삭제. 이 파일들은 `_nativePath`(비직렬화)라 저장/autosave 어디에도 참조되지 않아 안전. **`FocusDAW Recordings/Bounces/Consolidated` 하위폴더는 보존**(미저장 프로젝트 autosave 복원이 참조할 수 있음). 열린 파일(살아있는 엔진의 baked WAV)은 unlink 실패로 자동 skip → 다음 시작 때 정리.
- 버전 `1.43.18`.

### 검증
- `node --check electron/main.js`·`electron/preload.js` 통과.
- `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`).
- ⚠️ **실 DAW 시험 대기**: ① 미저장 녹음→저장 시 temp 원본 소거 + 프로젝트 폴더에만 잔존, ② Take Flatten 후 Clean Up 목록/휴지통 이동 + undo 참조 파일 제외 확인, ③ temp 산출물이 `%TEMP%/temporary/` 아래에 생성되는지.

---

## v1.43.17 — 업데이트 시험용 빌드 (2026-07-21)

> **재빌드: version metadata([package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)) + renderer build + Windows installer.** **기능 변경 없음.** 사용자 요청(2026-07-21): 업데이트 시험에 사용할 상위 버전 빌드를 만든다.

### 수정 내용
- [package.json](package.json), [package-lock.json](package-lock.json)
  - 앱/패키지 버전을 `1.43.17`로 올렸다.
- [version.js](version.js)
  - 런타임 UI 표시 버전을 `1.43.17`로 올렸다.

### 검증
- `node scripts/build-renderers.js` 통과.
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- `npx electron-builder --win --publish never --config.directories.output="dist\release-1.43.17"` 통과. `FocusDAW Studio Setup 1.43.17.exe`, `.blockmap`, `latest.yml` 생성 확인.

---

## v1.43.16 — 업데이트 UI 보완 (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + shell CSS([studio.html](studio.html)) + version metadata([package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)).** **엔진 무변경.** 사용자 요청(2026-07-21): update bottom bar 메시지 스타일을 낮추고 링크를 제거하며, Check for Updates 창에는 내부 오류 원문이 노출되지 않도록 한다.

### 수정 내용
- [app.jsx](app.jsx)
  - bottom bar update ticker를 클릭 가능한 GitHub 링크 버튼에서 순수 상태 표시 요소로 변경했다.
  - ticker 문구에서 `click to open GitHub Release`를 제거했다.
  - `Check for Updates`에서 업데이트 가능 상태만 `available`로 표시하고, 그 외 updater 미사용/미패키징/404/latest.yml 없음/네트워크 오류/다운로드 실패 등 사용자에게 노출되는 모든 실패 상태는 `FocusDAW Studio is up to date.`로 정규화했다.
  - 긴 GitHub HTML/HTTP header/error stack이 모달 본문에 표시되지 않도록 `error` 표시 분기를 제거했다.
- [electron/main.js](electron/main.js)
  - updater 오류, 미패키징 실행, 중복 확인 요청을 renderer에 `current` 상태로 전달해 내부 오류 원문이 UI에 도달하지 않도록 보강했다.
- [studio.html](studio.html)
  - bottom bar update ticker의 테두리와 hover 배경을 제거했다.
  - ticker 폰트를 bold에서 regular weight로 변경했다.
- [package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)
  - 앱 버전을 `1.43.16`으로 올렸다.

### 검증
- `node scripts/build-renderers.js` 통과.
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- `npx electron-builder --win --publish never --config.directories.output="dist\release-1.43.16"` 통과. `FocusDAW Studio Setup 1.43.16.exe`, `.blockmap`, `latest.yml` 생성 확인.

---

## v1.43.15 — 업데이트 시험용 빌드 (2026-07-21)

> **재빌드: version metadata([package.json](package.json), [package-lock.json](package-lock.json), [version.js](version.js)) + renderer build + Windows installer.** **기능 변경 없음.** 사용자 요청(2026-07-21): `electron-updater` 업데이트 경로 시험을 위해 현재 구현과 동일한 코드의 상위 버전 빌드를 만든다.

### 수정 내용
- [package.json](package.json), [package-lock.json](package-lock.json)
  - 앱/패키지 버전을 `1.43.15`로 올렸다.
- [version.js](version.js)
  - 런타임 UI 표시 버전을 `1.43.15`로 올렸다.

### 검증
- `node scripts/build-renderers.js` 통과.
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- `npx electron-builder --win --publish never --config.directories.output="dist\release-1.43.15"` 통과. `FocusDAW Studio Setup 1.43.15.exe`, `.blockmap`, `latest.yml` 생성 확인.

---

## v1.43.14 — electron-updater 다운로드/설치 연동 (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + Electron preload/main([electron/preload.js](electron/preload.js), [electron/main.js](electron/main.js)) + dependency([package.json](package.json), [package-lock.json](package-lock.json)).** **엔진 무변경.** 사용자 요청(2026-07-21): GitHub Release 확인 UI에 `electron-updater`를 구현한다.

### 구현 기준
- electron-builder 공식 Auto Update 흐름은 `electron-updater`가 활성화하고, builder가 게시한 `latest.yml`/blockmap/installer metadata를 사용한다.
- v1.43.12에서 이미 GitHub publish 설정과 Windows Release workflow를 추가했으므로, 이번 단계는 패키징된 앱이 해당 metadata를 읽어 update check/download/install을 수행하도록 main process를 연결한다.

### 수정 내용
- [package.json](package.json), [package-lock.json](package-lock.json)
  - `electron-updater` 의존성을 추가했다.
- [electron/main.js](electron/main.js)
  - `autoUpdater`를 require하고 `autoDownload=false`, `autoInstallOnAppQuit=false`로 설정했다.
  - updater 이벤트를 renderer로 전달한다: `checking`, `available`, `current`, `downloading`, `downloaded`, `error`.
  - IPC 추가:
    - `updater-check`: 패키징된 앱에서 `autoUpdater.checkForUpdates()` 실행
    - `updater-download`: `autoUpdater.downloadUpdate()` 실행
    - `updater-install`: 다운로드 완료 후 `autoUpdater.quitAndInstall(false, true)` 실행
  - `npm start` 개발 실행에서는 다운로드/설치를 시도하지 않고, 패키징된 앱에서 확인하라는 메시지를 보낸다.
- [electron/preload.js](electron/preload.js)
  - `checkForUpdates`, `downloadUpdate`, `installUpdate`, `onUpdaterState` API를 renderer에 노출했다.
- [app.jsx](app.jsx)
  - renderer의 직접 GitHub latest release API 호출을 제거하고, `electron-updater` 상태 이벤트 기반 UI로 변경했다.
  - Help > Check for Updates:
    - 조회 중: checking modal
    - 업데이트 있음: 현재/최신 버전 표시, `OK`로 다운로드 시작, `Cancel`로 닫기
    - 다운로드 중: 진행률 progress 표시
    - 다운로드 완료: `OK`로 재시작 후 설치, `Cancel`로 닫기
    - 최신 또는 오류: `OK`만 표시
  - 앱 실행 시 자동 확인도 `electron-updater` IPC로 실행한다. 새 버전이 있으면 기존 bottom bar ticker를 표시한다.

### 검증
- `npm install electron-updater --save` 완료. 전이 의존성 audit 경고 3건은 별도 보안 정리 대상으로 남김.
- `node scripts/build-renderers.js` 통과.
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- `npx electron-builder --win --publish never --config.directories.output="dist\release-1.43.14"` 통과. `FocusDAW Studio Setup 1.43.14.exe`, `.blockmap`, `latest.yml` 생성 확인.

---

## v1.43.13 — GitHub Release 업데이트 확인 UI (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + shell CSS([studio.html](studio.html)) + Electron preload/main([electron/preload.js](electron/preload.js), [electron/main.js](electron/main.js)).** **엔진 무변경.** 사용자 요청(2026-07-21): 앱 실행 시 또는 Help > Check for Updates 메뉴에서 GitHub 최신 Release를 확인하고, 업데이트 가능 여부를 앱 UI로 표시한다.

### 수정 내용
- [app.jsx](app.jsx)
  - GitHub latest release API(`https://api.github.com/repos/limsunglyong/FocusDAW-Studio/releases/latest`) 조회 함수를 추가했다.
  - `v` prefix를 허용하는 버전 파서/비교 함수를 추가했다.
  - Help 메뉴에 **Check for Updates** 항목을 추가했다.
  - 메뉴에서 실행하면 앱 테마 모달을 표시한다.
    - 조회 중: `Checking GitHub Releases...`
    - 최신 Release가 현재 앱보다 높음: 현재 버전/최신 버전을 표시하고 `OK`/`Cancel` 표시. `OK`는 GitHub Release 페이지를 연다.
    - 최신이 아님: 현재 앱이 최신이라는 메시지와 `OK`만 표시.
    - 네트워크 실패 또는 아직 Release 없음: 실패 메시지와 `OK`만 표시.
  - 앱 실행 후 `startupReady`가 되면 조용히 최신 Release를 확인한다.
  - 새 Release가 있으면 bottom bar 우측 `FocusDAW Studio` 글자 왼쪽에 오른쪽에서 왼쪽으로 흐르는 업데이트 배너를 표시한다. 배너 클릭 시 GitHub Release 페이지를 연다.
- [studio.html](studio.html)
  - `.update-ticker`, `.update-ticker-text`, `@keyframes updateTickerMove`를 추가해 하단 흐름 메시지 UI를 구현했다.
- [electron/main.js](electron/main.js), [electron/preload.js](electron/preload.js)
  - GitHub Release 페이지를 기본 브라우저로 열기 위한 `open-external-url` IPC를 추가했다.
  - `https://github.com/...` URL만 허용한다.

### 검증
- `node scripts/build-renderers.js` 통과.
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- GitHub latest release API 직접 확인 결과 현재 저장소에 아직 Release가 없어 404 응답. 첫 GitHub Release 게시 후 최신 버전 조회가 활성화된다.

---

## v1.43.12 — GitHub Releases Windows 배포 1단계 (2026-07-21)

> **재빌드 영향: packaging/CI 설정([package.json](package.json), [.github/workflows/release-windows.yml](.github/workflows/release-windows.yml)).** **앱 런타임·엔진·UI 동작 무변경.** 사용자 결정(2026-07-21): GitHub를 통해 앱을 배포하고 업데이트할 수 있도록 먼저 Windows GitHub Releases 배포 기반을 만든다.

### 현재 저장소 기준
- git remote: `https://github.com/limsunglyong/FocusDAW-Studio.git`
- 우선 범위: Windows NSIS installer를 GitHub Release에 업로드.
- 자동 업데이트 앱 내 UI/다운로드(`electron-updater`)는 다음 단계에서 진행한다.

### 수정 내용
- [package.json](package.json)
  - `repository`를 GitHub remote와 맞췄다.
  - `build.publish`에 GitHub provider/owner/repo를 추가했다.
  - `release:win` 스크립트를 추가했다: `npm run build:renderers && electron-builder --win --publish always`
- [.github/workflows/release-windows.yml](.github/workflows/release-windows.yml)
  - `v*` 태그 push 또는 수동 `workflow_dispatch`로 실행.
  - `windows-latest`에서 `npm ci` 후 `npm run release:win`.
  - `GITHUB_TOKEN`을 `GH_TOKEN`으로 전달해 electron-builder가 GitHub Release에 artifact를 업로드한다.
  - `permissions.contents: write`를 명시해 release 생성/업로드 권한을 확보했다.

### 사용 방법
1. 릴리스할 버전으로 `package.json`과 [version.js](version.js)를 맞춘다.
2. 변경사항을 commit한다.
3. 태그를 만든다: `git tag v1.43.12`
4. 태그를 push한다: `git push origin v1.43.12`
5. GitHub Actions의 `Release Windows` workflow가 Windows installer와 update metadata를 GitHub Releases에 게시한다.

### 검증
- `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package ok')"` 통과.
- `node scripts/build-renderers.js` 통과.
- `npm run dist:win`은 기존 `dist\win-unpacked\resources\app.asar` 파일 잠금(`EBUSY`)으로 로컬 기본 출력 위치에서 실패했다. 설정 오류가 아니라 이전 산출물/실행 파일 잠금으로 판단.
- 같은 설정을 임시 출력 폴더로 분리해 `npx electron-builder --win --publish never --config.directories.output="%TEMP%\focusdaw-dist-verify"` 실행 → NSIS installer 및 block map 생성까지 통과.

---

## v1.43.11 — 트랙 이름과 녹음 파일명 분리 정책 (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + Electron preload/main([electron/preload.js](electron/preload.js), [electron/main.js](electron/main.js)).** **엔진 무변경.** 사용자 질문/결정(2026-07-21): 트랙 이름 변경 시 파일 이름까지 따라 바꿀 필요는 없고, 트랙 이름과 파일 참조를 데이터 모델로 매칭하면 충분하므로 자동 WAV rename 정책을 제거한다.

### 이전 정책 참조
- [앱개발-아카이브.md](앱개발-아카이브.md) `Audio In 트랙 이름 변경 + WAV 파일명 변경 (수정요청 3)` 항목에는 다음 정책이 기록되어 있었다.
  - Audio In 트랙이 녹음 WAV를 소유한 경우, 트랙 이름 변경 시 디스크의 WAV 파일도 같은 이름으로 rename한다.
  - `track.filePath/fileName` 및 `sources[0].filePath/fileName`을 갱신한다.
  - Electron IPC `rename-recording`을 추가하고, 실패하면 트랙 이름 변경은 유지하되 경고한다.
- 이 정책은 Save As 오디오 수집/상대경로 정책(v1.43.0) 이후 위험이 커졌다. 수집된 녹음 파일은 `.focus` 기준 상대경로로 저장되는데, native 파일 rename IPC는 절대경로만 받기 때문에 트랙 이름 변경 직후 `Invalid recording path` 경고가 발생할 수 있었다.

### 새 정책
- **트랙 이름 변경은 UI/프로젝트 메타데이터만 변경한다.**
- 실제 WAV 파일명과 디스크 경로는 녹음 생성/수집 당시의 값을 유지한다.
- 매칭은 기존 데이터 모델로 수행한다.
  - 표시 이름: `track.name`
  - 대표 소스 경로/파일명: `track.filePath`, `track.fileName`
  - Take/Source별 참조: `track.sources[].filePath`, `track.sources[].fileName`
- 따라서 `Audio In 1`을 `보컬1`로 바꿔도 파일명은 그대로 둘 수 있고, 프로젝트 저장/재열기에서는 `track.name`과 `filePath`가 함께 저장되므로 트랙과 오디오 소스 매칭은 유지된다.
- 파일명을 실제로 정리해야 하는 경우는 향후 별도 명시 기능(`Clean Up Unused Recordings` 또는 프로젝트 오디오 정리 기능)에서 다룬다.

### 수정 내용
- [app.jsx](app.jsx) `renameTrack`에서 `window.electronAPI.renameRecording(...)` 호출과 실패 알림을 제거했다.
- [electron/preload.js](electron/preload.js)에서 `renameRecording` API 노출을 제거했다.
- [electron/main.js](electron/main.js)에서 `rename-recording` IPC handler를 제거했다.
- v1.43.9에서 앱 테마 모달로 교체했던 `Recording file rename failed` 알림은 이 정책 변경으로 더 이상 발생하지 않는다.

### 검증
- `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js` 포함).
- `node --check electron/main.js`, `node --check electron/preload.js` 통과.
- 코드 범위에서 `renameRecording`/`rename-recording` 잔존 호출 없음 확인(문서 참조만 유지).

---

## v1.43.10 — Audio Input 게인 표시 색상 보정 (UI 세부 수정) (2026-07-21)

> **재빌드: renderer([ui-tracks.jsx](ui-tracks.jsx) esbuild) + shell CSS([studio.html](studio.html)).** **엔진·네이티브 무변경.** 사용자 요청(2026-07-21): Audio Input Track Header의 Input slider gain 표시 글자가 Classical Ivory 및 Sage Mist 테마에서 어두운 배지 위에 어두운 글자로 보여 읽기 어려우므로, 해당 테마에서 밝은 계통 폰트 색상을 사용.

### 수정 대상
- **대상 UI**: Audio Input Track Header의 입력 슬라이더 왼쪽 gain 값 표시 배지(`fmtDb(inputGainValue)`).
- **문제 원인**: 배지 배경은 `var(--panel-deep, #171512)` 계열의 어두운 색인데, 글자색은 공통 `var(--cream-2)`를 사용했다. Classical Ivory와 Sage Mist는 밝은 테마라 `--cream-2`가 어두운 텍스트 색상으로 재정의되어 배지 배경과 대비가 낮았다.

### 수정 내용
- [studio.html](studio.html)에 `--input-gain-readout-fg` 테마 변수를 추가했다.
  - 기본값: `var(--cream-2)`로 기존 어두운 테마 동작 유지
  - Classical Ivory: `#efe6d4`
  - Sage Mist: `#eef8f2`
- [ui-tracks.jsx](ui-tracks.jsx)의 gain 표시 배지 글자색을 `var(--input-gain-readout-fg, var(--cream-2))`로 변경했다.
- 수정 범위는 요청된 두 테마의 Audio Input Track Header readout에 한정했고, 슬라이더/미터/포트 선택 UI는 변경하지 않았다.

### 검증
- `node scripts/build-renderers.js` 전체 빌드 통과(`built build\ui-tracks.js`, `built build\app.js` 포함).

---

## v1.43.9 — 기본 알림창을 앱 테마 모달로 교체 (UI 세부 수정) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx), [ui-dialogs.jsx](ui-dialogs.jsx) esbuild).** **엔진·네이티브 파일 대화상자 무변경.** 사용자 요청(2026-07-21): 파일 저장/로드와 관련된 OS 기본 창은 제외하고, 앱 UI에 맞춰야 할 내부 알림창만 앱 테마 창으로 변경. 오늘 UI 세부 수정은 별도 시험 항목을 작성하지 않음.

### 조사 기준
- **제외 유지**: `dialog.showOpenDialog`, `dialog.showSaveDialog`, 파일/폴더 선택용 `<input type="file">`은 운영체제 파일 저장/로드 흐름이므로 기본 OS 창을 유지했다.
- **전환 대상**: 사용자가 앱 안에서 작업 결과, 거부 사유, 실패 사유를 확인하는 `alert()` 계열 창. 원본은 사용자 확인 전까지 흐름을 막는 modal 알림이므로 앱 테마 모달로 구분했다.
- **최종 확인**: renderer JS/JSX 범위에서 `window.alert`, `alert`, `window.confirm`, `confirm`, `window.prompt`, `prompt` 호출이 남아 있지 않도록 정리했다.

### 수정 내용
- [app.jsx](app.jsx)에 공통 앱 알림 상태 `appNotice`와 `showAppNotice(title, message, tone)`을 추가했다.
- `appNotice`는 기존 `Flatten Comp`, `Missing audio` 모달과 같은 앱 테마 골격을 사용한다.
  - fixed overlay + blur backdrop
  - `var(--bg)`, `var(--line-strong)`, `var(--surface2)`, `var(--cream-2)`, `var(--amber)`, `var(--danger)` 등 테마 CSS 변수 사용
  - 정보/오류 tone에 따라 아이콘과 강조색을 분리
  - OK 버튼으로 닫는 modal 알림 동작 유지
- [app.jsx](app.jsx)의 다음 기본 알림을 `showAppNotice`로 교체했다.
  - Merge Tracks 실패 알림
  - Merge/Consolidate 거부 및 소스 로드 실패 알림
  - Flatten 거부 및 소스 로드 실패 알림
  - 녹음 기능 사용 불가, 녹음 시작 실패, 녹음 종료 처리 실패, 녹음 파일 이름 변경 실패 알림
  - Audio input 장치 사용 불가 알림
  - 동시에 하나의 Audio In 트랙만 녹음 가능하다는 안내
- [ui-dialogs.jsx](ui-dialogs.jsx)의 Settings 창 내부 `Reset Position` 완료 알림을 `settingsNotice` 테마 모달로 교체했다.
  - Settings 원본 창 위에서만 뜨는 내부 modal 알림으로 유지
  - 앱 테마 변수와 기존 Settings 버튼 계열 스타일을 사용
- v1.43.8의 `Flatten Comp` 확인창 교체와 함께, 앱 내부에서 확인/알림 목적으로 뜨던 Windows 기본 폼은 모두 앱 테마 창으로 통일했다.

### 검증
- `rg`로 JS/JSX의 기본 `alert/confirm/prompt` 호출 잔존 없음 확인.
- `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`, `built build\ui-dialogs.js` 포함).

---

## v1.43.8 — Flatten Comp 확인창을 앱 테마 모달로 교체 (UI 세부 수정) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild).** **엔진·네이티브 무변경.** 오늘 UI 세부 수정 항목 1번. 별도 시험 항목은 작성하지 않음(사용자 요청: 오늘 UI 세부 수정은 별도 요청 없으면 시험.md 생략).

### 수정 대상

- **대상 UI**: Take Lane 하단의 **Flatten Comp** 버튼 클릭 시 뜨는 확인 창.
- **기존 문제**: [app.jsx](app.jsx) `handleFlattenComp`가 `window.confirm(...)`을 사용해 Windows 기본 확인 창을 띄웠다. 이 창은 앱의 컬러 테마, 모달 골격, 버튼 스타일을 따르지 않아 FocusDAW UI 안에서 이질적으로 보였다.

### 수정 내용

- `window.confirm`을 제거하고, 앱 내부 상태 `flattenConfirm`으로 제어되는 **테마 모달**을 추가했다.
- 모달 골격은 기존 `Delete all tracks`/`Missing audio` 모달과 같은 패턴을 따른다.
  - fixed overlay + blur backdrop
  - `var(--bg)`, `var(--line-strong)`, `var(--surface2)`, `var(--cream-2)`, `var(--amber)` 등 테마 CSS 변수 사용
  - 앱 버튼 스타일에 맞춘 `Cancel`/`Flatten` 버튼
- Flatten 실행 로직은 `commitFlattenComp(trackId)`로 분리했다.
  - 버튼 클릭 → 검증(`canFlattenComp`) → `flattenConfirm` 표시
  - 모달의 `Flatten` 클릭 → 기존 `pushUndo`/`DAW.flattenComp`/`persistConsolidated` 흐름 실행
  - 모달 바깥 클릭 또는 `Cancel` → 아무 변경 없이 닫힘
- 안내 문구는 기존 기본 확인 창과 같은 의미를 유지한다.
  - active take와 shared audio가 ordinary clips가 됨
  - drop되는 다른 take 개수 표시
  - Undo로 복원 가능하며 WAV 파일은 디스크에 남음

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`).

---

## v1.43.7 — 일반 clip 이동 시 비활성 Take clip이 함께 움직이던 문제 수정 (사용자 보고 버그) (2026-07-21)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드) + renderer 번들.** **UI·네이티브 무변경.** 사용자 보고(2026-07-21): Audio In 트랙에 일반 clip 3개가 있고, 3번 clip에 punch-repeat로 Take A/B/C/D 4개를 만든 뒤 Take B가 active인 상태에서 1번 또는 2번 clip을 이동하면 **비활성 Take A/C/D가 같이 이동**했다. 3번 active Take B clip을 이동할 때 A/B/C/D가 함께 이동하는 것은 정상. T-1.43.7-1 사용자 시험 통과.

### 원인 — take sibling 이동 보정이 모든 clip 이동에 무조건 걸렸다

Stage 4/Comp 흐름에서 active Take clip을 움직이면 inactive Take clip도 같은 delta로 따라 움직여야 한다. 이를 위해 [audio-engine.js](audio-engine.js)의 `moveClip`/`moveClipsByResolved`/`moveClipsBy`/`nudgeClip`이 이동 후 `_shiftInactiveTakeClips(t, delta)`를 호출했다.

문제는 `_shiftInactiveTakeClips`가 **이동한 대상 clip이 active Take clip인지 확인하지 않고**, 현재 active take가 아닌 **모든 take clip**을 이동시켰다는 점이다. UI는 [ui-tracks.jsx](ui-tracks.jsx)에서 `takeId=null` 일반 clip과 active take clip만 메인 레인에 표시하므로 사용자는 1번/2번 일반 clip만 움직였지만, 엔진 커밋 단계에서 비활성 Take A/C/D가 추가로 밀렸다.

### 수정 — 이동 대상에 active Take clip이 포함된 경우에만 sibling Take를 이동

- `_shiftInactiveTakeClips(track, delta, movedClipIds)`로 호출 계약을 좁혔다.
- 함수 내부에서 `movedClipIds` 중 **현재 active takeId를 가진 clip이 실제로 있는 경우에만** inactive take clip을 이동한다.
- 일반/base clip(`takeId=null`) 이동은 sibling Take 이동을 트리거하지 않는다.
- 만약 다중 선택/미래 경로에서 이미 이동 대상에 inactive take clip이 들어온 경우를 대비해, 보조 이동 루프는 `movedClipIds`에 포함된 clip을 다시 움직이지 않게 했다.
- 적용 경로: `moveClip`, `nudgeClip`, `moveClipsBy`, `moveClipsByResolved`. 따라서 마우스 단일 드래그, 다중 선택 이동, 키보드 nudge 모두 같은 판정을 쓴다.

### 기대 동작

- 1번/2번 같은 일반 clip 이동: 해당 clip만 이동하고 Take A/B/C/D는 제자리 유지.
- 3번 active Take B clip 이동: Take B는 직접 이동, Take A/C/D는 같은 delta로 이동해 take 묶음 정렬 유지.
- 다중 선택 이동에서 active Take B가 포함되지 않으면 inactive Take가 따라 움직이지 않고, active Take B가 포함되면 take 묶음이 함께 이동.

### 검증

`node --check audio-engine.js` 통과, `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`). ⚠️ 실제 DAW 조작 확인은 T-1.43.7-1(사용자) — 사용자가 보고한 1번/2번/3번 clip 이동 케이스를 동일 프로젝트에서 재시험.

---

## v1.43.6 — 소스 로드 실패 시 테마 모달로 알림 (사용자 요청) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild).** **엔진·네이티브 무변경.** 사용자 요청(2026-07-21): 앱 시작·프로젝트 open(File>Open **또는** 드롭)에서 **소스를 못 찾으면 사용자에게 modal로 알릴 것**, 그리고 그 창은 **컬러 테마·앱 윈도우 UI와 동일한 form**일 것(네이티브 alert 금지). T-1.43.0-1·1-1·2-1 전부 통과 확인(2026-07-21). T-1.43.6-1 사용자 시험 통과.

### 무엇 — 재연결 실패를 한 곳에서 모아 테마 모달로

부팅 복원·File>Open·드롭 **세 경로 모두 `reconnectProjectAudio`를 거친다**(공통 choke point). 종전에는 읽기 실패가 `console.warn`으로만 남고 트랙은 조용히 "NO AUDIO"였다. 이제:

- `reconnectProjectAudio`가 primary/extra 루프의 **읽기 실패를 `failures[]`에 수집**(`{name, filePath}`)하고, 실행 끝에 `setMissingAudio({items})`.
- 로드 시작 시 `setMissingAudio(null)`로 **이전 모달을 클리어**(프로젝트마다 새로 판정).
- **`MissingAudioDialog`**: `confirmDeleteAll`과 동일한 모달 골격(오버레이 + `var(--bg)`/`var(--line)`/`var(--surface2)`/`var(--cream-2)`/`var(--amber)` 등 **테마 CSS 변수만 사용** → 10종 컬러 테마 자동 반영). 못 찾은 소스 목록(이름 + 경로)을 스크롤 박스로 보여주고 OK로 닫는다. 네이티브 `window.alert` 아님.

세 경로가 모두 같은 함수를 지나므로 **한 번의 구현으로 시작·File open·드롭이 전부 커버**된다.

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`), 번들에 모달 반영 확인. ✅ **T-1.43.6-1 사용자 시험 통과** — File>Open·드롭·부팅 복원 경로에서 소스 누락 모달과 테마 반영 정상 확인.

---

## v1.43.5 — 네이티브 엔진이 bounce WAV를 상대경로로 찾다 실패하던 로그 (사용자 보고) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드).** **네이티브 무변경.** 사용자 T-1.43.2-1(2026-07-21, v1.43.4): 오디오는 정상 로드되나 재시작 시 **터미널에 `[AudioEngine Err] File not found: CCC_untitled Audio/Bounces/….wav`**(bounce 트랙 2개). 재생은 됨. 시험 대기 1건([시험.md](시험.md) T-1.43.2-1 로그 확인).

### 원인 — 렌더러는 절대경로로 읽지만, 네이티브에는 상대경로를 넘겼다

수집 프로젝트의 소스 `filePath`는 **상대경로**(이식성). 렌더러 재연결은 `resolveSourcePath`로 **절대경로**를 만들어 `read-audio-file`로 읽지만, **네이티브 브리지의 `loadTrack`은 `track.filePath`(상대경로)를 그대로** 넘겼다([audio-bridge.js](audio-bridge.js) 2곳). 네이티브 엔진은 **경로로 파일을 열고 cwd가 앱 루트**라 상대경로를 못 찾는다. Audio In 트랙은 baked라 temp WAV(절대경로)로 넘어가 무사했고, **un-edited bounce/파일 트랙만** filePath 직접 경로라 실패했다(재생은 웹 엔진 폴백/baked 경로로 유지돼 사용자에겐 안 들렸을 수 있음).

### 수정 — 재연결 때 절대경로를 트랙에 stamp하고 네이티브에 그걸 넘긴다

- **`reconnectProjectAudio`**([app.jsx](app.jsx)): 해석한 절대경로 `abs`를 `addFileBuffer(..., { absPath: abs })`로 함께 전달.
- **`addFileBuffer`**([audio-engine.js](audio-engine.js)): 재연결 시 `ph._nativePath = options.absPath` **런타임 필드**로 stamp. `exportProject`는 필드 화이트리스트라 **직렬화 안 됨** → 저장 `filePath`는 상대경로 그대로(이식성 유지).
- **브리지 `loadTrack` 2곳**([audio-bridge.js](audio-bridge.js) `switchToNative` 재동기화·`syncTrackToNative`): `filePath: track._nativePath || track.filePath` — 절대(재연결)가 상대를 이긴다. import 스템·in-session 녹음/bounce는 filePath가 이미 절대라 무영향.

### 검증

`node --check`(engine·bridge) OK, `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`). ⚠️ **실제 네이티브 재생·로그 소거는 T-1.43.2-1(사용자)** — 재시작 시 `File not found` 로그가 사라지는지 확인.

---

## v1.43.4 — 재시작 "NO AUDIO"의 진짜 원인: Save As가 방금 저장한 projectPath를 rename으로 지우고 있었다 (사용자 로그로 확정) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild).** **엔진·네이티브 무변경.** 사용자 T-1.43.0-1 재재시험(2026-07-21, v1.43.3): 복제는 여전히 정상, 그러나 **재시작 시 bounce/수집 트랙만 NO AUDIO** 지속 + **터미널에 `read-audio-file: Invalid audio file path`(6회)**. 원본 스템(절대경로)은 정상. "불러오기"로 한 번 열면 이후 재시작은 정상. 시험 대기 1건([시험.md](시험.md) T-1.43.2-1 ①).

### 로그가 원인을 확정했다

`assertFilePath`([electron/main.js](electron/main.js):47)는 **절대경로가 아니면 거부**한다. 부팅 재연결이 **상대경로를 해석하지 못한 채**(base=null) `read-audio-file`에 넘겨 이 에러가 났다. 즉 부팅 시 base(프로젝트 경로)가 비어 있었다. v1.43.3에서 스냅샷의 `projectPath`를 명시로 넘기게 했는데도 실패 → **스냅샷의 `projectPath` 자체가 null**이라는 뜻.

### 진짜 원인 — `renameProject`가 projectPath를 null로 만들고, Save As가 그 순서를 잘못 밟았다

`renameProject`([app.jsx](app.jsx))는 이름을 바꾸면 **`setProjectPath(null)`** 을 한다(이름 변경 = "이제 미저장 프로젝트"라는 의미). 그런데 `saveProject`(Save As)는:

```
1) onProjectPathChange(result.path)  → projectPath = 저장경로   (먼저)
2) onRenameProject(savedName)         → renameProject → projectPath = null  (나중에 덮어씀!)
```

이름이 바뀌는 최초 저장/Save As에서 **2)가 1)을 지워** projectPath 상태가 **null로 끝난다**. 그러면 1.5초 주기 autosave가 `saveRecentProject(name, projectPath=null)`로 **스냅샷의 projectPath를 null로 클로버**한다 → 재시작 시 base=null → 상대경로 미해석 → NO AUDIO. **File>Open(`loadProjectJson`)은 rename을 먼저·pathChange를 나중에 불러 순서가 반대라 멀쩡**했고, 그래서 한 번 열면 이후엔 정상이었다.

### 수정 — 순서를 바로잡는다

`saveProject`에서 **`onProjectPathChange(result.path)`를 `onRenameProject` 뒤로** 옮겼다(File>Open과 동일 순서). 이제 rename이 null로 만든 뒤 pathChange가 저장경로로 되살려, projectPath 상태·autosave 스냅샷이 **모두 올바른 절대경로**로 유지된다.

- v1.43.3의 부팅 재연결 **스냅샷 경로 명시 전달 + 1회 실행 가드는 유지**(방어선). 이제 상태·스냅샷 둘 다 정상이라 어느 쪽으로도 base가 채워진다.

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`). 일반 Save(이름 불변)는 `onRenameProject` 미호출이라 무영향, 최초 저장/Save As만 해당. ⚠️ **실제 재시작 복원은 T-1.43.2-1(사용자)**.

---

## v1.43.3 — 재시작 시 "NO AUDIO": 부팅 재연결이 상대경로 base를 놓치던 문제 (사용자 보고 버그) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild).** **엔진·네이티브 무변경.** 사용자 T-1.43.0-1 재시험(2026-07-21, v1.43.2): 복제 버그는 사라졌으나(그림4 정상), **재시작 직후 화면(그림3)은 기다려도 "NO AUDIO"**. File>Open으로 열면 정상. 시험 대기 1건([시험.md](시험.md) T-1.43.2-1 ①·③에 통합).

### 원인 — 부팅 재연결의 base가 `projectPath` 상태 타이밍에 의존

v1.43.0에서 수집 소스는 **상대경로**라, 재연결이 base(프로젝트 경로)를 알아야 파일을 찾는다. File>Open은 `loadProjectJson`이 여는 경로를 **명시적 인자**로 넘겨(`reconnectProjectAudio(nextPath)`) 항상 정확하다. 반면 **부팅 자동복원**은 `reconnectProjectAudio()`를 **인자 없이** 불러 `base = projectPath` **상태**에 의존했다 — autosave 스냅샷 복원 직후 그 상태가 이 재연결 클로저에 **아직 전파되지 않았으면 base=null**이 되고, 모든 상대경로가 풀리지 않아 트랙이 "NO AUDIO"로 남는다. (v1.43.1·1.43.2에서도 동일 증상 — 이번 변경과 무관한 기존 문제.)

### 수정 — 스냅샷의 projectPath를 명시적으로 넘기고, 부팅 재연결은 1회만

- 부팅 재연결 effect가 **복원된 autosave 스냅샷에서 `projectPath`를 직접 읽어**(`readRecentProjectSnapshot().projectPath`) `reconnectProjectAudio(snapPath)`로 넘긴다. `reconnectProjectAudio`의 `base = pathForOpen || projectPath`라, 명시 경로가 상태 타이밍을 앞선다.
- **`bootReconnectedRef` 가드**로 이 effect가 **딱 한 번만** 실행되게 한다(dep 변화로 재실행돼 v1.43.2 seq 가드가 로딩 중인 재연결을 중단시키는 일 방지).

⚠️ **전제**: 스냅샷의 `projectPath`가 실제로 채워져 있어야 한다(Save As가 `saveRecentProject(…, savedPath)`로 기록). 만약 재시험에서도 부팅만으로 오디오가 안 붙으면, 스냅샷에 경로가 비어 있는 더 깊은 문제이므로 그때 autosave 경로 기록을 손본다.

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`), 번들 반영 확인. ⚠️ **실제 재시작 복원은 T-1.43.2-1(사용자)** — 재시작이라 자동화 불가.

---

## v1.43.2 — 재열기 시 트랙 전체가 복제되던 버그 (동시 재연결 경쟁) (사용자 보고 버그) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)(플레인 로드).** **네이티브 무변경.** 사용자 T-1.43.0-1 시험 중 발견(2026-07-21): 저장 후 앱 재시작→그 상태에서 프로젝트 열기 시 **트랙이 전부 2배로 복제**됨(그림3·4, "옛 사본 + 현재 사본" 패턴). 수집(collect)·상대경로·명시적 열기 자체는 정상(오디오 로드됨). 시험 대기 1건([시험.md](시험.md) T-1.43.2-1).

### 원인 — 두 재연결 루프가 겹치고, `addFileBuffer`가 자리를 못 찾으면 새 트랙을 만든다

부팅 시 [app.jsx](app.jsx) `loadRecentProject`가 **autosave 스냅샷**을 `importProject`로 복원하고, `reconnectProjectAudio`가 각 트랙의 `filePath`로 오디오를 **비동기 재연결**한다. 이 재연결이 **느리게 도는 도중**(WAV를 하나씩 읽음) 사용자가 **File>Open**을 하면, `importProject`가 트랙을 갈아끼우고 **두 번째 재연결**이 시작돼 **두 루프가 동시에** 같은 트랙들을 훑는다.

- `importProject`는 **json의 트랙 id를 보존**한다([audio-engine.js](audio-engine.js) `track.id = td.id`) → 부팅본과 열기본이 **같은 id**의 트랙을 각각 만든다.
- `addFileBuffer`의 재연결 매칭은 **`t.needsAudio && (id 일치 || filePath 일치)`**. 한 루프가 트랙을 재연결해 **`needsAudio=false`**로 만들면, 다른(낡은) 루프의 `addFileBuffer`는 그 트랙을 **못 찾고**(placeholder 없음) → **새 트랙을 추가**한다. 이게 프로젝트 전체를 복제했다.

### 수정 — 재연결 모드에서는 트랙을 만들지 않는다 + 재진입 가드

1. **`addFileBuffer`: `reconnectTrackId`가 있는데 placeholder를 못 찾으면 `return null`**(새 트랙 생성 안 함). 일반 import(reconnectTrackId 없음)는 종전대로 트랙을 추가. **복제의 직접 원인을 차단.**
2. **`reconnectProjectAudio` 재진입 가드**: 매 실행이 티켓(`reconnectSeqRef`)을 받고, **await마다 더 새로운 실행이 시작됐는지 확인해 낡은 루프를 즉시 중단**. 부팅 재연결이 열기 재연결과 겹치지 않게 해, 낡은 루프가 트랙을 건드리지 못하게 한다.

두 수정은 **서로 다른 방어선**이다 — ①은 어떤 경쟁 상황에서도 복제를 막고, ②는 애초에 두 루프가 겹치지 않게 한다.

### ⚠️ 함께 관찰할 것 — 그림2 재시작 시 "NO AUDIO"

autosave 복원 직후 오디오가 안 붙어 보인 것(그림2)은 **재연결이 느리게 진행 중**이었거나(파일은 존재·경로도 정상, 사용자 확인), 부팅 재연결의 base/타이밍 문제일 수 있다. 이번 수정으로 열기 경쟁 복제는 사라지지만, **부팅만으로(열지 않고) 오디오가 끝내 안 붙는지**는 재시험에서 별도로 확인해야 한다(T-1.43.2-1 ③).

### 검증

`node --check audio-engine.js` OK, `node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`), 번들에 가드 반영 확인. ⚠️ **실제 재시작→열기 복원은 T-1.43.2-1(사용자)** — 네이티브 파일 다이얼로그·재시작이라 자동화 불가.

---

## v1.43.1 — Punch 버튼은 Repeat 구간이 있을 때만 켤 수 있다 (사용자 보고 버그) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild).** **엔진·네이티브 무변경.** 사용자 보고(2026-07-21): "Audio In 트랙 1차 녹음 후, **Repeat 구간을 지정하지 않은 채** Punch를 켜고 30초 부근에서 ~3초 녹음 → **1차 녹음 전체가 사라지고** 새 3초만 남았다"(스크린샷 2장). 사용자 결정(2026-07-21): **메시지 창 대신 버튼 자체를 비활성화** · 게이트 조건 = **"Repeat 구간이 있으면"(ON/OFF 무관)** — 단일 Punch(Repeat OFF) 보존을 위해(대안 "Repeat ON일 때만"은 Loop-Punch만 남고 단일 Punch가 UI에서 사라짐). 시험 대기 1건([시험.md](시험.md) T-1.43.1-1).

### 원인 — Punch가 구간 없이는 "일반 녹음"으로 조용히 강등되고, 일반 녹음은 v1.40.2부터 전체 교체다

이 앱의 Punch는 **Repeat 구간(`DAW.loopRange`)을 [in→out] 스팬으로 재사용**한다([app.jsx](app.jsx) `startRecordFlow`). 그래서 punch 판정이 `region`(구간 유효)에 걸려 있다:

```js
const region = !!(lr && (lr.end - lr.start) > 0.05);
const punch  = !!(isPunchOn() && !DAW.repeatPlayEnabled && region);
```

**구간이 없으면 `region=false` → Punch 버튼이 켜져 있어도 `punch`/`loopPunch`가 둘 다 false** → punch 경로를 타지 않고 **일반 `attachRecording`** 으로 흐른다. 그런데 일반 녹음은 **v1.40.2에서 "트랙 전체 교체"로 바뀐 파괴적 경로**다(`track.clips = [새 클립 하나]`). 결과적으로 1차 녹음이 통째로 지워지고 새 클립만 남는다 — 스크린샷과 정확히 일치. Punch 버튼이 **구간 없이도 눌러졌던 것**이 근본 함정이었다.

### 수정 — 버튼을 구간에 게이팅(모달 없음)

- **Punch 버튼 `disabled={!punchRegion}`** — 유효한 Repeat 구간이 없으면 버튼 자체를 비활성화(기존 메트로놈 버튼의 `disabled`+`title` 패턴과 동일). 무효-구간 툴팁을 **"To use Punch, set a Repeat region first — drag one on the timeline."** 로 교체.
- **`MenuTransport` 본문 auto-heal** — `if (!punchRegion && isPunchOn()) setPunchOn(false)`. `useTick()`로 매 프레임 리렌더되므로, 구간을 **나중에 지운 경우**([ui-mixer.jsx](ui-mixer.jsx)의 `setLoopRange(null)`)에도 Punch 플래그가 즉시 꺼진다 → **"구간 없이 Punch ON 고착" 상태가 원천 봉쇄**되어, 녹음이 파괴적 전체 재녹음으로 새지 않는다.
- **Punch 기본값 off** — 이미 그렇다(`focusdaw-punch` 미설정 시 false).
- 버튼 게이트 + auto-heal이 모든 진입을 막으므로 `startRecordFlow`에 별도 record-time 가드(모달)는 두지 않는다.

**게이트 조건은 "구간이 있으면"(Repeat ON/OFF 무관)** 이라 **단일 Punch(Repeat OFF·구간)** 와 **Loop-Punch(Repeat ON·구간)** 가 **둘 다 유지**된다. 일반(전체 교체) 녹음을 원하면 Punch를 끈다(구간이 있어도 버튼은 자유롭게 토글 가능).

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과(`built build\app.js`), 번들에 새 툴팁 반영 확인. ⚠️ **실제 녹음 흐름은 T-1.43.1-1(사용자)** — 마이크 입력이 필요해 자동화 불가.

---

## v1.43.0 — Save As 오디오 수집 + 상대경로 (Phase 7) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [electron/main.js](electron/main.js)·[electron/preload.js](electron/preload.js)·[audio-engine.js](audio-engine.js)(플레인).** **네이티브 무변경.** 사용자 결정(2026-07-21): 수집 범위 = **앱 생성 오디오만**(import 스템은 제자리 참조) · 경로 = **상대경로(이식가능)**. 시험 대기 1건([시험.md](시험.md) T-1.43.0-1).

### 무엇 / 왜

종전 `save-project`는 **JSON만 쓰고 오디오는 복사하지 않았다.** 그래서 ① 미저장 프로젝트의 녹음·consolidation은 temp(`FocusDAW Recordings`/`Consolidated`)에 남아 **OS가 temp를 청소하면 재열기에서 소실**되고, ② `.focus`가 자기완결적이지 않아 **폴더를 옮기면 깨졌다**. Save As가 프로젝트의 앱 생성 오디오를 자기 폴더로 수집하고 경로를 상대로 바꿔 **자기완결·이식가능**하게 만든다.

### 흐름 (경로 선택 → 수집 → JSON → 기록)

`save-project`가 다이얼로그+기록을 한 번에 하던 것을 분리했다. `saveProject`는:
1. **경로 확보** — Save As/최초 저장이면 `choose-project-path`(다이얼로그만, 기록 안 함)로 경로를 먼저 받는다. 기존 저장 프로젝트의 일반 Save는 현재 경로 사용.
2. **수집** — `collectProjectAudioForSave` → `collect-project-audio` IPC가 앱 생성 소스(녹음/Bounce/Consolidated)를 `<Project> Audio/<category>/`로 복사(충돌 시 유니크 이름)하고, 각 소스를 **`.focus` 기준 상대경로**로 되돌려준다. 이미 프로젝트 폴더 안이면 복사 없이 상대경로만(멱등). 실패는 best-effort(그 소스는 옛 경로 유지).
3. **JSON export** — 수집 **후**에 export해 새 상대경로가 담기게 한다.
4. **기록** — `save-project`가 JSON을 쓴다.

- **`DAW.setSourcePath(trackId, sourceId, filePath, fileName)`** 신설 — 수집 결과 경로를 라이브 소스에 반영(+primary면 `track.filePath`도). 로드된 버퍼는 불변, 재열기 메타만 갱신.
- **import 스템은 수집 안 함**(사용자 결정) — `track.kind==="file"`이고 consolidated 아님 → skip. 절대경로 그대로 참조.
- **카테고리 판정**: `kind==="bounce"`→Bounces, consolidated(경로/이름에 "Consolidated")→Consolidated, `kind==="audioIn"`→Recordings.

### 상대경로 재열기 (하위호환)

- `resolveSourcePath(filePath, projectPath)`: **절대경로**(import 스템·구프로젝트)는 그대로, **상대경로**(수집된 파일)는 프로젝트 폴더 기준으로 해석. `read-audio-file`의 `path.resolve`가 혼합 구분자를 정규화.
- ⚠️ **`reconnectProjectAudio(pathForOpen)`** — 여는 프로젝트 경로를 **인자로 명시 전달**한다. open 흐름(`loadProjectJson`)에서 `projectPath` **prop은 아직 리렌더 전**이라, 클로저의 낡은 경로로 상대해석하면 깨진다. 그래서 `nextPath`를 직접 넘긴다.
- 절대경로 분기가 그대로라 **기존 프로젝트(절대경로 저장분)는 무영향**.

### 검증

`node --check`(main/preload/engine) + 전체 빌드 통과 · 격리 부팅 정상(에러 없음). 스탠드얼론 검사: **경로 헬퍼 13검사**(Windows 드라이브/UNC/POSIX 절대 판정·상대해석·`path.resolve` 정규화) + **collect 로직 11검사**(temp→폴더 복사·상대경로·멱등 재저장·Save As 새 폴더·이름 충돌 유니크·원본 불변) 전부 통과. ⚠️ **실제 다이얼로그 Save As·재열기 복원은 T-1.43.0-1(사용자)** — 네이티브 다이얼로그라 자동화 불가.

---

## v1.42.0 — Comp Lane 구성: Take 레인 스와이프 (Phase 7) (2026-07-21)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드).** **네이티브 무변경**(comp는 기존 bake→baked buffer 경로로 흘러 native는 평탄화 결과만 받는다). 사용자 결정(2026-07-21): 조작 방식은 **스와이프(대표 DAW 방식)**. ✅ **T-1.42.0-1 통과**(2026-07-21 → [시험-아카이브.md](시험-아카이브.md)). **Phase 7 "Comp Lane 구성" 완료 — Phase 7의 마지막 comp 조각.**

### 무엇

Take 레인 위에서 **마우스로 구간을 드래그(스와이프)**하면 그 구간이 해당 Take로 comp된다. 구간마다 다른 Take를 골라 하나의 테이크를 합성하는 것(vocal comping). 종전에는 `activeTakeId` **하나**가 트랙 전체에 적용돼 "구간별로 다른 Take"를 담을 그릇이 없었다.

### 모델 — `track.comp` 구간 맵 (비파괴)

`track.comp = [{ start, end, takeId }]` — 정렬·무겹침·gap 없이 take 구간을 타일링하는 세그먼트 목록. 각 구간은 지정된 Take의 오디오를 그 구간에 맞게 **슬라이스**해 재생한다.

- **단일 진실원본 `_renderClips(track)`**: comp가 있으면 base 클립(takeId=null, 항상 렌더) + 각 세그먼트의 Take를 슬라이스한 클립을, 없으면 기존 `_activeClips`(단일 활성 레인)를 반환. **bake·볼륨 커브·Flatten이 모두 이걸 읽으므로** comp가 재생/Export/native/커밋에서 동일하게 나온다.
- **crossfade 공짜**: comp 경계(다른 Take가 맞닿는 지점)는 `_planClipEdges`가 **seam**으로 분류해 v1.38.0 equal-power crossfade를 그대로 탄다. ⚠️ 루프 Take는 **같은 소스**를 `sourceOffset`으로만 구분하는데, seam 판정이 sourceId만이 아니라 **offset 연속성**(`b.srcStart === a.srcStart + a.n`)까지 보므로 A→B 경계(offset이 passDur만큼 점프)가 continuous로 오판되지 않는다.
- **seed + interval-replace**: 첫 스와이프 때 comp를 **현재 활성 Take로 전 구간 시드**해 gap 없이 타일링하고, 이후 스와이프가 구간을 분할·재지정. 전 구간이 한 Take로 collapse되면 comp=null(그 Take를 active로) — 되돌아갈 escape hatch.
- **Flatten 연동**: comp가 있으면 `flattenComp`가 **comp된 오디오**(구간별 Take)를 확정. 없으면 종전처럼 단일 활성 레인. 커밋 후 comp=null·takes=[].
- **hygiene**: `_normalizeTrackLayout`이 매 serialize/undo/import마다 comp를 sanitize — 삭제된 Take를 가리키는 세그먼트는 활성 fallback으로 재지정, 구간 클램프, 한 Take/무 Take로 degenerate 시 null. `setActiveTake`/`deleteTake`는 comp를 폐기(레인 전체 선택이 per-region 선택을 대체).
- **직렬화·undo**: `comp`를 `_addTrack`·serialize 2곳·getSnapshot·applySnapshot·importProject·app.jsx `snapshotSig`에 추가 → 저장/재열기·undo/redo 정합.

### UI — [ui-tracks.jsx](ui-tracks.jsx) `TakeLaneRow`

- **스와이프**: 레인에서 4px 이상 드래그하면 구간 선택 사각형이 뜨고, 놓으면 `setCompRegion`. 4px 미만은 클릭으로 간주해 종전처럼 **Take 활성화**(= 그 Take로 comp 리셋).
- **오버레이**: comp가 있으면 각 레인을 어둡게 덮고, **그 Take가 comp에 기여하는 구간만 밝게**(앰버 하이라이트) 뚫어 한눈에 comp 구성을 보여준다.
- **Flatten 행**: comp 활성 시 `Clear` 버튼 추가(comp 폐기 → 단일 Take), 안내 문구를 스와이프 설명으로 교체.
- 메인 레인은 baked buffer를 그리므로 **comp 결과가 자동 반영**(별도 코드 없음).

### 검증

`node scripts/build-renderers.js` 전체 빌드 통과 · `node --check` 통과. 인터벌 로직(setCompRegion interval-replace + coalesce + renderClips 슬라이스) **스탠드얼론 8검사 통과**(A/B/A 타일링·재지정 merge·전구간 collapse·클램프·슬라이스 sourceOffset·base 보존). ✅ **실제 comp 재생/Export/재열기/Flatten/Take삭제 경계까지 T-1.42.0-1 통과**(2026-07-21, 사용자) — 판정 기준 ①~⑦ 전부 정상.

---

## v1.41.4 — 성능 계측 계장: `[perf bake]` 로그 (2026-07-20)

> **재빌드: 불필요** — [audio-engine.js](audio-engine.js)는 플레인 로드(esbuild 대상 아님), 네이티브 무변경. 시험 대기 2건([시험.md](시험.md) T-1.41.4-1·2).

### 왜

"향후 개발 후보 · 우선순위 높음"에 **전략 B bake의 편집 반응 속도 계측**과 **30분+ 장시간 녹음 실측**이 올라 있었는데, **둘 다 실측 기록이 없어 최적화가 필요한지조차 판단할 수 없는 상태**였다. 계측 수단이 없으면 시험이 "느낌상 느린가"가 되므로, 먼저 숫자를 볼 수 있게 만들었다.

### 수정

`_ensureBaked`에 **bake / peaks / total 3구간 타이밍**을 넣고 한 줄로 출력한다:

```
[perf bake] <트랙명> len=63.2s clips=2 bake=18.4ms peaks=25.1ms total=47.3ms
```

- **`bake`와 `peaks`를 분리한 것이 핵심.** 어느 쪽이 지배적이냐가 최적화 방향을 가른다 — `peaks`가 크면 **피크 계산 Web Worker 이전**(작업량 작음), `bake`가 크면 **부분 재렌더**(전략 B 전제를 건드리는 큰 작업)다. 합계만 재면 이 판단을 못 한다.
- `len`(트랙 전체 길이)을 함께 찍어 **비용이 트랙 길이에 비례**한다는 전략 B의 특성을 수치로 확인할 수 있게 했다. 정비례를 벗어나 튀는 지점이 있으면 별도 병목이다.
- 편집은 사용자 조작(드래그 종료 등)마다 1회이므로 로그가 넘치지 않는다.

⚠️ **배포 빌드에서는 콘솔을 열 수 없다** — [electron/main.js](electron/main.js) `devTools: !app.isPackaged`. 계측은 반드시 `npm start`로 한다.

### 검증

`node --check audio-engine.js` 통과. **계측 코드는 값을 읽기만 하므로 동작에 영향이 없다**(무해함은 T-1.41.4-1 판정 기준 ⑥으로 확인). 수치 자체는 사용자 시험으로 확보한다.

---

## v1.41.3 — Export 리미터: 룩어헤드 8ms + 릴리즈 글라이드 (2026-07-20)

> **재빌드: ⚠️ 네이티브 필수** — [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) → 증분 빌드 → `bin/` 교체 완료. 사용자 결정(2026-07-20): **룩어헤드/릴리즈까지만 개선하고 마스터링급 리미터는 추후 과제**. ✅ **시험 통과**(2026-07-20, T-1.41.3-1·2 → [시험-아카이브.md](시험-아카이브.md)) — 노이즈 개선 확인, **Export 속도도 빨라짐**, Export 전반 무회귀.

### 배경 — v1.41.2로 하모닉 왜곡은 사라졌고, 남은 것은 압축 아티팩트

사용자 실측(같은 곡, crest 17.4 dB, 이펙트 전부 OFF):

| 목표 | Crest Δ | LRA Δ | High >4kHz Δ | 청감 |
|---|---|---|---|---|
| −14 | −2.7 | −0.1 | −0.1 | 깨끗 |
| −12 | −4.2 | −0.6 | −0.1 | 미세하게 들림 |
| −9 | −5.9 | −1.7 | −0.1 | 확실히 들림 |

**High >4kHz가 세 경우 모두 −0.1** → v1.41.2에서 클리퍼를 경로에서 빼낸 것으로 **하모닉 왜곡은 종결**(수정 전 −9는 +2.5였다). 남은 노이즈는 성격이 다르다 — **LRA가 함께 무너지는 것**이 단서로, 리미터가 피크를 넘어 **음악의 다이내믹 자체**를 건드리는 구간에서 들린다. 가청 경계는 **crest 감소 −3 dB / LRA −0.3 부근**.

### 수정

- **룩어헤드 2ms → 8ms.** 게인 램프가 룩어헤드 구간에 걸쳐 있으므로, 2ms는 킥 한 주기(55Hz=18ms)보다 훨씬 짧아 **저역 파형 자체를 변형**시켰다. 8ms는 킥 반 주기 수준으로 마스터링 리미터의 통상 범위다. 샘플당 게인 이동이 **1.136% → 0.284%** 로 완만해졌다.
- **릴리즈 50ms → 80ms + 게인에 60ms 글라이드.** 글라이드는 **회복할 때만** 적용하고 **감쇠는 즉시** 반영하므로 천장은 그대로 보장된다.
- **피크 검출을 단조 deque 슬라이딩 최대값으로 교체.** 종전에는 샘플마다 버퍼 전체를 스캔했다(8ms면 샘플당 ~700회 비교가 될 뻔했다). 이제 O(1) amortised라 **룩어헤드를 4배로 늘리고도 종전보다 빠르다**.

### 검증

증분 빌드 성공 → `bin/` 교체 → 격리 프로필 부팅 정상. **시뮬레이션 6검사 통과**(소스가 기대 형태인지 정규식으로 먼저 확인):

- **deque 슬라이딩 최대값이 브루트포스와 132,300샘플 전부 일치** — off-by-one이 있으면 피크 검출이 조용히 깨지므로 매 샘플 대조했다
- 천장 −1.000 dBFS 유지 · 게인 변화가 램프 상한 이하 · 무음 뒤 게인 1.0 복귀 · **임계 아래 신호는 게인 편차 0**(Normalize OFF 경로 무회귀)

✅ **사용자 시험 결과(2026-07-20)** — 노이즈가 **어느 정도 개선**됐고, **Export 소요 시간이 빨라졌다**(O(1) 피크 검출 효과가 룩어헤드 4배 증가를 상쇄하고도 남았다). Export 전반 무회귀도 정상. **−9를 깨끗하게 만들지는 못한다** — 아래 한계 참조(추후 과제).

---

## v1.41.2 — 라우드니스 메이크업 게인이 소프트 클리퍼를 때리고 있었다 (2026-07-20)

> **재빌드: ⚠️ 네이티브 필수** — [AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) 수정 → 증분 빌드 → `bin/` 교체 완료. **renderer·main.js 무변경.** 시험 대기 1건([시험.md](시험.md) T-1.41.2-1).

### 사용자 제공 실측이 원인을 확정했다

사용자가 같은 곡을 Normalize OFF / −9·−12·−14 LUFS로 Export해 비교 자료를 제공했다(이펙트 **전부 OFF**). 그 파일들을 직접 분석한 결과:

| | peak dBFS | RMS dBFS | **crest dB** | >13.2kHz(동일 RMS) |
|---|---|---|---|---|
| Normalize OFF | +1.25 | −16.83 | **18.08** | −69.2 |
| −14 LUFS | +2.03 | −12.41 | 14.43 | −69.1 |
| −9 LUFS | +2.08 | −8.69 | **10.77** | −67.3 |

**crest가 7.3 dB 붕괴**했다. 산술이 정확히 들어맞는다 — 원본 피크 −1.3 dBFS(0.861)에 메이크업 +8.4 dB를 곱하면 **2.26**인데, 마스터 FX의 마지막 단인 **소프트 클리퍼가 1.0에서 포화**한다(`applySoftClipping`, knee 0.9, tanh, **바이패스 없음**). 즉 **−7.1 dB가 클리퍼 몫**이고, 이펙트가 전부 꺼져 있었으므로 체인의 **유일한 비선형**이었다.

잔차 분석(정규화본을 OFF본에 레벨·정렬 맞춰 뺀 것)에서도 −9는 잔차가 신호 대비 **−15.1 dB**로 매우 컸고 **광대역**이었다 — 고역 하모닉보다 **다이내믹 압착**이 지배적이라는 뜻으로, 클리퍼가 매 타격을 깎는 그림과 일치한다.

### 원인 — 체인 순서

```
tracks → masterGainSource(★메이크업 게인) → MasterEffects(… → 소프트 클리퍼) → 리미터 → WAV
```

라우드니스 정규화는 **마스터 체인 뒤**에 와야 하는데 **앞**에 있었다. 게인을 올릴수록 클리퍼를 세게 때렸다.

### 수정

- `masterGainSource`는 **마스터 볼륨만** 유지(PASS 1이 측정한 그대로).
- **메이크업 게인은 기록 루프에서 FX 뒤·리미터 앞**에 곱한다.
- `targetGain` 초기값을 `masterVolume` → **1.0**으로. 둘을 분리했으므로 그대로 두면 마스터 볼륨이 **두 번** 곱해진다.

부수 효과로 **목표 라우드니스 정확도도 개선**된다 — 종전에는 PASS 1을 게인 1.0으로 측정하고 PASS 2에서 비선형 단 앞에 게인을 넣어 출력이 측정값×게인이 아니었다.

### 검증

증분 빌드 성공 → `bin/` 교체 → 격리 프로필 부팅 정상. ⚠️ **청감은 T-1.41.2-1**(사용자).

⚠️ **물리적 한계는 남는다**: crest 18 dB 소재를 −9 LUFS·−1 dBTP로 만들려면 **7~8 dB의 다이내믹 압축이 불가피**하다. 이번 수정의 요지는 "압축을 없앤다"가 아니라, 그 일을 **memoryless tanh 웨이브셰이퍼 대신 룩어헤드 리미터가 하게 만든다**는 것이다.

### 조사 기록 — 헛다리 두 번 (재발 방지)

1. **v1.41.0**: ffmpeg loudnorm 체인을 고쳤으나 **네이티브 Export에서는 실행되지 않는 경로**였다(폴백·템포 전용). → **어느 코드가 실제로 도는지 먼저 확인할 것.**
2. **v1.41.1**: 리미터 어택 부재를 고쳤다(실제 결함이라 유지). 그러나 사용자 파일은 v1.41.1 적용 후 Export된 것인데도 증상이 남아 **주원인이 아님**이 드러났다.
3. 합성 신호 시뮬레이션은 사용자의 고역 증가를 **재현하지 못했다**(저역 위주 신호 → 광대역으로 바꿔도 미달). **실제 파일 분석으로 전환한 뒤에야** 결론이 났다 — 합성 신호를 계속 조정하는 것은 **확증편향** 위험이 크다. 사용자 산출물이 있으면 그것부터 측정할 것.

---

## v1.41.1 — 네이티브 Export 리미터에 어택이 없던 문제 (부분 원인 · v1.41.2에서 주원인 규명) (2026-07-20)

> **재빌드: ⚠️ 네이티브 필수** — [juce_skeleton/Source/AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp) 수정 후 증분 빌드 → `bin/FocusDAW-AudioEngine.exe` 교체 완료. **renderer·엔진(JS)·main.js 무변경.** 시험 대기 1건([시험.md](시험.md) T-1.41.1-1).

### ⚠️ v1.41.0은 쓰이지 않는 경로를 고쳤다 (정정)

[ui-dialogs.jsx](ui-dialogs.jsx)에서 `DAW.isNative`(일반 데스크톱)이면 Export는 **네이티브 엔진의 renderMix**로 가며 `normalize`·`lufsTarget`을 네이티브에 넘기고 **early return**한다. v1.41.0에서 고친 ffmpeg `processAudioFfmpeg`는 **네이티브 실패 시 폴백**과 **템포 처리** 경로일 뿐, 사용자의 Export에서는 실행되지 않는다.

**교훈: ffmpeg 체인만 측정하고 "원인 확정"이라 단정했다. Export가 어느 코드로 가는지 먼저 확인했어야 했다.** v1.41.0 자체는 폴백·템포 경로의 실질적 개선이라 사용자 결정으로 유지한다.

### 진짜 원인 — 리미터 게인이 한 샘플 만에 점프

[AudioEngine.cpp](juce_skeleton/Source/AudioEngine.cpp)의 Export 리미터:

```cpp
envelope = std::max(envelope * releaseFactor, reduction);   // ← 즉시 점프
```

릴리즈는 있는데 **어택 스무딩이 없다.** 필요한 감쇠량이 나오는 즉시 게인이 **한 샘플 만에** 그 값으로 뛴다 → 파형에 계단(불연속) → **클릭**. 측정: 순간적으로 튀는 트랜지언트(스네어)에서 한 샘플에 **게인 42.2% 급락**.

증상 대조표가 전부 맞는다:

| 관찰 | 설명 |
|---|---|
| 드럼 타격에서 노이즈 | 피크가 임계를 넘는 **매 타격마다** 게인 계단 |
| −9/−12/−14 **동일** | 게인만 충분하면 어느 목표든 피크가 임계 초과 → 계단 발생. **감쇠량이 아니라 "넘었는가"가 조건** |
| Normalize OFF 정상 | `targetGain=1.0` → 피크가 −1 dBFS 아래 → 리미터 미작동 |
| −23 LUFS 정상 | 감쇠 방향 → 임계 아래 |
| 인앱 재생 정상 | 이 리미터는 **Export 전용** |

### 수정 — 룩어헤드 구간에 걸친 램프

```cpp
const float riseStep = 1.0f / (float)limiterLookahead;
...
if (reduction > envelope) envelope = std::min(reduction, envelope + riseStep);
else                      envelope = std::max(reduction, envelope * releaseFactor);
```

새 피크는 룩어헤드(2ms/88샘플) **앞서** 버퍼에 들어오므로, 샘플당 `1/limiterLookahead`씩 올리면 **피크가 출력에 도달하기 전에 게인이 제자리에 닿는다** — 계단이 램프가 된다. 하강은 종전 릴리즈 곡선을 쓰되 `std::max(reduction, …)`로 **천장은 계속 지킨다**.

### 검증

증분 빌드 성공(C4819 코드페이지 경고만, 종전과 동일) → `bin/` 교체 → 격리 프로필 부팅 정상. **구·신 리미터 루프를 그대로 포팅한 시뮬레이션 7검사 통과**(소스에 램프가 실제로 있는지 정규식으로 먼저 확인):

| | 최대 게인 변화/샘플 | 램프보다 빠른 급변 | 출력 피크 |
|---|---|---|---|
| 기존 | **42.18%** | 19회 | −1 dBFS |
| 수정 | **1.14%**(=1/88, 램프 상한) | **0회** | −1 dBFS |

천장 유지·무음 구간 게인 1.0 복귀·**임계 아래 신호 무변화(Normalize OFF 경로)** 도 확인.

⚠️ **시뮬레이션 작성 중 한 번 헛다리를 짚었다**: 첫 시험 신호가 55Hz 사인 킥이라 상승이 완만해 **구·신 결과가 동일**하게 나왔다(계단 미발생). 순간적으로 튀는 트랜지언트를 넣고서야 재현됐다 — **리미터 시험 신호는 반드시 zero-attack 성분을 포함할 것.**

### 남는 것

실제 청감은 T-1.41.1-1(사용자). 리미터 피크 검출이 샘플마다 88샘플 버퍼를 전부 스캔하는 O(N) 구조인 점은 **성능 이슈로 남겨둔다**(이번엔 동작만 최소 변경).

---

## v1.41.0 — Export LUFS Normalize: 음량을 올릴 때 리미터 체인으로 교체 (2026-07-20)

> **재빌드: [electron/main.js](electron/main.js)·[version.js](version.js).** **renderer·엔진·네이티브 무변경.** 사용자 보고(T-1.38.0-3 중 발견): "−9 LUFS Export에서 드럼 쿵쿵대는 부분에 노이즈가 심하다, −14에서도 미묘하다, 인앱 재생은 정상". 사용자 결정: **체인만 수정**(UI 알림은 추가하지 않음). 시험 대기 1건([시험.md](시험.md) T-1.41.0-1).

### 원인 (v1.38.0 조사에서 확정)

`loudnorm`은 **`linear=true`를 필요 게인이 음수일 때만 지킨다.** 게인이 양수면 true peak가 `TP=-1`을 넘기므로 **Dynamic 모드로 폴백**하고, 시간에 따라 게인을 바꾸는 내장 리미터가 **트랜지언트를 찌그러뜨린다.** 요구 게인이 클수록 심해져 −9(+8.5dB) 심함 / −14(+3.5dB) 미묘함과 정확히 일치. 인앱 재생은 loudnorm을 거치지 않아 깨끗했다.

### 수정 — 방향에 따라 도구를 나눈다

- **음량을 낮출 때**(예: −23): 종전 **linear loudnorm 유지** — 이미 정확·투명하다(−23.00 정확, gain swing 0).
- **음량을 올릴 때**: **정적 게인 → look-ahead 리미터 → 측정 → 트림 1회**.
  - `volume=<G>dB, alimiter=limit=<ceil>:attack=5:release=50:level=disabled`
  - 리미터 실링은 목표 TP보다 **0.3 dB 낮게**(`LIMITER_ISP_HEADROOM_DB`) — `alimiter`는 **샘플 피크만** 보고 인터샘플 피크는 못 보므로, 이 여유가 없으면 리샘플 후 −1 dBTP를 넘길 수 있다.
  - 리미팅이 라우드니스를 조금 깎으므로 **측정 후 트림 1회**. 2회 이상은 목표에 더 가까워지지만 **다시 게인을 흔들기 시작**(측정: 3회 반복 시 흔들림 5.66→7.74dB)해서 제거하려던 문제로 돌아간다.

### 측정 결과 (실제 출고 함수 실행)

**gain swing** = 10ms 블록별 (처리후/원본) RMS 비의 5~95 퍼센타일 폭. 0이면 정적 게인(투명), 클수록 게인이 움직임 = 펌핑·왜곡.

| 목표 | 달성(이전→이후) | 목표 대비 | gain swing(이전→이후) |
|---|---|---|---|
| −23 | −23.00 → −23.00 | 0 | 0 → **0** (감쇠, 경로 무변경) |
| −14 | −13.70 → **−14.06** | +0.30 → **−0.06** | 5.64 → **2.30** |
| −9 | **−12.75** → **−9.74** | −3.75 → **−0.74** | **16.92** → **5.66** |

−9에서 게인이 **−1 ~ +15.9 dB로 요동**치던 것이 3배 줄었고, 목표 미달도 3.75 → 0.74 dB로 개선됐다.

### 남는 한계 (버그 아님)

−9 LUFS는 이 소재에서 **−9.74가 한계**다. 크레스트가 큰 믹스를 그 이상 밀면 왜곡이 늘어난다 — 물리적 제약이다. **사용자 결정으로 UI 알림은 넣지 않았으므로**, 목표 미달은 여전히 조용히 발생한다. 확인이 필요하면 `node tools/loudnorm-probe.js <파일.wav>`로 본인 음원의 달성값을 볼 수 있다.

### 검증

`node --check` OK, 격리 프로필 부팅 정상. **출고 함수(`processAudioFfmpeg`)를 헬퍼째 추출해 실제 실행하는 검증 10건 통과** — 감쇠 정확도 / −14·−9 목표 근접 / gain swing 임계 / **전 목표 TP ≤ −1 dBTP 준수** / **tempo 1.25x 동시 적용 시 길이·라우드니스** / loudnorm 미사용 시 무변경. ⚠️ **실제 청감은 T-1.41.0-1**(사용자) — "노이즈가 사라졌는가"는 측정이 아니라 귀로 판정해야 한다.

### 도구

[tools/loudnorm-probe.js](tools/loudnorm-probe.js)를 **출고 체인을 그대로 실행**하도록 교체(종전에는 옛 체인을 재현했다). 목표별 달성 LUFS·TP·gain swing을 표로 출력하며, 사용자 음원으로도 돌릴 수 있다.

---

## v1.40.4 — Repeat/Punch 하이라이트가 컬러 테마를 따르지 않던 문제 (사용자 확인 요청) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-mixer.jsx](ui-mixer.jsx) esbuild).** **엔진·네이티브 무변경.** 사용자 확인 요청: "Repeat ON highlight가 color theme에 맞게 쓰이나요, 고정값이 아닌가요?"

### 확인 결과 — 고정값이 맞다

[studio.html](studio.html)은 **테마 10종**을 정의하고 각 테마가 `--amber`(액센트)를 **완전히 다른 색으로 재정의**한다(Modern Blue `#5b9bd5`, Forest Green `#5de87a`, Cyan `#22d3ee`, Classical Ivory `#bf7f2e` …). 그런데 Repeat 하이라이트는 **기본 테마의 앰버 리터럴 `rgba(232,176,75,…)`** 를 직접 쓰고 있어, 다른 테마에서도 **혼자 노란색으로 남았다.** 같은 컴포넌트 안에서 `var(--amber)`를 쓰는 줄과 섞여 있어 더 눈에 띈다(테두리는 테마를 따르는데 채움만 노랑).

### 수정 — Repeat/Punch 경로를 테마 액센트로

`color-mix(in srgb, var(--amber) N%, transparent)`로 교체(이미 [ui-tracks.jsx](ui-tracks.jsx)·[ui-mixer.jsx](ui-mixer.jsx)에서 34곳 쓰이던 방식). 고친 곳:

- [app.jsx](app.jsx) — 상단 미니 타임라인의 Repeat 구간 오버레이, 타임라인 Repeat 구간 오버레이(채움 + 좌우 dashed 테두리)
- [ui-mixer.jsx](ui-mixer.jsx) — OUTPUT FX 레인 배경, 구간 하이라이트, `PUNCH` 글자 glow, `Repeat ON` 버튼 glow

**Repeat/Punch 경로의 하드코딩 잔여 0건.**

### ⚠️ 남은 작업 — 같은 문제가 앱 전반에 38곳

이 리터럴은 Repeat 말고도 곳곳에 있다. 사용자 요청 범위(Repeat)만 먼저 고쳤고, 나머지는 **시각 확인이 테마별로 필요**하므로 별도로 진행한다.

| 파일 | 잔여 | 대표 위치 |
|---|---|---|
| [ui-tracks.jsx](ui-tracks.jsx) | 12 | 클립 선택/드래그 하이라이트, `N takes` 배지, Take 레인 활성 배경, BPM 소스 glow, 자동화 곡선 fill |
| [app.jsx](app.jsx) | 11 | 카운트인 오버레이, 녹음 표시 glow |
| [ui-mixer.jsx](ui-mixer.jsx) | 10 | EQ 곡선 fill, Solo 배경, 밴드 버튼 활성 |
| [ui-dialogs.jsx](ui-dialogs.jsx) | 4 | 안내 박스, 강조 테두리 |
| [ui-kit.jsx](ui-kit.jsx) | 1 | 토글 glow |

### 검증

esbuild OK, `node --check` OK, 격리 프로필 부팅 정상. **육안 확인 T-1.40.4-1 통과**(2026-07-20, 사용자) → [시험-아카이브.md](시험-아카이브.md). 색상 변경은 시뮬레이션으로 검증할 수 없어 육안 확인에 의존했다.

---

## v1.40.3 — Loop-Take 녹음(Repeat ON·Punch OFF)도 트랙 전체를 교체 (사용자 보고) (2026-07-20)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드)·[version.js](version.js).** **renderer·네이티브 무변경.** 사용자 보고(T-1.40.1-1 시험 중 발견): "Repeat ON·Punch OFF에서 원본 트랙이 사라지지 않고 Take A와 원래 있던 트랙이 섞인다".

### 원인 — 마지막 남은 append 경로

`attachLoopRecording`이 `track.clips = [...track.clips, 새클립]`으로 **덧붙이고 있었다**. 기존 클립이 base(`takeId=null`)면 **모든 레인에서 렌더**되므로 Take A와 함께 소리난다 — v1.40.1·v1.40.2와 **동일한 뿌리**이며, 세 녹음 경로 중 마지막으로 남아 있던 곳이다.

### 수정 — 녹음 경로의 규칙을 하나로 통일

**Punch OFF = "이 트랙을 다시 녹음한다" → 이전 내용은 사라진다.** `attachLoopRecording`이 시작 시 `clips`·`takes`·`activeTakeId`를 비우고, 각 pass를 **녹음 순서대로 Take A·B·C**로 등록한다(사용자 명세).

| 녹음 모드 | 이전 오디오 | Take |
|---|---|---|
| 일반 (Punch OFF·Repeat OFF) | **사라짐**(트랙 전체 교체) | 만들지 않음 |
| Loop-Take (Punch OFF·**Repeat ON**) | **사라짐**(트랙 전체 교체) | pass별 A·B·C |
| 단일 Punch | **구간만 교체**, 원본은 `Original` 레인으로 보존 | 활성 take에 합류 |
| Loop-Punch | **구간만 교체**, 구간 밖 base 유지 | pass별 + 원본 보존 |

**Punch가 "기존 것을 지키는 모드", Punch OFF가 "새로 하는 모드"** 로 갈린다.

### 검증

`node --check` OK, 재빌드 OK, Electron 부팅 렌더러 에러 0. 시뮬레이션에 loop-Take 시나리오 추가(6검사) — 이전 오디오·이전 take가 **모두 사라지고**, pass가 A·B·C 순서로 등록되며, 각 pass가 자기 slice(`sourceOffset` 0·5·10)를 읽고, 레인 전환 시에도 **겹침 0**. 전체 시뮬 7종 **140검사 무회귀**.

---

## v1.40.2 — 일반 재녹음은 트랙 전체를 교체 (사용자 보고) (2026-07-20)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드)·[version.js](version.js).** **renderer·네이티브 무변경.** 사용자 보고: v1.40.1의 재녹음이 **새로 녹음한 구간만 대체**해 기존 클립이 앞뒤로 쪼개져 **클립이 3개**가 됐다(첨부 그림: 30초~1:00 트랙에 40~47초 재녹음 → 3조각).

### 정정 — "이전부터 있던 문제"라는 v1.40.1 서술은 틀렸다

Stage 3a(v1.29.x) 이후 일반 녹음은 Take를 append했지만 **이전 take가 비활성으로 가려져** 사용자에게는 **항상 "전체 교체"로 보이고 그렇게 들렸다.** 겹침이 드러난 것은 **base 클립(`takeId=null`, 항상 렌더)** 이 존재할 때뿐이고, 그건 **Comp Flatten(v1.39.0)** 이 트랙을 base로 만들면서 생긴 새 조건이다. 즉 **증상 자체가 v1.39.0 이후에 생긴 것**이 맞다 — v1.40.1의 "이전부터 존재" 서술을 정정한다.

### 수정 — 구간 대체가 아니라 전체 교체

v1.40.1은 `_sliceActiveAtRegion`으로 **새 녹음이 덮는 구간만** 비웠다. 그래서 기존 오디오가 앞뒤로 남아 3조각이 됐다. 사용자 기대는 **pre-Stage-3a 동작** — 원본 트랙을 지우고 새 녹음만 올린다.

`attachRecording`이 이제 `track.clips = [새 클립 하나]`, `activeTakeId = null`로 두고 `_normalizeTrackLayout`이 참조 없는 take를 전부 정리한다. **되돌리기는 Undo.**

⚠️ **Take 기반 녹음은 그것이 목적인 경로에만 남는다** — Loop-Take(`attachLoopRecording`)·단일 Punch·Loop-Punch. 일반 녹음은 Take를 만들지 않는다.

### 검증

`node --check` OK, 재빌드 OK, Electron 부팅 렌더러 에러 0. 시뮬레이션을 **"클립 1개만 남고 이전 오디오·take가 전부 사라진다"** 로 갱신해 통과(재녹음 시나리오 6검사). 전체 시뮬 7종 **134검사 무회귀**.

---

## v1.40.1 — 재녹음/단일 Punch가 기존 오디오와 겹쳐 소리나던 버그 2건 (사용자 보고) (2026-07-20)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드)·[version.js](version.js).** **renderer·네이티브 무변경.** 사용자 시험 보고(2026-07-20): ①일반 재녹음에서 원본 클립과 Take A가 **겹쳐 그려지고 합산** ②단일 Punch에서 **Take A가 원본에 sum**되고 선택 불가. ③Loop-Punch는 정상.

### 공통 뿌리 — base 클립은 **항상** 렌더된다

`_isActiveClip`은 `takeId === null`인 클립을 **활성 여부와 무관하게 항상 true**로 판정한다(파일/bounce/consolidation 등 레인 공유 오디오를 위한 규칙). 그래서 그 위에 새 녹음 클립이 겹치면 **둘 다 소리난다.** Comp Flatten(v1.39.0)이 트랙 전체를 base 클립으로 만들기 때문에 이 조건에 아주 쉽게 걸린다.

### ① 일반 재녹음이 구간을 비우지 않았다 (⚠️ 아래 "v1.40.0 이전부터 존재" 서술은 v1.40.2에서 정정됨)

`attachRecording`이 `track.clips = [...track.clips, 새클립]`으로 **덧붙이기만** 했다. Stage 3a에서 "녹음은 Take를 append한다"로 바꿀 때, 기존 클립이 **take에 속해 있으면** 비활성으로 가려지므로 문제가 없었는데 — **base 클립은 가려지지 않는다.**

**수정(사용자 결정)**: 일반 재녹음은 **덮어쓰기**로 되돌린다. `_sliceActiveAtRegion`으로 새 녹음이 덮는 구간을 비우고, **새 Take를 만들지 않으며**(현재 활성 레인에 합류, 없으면 base), 되돌리기는 Undo로 한다. "재녹음"은 comp가 아니라 "다시 하기"라는 사용자 판단. ⚠️ **Stage 3a 동작 변경** — 일반 녹음은 더 이상 Take를 쌓지 않는다(Loop-Take·Punch는 각자 take 의미를 유지).

### ② 단일 Punch가 밀려난 조각을 **자기가 합류할 take에** 남겼다 (v1.40.0 결함)

v1.40.0의 `_parkDisplacedAsTakes`는 "이미 take에 속한 조각은 그 take를 유지"한다. 그런데 단일 Punch의 새 클립은 **활성 take에 합류**하므로, 활성 take에서 밀려난 조각이 **같은 take 안에서 punch 클립과 겹쳤다.** Loop-Punch는 pass가 **새 take**라 충돌이 없어 정상이었다(사용자 보고 ③과 일치).

**수정**: `_parkDisplacedAsTakes(..., { separate: true })` 옵션 신설 — 단일 Punch는 밀려난 조각을 **항상 새 "Original" take로 분리**한다. Loop-Punch는 종전대로(take 정체성 보존).

### 검증

`node --check` OK, 재빌드 OK, Electron 부팅 렌더러 에러 0. **신규 시뮬레이션 4시나리오 18검사 통과** — 판정을 "**어느 순간에도 활성 클립이 2개 이상 겹치지 않는다**"(`maxOverlap === 1`)로 두고 보고된 두 경우를 재현·수정 확인. 재녹음 전후 오디오 보존, Punch 후 Original 레인 선택 시 pre-punch 오디오 복귀, take 없는 트랙의 단일 Punch(레인 = Original + Take A), **Loop-Punch 무회귀**까지 포함. 기존 시뮬 6종 전부 무회귀(총 133검사). 실제 녹음 확인은 T-1.40.1-*(사용자).

---

## v1.40.0 — Punch가 원본을 버리지 않고 Take로 보존 (사용자 요청) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)(플레인 로드).** **네이티브 무변경.** 사용자 요청(2026-07-20): "punch-repeat로 여러 take를 녹음한 뒤 Take A·B·C 중 하나만 고를 수 있다 — **원본을 선택할 방법이 필요하다**". 사용자 결정: **단일 Punch에도 함께 적용**. 시험 대기 4건([시험.md](시험.md) T-1.40.0-1~4).

### 원인 — punch가 구간의 기존 클립을 삭제하고 있었다

`attachLoopPunchRecording`·`attachPunchRecording` 양쪽에 `if (cS >= inPt && cE <= outPt) continue;  // fully inside → removed`가 있었다. **오디오 자체는 살아 있다** — 소스는 `track.sources`에 등록된 채고 WAV도 `Recordings/`에 있다. 사라진 것은 **그것을 가리키는 클립**뿐이라, 되살리는 데 재렌더가 필요 없다.

### 수정 — 버리지 않고 take로 park

- 신규 **`_sliceActiveAtRegion(track, inPt, outPt)`**: 활성 클립을 구간 기준으로 잘라 `{untouched, outside, inside}`로 돌려준다. 두 punch 함수가 각자 갖고 있던 split/trim 산술을 **한 곳으로 합쳤다**(inactive take는 그대로 통과). 클립에서 잘린 **첫 조각은 원래 id를 재사용**(선택·Undo 안정), 나머지는 새 id. 시작점이 **이동한** 조각만 `recordedStart`/`recordedOffsetMs`를 비운다(Cal. 배지 오해 방지 — deleteRange와 동일 규칙).
- 신규 **`_parkDisplacedAsTakes(track, inside)`**: 밀려난 구간 오디오를 take에 담는다.
  - 이미 **어떤 take에 속해 있었으면 그 take를 유지** → 종전에는 **활성 take가 통째로 소멸**했다(punch를 두 번 하면 첫 punch의 take가 사라졌다). 이제 살아남는다.
  - **base(공유 오디오)에서 왔으면** `kind: "original"` take를 신설.
- **`_normalizeTrackLayout` 라벨링 분리**: `kind === "original"`은 `Original`(2개째부터 `Original 2`…)로 이름을 유지하고 **A/B/C 카운터를 소비하지 않는다** → 녹음 pass는 계속 Take A, B, C. 라이브 녹음 배지(v1.36.1)가 예측하는 letter와 일치.
- **단일 Punch의 take 없는 트랙**: punch 클립이 base(`takeId=null`, 항상 렌더)가 되면 park한 Original과 **구간에서 동시에 소리난다**. 그래서 이 경우 punch 클립에도 take를 만들어 **둘을 정상적인 대안 관계**로 두고 그것을 활성으로 삼는다.
- [app.jsx](app.jsx) `takeBase`(녹음 중 Take 배지) 갱신: 더 이상 활성 take가 소비되지 않으므로 **기존 take 전부가 앞에 남는다**. `kind === "original"`은 letter를 쓰지 않으므로 카운트에서 제외.

### 사용자에게 보이는 결과

- 일반 녹음(Take A) 후 punch-repeat → **Take A(원본) · Take B · Take C…** — Take A를 고르면 원본으로 돌아간다.
- take가 없던 base 오디오에 punch → **Original · Take A…**
- ⚠️ **소급 적용되지 않는다**: v1.40.0 이전에 punch해서 저장한 트랙은 클립이 이미 지워져 Original 레인이 생기지 않는다(WAV는 남아 있으므로 수동 재임포트는 가능).

### 검증

esbuild OK, `node --check` OK, 전역 충돌 신규 0건, Electron 부팅 렌더러 에러 0. **시뮬레이션 7케이스 22검사 통과**(구간 분할 산술·offset 정확도 / take 소속 유지 vs Original 신설 / 라벨링 `Original, Take A, Take B`·`Original 2` / 완전 포함 클립 / **inactive take 무변경** / 구간 밖 클립 identity 유지). 기존 시뮬 5종(flatten·crossfade·merge·strategy·refusal) **전부 무회귀**. 실제 녹음·선택은 T-1.40.0-*(사용자).

> ⚠️ **구현 중 사고**: 패치 스크립트의 slice 범위 오류로 `attachPunchRecording`에서 **punch 클립을 삽입하는 코드가 통째로 삭제**됐다(녹음이 반영되지 않는 상태). 직후 확인에서 발견·복구했으나, 텍스트 치환으로 함수 본문을 자를 때는 **치환 후 해당 함수 전체를 눈으로 확인**할 것.

---

## v1.39.0 — Phase 7 Stage 4: Comp Flatten (활성 Take 확정) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드).** **네이티브 무변경.** 사용자 요청(2026-07-20): "punch-repeat로 만든 take를 base에 연결해 하나의 waveform으로". 사용자 시험 T-1.39.0-1~5 **전부 통과**(2026-07-20) → [시험-아카이브.md](시험-아카이브.md).

### 개요 — Merge가 거부하던 조작을 "확정"으로 허용

`Merge Clips`는 **base + Take 혼합을 거부**한다(v1.37.x) — take 선택지가 살아 있는 동안 합치면 되돌릴 수 없기 때문이다. Comp Flatten은 **그 선택지를 없애는 것이 목적**이므로 같은 위험이 없다. **레인을 넘어 병합할 수 있는 유일한 조작.**

재료는 기존 것을 그대로 쓴다: `_renderClipsToSource`(N개 병합 + v1.38.0 crossfade) · `persistConsolidatedSources`(`Consolidated/` 기록) · 기존 bake sync.

### 동작 (`flattenComp(trackId)`)

1. **활성 클립 수집** = base(`takeId=null`) + **활성 take**의 클립, 시작 시각 순.
2. **붙어있는 덩어리(run)별로** 병합 — 클립 사이 gap은 **그대로 둔다**(무음을 WAV로 굽지 않고, 빈 구간을 계속 쓸 수 있다). 사용자 확인(2026-07-20): punch-repeat 워크플로에서는 base와 take가 맞닿으므로 **run이 1개 = 결과도 클립 1개**이며, gap은 클립을 지우거나 옮긴 트랙에서만 생긴다.
3. run에 클립이 **1개뿐이면 재렌더 없이** take 태그만 떼어낸다 → 오디오가 **비트 단위로 동일**하게 남는다.
4. `takes = []`, `activeTakeId = null` — 확정. **비활성 take의 클립은 넘겨받지 않는 것이 곧 commit.**

### 안전장치

- **`canFlattenComp`가 모든 활성 클립의 raw를 먼저 검사**한다. `_renderClipsToSource`는 소스를 등록하고 디스크 쓰기를 큐잉하는 **변이 함수**라, 중간에 실패하면 트랙이 반쯤 flatten된 상태로 남는다 — 착수 전에 거부한다(시뮬레이션으로 "거부 시 clips·takes 무변경" 확인).
- `pushUndo` 1회 → **Ctrl+Z로 take 전부 복원**. 거부 시 `cancelUndo(savedRedo)`.
- **녹음 원본 WAV는 삭제하지 않는다** — `Recordings/`에 그대로 남고 프로젝트의 참조만 정리된다.
- 실행 전 확인 대화상자에 **버려질 take 수 + Undo 복원 + 파일 유지**를 명시(단순 "정말?"이 아니라 무엇이 남는지를 알린다).

### UI

Take Lanes를 **펼쳤을 때만** 그 영역 하단에 `Flatten Comp` 버튼 + 설명 줄. take를 실제로 고르는 중에만 보이므로 오조작 위험이 낮다.

### 검증

esbuild OK, `node --check` OK(engine·bridge·built app·ui-tracks), 전역 충돌 신규 0건, Electron 부팅 렌더러 에러 0. **시뮬레이션 4케이스 27검사 통과** — punch comp 형태에서 **활성 take의 오디오가 구간에 들어가고 base가 바깥에 남는지 샘플값으로 확인**, gap 보존(run 분리·디스크 쓰기 1회만), 단일 클립 run 무재렌더, 거부 4종(takes 없음·raw 없음·트랙 없음)과 **거부 시 무변경**. 실제 청감·Undo 복원은 T-1.39.0-*(사용자).

---

## v1.38.0 — Phase 7 Stage 3: 클립 경계 equal-power crossfade (2026-07-20)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드)·[version.js](version.js).** **renderer·네이티브 무변경**(네이티브는 구워진 WAV를 받으므로 자동으로 같은 소리). 사용자 결정(2026-07-20): **고정 10ms**, 설정 UI 없음. 사용자 시험 T-1.38.0-1·2·3 **전부 통과**(2026-07-20) → [시험-아카이브.md](시험-아카이브.md).

### 설계 — 모델 불변, 렌더링만 변경

crossfade에는 경계 **너머의 오디오**가 필요하다. 클립을 트림해도 원본 WAV는 잘리지 않으므로 그 여분(**handle**)이 소스에 남아 있다. 이걸 **굽는 순간에만** 읽어 겹친다 → `clip.start`/`duration`은 **그대로**이므로 **겹침 금지 불변식·Undo 구조·네이티브 동기화가 전부 무영향**이다.

### 경계 3분류 (`_planClipEdges`)

| 경계 | 조건 | 처리 |
|---|---|---|
| **continuous** | 같은 source + offset 이어짐 + gap 없음 (=안 움직인 split) | **페이드 없음** |
| **seam** | 다른 source 또는 offset 불연속 | **equal-power crossfade** (`sin`/`cos`, 10ms) |
| **gap** | 사이가 무음 | 종전 micro-fade |

- **continuous에서 페이드를 없앤 것은 버그 수정이기도 하다** — 나눴지만 움직이지 않은 클립은 오디오가 원래 이어져 있는데도 양쪽에서 4ms씩 볼륨이 꺼졌다 켜지는 **원본에 없던 딥**이 있었다.
- handle이 부족하면 있는 만큼만 짧게(`useTail`+`useHead`), 양쪽 다 없으면 **종전 micro-fade로 폴백** → 회귀 없음.
- fade 길이는 **양쪽 클립 길이의 절반**을 넘지 않게 clamp(짧은 클립이 자기보다 긴 페이드를 먹지 않도록).

### 두 렌더 경로 공용

`_bakeTrackLayout`(재생·Export)와 `_renderClipsToSource`(Merge 렌더)가 **같은 `_planClipEdges` + `_renderDescs`** 를 쓴다 → **편집 중 들리던 소리 그대로 병합된다.** 종전에 각자 페이드를 계산하던 코드는 제거.

⚠️ **`outer` 옵션이 두 경로에서 다르다**: bake는 첫/마지막 가장자리가 타임라인의 **무음과 만나므로 `micro`**, 병합 렌더는 그 가장자리가 **병합 클립 자신의 가장자리**가 되어 bake가 다시 페이드하므로 **`none`**(안 그러면 이중 페이드). 초기 구현에서 이걸 `micro`로 통일했다가 **시뮬레이션이 회귀를 잡았다**(병합 오디오가 0에서 시작·끝남).

### 검증

`node --check` OK, 재빌드 OK, 전역 충돌 신규 0건, Electron 부팅 렌더러 에러 0. 시뮬레이션은 **파일에서 함수를 추출해 실행**하는 방식으로 3종 전부 통과 — crossfade 7케이스 22검사(continuous 평탄·equal-power `sin²+cos²=1`·handle 없음 폴백·head만 있는 경우·gap·짧은 클립 clamp·버퍼 경계), 병합 렌더 8케이스 27검사, 전략 판정 6검사. 실제 청감(경계 클릭/딥 없음)은 T-1.38.0-*(사용자).

---

## v1.37.3 — Join과 Consolidate를 `Merge Clips` 하나로 통합 (사용자 결정) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx) esbuild) + [audio-engine.js](audio-engine.js)(플레인 로드).** **네이티브 무변경.** 사용자 결정(2026-07-20): "사용자 입장에서 Join과 Consolidate가 혼동된다" → 하나로 통합.

### 문제 — 같은 일을 두 이름으로

사용자에게 보이는 결과가 **동일**했다(N개 클립 → 1개). 진짜 차이는 "새 WAV를 쓰는가"인데 그건 내부 구현이고, 게다가 **Join은 소스 비연속이면 조용히 consolidate로 폴백**했다. 즉 이름은 Join인데 하는 일은 Consolidate. 구분할 수 없는 것을 두 명령으로 노출하고 있었다.

### 통합

- **`Merge Clips` 하나**로 노출. 2개 이상 선택 시 메뉴에서 **즉시 병합**(`Merge N clips`), 선택이 없으면 **병합 도구**를 켜서 클릭으로 이웃과 병합(`J` 유지). 두 경로 모두 `handleConsolidateClips` 하나를 타므로 **거부 메시지·Undo·WAV 기록이 동일**.
- 전략은 **엔진이 판단**: 신규 **`_isHealableRun(clips)`** 이 "같은 source + 타임라인 gap 없음 + sourceOffset 연속"(=split 이후 아무도 안 움직인 상태)을 판정해 **heal**(단일 연속 소스 읽기, 파일 생성·재렌더 없음) / **render**(새 WAV) 중 하나를 고른다. `canConsolidateClips`가 `strategy`로 함께 반환.
- **N개로 일반화** — 3조각으로 split한 것도 한 번에 heal. 기존 Join은 2개씩만 가능했다.
- **`joinClips`는 2줄 위임**으로 축소(병합 도구 진입점으로만 유지, 이름은 내부 API·bridge 호환 위해 보존). 2개 전용 헬퍼 **`_consolidateClips`는 삭제**(사용처 소멸).

### 동반 수정 — Undo 스냅샷 정합성 위반

기존 `handleJoin`이 **거부당하는 경우에도 `pushUndo()`를 먼저** 호출해 no-op 스냅샷을 쌓고 있었다(레인·between 가드 도입으로 거부 경로가 늘며 실제 문제가 됨). "Undo 스냅샷 정합성" 노트 위반 — **다음 Ctrl+Z를 먹고 Redo까지 지운다**. 이제 `canConsolidateClips`로 **먼저 검사하고 통과할 때만** 스냅샷을 뜬다.

### 검증

esbuild OK, `node --check` OK, 전역 심볼 충돌 신규 0건, Electron 부팅 렌더러 에러 0. 전략 판정 **시뮬레이션 6검사 통과**(2조각·3조각 split→heal / 이동·트림·복사붙여넣기→render / 연속이어도 base+take면 거부가 우선). 사용자 시험 T-1.37.0-1·2·3 **전부 통과**(2026-07-20) → [시험-아카이브.md](시험-아카이브.md).

---

## v1.37.2 — 선택 구간을 가로지르는 클립이 있으면 병합 거부 (사용자 보고) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)(플레인 로드).** **네이티브 무변경.** 사용자 보고("3개 중 1번과 3번을 consolidate하면 중간 clip이 그대로 남아 있는데 정상인가?").

### 원인 — 정상 아님. 중간 클립이 **밀려나고 있었다**

병합 클립은 `first.start ~ last.end` 전체를 차지하므로 중간 클립의 자리를 덮는다. 그러면 [`_reindexClips`](audio-engine.js)의 "트랙 내 겹침 금지" 불변식이 그 클립을 **오른쪽으로 밀어낸다** — 화면엔 남아 보이지만 **원래 시간 위치를 잃은** 상태이고, 병합 결과 안에서 그 구간은 무음으로 렌더된다. `joinClips`에는 이미 같은 형태를 막는 `between` 가드가 있었는데 consolidate에 넣지 않은 누락.

### 수정 — 판정 분리 + 사유별 안내

- 신규 **`canConsolidateClips(trackId, clipIds)`** 이 `{ok, reason}`을 반환(`few`/`lane`/`between`/`track`). 구간을 가로지르는 **활성** 클립이 있으면 `between`으로 거부(비활성 take 클립·0길이 placeholder는 렌더되지 않으므로 막지 않는다).
- [app.jsx](app.jsx)가 사유별 안내 메시지를 띄운다 — 종전 "same Take lane..." 한 줄로는 무엇이 문제인지 알 수 없었다(T-1.37.0-3에서 사용자가 실제로 확인 요청). `between`은 "중간 클립을 Ctrl+클릭해 포함하거나 인접한 것끼리 나눠서 합치라"고 다음 행동을 알려준다.

### 검증

거부 규칙 **시뮬레이션 5케이스 11검사 통과**(1번+3번→between / 셋 다 선택→ok / 인접 쌍→ok / 비활성 take 무시 / 0길이 placeholder 무시).

---

## v1.37.1 — 저장 → 재열기에서 병합 결과가 사라지던 버그 2건 수정 (사용자 시험 T-1.37.0-1 실패) (2026-07-20)

> **재빌드: [audio-engine.js](audio-engine.js)(플레인 로드)·[version.js](version.js).** **renderer·네이티브 무변경.** 사용자 시험 실패 보고 → 사용자 저장 파일 `untitled_20260720_3.focus`를 직접 열어 **원인 확정**.

### 증거

저장된 프로젝트에서 consolidate 소스의 **`filePath`가 `null`** 이었다 — `Consolidated/` 폴더에는 WAV가 정상 기록돼 있는데도.

```
bounce 트랙  src_5s4bnul7  dur=342.54  filePath = null
Audio In    src_7tp0wyg0  dur=97.22   filePath = null
```

### ① 비동기 경합 — `filePath`가 버려진 객체에 기록됨

`persistConsolidatedSources`가 **`await`(WAV 인코딩+IPC) 전에** source 객체를 잡아두고, 완료 후 그 객체에 `filePath`를 썼다. 그런데 **`_normalizeTrackLayout`은 `track.sources`를 새 복사본 배열로 교체**한다(`[primary, ...slice(1).map(s => ({...s}))]`) — 그리고 이건 serialize/snapshot/import마다 실행되며 **autosave가 1.5초마다** 유발한다. 그 사이에 교체가 일어나면 기록이 **고아 객체**에 남고, 프로젝트에는 `filePath: null`이 저장된다 → 재열기 시 `reconnectProjectAudio`의 hydrate 대상(`s.needsAudio && s.filePath`)에서 빠짐 → raw 없음 → 베이크 스킵 → **파형 없음**(사용자 보고한 Audio In 증상).
**수정**: `await` **이후에 source를 id로 재조회**해서 기록한다.

### ② 재접속 휴리스틱이 병합 클립을 되돌림

[`_assignDecodedToTrack`](audio-engine.js)의 "클립 1개 + `start === 0`이면 파일 전체"라는 판정이, **병합 결과가 정확히 그 모양**이라 직격했다. 그 클립을 **primary 소스로 다시 가리키게 하고 원본 길이로 늘려** 재열기 시 병합 이전 상태로 보였다(사용자 의심대로 **bounce 원본을 불러온 것이 맞다**).
**수정**: 그 클립이 **실제로 primary 소스에 속할 때만** 적용(`!c.sourceId || c.sourceId === primaryId`).

### ③ 동반 — Join이 base 클립을 Take에 흡수

`joinClips`가 `first.takeId || second.takeId`로 태그를 정해, **base 클립(`takeId=null`, 항상 렌더)을 Take 안으로 흡수**했다. Loop-Punch Comp 구간 경계를 걸쳐 join하면 흡수된 base 오디오가 **Take 전환 시 사라진다**. Consolidate는 거부하는데 Join은 조용히 수행하던 **불일치**(사용자 질문으로 발견). Join에도 같은 레인 가드를 적용 — ⚠️ **기존 동작 변경**.

### 한계

**v1.37.1 이전에 병합해 이미 저장한** 클립은 `filePath`가 null로 굳어 소급 복구 불가 — 다시 병합해야 한다(원본 WAV는 `Consolidated/`에 남아 있다).

---

## v1.37.0 — Phase 7 Stage 2: Consolidate Clips (+ Consolidate source 소멸 버그 근본 수정) (2026-07-20)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드) + [electron/main.js](electron/main.js)·[electron/preload.js](electron/preload.js).** **네이티브 무변경**(기존 `loadTrack` 베이크 sync 재사용). 사용자 승인(2026-07-20). ⚠️ 이 버전의 초회 시험에서 **저장/재열기 실패 2건**이 나와 v1.37.1에서 수정했고, 명령 자체는 v1.37.3에서 `Merge Clips`로 통합됐다 — 최종 동작은 위 v1.37.1~3 항목을 함께 볼 것.

### 개요 — "새 WAV로 렌더"가 곧 버그 수정

Phase 7 계획의 `Consolidate Clips`(선택 clip을 새 WAV로 렌더)를 구현하면서, **이월돼 있던 "Consolidate source가 저장/재열기에서 소멸" 버그를 같은 변경으로 해소**했다. 원인은 `_consolidateClips`가 만든 소스가 `filePath: null`(메모리 raw)인데 재열기 복원 경로([app.jsx](app.jsx) `reconnectProjectAudio`)가 **`s.needsAudio && s.filePath`인 소스만** `hydrateSource`로 재로드하는 것 — 경로 없는 소스는 복원 수단이 아예 없어 clip이 primary 오디오로 폴백했다. **소스에 실제 WAV 경로만 생기면 기존 복원 경로가 그대로 살려내므로 재열기 로직은 무변경.**

### ① 디스크 기록 ([electron/main.js](electron/main.js)·[electron/preload.js](electron/preload.js))

- 신규 IPC **`save-consolidated-audio`**(`save-bounce-audio` 패턴) → **`<Project> Audio/Consolidated/`**. Bounce(트랙 합치기 결과)와 Consolidate(클립 편집 확정 결과)는 성격이 달라 폴더를 분리했다(사용자 결정 2026-07-20). 정리 도구가 폴더 단위로 판정하기도 쉽다.
- **미저장 프로젝트 폴백**: 프로젝트 경로가 없으면 첫 트랙 소스 폴더, 그것도 없으면 **temp**(`FocusDAW Consolidated`)에 기록. 경로 없는 소스는 재열기에서 소멸하므로 "저장 전이라 못 쓴다"가 곧 버그 재발이다 — 반드시 어딘가에는 기록한다. temp→프로젝트 폴더 이동은 Phase 7 "Save As 복사 규칙"에서 처리.

### ② 엔진 — N-클립 렌더 + persist 큐 ([audio-engine.js](audio-engine.js))

- `_consolidateClips`(2개 전용)를 **`_renderClipsToSource(track, clips, gaps)`(N개 일반화)** 로 교체하고 기존 함수는 위임만 한다 → **`joinClips` 시그니처·동작 무변경**(비연속 join도 그대로 consolidate 경로).
  - 타임라인 gap은 무음으로 기록해 각 clip이 제자리를 지키고, **이음매마다** micro-fade(양쪽), **바깥 가장자리는 페이드 없음**. `n`(raw가 실제 보유한 길이)과 `dur`(clip 선언 길이)를 분리해 **raw가 짧아도 뒤 clip이 앞으로 밀리지 않는다**.
- 새 소스는 `_pendingConsolidations[]`에 큐잉. 신규 **`async persistConsolidatedSources(saveFn)`** 이 큐를 비우며 `saveFn`으로 WAV를 쓰고 `source.filePath`/`fileName`을 채운다. **best-effort** — 실패하면 메모리 소스로 남아 **v1.37.0 이전과 동일**, 편집 자체는 성립(디스크 문제로 편집이 실패하지 않는다).
- 신규 **`consolidateClips(trackId, clipIds)`**: 선택 clip을 하나로 평탄화. **같은 레인만 허용**. raw 누락 시 `null` 반환하고 **clip을 건드리지 않는다**.
  - ⚠️ 이 가드가 실제로 막는 것은 **"Take A + Take B"가 아니다** — Take Lane 행은 **표시 전용**(클릭=활성화, 선택 가능한 clip rect 없음)이고 메인 레인은 `_activeClips`만 렌더하므로, **서로 다른 Take의 clip을 함께 선택하는 경로 자체가 없다**. 실제 대상은 메인 레인에 나란히 보이는 **base clip(`takeId=null`, 항상 렌더) + 활성 Take clip**의 혼합이며, 이는 정확히 **Loop-Punch Comp(v1.36.0)의 레이아웃**(구간 밖=base, 구간 안=pass별 take)이다.
  - 이 조합을 합치면 어느 쪽으로 태깅해도 깨진다: 결과를 `null`로 두면 그 Take 오디오가 **모든 Take에서 겹쳐** 재생되고, Take id를 붙이면 **base 오디오가 Take 전환 시 사라진다**. 그래서 틀린 답을 고르는 대신 거부한다.

### ③ 브리지·UI

- [audio-bridge.js](audio-bridge.js) `consolidateClips` 래퍼가 재베이크된 레이아웃을 native로 sync(joinClips와 동일 패턴). `persistConsolidatedSources`는 `DAWProxy` 위임으로 도달.
- [ui-tracks.jsx](ui-tracks.jsx) 클립 우클릭 메뉴에 **Consolidate**(Split/Join과 같은 그룹). Split/Join과 달리 **즉시 실행**이며 **선택 전체**에 작용 — 2개 미만이면 비활성 + 안내 title(`ClipContextMenu`에 `title` 지원 추가).
- [app.jsx](app.jsx) `handleConsolidateClips`: `pushUndo` 1회 → 실패 시 **`cancelUndo(savedRedo)`**(no-op 스냅샷이 다음 Ctrl+Z를 먹지 않게 — "Undo 스냅샷 정합성" 노트) → 성공 시 새 clip 선택 + `persistConsolidated()`. `handleJoin`도 같은 persist를 호출(비연속 join = consolidate).

### 검증

esbuild OK, `node --check` OK(engine·bridge·main·preload·built app·ui-tracks), **전역 심볼 충돌 신규 0건**(기존 알려진 `buildInputPortOptions` 1건만), Electron 부팅 **렌더러 에러 0**(브리지 native 연결 확인, 표준 dev CSP 경고만). 렌더 산술은 **파일에서 함수를 추출해 스텁 컨텍스트로 실행하는 시뮬레이션 8케이스 26검사 전부 통과**(인접·gap 무음·3클립 혼합·sourceOffset/gain·짧은 raw 비슬라이드·이음매만 페이드·raw 누락 abort·mono+stereo). 실제 저장→재열기 후 소리 정합은 T-1.37.0-1(사용자).

---

## v1.36.7 — 루프 녹음 중 현재 녹음 pass를 빨강으로 구분 (사용자 보고) (2026-07-19)

> **재빌드: renderer만**([ui-tracks.jsx](ui-tracks.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 보고("Take B로 넘어갈 때 Take A가 안 흐려진다").

### 확인 — v1.36.6 흐림은 정상, 겹침 색이 원인

`drawLiveRecording` 로직을 떼어내 합성 2.5-pass 데이터로 렌더 검증한 결과, **이전 pass는 정상적으로 흐려짐**(헤드 오른쪽 흐림, 왼쪽 진함). 문제는 모든 pass가 **같은 구간에 wrap**되어 현재 녹음 pass가 이전 pass **위에 같은 색으로** 겹쳐 그려지는 것 — playbar **왼쪽의 밝은 부분(=지금 녹음 중인 새 take)** 을 "이전 take가 안 흐려짐"으로 오인하게 됨.

### 수정 — 현재 pass = 녹음 빨강

- 완료된 이전 pass는 **흐린 트랙색(α .2)**, **현재 녹음 중 pass는 녹음 빨강(`#df5b52`, α .95)** 으로 그린다(빨간 진행 헤드와 동일 색). DAW 표준(녹음 중 테이크=빨강)이라 "빨강=지금 녹음 / 흐린 트랙색=이전 take"로 즉시 구분. 흐림 판정(`curPass = floor(경과초/passDur)`) 로직 무변경.

---

## v1.36.6 — 루프 녹음 중 완료된 pass(Take A…)도 흐리게 (사용자 보고) (2026-07-19)

> **재빌드: renderer만**([ui-tracks.jsx](ui-tracks.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 보고(v1.36.5 후속: 기존 take는 흐려지나 **Take B로 넘어갈 때 Take A(완료된 pass)는 안 흐려짐**).

### 원인

v1.36.5는 **베이크된 기존 파형**만 흐리게 했다. 루프 녹음의 **완료된 pass**는 `drawLiveRecording`가 연속 라이브 파형으로 그리는데, 여기서 **모든 pass를 균일하게 α .9**로 그려 완료된 pass(Take A)가 현재 pass(Take B)와 같은 진하기였다.

### 수정

`drawLiveRecording`의 루프(rLoop) 분기를 **pass 단위로 분리** — `curPass = floor(경과초/passDur)` 기준으로 각 point의 pass를 판정해, **이전 pass(A…N-1)는 α .18로 흐리게**, **현재 녹음 중 pass만 α .9**로 그린다(빨간 진행 헤드는 그대로). stride 집계·wrap 로직 무변경, 이전/현재 두 번의 stroke로 나눔.

---

## v1.36.5 — 루프 녹음(punch-repeat / repeat) 중 이전 take 파형을 더 흐리게 (사용자 요청) (2026-07-19)

> **재빌드: renderer만**([ui-tracks.jsx](ui-tracks.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 요청.

- 녹음 중 파형은 이미 어느 정도 흐려지지만(`fill 12`≈7%, `stroke α .22`), **루프 녹음(Loop-Punch Comp / loop-Take)** 에서는 라이브 테이크가 구간에 겹쳐 그려지므로 이전 파형(이전 take + base)을 **더 흐리게** 해달라는 요청.
- [ui-tracks.jsx](ui-tracks.jsx) `Waveform` draw에 `loopRec = track.recording && track._recordLoop` 분기 추가 → 루프 녹음일 때 기존 파형 `fill 08`(≈3%)·`stroke α .12`로 낮춤(단일 punch·일반 녹음은 종전 `.22`/`12` 유지). `_recordLoop`가 두 루프 모드에서만 설정되므로 정확히 해당 경우만 적용. 라이브 테이크(빨간 헤드) 그리기는 무변경.

---

## v1.36.4 — 곡 끝 부근 구간에서 녹음 중 Take 배지가 안 보이던 문제 수정 (사용자 보고) (2026-07-19)

> **재빌드: renderer만**([ui-tracks.jsx](ui-tracks.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 보고(음원 끝부분에서 녹음 시 REC Take 배지 미표시).

### 원인

배지를 `left: rLoop.end*pxPerSec + 6`(구간 오른쪽 바깥)에 두는데, 레인 div가 `width: laneW(=duration*pxPerSec)` + `overflow: hidden`([ui-tracks.jsx](ui-tracks.jsx)). 구간이 **프로젝트 끝**이면 `regionRight ≈ laneW`라 배지가 laneW를 넘어 **잘림**. v1.36.3에서 루프 녹음 중 duration을 더 키우지 않게 되며 오른쪽 여유가 사라져 드러남(그 전엔 duration이 자라 우측에 공간이 있었다).

### 수정 — 오른쪽에 공간 없으면 구간 안쪽으로 flip

- `fitsRight = regionRight + 6 + EST_W(≈124) <= laneW`이면 종전대로 **구간 우측 바깥**(left-align, 오른쪽으로 뻗음).
- 아니면(=곡 끝 구간) **구간 오른쪽 끝에 우측 정렬**(`translateX(-100%)`, 왼쪽=구간 안쪽으로 뻗음) → 항상 laneW 안이라 잘리지 않음. pass 계산·라벨 로직 무변경.

---

## v1.36.3 — 루프 녹음 중 프로젝트 길이(타임라인/OUTPUT FX)가 자라던 버그 수정 (사용자 보고) (2026-07-19)

> **재빌드: 없음**([audio-bridge.js](audio-bridge.js) 플레인 로드·[version.js](version.js)). **엔진·네이티브·renderer 무변경.** 사용자 보고(구간이 곡 오른쪽 끝 부근일 때 loop-punch 녹음 중 OUTPUT FX 트랙이 우측으로 계속 늘어남, 정지하면 복귀).

### 원인 (배지가 아니라 duration 증가 로직)

`recordingPeaks` 핸들러([audio-bridge.js](audio-bridge.js))가 `recordedEnd = 녹음시작(loopStart) + 경과시간`으로 **선형** 계산한 뒤 `recordedEnd >= duration-1`이면 `duration = recordedEnd+60`으로 키운다. 루프 녹음(loop-Take/loop-punch)은 연속 WAV를 **고정 구간 [loopStart, loopEnd]에 wrap**하는데도 recordedEnd는 계속 증가 → 구간이 **곡 끝 부근**(loopStart ≈ duration)이면 매 peak 배치마다 가드가 발동해 타임라인/OUTPUT FX가 우측으로 무한 확장. loop-punch는 `session.end=0`이라 `_recordingDurationLimit`이 없어 가드가 열려 있었다. **v1.36.1 배지와 무관**(배지는 고정 `rLoop.end` 위치) — v1.30.0(loop-Take)부터 잠재하던 문제가 배지로 눈에 띈 것. T-1.36.0-1은 구간이 곡 중간이라 미발현.

### 수정

증가 가드에 **`!track._recordLoop`** 추가 — 루프 녹음이면 duration을 키우지 않는다(구간은 이미 프로젝트 안). 비루프 녹음(곡 끝 너머로 실제 확장)은 종전대로 확장. loop-Take에도 동일 적용(잠재 버그 동반 수정). `node --check` OK.

---

## v1.36.2 — 녹음 중 Take 배지 위치 수정(구간 우측으로) (사용자 요청) (2026-07-19)

> **재빌드: renderer만**([ui-tracks.jsx](ui-tracks.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 지적(T-1.36.1-1 기능은 통과, 표시 위치만).

- v1.36.1 배지를 `left: rLoop.end*pxPerSec - 5` + `translateX(-100%)`로 **구간 오른쪽 끝에 우측 정렬**했는데, 배지 폭이 짧은 구간(예: 10초)보다 넓으면 **왼쪽으로 흘러넘쳐 구간 좌측에 있는 것처럼** 보였다(사용자 첨부 그림). → `left: rLoop.end*pxPerSec + 6`(transform 제거)로 **구간 오른쪽 끝 바깥에 좌측 정렬**(오른쪽으로 뻗음)해 항상 구간 우측에 위치. 기능·계산 로직 무변경.

---

## v1.36.1 — 녹음 중 현재 Take 표시(구간 우상단 배지) + Loop-Punch 라벨 정합 (사용자 요청) (2026-07-19)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-tracks.jsx](ui-tracks.jsx) esbuild) + [audio-engine.js](audio-engine.js)(플레인 로드).** **네이티브 무변경.** 사용자 요청(v1.36.0 T-1.36.0-1 통과 후속). 시험 대기 1건([시험.md](시험.md) T-1.36.1-1).

### 요청

Loop-Punch Comp(punch+repeat) 녹음 진행 중, **현재 녹음 중인 Take(pass)를 트랙 Repeat 구간 우상단에 표시**(예: Take A, Take B, …).

### ① 배지 렌더 ([ui-tracks.jsx](ui-tracks.jsx) `TrackRow`)

- 루프 녹음 중(`track.recording && track._recordLoop`)일 때 구간 **우상단**(`rLoop.end*pxPerSec`, 오른쪽 정렬, top 6px)에 **"REC · Take X"** 배지(빨간 점 + 앰버 라벨, `record-blink`). Loop-Punch Comp + 일반 loop-Take 공통.
- **X 계산(라이브)**: `_recordingPeaks`의 마지막 샘플/`_recordingSampleRate` = 경과초 → `takeBase + floor(경과초/passDur)`. peaks가 도착할 때마다(=매 재렌더) 갱신되어 pass가 넘어가면 A→B→C로 바뀜. `_recordLoop` 프리뷰 wrap과 동일 타이밍원.

### ② 배지 데이터 ([app.jsx](app.jsx) 녹음 시작)

- `track._recordLoop`에 **`takeBase`** 추가: loopPunch = **비활성 take 수**(활성 take는 소비되어 null base가 되므로 그 앞의 비활성만 남음 → 흔한 경우 0=Take A부터), loopTake = **기존 take 수**(뒤에 append). 배지 라벨을 최종 Take Lane 라벨과 정합.

### ③ 라벨 정합성 수정 ([audio-engine.js](audio-engine.js) `attachLoopPunchRecording`)

- `_reindexClips` 뒤 **`_normalizeTrackLayout` 추가**(deleteTake와 동일 패턴). Loop-Punch에서 **소비된 base 활성 take(고아)를 즉시 프룬 + pass를 Take A부터 재번호**. 종전엔 정규화가 autosave/재열기 때만 실행돼 **빈 팬텀 Take lane이 남고 라벨이 B부터** 시작할 수 있었음(pre-existing base take가 있는 경우). 이제 배지·Take Lanes가 즉시 A,B,C로 정합.

### 검증

esbuild OK, `node --check` OK(engine·built app·ui-tracks), 전역 심볼 충돌 신규 0건(배지 계산은 컴포넌트 로컬 IIFE), Electron 부팅 렌더러 에러 0. 실제 pass 넘어갈 때 A→B→C 갱신·최종 Take Lane 라벨 일치는 T-1.36.1-1(마이크 필요, 사용자).

---

## v1.36.0 — Phase 7 Stage 1: Loop-Punch Comp (구간 다중 pass 녹음 + 선택) (2026-07-19)

> **재빌드: renderer([app.jsx](app.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드).** **네이티브 무변경**(기존 루프 녹음·구간 교체 인프라 재사용). 사용자 승인(2026-07-19): 둘 다 ON 시 단일 punch → 다중 pass comp로 승격, crossfade는 보류. 시험 대기 1건([시험.md](시험.md) T-1.36.0-1). **Phase 7 착수.**

### 개요 (Punch ∩ 루프 Take)

**Punch ON + Repeat ON** 동시 상태에서 Record 시, Repeat 구간에서 **여러 pass를 연속 녹음**하고 **구간 밖(base)은 유지한 채 pass(=Take) 중 하나를 선택**. 기존 **Punch(v1.35.0) + 루프 Take(v1.30.0) + Take Lanes(v1.31.0)** 조합 — 신규 building block 없음. 캡처는 루프 Take와 동일(repeat-play ON 유지, 연속 1개 WAV, 수동 정지, `_recordLoop` wrap 프리뷰), 단일 punch의 repeat-off·자동 punch-out은 미적용.

### 트리거 매트릭스 ([app.jsx](app.jsx) `startRecordFlow`)

- 신규 `loopPunch = isPunchOn() && DAW.repeatPlayEnabled && region>0.05`.
- `punch`를 `isPunchOn() && !DAW.repeatPlayEnabled && region`로 **축소**(단일 punch = Punch만 켜짐).
- `loopTake`는 `!isPunchOn() && repeatPlayEnabled && region`.
- 결과: **둘 다 ON = Loop-Punch Comp** / Punch만 = 단일 punch / Repeat만 = 루프 Take / 둘 다 OFF = 일반. (v1.35.3에서 "둘 다 ON=단일 punch"였던 것을 comp로 승격.)
- 세션/`recordingRef`에 `loopPunch` 플래그 추가. start/end/loopStart/loopEnd·`_recordLoop`·자동정지 monitor는 `(loopTake || loopPunch)`로 공유. finalize는 `active.loopPunch`면 `attachLoopPunchRecording` 호출(loop offset shift는 루프 Take와 동일).

### 엔진 ([audio-engine.js](audio-engine.js) `attachLoopPunchRecording`)

- 연속 WAV를 **1개 source**로 등록, `passDur=loopEnd-loopStart`로 nFull+partial pass 산출(`attachLoopRecording`과 동일 회계).
- **(1) 구간 비움 + base 분리**: `[in,out]`을 **활성 클립에서** 비우고(deleteRange의 split/trim 규칙), 살아남는 **base 조각은 `takeId=null`(항상 렌더)로 전환**. 시작점이 이동하는 조각은 `recordedStart/recordedOffsetMs` null(Cal. 오해 방지). 비활성 take는 손대지 않음.
- **(2) pass별 take**: 각 pass를 `[in,out]` 한 클립짜리 take로 등록(활성=마지막 full pass). `_isActiveClip` 규칙상 **base(null) 항상 + 활성 take 구간 클립만** 렌더 → Take Lanes에서 선택 전환 시 **재-splice 없이** 구간만 교체.
- `_reindexClips`+`_ensureBaked`. 브리지 `attachLoopPunchRecording`가 재베이크된 활성 레이아웃을 native로 sync. `pushUndo`로 원본 복구.

### UI

Take Lanes가 이미 구간 스코프로 동작(`setActiveTake`/`deleteTake`/`getTakeLanes` 재사용) — **신설 없음**. Punch 버튼 title에 "Punch + Repeat = Loop-Punch Comp" 안내 추가(Repeat 상태를 라이브로 읽어 3분기: comp / 단일 punch / off).

### 검증

esbuild OK, `node --check` OK(engine·bridge), 전역 심볼 충돌 신규 0건, Electron 부팅 렌더러 에러 0. 스플라이스/슬라이스 산술은 시뮬레이션 6케이스(span·outside·fully-inside·trim-right·trim-left·empty)로 base 분리·오프셋 시프트·pass 수·partial·active=마지막 full 정합 확인. 실제 다중 pass 녹음·pass 전환 시 base 유지는 T-1.36.0-1(마이크 필요, 사용자).

### 보류(Phase 7 다음 stage)

comp 경계(base↔구간 pass) **equal-power crossfade** 자동 생성(사용자 결정으로 이번엔 보류 — 엔진 bake의 기존 3~5ms micro-fade로 클릭은 이미 완화). Consolidate·Join Edits·정리 도구.

---

## v1.35.3 — 구간의 PUNCH 표시를 Repeat 버튼과 분리 (사용자 요청) (2026-07-19)

> **재빌드: renderer만**([ui-mixer.jsx](ui-mixer.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 요청(v1.35.1 후속).

### 문제 (v1.35.1)

구간 버튼 하나가 `Repeat ON`/`PUNCH`/`Repeat OFF`를 겸했다. **PUNCH가 표시된 상태에서 누르면** onClick이 repeat-play를 토글해 **`Repeat ON`으로 바뀌어** 버렸다(punch 표시가 사라짐).

### 수정 — PUNCH = 별도 비interactive 텍스트

- Repeat 버튼은 **원래대로 복원**(`Repeat ON`/`Repeat OFF` 토글만; punchRole 분기 제거).
- 그 **위에 "PUNCH" 텍스트**(버튼 아님, `pointerEvents:none`)를 추가: **punch 모드 ON = 진하게(앰버·bold·glow), OFF = 흐리게**(`--faint`, opacity .5). `focusdaw-punch`를 매 렌더 읽어 라이브 반영.
- 결과 동작(사용자 명세대로): ① PUNCH 클릭 불가(=Repeat ON으로 안 바뀜) ② 구간 선택 시 Repeat 버튼 위 PUNCH 글자 ③ Repeat 버튼 누르면 Repeat ON ④ 그 상태로 Play=repeat play ⑤ PUNCH 글자는 punch 켜짐=진하게/꺼짐=흐리게.

### 로직 확인 (사용자 문의)

Punch(**녹음** 동작)와 Repeat-play(**재생** 동작)는 **독립**이라 충돌 없음. 매트릭스: **Punch ON → Record=punch**(repeat 상태 무관, 녹음 중에만 repeat-play 잠시 off 후 복원) · **Punch OFF+Repeat ON → Record=루프 Take** · 둘 다 OFF → 일반 녹음. ⚠️ 단, **Repeat+Punch 동시 켜짐에서 Record는 단일 punch**이며 "여러 pass 중 선택(loop-punch comp)"은 **Phase 7**.

---

## v1.35.2 — 인앱 릴리즈 노트 갱신 (v1.30.0~v1.35.1 주요 내용) (2026-07-19)

> **재빌드: renderer만**([ui-help.jsx](ui-help.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 요청.

- Help ▸ RELEASE NOTES(`RELEASE_NOTES`, [ui-help.jsx](ui-help.jsx))가 v1.17.x로 오래돼 **v1.30.0~v1.35.1 주요 내용**으로 교체(range/date 갱신). Features = 루프 Take 녹음+Take Lanes / Punch 녹음 / 입력 지연 Recording offset / Recording Offset Cal. Improvements = 원점 대비 이동량 배지 / 녹음 피드백 폴리시 / Take 배지 뷰포트 고정 / 구간 버튼 PUNCH 표시. Fixes = 룰러 라벨 정렬 / play bar seek 2건. 기존 영문 톤 유지, **주요 항목만** 발췌.

---

## v1.35.1 — 구간 버튼에 PUNCH 역할 표시 (2026-07-19)

> **재빌드: renderer만**([ui-mixer.jsx](ui-mixer.jsx) esbuild). **엔진·네이티브 무변경.** 사용자 요청(v1.35.0 후속). T-1.35.0-1 통과 → [시험-아카이브.md](시험-아카이브.md). 경미한 표시 변경이라 별도 시험 항목 없음(T-1.35.0-1 재확인 시 함께 육안).

- OUTPUT FX 레인의 구간 버튼 라벨을 역할 반영: `repeatOn` → **Repeat ON**, `punchModeOn && !repeatOn`(구간이 오로지 punch용) → **PUNCH**(앰버 아웃라인 강조), 그 외 → **Repeat OFF**. `focusdaw-punch`를 매 렌더 localStorage에서 읽어(구간 버튼이 이미 매 렌더 `repeatPlayEnabled`를 읽는 것과 동일 주기) 라이브 반영. 버튼 클릭은 종전대로 repeat-play 토글(이 경우 Repeat ON으로 전환). "Repeat OFF"가 "미사용"처럼 읽히던 것을 punch 사용 중임을 명시하도록 개선.

---

## v1.35.0 — Phase 6 Stage 6: Punch Recording (구간 교체 녹음) (2026-07-19)

> **재빌드: renderer([app.jsx](app.jsx)·[ui-kit.jsx](ui-kit.jsx) esbuild) + [audio-engine.js](audio-engine.js)·[audio-bridge.js](audio-bridge.js)(플레인 로드).** **네이티브 무변경**(기존 구간지정 녹음·pre-roll·자동정지·deleteRange 인프라 재사용). 사용자 결정(2026-07-19): **Repeat 구간 재사용 + Punch 토글**. 시험 대기 1건([시험.md](시험.md) T-1.35.0-1). **Phase 6 종결.**

### 개요 (Q4 결정 = "단일 구간 클립 교체 + 자동 in/out")

Punch = 기존 Repeat REGION(`DAW.loopRange`)을 [in,out]로 삼아, pre-roll로 in에 진입 → in에서 자동 punch-in → out에서 자동 punch-out → **활성 Take의 그 구간만 새 녹음으로 교체**(Take 누적 아님). 녹음/pre-roll/상태머신/자동정지/`deleteRange` 스플라이스는 전부 기존 인프라 재사용 — Stage 6는 "구간 교체" 변형만 얹었다.

### 진입/트리거 ([app.jsx](app.jsx) MenuTransport)

- 트랜스포트에 **Punch 토글 버튼**(신규 `punch` 아이콘, [ui-kit.jsx](ui-kit.jsx)) 추가. localStorage `focusdaw-punch`로 지속(`isPunchOn`/`setPunchOn`). Repeat 구간이 없으면 title로 안내하고 Record는 일반 동작으로 폴백(`punchRegionValid`).
- **우선순위**: Punch ON + 유효 구간이면 loop-Take보다 우선(`punch = isPunchOn() && region>0.05`; `loopTake = !punch && repeatPlayEnabled && region`).

### 녹음 흐름 ([app.jsx](app.jsx) `startRecordFlow`)

- punch면 `punchAt/start = lr.start`, `end = lr.end`(자동 punch-out 지점), pre-roll 앵커 = lr.start. **repeat-play를 OFF**(신규 `prevRepeatRef`로 저장·복원)해 트랜스포트가 구간에 갇히지 않고 in으로 굴러들어와 out을 통과하게 함(loop-Take는 repeat-play ON 유지와 대비). 나머지(count-in/자동정지 monitor=`session.end`)는 기존과 동일.
- `recordingRef`에 `punch/punchStart/punchEnd` 전달. finalize에서 offset 보정(start/end를 shift)까지 기존과 동일하게 적용.

### 구간 교체 ([audio-engine.js](audio-engine.js) `attachPunchRecording`)

- Take를 append하지 않고, **활성 Take clip에 한해**(`_isActiveClip`) [in,out]를 비운 뒤(=deleteRange의 split/trim 규칙을 활성 clip으로 스코프) 새 소스의 clip을 **활성 Take id로 태깅**해 삽입. 비활성 Take는 손대지 않음. 시작점이 바뀌는 조각은 `recordedStart/recordedOffsetMs`를 null로(Cal. 오해 방지, deleteRange와 동일 규칙). `_reindexClips`+`_ensureBaked`로 마무리. 브리지 `attachPunchRecording`가 재베이크된 활성 레이아웃을 native로 sync. **pushUndo로 원본 복구 가능.**

### 검증

esbuild OK, `node --check` OK(engine·bridge), Electron 부팅 무에러(렌더러 에러 0, Punch 버튼 렌더 확인). 스플라이스 산술은 시뮬레이션으로 span-split/trim/full/outside 5케이스 정합 확인. 실제 in/out punch·구간 외 오디오 불변은 T-1.35.0-1(마이크 필요, 사용자).

---

## ⚠️ 상시 개발 노트 — 버퍼 인덱싱 이원성 (전략 B, 반드시 숙지)

> Phase 5에서 도입된 **레이아웃 베이크(전략 B)**의 결과로 생긴 **상시 제약**이다. 완료 기록은 아카이브에 있으나, 이 노트는 이후 모든 파형/재생/피크 코드에 계속 적용되므로 여기에 남긴다.

전략 B에서 `track.buffer`는 두 가지 인덱싱 체계 중 하나이며, **`track._layoutBaked` 플래그가 이를 구분한다.** 이 둘을 혼동하면 **이동한 클립이 빈 파형으로 그려지는 버그**가 재발한다(2026-07-14 실제 발생, 수정 완료).

| 상태 | 버퍼 | 오디오 위치 | 파형 샘플링 기준 | 재생 |
|---|---|---|---|---|
| `_layoutBaked = true` (baked) | 평탄화 렌더 결과 | **타임라인 위치**(`clip.start`) | `clip.start` 기준(타임라인) | `_offset`(플레이헤드)로 그냥 재생 |
| `_layoutBaked = false` (raw/trivial) | 원본 디코드 버퍼 | 소스 원점 | `clip.offset` 기준(소스) | 동일(정렬돼 있어 무관) |

- `_ensureBaked`가 bake하면 `_layoutBaked=true`, trivial로 raw 복원하면 `false`로 설정한다.
- [ui-tracks.jsx](ui-tracks.jsx) `Waveform.bucketAt`는 `track._layoutBaked`로 분기해 `bufPos`를 **타임라인(clip.start) vs 소스(clip.offset)** 로 계산한다. peaks 배열도 `_ensureBaked`가 baked 버퍼에서 재계산하므로 타임라인 인덱싱이다.
- **새 파형/재생/피크 코드를 추가할 때**: baked 버퍼를 `clip.offset`로 접근하면 안 된다(그 위치는 침묵). 반드시 `_layoutBaked`를 확인하고 baked면 타임라인 위치로 접근.
- 재접속(`_assignDecodedToTrack`)은 raw 디코드 후 non-trivial이면 `_ensureBaked`를 호출해 재시작 후에도 이 불변식을 복원한다.
- 관련: `_rawBuffers[sourceId]`(원본 보관), `_isTrivialLayout`(bake 스킵 판정), `clip.gain`은 baked 버퍼에 반영·`clip.params.volume`/automation은 합성커브(이중적용 방지).

---

## ⚠️ 상시 개발 노트 — 렌더러는 번들이 아니라 **전역 스코프 공유** (반드시 숙지)

> 2026-07-17 세션에서 **실제로 앱을 죽인** 함정이다(v1.22.0). 빌드가 잡아주지 않으므로 노트로 남긴다.

`scripts/build-renderers.js`는 esbuild **`transform`만** 하고 **번들링하지 않는다.** 결과물은 [studio.html](studio.html)에서 각각 `<script>`로 로드되므로 **모든 렌더러(`ui-kit` · `ui-tracks` · `ui-mixer` · `ui-dialogs` · `ui-help` · `app`)가 하나의 전역 스코프를 공유**한다.

- **최상단 `const`/`let` 이름이 겹치면 `Identifier 'X' has already been declared`로 앱 전체가 죽는다.** 빌드는 통과한다. (v1.22.0에서 `EMPTY_CLIP_IDS`를 app.jsx·ui-tracks.jsx 양쪽에 선언해 발생 — 정적 검사로 발견.)
- **최상단 `function`이 겹치면 오류 없이 조용히 덮인다** — 로드가 늦은 쪽이 이긴다. 더 찾기 어렵다. (실제 사례: `buildInputPortOptions`가 ui-tracks·ui-mixer에 중복 정의 → 항상 mixer 버전이 이김. 위 "미처리 발견" 참조.)
- **로드 순서**(studio.html): `ui-kit` → `ui-tracks` → `ui-mixer` → … → `app`. 그래서 app.jsx는 ui-tracks의 전역을 그대로 읽을 수 있다(의도적으로 쓰는 경우 주석으로 명시할 것).
- **새 최상단 심볼을 추가하기 전 이름 충돌을 확인할 것**:
  ```bash
  node -e "const fs=require('fs');const n=f=>[...fs.readFileSync(f,'utf8').matchAll(/^(?:const|function)\s+([A-Za-z0-9_\$]+)/gm)].map(m=>m[1]);const F=['build/app.js','build/ui-tracks.js','build/ui-kit.js','build/ui-mixer.js','build/ui-dialogs.js','build/ui-help.js'];const s={},d=[];for(const f of F)for(const x of n(f)){if(s[x])d.push(x+' ['+s[x]+' vs '+f+']');else s[x]=f}console.log(d.length?d:'없음')"
  ```
- 공용으로 쓸 심볼은 **[ui-kit.jsx](ui-kit.jsx)에 1곳만** 두는 것이 원칙(가장 먼저 로드됨).

---

## ⚠️ 상시 개발 노트 — Undo 스냅샷 정합성 (반드시 숙지)

> 2026-07-17 세션에서 이 규칙 위반으로 버그 3건(v1.23.2 · v1.23.3)이 나왔다. 새 편집 UI를 붙일 때마다 적용된다.

판정 기준은 하나 — **`getSnapshot()`이 그 상태를 담는가?**

| 상태 | 스냅샷 | `onBeforeChange`/`pushUndo` |
|---|---|---|
| `master`(volume·reverb·echo·fadeIn·fadeOut·room·roomParams·saturation·widener·exciter), `eqBands`, `tempo`, `duration`, 트랙 `params`·`clips`·`sources`·`takes` | **담음** | **필수** — 없으면 그 기능은 Undo 불가(v1.23.3 fade) |
| 플레이헤드, `loopRange`, `repeatPlayEnabled`, `masterFxBypassed` | **안 담음** | **금지** — 100% no-op 항목이 되어 **다음 Ctrl+Z를 먹고 Redo까지 지운다**(v1.23.2 OUTPUT FX / v1.23.3 Repeat 버튼) |

- **드래그 핸들러는 mousedown이 아니라 "첫 실제 변경 직전"에 1회만** 스냅샷(클릭만 했을 때 no-op 항목이 생기지 않게). 값 비교 후 같으면 조기 반환하는 것이 정석 — 각 컴포넌트의 **휠 핸들러가 원래부터 `if (nv === value) return;`으로 이 패턴**을 쓴다. 단 `Fader`·`SleekSlider`는 mousedown에서 클릭 위치로 값을 점프시키므로 그 자리의 스냅샷이 정당하다.
- **편집이 아무것도 바꾸지 않았으면 `cancelUndo(token)`** 으로 되돌린다. `pushUndo()`는 **redo 스택을 비우므로** `undoStack.pop()`만 하면 **Redo가 조용히 죽는다**(v1.23.1).
- **가드**: `undo()`가 복원 전후 `snapshotSig`를 비교해 같으면 `console.warn("[undo] no-op entry ...")`. 정상 동작 시 무출력이므로, **이 경고가 보이면 위 규칙을 어긴 핸들러가 있다는 뜻.**

---

## 남은 항목 및 계획

> **Phase 1–7 완료**(녹음·편집·Take·Punch·Comp/Merge/정리 도구) **+ 보컬 채널 스트립 A~F 완료(2026-08-04)**. 이 섹션이 **앞으로의 계획·미해결·보류를 한곳에** 모은 단일 진실원본이다. 완료된 버전별 기록은 [앱개발-아카이브.md](앱개발-아카이브.md).
>
> **v1 계열은 v1.46.3으로 마감했다** — 도움말 문구 정리(v1.46.3 ①)와 설치본 스모크 ①~⑥이 끝났고, 마지막까지 남아 있던 **T-1.46.3-2(설치본 Export·오토메이션)도 2026-08-31 사용자 판정으로 close**했다(v2.3.4에서 같은 경로를 실제로 시험해 통과 — 근거 [시험-아카이브.md](시험-아카이브.md) 최상단). **v1 계열에 남은 작업 없음.**
>
> **진행 중인 큰 작업은 Pitch Editor**(사용자 결정 2026-08-25). **Stage A·B·C 종결 + 에디터 UI 완성(v2.4.0까지)**, **다음은 Stage D(편집) → E(렌더·프린트) → F(마무리)**. Stage C(v2.4.0)에는 **2026-08-31 사용자 요구 2건**(BPM 기반 노트 밀도 규칙 · 블록 내 음이름 표시)이 들어갔다 — 설계 §12. 목표 워크플로는 **보컬 녹음 → Pitch Editor로 음정 수정 → 보컬 채널 스트립으로 이펙트 → Export**이며, 피치 보정은 **오프라인 프린트**(클립 오디오를 교체)라 스트립·Export 경로는 손대지 않아도 자동으로 따라온다. 상세 설계 [피치-에디터-설계.md](피치-에디터-설계.md).
>
> **레포지토리 분리(배포 인프라)** 는 v2와 독립된 트랙으로 남겨 둔다([(승룡)해야할일.txt](<(승룡)해야할일.txt>) — Private 전환 + Releases 전용 Public repo + electron-updater). 그 외는 전부 "향후 개발 후보"(착수 결정 없음) 또는 "보류".

### ✅ Phase 7 — Comp / Consolidate / 정리 도구 (완료, 2026-07-22)

- ✅ **Loop-Punch Comp** (v1.36.0, **T-1.36.0-1 통과** · 녹음 중 Take 배지+라벨 정합 v1.36.1): Punch ON + Repeat ON에서 Repeat 구간의 여러 pass를 녹음해 **구간 밖(base)은 유지한 채 pass 중 하나를 선택**. base clip을 `takeId=null`(항상 렌더)로 두고 구간을 비운 뒤 각 pass를 [in,out] 한 클립짜리 take로 등록(`attachLoopPunchRecording`) → `_isActiveClip` 규칙으로 재-splice 없이 선택 전환. Take Lanes UI 재사용. 녹음 중 현재 pass를 구간 우상단 "REC · Take X" 배지로 표시(v1.36.1). 상세 [앱개발.md](앱개발.md) v1.36.0·v1.36.1.
- ✅ **경계 equal-power crossfade** (v1.38.0, **T-1.38.0-1~3 통과**): 클립 경계를 continuous/seam/gap으로 분류해 seam에만 10ms equal-power crossfade를 걸고, 안 움직인 split은 **페이드 없이** 렌더(원본에 없던 딥 제거). 재생·Export·Merge가 같은 규칙을 공유. 상세 [앱개발.md](앱개발.md) v1.38.0.
- ✅ **Comp Lane 구성** (v1.42.0, **T-1.42.0-1 통과**): Take 레인 위 **스와이프**로 구간별 take를 골라 하나의 comp 합성(사용자 결정: 대표 DAW 방식). `track.comp` 구간 맵(비파괴) + `_renderClips` 단일 진실원본으로 bake/Export/native/Flatten이 동일하게 반영, 경계는 v1.38.0 crossfade 재사용. 각 레인은 자기 기여 구간을 하이라이트. 상세 [앱개발.md](앱개발.md) v1.42.0.
- ✅ **Merge Clips** (v1.37.0~v1.37.3, **T-1.37.0-1·2·3 통과**): 선택 clip(+사이 무음)을 하나로 병합. 엔진이 **heal**(split 되돌리기 = 단일 소스 읽기, 파일 생성 없음) / **render**(새 WAV를 `<Project> Audio/Consolidated/`에 기록) 전략을 판단. **Join과 Consolidate를 하나의 명령으로 통합**(v1.37.3, 사용자 결정) → **"Consolidate source 소멸" 버그 근본 해소**. 상세 [앱개발.md](앱개발.md) v1.37.0~v1.37.3.
- ✅ **Comp Flatten** (v1.39.0, **T-1.39.0-1~5 통과**): 활성 Take + base를 확정 병합하고 나머지 take를 정리. **레인을 넘어 병합할 수 있는 유일한 조작**(선택지를 없애는 것이 목적이므로 Merge의 거부 사유가 성립하지 않는다). Undo 복원 가능·녹음 원본 WAV 유지. 상세 [앱개발.md](앱개발.md) v1.39.0.
- ✅ **Join Edits — 별도 명령 없이 종결(사용자 결정 2026-07-22)**: 같은 source의 연속 편집 경계 제거는 **v1.37.3 Merge Clips의 heal 경로가 사실상 수행**하므로 현재 큰 문제 없음. 필요하면 추후 재검토.
- ✅ **Clean Up Unused Recordings...** 정리 도구 (v1.43.18~19, **시험 통과 2026-07-22**). `Recordings/`·`Consolidated/`·`Bounces/`에서 프로젝트가 참조하지 않는 WAV를 찾아 **휴지통으로 이동**(복구 가능). 참조 판정은 **현재 트랙 + 모든 undo/redo 스냅샷** 합집합이라 undo로 되살아날 파일은 제외(사용자 결정 2026-07-22로 삭제 정책 확정). **+ 저장 시 수집된 temp 원본 자동 삭제(영구), 미저장 산출물·세션 스크래치를 `Documents\FocusDAW\Temp\`로 스테이징(시작/종료 시 스크래치 정리), 공유 녹음 dedup/heal.** 상세 [앱개발.md](앱개발.md) v1.43.18·v1.43.19.
- ✅ **Save As 파일 복사 규칙** (v1.43.0, **T-1.43.0-1 통과 2026-07-21**): Save As/최초 저장 시 앱 생성 오디오(녹음/Bounce/Consolidated)를 `<Project> Audio/`로 수집하고 소스를 **상대경로**로 재지정 → 자기완결·이식가능. temp에 있던 미저장 병합물도 이때 프로젝트 폴더로 들어온다. import 스템은 제자리 참조(사용자 결정). 상세 [앱개발.md](앱개발.md) v1.43.0. ⚠️ 남은 것: **`.wav.part`→`.wav` 확정 스테이징**은 현재 finalize-recording에서 정상 Stop 시 이미 rename하므로 별도 작업 불요(Phase 4에서 확인).
- 완료 기준: 원본 WAV 불변 비파괴 comp, 안전한 미사용 파일 정리, Undo/Redo 후 파일 참조 정합.

### 알려진 버그 / 한계

- 🎚️ **라우드한 목표(−12 이하)에서 리미터 압축이 가청 — 추후 과제**(사용자 결정 2026-07-20: 룩어헤드/릴리즈 개선까지만 하고 나머지는 보류).
  - **원 증상(하모닉 왜곡)은 v1.41.2에서 해결**됐다(High >4kHz +2.5 → −0.1). 남은 것은 **단순 리미터의 성능 한계**이지 결함이 아니다.
  - **실측 가청 경계**(crest 17.4 dB 소재): crest 감소 **−2.7 dB(−14 LUFS)=깨끗** / **−4.2(−12)=미세하게 들림** / **−5.9(−9)=확실히 들림**. `LRA` 감소가 −0.3을 넘어가면 들리기 시작한다 — 피크를 넘어 **음악의 다이내믹**을 건드린다는 신호다.
  - **이 소재의 투명한 한계는 −13 LUFS 부근.** crest 17.4 소재를 −9로 올리는 것은 원래 공격적인 마스터링이며, 7~8 dB의 다이내믹 압축이 물리적으로 필요하다.
  - **남은 개선안**(착수 시): 오버샘플링(인터샘플 피크·앨리어싱), 프로그램 종속 릴리즈(2단), 다밴드. ⚠️ **별도 마스터링 앱(`FocusDAW-Mastering`)이 있다면 그쪽이 맞는 자리** — 리미터를 두 곳에 만들 이유가 없고, Studio의 Export는 투명한 −14 타깃까지 담당하면 된다.

- ✅ **도움말/메뉴얼 문구 미추종 — v1.45.0(메뉴얼 전면 개정)과 v1.46.3에서 해소.** 인앱 메뉴얼은 v1.45.0에서 `SOURCE`/`NO SRC`로 전부 갱신됐고, 마지막까지 남아 있던 "오디오 파일을 찾을 수 없음" 모달([app.jsx](app.jsx))이 v1.46.3에서 정정됐다. 남은 `NO AUDIO` 1건은 [ui-help.jsx](ui-help.jsx)의 **과거 릴리즈 노트 본문**이라 의도적으로 유지.

- ✅ **하드코딩된 액센트 색 — v1.44.21에서 52곳 일괄 해소·시험 통과(T-1.44.21-1, 2026-08-04)**. 상세는 아래 v1.44.21 항목.

- ✅ **Export LUFS Normalize의 트랜지언트 왜곡 — v1.41.2에서 해소**(T-1.41.2-1 측정 확인, 이어 **T-1.41.3-1·2 통과**). ⚠️ 원인 추적이 두 번 빗나갔다: **v1.41.0은 쓰이지 않는 경로**(ffmpeg loudnorm)를 고쳐 증상 그대로(**T-1.41.0-1 실패**), **v1.41.1의 리미터 어택 추가도 실패**(**T-1.41.1-1 실패**). 진짜 원인은 **메이크업 게인이 마스터 FX 앞에 있어 소프트 클리퍼를 때린 것** — v1.41.2에서 게인을 FX 뒤로 옮겨 해소(사용자 실측 High >4kHz **+2.5 → −0.1**). v1.41.3에서 리미터 룩어헤드/릴리즈까지 개선. 상세 [시험-아카이브.md](시험-아카이브.md) v1.41.0~v1.41.2.
  - ⚠️ 남은 한계: 크레스트가 큰 믹스는 **−9 LUFS에 도달하지 못한다**(측정 −9.74). 물리적 제약이며, **사용자 결정으로 UI 경고는 넣지 않았다** — 조용히 미달한다. 확인은 `node tools/loudnorm-probe.js <파일.wav>`.

### 해소된 항목

- ✅ **Consolidate(떨어진 클립 Join) source가 저장/재열기에서 소멸 — v1.37.0~v1.37.1에서 근본 수정, T-1.37.0-1 통과**(2026-07-20). 병합 소스가 `filePath:null`(메모리 raw)이라 재열기 복원 대상에서 빠지던 것을, 생성 즉시 `<Project> Audio/Consolidated/`에 WAV로 기록하고 `source.filePath`를 채워 **기존 `hydrateSource` 복원 경로에 태우는** 방식으로 해소(v1.37.0). 초회 시험에서 **경로가 실제로는 저장되지 않는 비동기 경합**과 **재접속 휴리스틱이 병합 클립을 되돌리는** 두 결함이 드러나 v1.37.1에서 마저 수정했다.
  - ⚠️ 남은 한계 ①: **v1.37.1 이전에 병합해 이미 저장한** clip은 `filePath`가 null로 굳어 소급 복구 불가 — 다시 병합해야 한다(렌더된 WAV 자체는 `Consolidated/`에 남아 있다).
  - ⚠️ 남은 한계 ②: 디스크 기록이 실패하면 종전처럼 메모리 전용 소스로 폴백하므로, 그 세션 한정으로 같은 증상이 날 수 있다(설계상 의도 — 디스크 문제로 편집 자체가 실패하지는 않는다).
- `clip.gain`은 baked WAV로 native에 정상 반영됨(문제 아님).

### 사용자 결정으로 종결한 항목 (구현 안 함)

- **clip 단위 volume UI** → ~~구현 안 함(2026-07-20)~~ → **결정 번복: v1.43.20에서 clip gain 라인 UI로 구현**(사용자 요청 2026-07-22, 컷 전용). 상세 [앱개발.md](앱개발.md) v1.43.20. **clip 단위 automation UI**(동적 커브)는 여전히 신설 안 함 — 트랙 automation으로 대체. `setClipParam`/`clip.params`는 계속 미사용(호출 UI 없음), 정적 `clip.gain`만 라인 UI로 노출.
- **micro fade 시각화** → **불필요(사용자 결정 2026-07-20)**. 엔진 bake에 3~5ms로 이미 반영되어 소리에는 나타나므로 UI 표시는 하지 않는다.
- **Input source 선택(mic/line/Hi-Z)** → Skip(WASAPI 한계, ASIO 필요 — 사용자 협의).

### ✅ 신규 기능 — 보컬 채널 스트립 (**A~F 전 단계 완료·시험 통과, 2026-08-04**)

- **보컬 트랙(audioIn/bounce) 전용 채널 스트립** — 스템(MR)과 보컬 이펙트 분리. 인서트 순서 **HPF → Noise Gate → EQ → Compressor → De-esser**(pre-fader) + 기존 reverb/echo(센드) + **오프라인 광대역 De-noise 프린트**. UI=별도 창, 풀스택(웹+네이티브). 상세 설계는 [보컬-채널-스트립-설계.md](보컬-채널-스트립-설계.md). 근거: 이 앱은 SUNO stem+사용자 보컬 녹음으로 완성본을 만드는 용도.
  - ✅ **Stage A+B — 9밴드 EQ + Compressor** (v1.44.0~v1.44.12): 데이터 모델(`vocalFx`)·kind 게이팅·pre-fader 인서트(웹+네이티브+Export)·브리지 재동기화·별창 UI·프리셋·A/B. **Stage B 전건 통과(2026-07-26)**.
  - ✅ **Stage C — 적용 전/후 FFT 스펙트럼** (v1.44.14, T-1.44.14-1 통과): 트랙 버퍼의 **정적 FFT(N=4096)** PRE + EQ/HPF 응답을 얹은 POST. 실시간 analyser가 아니라 버퍼를 읽어 **네이티브/웹 출력과 무관하게 동작**(설계 §5-1의 analyser 안보다 단순·견고해 채택).
  - ✅ **Stage D — HPF + Noise Gate** (v1.44.15~16, T-1.44.15-1 통과): HPF는 웹+네이티브, **Gate는 네이티브 권위**(WebAudio에 게이트 노드 없음 — 설계 §4-1 근사 허용).
  - ✅ **Stage E — De-esser** (v1.44.17, T-1.44.17-1 통과): 스플릿밴드(치찰음 대역 사이드체인 → 그 대역만 동적 감쇠), **컴프 뒤**(설계 §11 제안대로).
  - ✅ **Stage F — 광대역 De-noise 오프라인 프린트** (v1.44.20, T-1.44.20-1 통과): 무음 구간에서 학습한 지문으로 **STFT 스펙트럼 감산 → 새 WAV 프린트**(원본 불변·Undo 복원). 엔진은 **자체 STFT**(사용자 결정 — ffmpeg `afftdn`은 사용자 선택 구간 프로파일을 못 씀).
  - ✅ **부가**: Gate/Comp/De-esser **실시간 GR 미터**(v1.44.18~19), 프리셋이 **HPF·De-esser까지 세팅**(v1.44.23, Gate는 의도적 제외), PRESET **Reset**(v1.44.24), 창 영문화(v1.44.22).
  - ⬜ **후속 페이즈(이 문서 범위 밖)**: **Pitch Editor**(피아노롤 음표 단위 피치 편집) — 설계 §8에서 "채널 스트립보다 큰 별도 DSP"로 분리, 착수 결정 없음.

### 릴리즈 전 작업

0. ✅ **v1.46.3 배포 빌드 + 설치본 스모크 ①~⑥ 통과**(2026-08-26) — `dist/release-1.46.3/`. 잔여 Export·오토메이션 확인은 [시험.md](시험.md) **T-1.46.3-2**로 분리(사용자 결정으로 보류).
1. **배포 빌드**: `npm run dist:win`. 네이티브 엔진(`bin/FocusDAW-AudioEngine.exe`)·Help 이미지·ffmpeg 경로가 패키징에 포함되는지 확인. ⚠️ 잠긴 `dist/win-unpacked.tmp` 때문에 EBUSY가 나면 `-c.directories.output=dist/release-<버전>`으로 출력 폴더를 옮긴다(v1.45.0·v1.46.3에서 사용).
2. **스모크 테스트**: 실행/종료·프로젝트 신규·오디오 import·최근 프로젝트 복원; 재생/정지/seek/repeat·Solo/Mute·볼륨/팬·Mixer/Advanced 창; Vari BPM/Key·Fade·Volume Automation이 재생·Export 모두 반영; MP3/WAV Export 결과 재생.
3. **시험 문서**: 대기 항목은 [시험.md](시험.md), 완료는 [시험-아카이브.md](시험-아카이브.md).

### 향후 개발 후보

**우선순위 높음**
- **성능**: 대용량/다트랙 peak 캐시·Web Worker 디코딩·automation 곡선 memoize·스크롤/줌. 장시간 세션 메모리·Export 캐시 무효화 정책 점검.
  - 🔵 **전략 B bake 편집 반응 속도 — 계측 완료(v1.41.4, T-1.41.4-1), 최적화는 보류·백로그**(사용자 결정 2026-07-20). 5초 이동 실측: **1분 52.9ms · 3분 159.6ms · 5분 278.1ms**, **선형(~50 ms/min, 절편 0)**. **bake가 ~88%·peaks 12%**로 지배 → ⓐ **peaks→Web Worker**(작은 작업·낮은 위험, total ~12%만 절감·비동기화로 메인 스레드 블로킹만 감소) ⓑ **편집 구간 부분 재렌더**(bake 근본 축소·근본 해결이나 `_layoutBaked` 인덱싱 이원성을 건드리는 큰 작업·높은 위험). **중간 지점이 없어** 양자택일이며, 착수 시 이 트레이드오프부터 결정. 상세 [시험-아카이브.md](시험-아카이브.md) v1.41.4.
    - **재확인(사용자 결정 2026-07-24): 현재 버전에서는 그대로 둔다.** 근거 — 주 작업이 **5분 이하 보컬 녹음**이고(실측 5분 278ms = 관찰~경계 밴드), **현재 기준으로 실사용에 큰 지장이 없다.** 백로그 유지, 착수 시 위 ⓐ/ⓑ 트레이드오프부터.
- ✅ **장시간 녹음 실측(30분+) — 완료(T-1.41.4-2 통과, 2026-07-20)**: 드롭아웃·무음·콘솔 에러 없음, 파일 정상, 재열기 복원 정상, 메모리 우상향 없음(누수 아님). ThreadedWriter+FIFO 설계 실측 확인. 상세 [시험-아카이브.md](시험-아카이브.md) v1.41.4.

**중간**
- **트랙 재정렬**: 트랙 헤더 드래그로 순서 변경(클립 드래그 이동은 Phase 5 완료).
- **자동 업데이트 + CI**: `electron-updater` + GitHub Releases, `electron-builder --publish`.
- **리버브 IR 개선**: 절차적 노이즈 IR → 실측 IR 로딩/프리셋.

**검토**
- **EQ 밴드 표현 통일**: OUTPUT FX 3밴드 vs Mixer Master/Advanced EQ 9밴드.
- **VST3 / 고급 미터링**(True Peak, LUFS) 장기 조사.
- **Cloud Sync**(.focus + stem을 클라우드 동기화) 장기.
- **Auto-tune(피아노 롤 피치 편집)**: 대형 신규 모듈, 착수 전 결정 필요(모노포닉 범위·피치 엔진·오프라인 렌더). 상세 아카이브 v1.21.0 Q5.

### 보류

- **split/join 툴바 버튼**만 숨김(우클릭 메뉴·단축키 C/J는 노출). 재노출 시 [app.jsx](app.jsx) `<ToolBar tool={tool} setTool={setTool} />` 주석 해제.
- **변동 BPM / Tempo Map 분석**: 현 전체곡 비례 스케일링에서는 불필요. 비트 그리드/워프 편집 도입 시 재검토.
- **AudioWorklet 기반 실시간 DSP 재시도**: 현 JUCE + WebAudio fallback 동작으로 즉시 착수 안 함.
